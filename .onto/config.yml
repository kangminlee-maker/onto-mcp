output_language: ko

domains:
  -

review:
  teamlead:
    model: main
  subagent:
    provider: main-native
  lens_deliberation: controlled-lens-deliberation
lens_agent_teams_mode: true
review_mode: full

llm:
  auth: oauth
  provider: openai
  model: gpt-5.4
  effort: high
