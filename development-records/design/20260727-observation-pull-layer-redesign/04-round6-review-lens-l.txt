MATERIAL FINDINGS: 3

F1 | high | src/core-runtime/reconstruct/observation-read-grant.ts:653 | Equal-count partitions alias into false whole-observation evidence
FAILURE PATH: 66,000-char observation → fetch part 1/2 in a 16-id request ending at char 64,774 → submit a valid client-built cursor for part 2/2 of the same observation fetched alone, beginning at char 65,068 → both partitions have part_count=2, so the receipt unions indexes {1,2} → observationIdsServed authorizes the id despite a 293-char gap never being delivered.
EVIDENCE: The reader explicitly permits hand-built cursors at src/core-runtime/reconstruct/observation-read.ts:661 and accepts their coordinates at src/core-runtime/reconstruct/observation-read.ts:695; partition boundaries depend on the request’s id list at src/core-runtime/reconstruct/observation-read.ts:725. The accumulator distinguishes partitions only by part_count at src/core-runtime/reconstruct/observation-read-grant.ts:663, and the citation reducer treats {1,2}/2 as whole at src/core-runtime/reconstruct/observation-read-facade.ts:848. A read-only production-function probe measured grouped part 1 length 64,775, solo boundary 65,068, returned solo part 2 length 932, missing 293 chars, and authorization=true. The existing negative control forces unequal counts at src/core-runtime/reconstruct/observation-read-grant.test.ts:260, so this equal-count input stays green.
FALSIFIED PREMISE: The cursor decision assumed a hand-built coordinate could only narrow what the client receives (src/core-runtime/reconstruct/observation-read.ts:661); cross-call whole-observation aggregation makes two narrowed reads widen the runtime’s evidence inference.
FIX: Give every decomposition an exact identity derived from snapshot digest, page budget and ordered request ids/part allowance; merge part indexes only when that identity matches, and persist it in the receipt.

F2 | high | src/core-runtime/reconstruct/observation-read-facade-server.ts:105 | An EPIPE write callback commits an undelivered page
FAILURE PATH: Worker requests a page → Codex closes/cancels the MCP read side before receiving the response → Node invokes the write callback with EPIPE → the zero-argument callback unconditionally calls session.commit() → the receipt records the page → a worker result citing its catalog id passes the served-id check although the response was never delivered.
EVIDENCE: callTool mutates served state before transport at src/core-runtime/reconstruct/observation-read-facade.ts:503; the shell ignores the write callback’s error argument and commits at src/core-runtime/reconstruct/observation-read-facade-server.ts:105; commit atomically publishes current state at src/core-runtime/reconstruct/observation-read-facade.ts:526 and src/core-runtime/reconstruct/observation-read-facade.ts:601; the author then trusts that receipt at src/core-runtime/reconstruct/direct-call-directive-author.ts:3391 and admits its ids at src/core-runtime/reconstruct/direct-call-directive-author.ts:3445. A process probe with the parent destroying stdout produced `COMMIT_CALLBACK:EPIPE` before the child’s unhandled EPIPE exit. The real-process test always drains stdout at src/core-runtime/reconstruct/observation-read-facade.test.ts:815, so it never exercises this branch.
FALSIFIED PREMISE: The delivery repair assumed entering a Writable callback proves that bytes went out; Node invokes the same callback with an error when delivery fails.
FIX: Accept the callback error argument, commit only when it is absent, and terminate without publishing the pending state on write error.

F3 | medium | src/core-runtime/reconstruct/authored-artifact-reuse.ts:395 | An ON-only acceptance repair invalidates every historical OFF resume
FAILURE PATH: source_observation_catalog_tool absent → explicitly resume a valid OFF session whose provenance predates authored_output_contract_version → the current OFF reuse match nevertheless adds version 2 → its hash differs → writeFreshAuthoredYamlDocument throws `resume provenance mismatch` before reuse, even though the OFF author’s acceptance rule did not change.
EVIDENCE: Version 2 exists specifically for the answer-claims cluster boundary at src/core-runtime/reconstruct/authored-artifact-reuse.ts:42, but is inserted into every match at src/core-runtime/reconstruct/authored-artifact-reuse.ts:388; hash mismatch is terminal at src/core-runtime/reconstruct/authored-artifact-reuse.ts:477. The actual boundary remains ON-gated at src/core-runtime/reconstruct/direct-call-directive-author.ts:3654 and src/core-runtime/reconstruct/direct-call-directive-author.ts:3678. The test explicitly requires both ON and OFF keys to carry the version at src/core-runtime/reconstruct/observation-catalog-tool.test.ts:925, while the untracked test covers only fresh authors at src/core-runtime/reconstruct/answer-claims-evidence-boundary.test.ts:128. The design placed the tool announcement in the per-call payload specifically to avoid rotating unused OFF reuse keys at development-records/design/20260726-observation-catalog-tool-design.md:899.
FALSIFIED PREMISE: The repair assumed global conservative key rotation preserves OFF behavior; the rule being versioned is ON-only, while reuse mismatch terminates otherwise-valid OFF resumes.
FIX: Make the acceptance-contract version an optional ON-only reuse field, absent when the opt-in is false, and pin a historical OFF provenance/resume fixture.

CLAIMS REFUTED: “commit() after the stdout callback makes the crash window under-record” — refuted by F2.
“observationIdsServed admits whole observations only” — refuted by F1’s equal-count partition splice.
“The claims boundary is gated on the opt-in, so OFF behavior is unchanged” — the author branch is gated, but F3 changes OFF resume behavior.

CLAIMS CONFIRMED: chargeRejectedCall uses the exact response cost, rejects before mutation, and therefore needs no postcondition under current callers (src/core-runtime/reconstruct/observation-read-grant.ts:585).
Receipt launch-token validation is sufficient: the file token must first be a non-empty string and then equal the expectation (src/core-runtime/reconstruct/observation-read-facade.ts:792).
prepareObservationReadFacadeLaunch removes and verifies absence of the stale receipt before launch, with token binding remaining independent (src/core-runtime/reconstruct/observation-read-facade.ts:140).
The published ASCII id pattern makes JSON-Schema code-point maxLength equal the reader’s UTF-16 length for every admissible id (src/core-runtime/reconstruct/observation-read-facade.ts:386; src/core-runtime/reconstruct/observation-read.ts:603).

CHECKED AND CLEAN: The run→author→Codex route is ON-gated at src/core-runtime/reconstruct/run.ts:3974; the route writes the descriptor from the exact prompt parts at src/core-runtime/llm/llm-caller.ts:1029; plan/provider precedence matches dispatch at src/core-runtime/llm/llm-caller.ts:1550; parse repair drops the one-dispatch facade at src/core-runtime/reconstruct/authoring-llm-call.ts:307.
OBS-1 and OBS-2 hold outside F1’s inference defect: bodies are serialized from artifact records, frozen, consumption-filtered and resolved only through the sealed snapshot lookup at src/core-runtime/reconstruct/observation-read.ts:320 and src/core-runtime/reconstruct/observation-read.ts:405; per-call drift checks re-read only the fixed source paths at src/core-runtime/reconstruct/observation-read-grant.ts:728.
OBS-3 holds literally: cursors and pages never become evidence refs; only observation ids surviving both the prompt-catalog and receipt gates reach evidenceRefsFromIds at src/core-runtime/reconstruct/direct-call-directive-author.ts:3429.
OBS-4 uses the same prompt_context row authority, last-wins lookup, missing-row denial and consumption_allowed predicate on push and pull at src/core-runtime/reconstruct/authoring-prompt-payloads.ts:2483 and src/core-runtime/reconstruct/observation-read.ts:379.
Fresh OFF dispatch bytes remain unchanged: the current probe reproduced 1,333,472 / 1,453,819 / 112,156 chars with SHA-256 prefixes 8658b612 / 2575c57f / fd7f05cc respectively; F3 concerns resume semantics, which that byte probe does not exercise.
