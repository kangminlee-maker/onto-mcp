# 구현 설계 — 배달 재조정(delivery reconciliation) (2026-07-27)

> owner 결정: 3안(원자 프레임 + 배달 증명) 채택 → 교차검증에서 **방향 F**로 재조준.
> 근거 측정: [05](05-measurement-tool-result-truncation.md) 절단 · [09](09-measurement-result-field-authority.md) 필드 권위 ·
> [10](10-measurement-ack-roundtrip.md) ack 왕복 · 본 문서 §1 rollout 실측.
> 종합: [07](07-synthesis-delivery-proof.md). 대상: codex-cli **0.145.0**.

## 0. 한 문장

**워커가 끝난 뒤, codex 자신의 rollout에서 "모델 컨텍스트에 실제로 들어간 바이트"를 읽어
전송분과 대조하고, 온전히 도달한 것만 `served`로 인정한다.**

## 1. 이 설계를 가능하게 한 실측 (본 세션)

### 1-1. rollout은 전송분과 수신분을 **둘 다** 기록한다

| 레코드 | 내용 | 65,553자 실행 | 128,049자 실행 |
|---|---|---|---|
| `event_msg` / `mcp_tool_call_end` | MCP 서버가 **보낸** 원본 | 9지점 전부(생존6+소실3) | 9지점 전부(생존4+소실5) |
| `response_item` / `custom_tool_call_output` | 모델 컨텍스트에 **들어간** 것 | 생존 6 · **소실 0** | 생존 4 · **소실 0** |

두 독립 실행에서 수신분 레코드가 내 quantile 측정과 **완전 일치**했다. 수신분 길이는 양쪽 다
**약 40,490자** — 실효 천장이 거기 있다.

### 1-2. 우리 디스패치에 결속할 수 있다

- **codex가 stderr에 `session id: <uuid>`를 출력한다.** 런타임은 이미 자식 stderr를 캡처하므로
  자기 id를 직접 얻는다. "최신 파일" 휴리스틱이 필요 없다.
- rollout 파일명에 그 uuid가 박혀 있고, 첫 줄 `session_meta`가
  `session_id` · `cwd` · **`cli_version`** · `git.commit_hash` · `git.branch`를 담는다 → 교차 확인과
  **버전 고정**의 재료.

### 1-3. 절단의 정체 — exec 출력 절단이다

모델은 MCP 도구를 직접 부르지 않는다. codex의 **JS `exec`** 안에서
`tools.mcp__<server>__<tool>(...)`를 호출한다(패킷 측정 제약 3과 일치). 따라서 결과는 exec의
출력 렌더링을 거쳐 컨텍스트에 들어가고, **exec 출력 상한**에 걸린다. 수신분 레코드에는
`Warning: truncated output (orig…` 문자열이 함께 남는다.

**설계에 미치는 영향**: 수신분은 페이지 JSON의 깨끗한 사본이 아니라 **모델이 쓴 JS의 렌더링 결과**다.
그러므로 대조는 "레코드 전체 다이제스트 일치"가 아니라 **"페이지 본문이 수신분 안에 온전히 들어있는가"**로
해야 한다. 모델이 일부러 잘라 출력했다면 그것도 "도달하지 않음"이며, **그 판정은 의미상 옳다** —
컨텍스트에 안 들어간 것은 인용 근거가 될 수 없다.

## 2. 모양

```
디스패치 중 (기존과 동일)
  façade → 페이지 서빙 → EMISSION LOG (보낸 것 + 페이지 다이제스트)   ← 주장 아님, 입력임

워커 종료 후 (신규, 런타임 부모)
  stderr에서 session id 확보
    → rollout 파일 특정 · session_meta 검증(session_id·cwd·cli_version)
    → mcp_tool_call_end 중 invocation.server == 우리 façade 서버명 인 것만 선별
    → 뒤따르는 custom_tool_call_output(수신분)에서 각 페이지 본문의 온전 존재 확인
    → DELIVERED = 온전 도달한 페이지
    → RECEIPT.served = DELIVERED 로 기록

소비자 (무변경)
  observationIdsServed(receipt) — 완전 커버리지 투영, 한 글자도 안 바뀜
```

### 왜 `served`가 하나로 유지되는가

지금 façade는 서빙 즉시 `served`에 넣고 `commit()`이 영구화한다. 그 기록은 **emit 기반**이라
틀린 주장을 한다. 그러나 **개념을 하나 더 만들지 않는다** — 이 저장소의 지배적 결함 유형이
"기존 메커니즘 옆에 성질을 안 물려받은 두 번째 메커니즘"이기 때문이다(패킷 8번 분류).

대신 **이름을 정직하게 가르고 생산자를 하나로 만든다**:

- 실행 중 기록 = `emissions` — "보냈다"는 사실. **영수증에 안 간다.**
- 종료 후 재조정 = `served`의 **유일한 생산자**.
- `observationIdsServed()` = **무변경**. 규칙은 여전히 한 곳에만 선언돼 있다.

## 3. Fail-closed 조건 (전부 "인용 0"으로 떨어진다)

소비자는 이미 *"영수증 없음 = 아무것도 서빙 안 됨"*으로 fail-closed다(facade.ts:766). 여기에 얹는다.

| 조건 | 처분 |
|---|---|
| stderr에 session id 없음 | served = ∅ |
| rollout 파일 없음/읽기 실패 | served = ∅ |
| `session_meta.session_id` 불일치 | served = ∅ |
| `cli_version`이 검증된 목록 밖 | served = ∅ (**버전 고정**) |
| 기대 레코드 종류 부재(`custom_tool_call_output` 없음) | served = ∅ |
| 재조정 전 런타임 사망 | **§9-F3 참조 — 초안의 주장이 거짓이었다. 개정 후: emissions만 남고 유효 영수증 없음 → ∅** |

**원칙**: 모든 미지는 "덜 인정"으로 떨어진다. 잘못 인정하는 방향으로 깨지는 경로를 두지 않는다.

## 4. 도입되는 개념 (최소)

| 이름 | 소유하는 규칙 | 선언 위치 |
|---|---|---|
| `emissions` | façade가 **보낸** 페이지와 그 다이제스트 (주장 아님) | façade 세션 상태 |
| `DeliveryReconciliation` | rollout 수신분에서 `served`를 **유일하게** 파생 | 신규 순수 모듈 |
| `verified_cli_versions` | 어떤 codex 버전에서 이 파생을 신뢰하는가 | 설정/상수 1곳 |

`challenge` · `ack` · 새 도구 op는 **도입하지 않는다.** 도구 스키마 변경 0, 호출 예산 증가 0.

## 5. 8개 실패 분류에 대한 처분

1. **증거 신원** — 재조정이 `session_id`·`cwd`·`git.commit_hash`로 결속. 다른 디스패치의 rollout은 불일치로 탈락. **한 곳 검사**
2. **보냄 vs 받음** — 이 설계의 본론. `served`가 **수신분**에서만 파생된다. **표현 불가**
3. **완전성** — 기존 완전 커버리지 투영 유지 + 페이지 단위 온전 존재 확인. **표현 불가**
4. **모든 결과 계량** — 기존 예산 기계 그대로(변경 없음). **기존 상태 유지**
5. **종결 상태** — 기존 latch 그대로. **기존 상태 유지**
6. **에러 채널** — 변경 없음(재조정은 워커에게 아무것도 안 보낸다). **기존 상태 유지**
7. **재개·재사용** — 재조정 산출이 영수증이고, 재사용 키가 이미 영수증을 본다. **한 곳 검사**
8. **한 규칙 두 선언** — `served`의 생산자를 **하나로 줄인다**(현재는 서빙 시점). 순증 0. **개선**

## 6. 단계 계획

| 단계 | 내용 | 검증 |
|---|---|---|
| **1** | rollout 리더 (순수 모듈): 파일 특정·`session_meta` 검증·레코드 선별. 소비자 없음 | 실 rollout 아티팩트 고정본으로 단위 테스트 |
| **2** | 재조정기 (순수 함수): `emissions` × 수신분 → `served`. 여전히 미배선 | 실 아티팩트 replay — 이미 **알려진 답**이 있다(§1-1 두 실행) |
| **3** | 배선: façade가 `emissions` 기록, 종료 후 재조정이 영수증 생산. **opt-in 뒤** | OFF byte-identical · 라이브 N=1 |
| **4** | 버전 게이트: `cli_version` 화이트리스트 + probe 재실행 절차 | 미지 버전에서 fail-closed 확인 |

**단계 2가 이 설계의 강점이다** — 실 rollout 두 개에 대해 **정답을 이미 안다**(어느 태그가 소실됐는지
독립 측정됨). 즉 재조정기는 라이브 없이 **결정론적으로 반증 가능**하다.

## 7. 해결하지 못하는 것

- **배달 ≠ 주의.** 바이트가 컨텍스트에 들어갔음을 증명할 뿐, 모델이 읽었음이 아니다.
- **버전 취약성.** 문서화되지 않은 내부 아티팩트에 의존한다. 완화는 버전 고정 + fail-closed이지 제거가 아니다.
- **codex 이후의 변형.** 컨텍스트 진입 후의 압축·축약은 보이지 않는다.
- **인용의 타당성.** 인용이 주장을 실제로 뒷받침하는지는 이 층 밖이다.
- 원자 프레임(A)을 **대체하지 않는다.** 안 잘리게 하는 것이 여전히 1차 방어이고, F는 그 실패의 탐지기다.

## 9. 교차검증 반영 — 초안이 틀린 곳 (2026-07-28)

리뷰어 B(`gpt-5.6-sol`/max, 교차 계열)의 **부분 리뷰**. 지정 형식(VERDICT/FINDINGS/…)을 따르지
않고 356자 요약만 반환했으므로 **부분으로 계상**하고 clean으로 세지 않는다. 그럼에도 3건 전부
구체적이고, 그중 2건은 초안을 바꾼다. 리뷰어 A(`claude-fable-5`)는 두 차례 요청에도 판정문을
반환하지 않았다 — **NOT-RUN**(동일 계열 바닥이므로 교차 계열 1벌은 유지됨).

### F3 — 충돌 전 사망이 fail-open이었다 (BLOCKER · 실코드 확인함)

초안 §3은 "재조정 전 사망 → 영수증 미기록 → ∅"이라고 썼다. **거짓이다.**
`facade-server.ts:114`가 매 응답마다 `session.commit()`으로 영수증을 **증분 기록**하고,
스키마 `observation-read-facade-receipt/v2`에 **완료 표식이 없다.** 따라서 재조정이 돌지 않으면
**emit 기반 영수증이 그대로 권위로 읽힌다.**

**처방(구조로)**: façade는 `served` 영수증을 **아예 쓰지 않는다.** 실행 중에는 `emissions`를 별도
스키마로 기록하고, `served` 영수증(v3)은 **재조정만** 쓴다. 소비자는 v3만 받는다.
→ 사망 시 유효 영수증이 존재하지 않으므로 ∅. 규약이 아니라 **스키마로** 막힌다.
→ 부수 효과: `served`의 생산자가 하나라는 성질이 파일 수준에서도 성립한다.

### F2 — 새 생산자가 기존 누적 규칙을 물려받아야 한다 (BLOCKER)

재조정이 파트 인정 규칙을 **다시 유도하면**, 실제로 보지 못한 중간 구간을 "완전 수신"으로
조립할 수 있다. 파트 신원은 `part_allowance` 기반이며(이 트랙에서 `part_count`로 두 번 헛짚은
바로 그 자리), 재조정은 그 파티션 규칙을 **재사용**해야 한다.
→ 이것이 8번 실패 분류 그 자체다. 처방: 파티션 규칙을 **한 함수에서만** 유도하고 façade와
재조정이 **같은 것을 import**한다. 재조정 안에 파트 산술을 다시 쓰지 않는다.

### F1 — 순서 짝짓기는 즉시 어긋난다 (MATERIAL · 주 세션도 사전 지목)

한 `exec` 안에 MCP 호출이 여러 개면 순서 짝짓기가 깨진다. 실제로 관측된 호출 패턴이다.

**처방: 짝짓기를 없앤다.** 우리가 묻는 것은 "이 페이지 본문이 모델 컨텍스트에 들어갔는가" 하나뿐이다.
→ 전사본의 **모든 수신분 레코드를 통틀어** 각 페이지 본문의 온전 존재를 확인한다. 레코드 대응이
필요 없다. 다른 경로로 본문이 컨텍스트에 들어갔더라도 **들어간 것은 사실**이므로 판정은 여전히 옳다.

## 8. 남은 측정

1. **직접 MCP 호출 경로가 있는가.** 모델이 exec을 거치지 않고 도구를 부를 수 있다면 exec 출력 상한을
   피할 수 있고, 절단 문제 자체가 축소된다. **우선순위 최상** — 그 경우 A의 상수가 크게 완화된다.
2. **exec 출력 상한의 정확한 값과 단위.** 수신분이 양쪽 다 ≈40,490자였다 — 상수인지 확인.
3. **절단 비결정성**(10 §2). F가 있으면 블로커는 아니지만 마진 산정에 필요.
4. `custom_tool_call_output`과 `mcp_tool_call_end`의 **짝짓기 규칙** — call_id 공간이 다르다(exec 스코프 vs
   도구 호출). 현재는 순서 의존이며, 다중 호출이 한 exec 안에 섞일 때의 규칙이 미확정.
5. **rollout 영속을 끄는 플래그**가 프로덕션 경로에 유입될 수 있는가(현재 `--ephemeral` 미사용).
6. **flush 타이밍** — 워커 종료 시점에 rollout이 완전히 기록됐는가.
