MATERIAL FINDINGS: 6

F1 | high | src/core-runtime/reconstruct/observation-read-grant.ts:637 | Cross-request partitions can forge a “whole observation” receipt
FAILURE PATH: Fetch a large observation alone, yielding part 1/2 → fetch the same observation within a 16-id request, yielding parts 1/3 and 2/3 → the grant retains the first `part_count=2` and unions bare indexes `{1,2}` → `observationIdsServed` authorizes the id although part 3 and the body tail were never served.
EVIDENCE: `observation-read.ts:725-750` makes part allowance depend on the whole id list; `observation-read.ts:758-780` recomputes that partition per request; `observation-read-grant.ts:637-645` preserves the first count while merging later indexes; `observation-read-facade.ts:846-850` treats the resulting record as complete. A read-only probe produced solo `1/2`, grouped `1/3` and `2/3`, then authorized `{part_count:2, part_indexes:[1,2]}` with 315 of 129,462 body characters missing.
FALSIFIED PREMISE: The round-4 repair assumed numeric part indexes from separate requests describe one canonical partition; the reader guarantees stability only within the same request’s id set.
FIX: Bind completeness to one decomposition—either make splitting request-independent or track a decomposition fingerprint and never union indexes across fingerprints.

F2 | high | src/core-runtime/reconstruct/observation-read-facade-server.ts:84 | One response callback commits later, undelivered calls
FAILURE PATH: Pipeline calls A and B while stdout is backpressured → both mutate the shared session before callback A → callback A invokes the no-argument `session.commit()` → the receipt includes B → kill the facade before B’s delivery callback, and runtime read-back admits evidence from B although B was not delivered under the callback-based contract.
EVIDENCE: `observation-read-facade-server.ts:71-89` accepts further lines while callbacks are pending; `observation-read-facade.ts:503` mutates served state immediately; `observation-read-facade.ts:525-526` snapshots nothing; `observation-read-facade.ts:600-607` serializes current global state. A Node ordering probe handled A, B, terminal, and after-terminal before any callback; callback A observed the final state. `observation-read-facade.test.ts:625-648` exercises only one call and therefore misses this interleaving.
FALSIFIED PREMISE: The commit split assumed session state could not advance between producing one response and that response’s stdout callback.
FIX: Capture an immutable receipt snapshot per response and publish that snapshot from its callback; later callbacks then publish later snapshots in FIFO order.

F3 | medium | src/core-runtime/reconstruct/observation-read-facade-server.ts:96 | `drain`-triggered exit can preempt receipt commits
FAILURE PATH: Produce a response larger than stdout’s high-water mark → client closes stdin before its write callback → `closeAfterFlush` waits for `drain` → Node emits `drain` before the per-write callback → `process.exit()` runs and the delivered response is never committed, so runtime rejects legitimate citations from the stale opening receipt.
EVIDENCE: `observation-read-facade-server.ts:96-105` exits directly from `drain`; the local stdout high-water mark is 65,536 bytes while a full page is additionally duplicated in the MCP result. A read-only Writable probe returned `["drain/exit","write-callback/commit"]`. The real-process test ends stdin immediately at `observation-read-facade.test.ts:805-807` but requests only a small record at `:821-824`, so it does not force backpressure.
FALSIFIED PREMISE: `closeAfterFlush` assumed `drain` means all write callbacks have completed; Node orders `drain` before those callbacks.
FIX: Track pending response callbacks and exit only after each has committed, or close stdout with a completion callback; do not use `drain` as delivery completion.

F4 | medium | src/core-runtime/reconstruct/observation-read-facade.ts:576 | Grant expiry is terminal but does not close the facade
FAILURE PATH: The injected or real clock reaches the grant TTL → `#resolve` latches expiry and rejects before either meter → `#failureResult` omits `grant_expired` from `isSpent` → the shell remains open and can return unlimited unmetered expiry responses.
EVIDENCE: `observation-read-grant.ts:680-685` explicitly defines expiry as latched terminal state; `observation-read-facade.ts:573-576` recognizes only call and fetch exhaustion; `observation-read-facade-server.ts:88` closes only on `isSpent`. The new test at `observation-read-facade.test.ts:651-664` reaches only call-limit exhaustion.
FALSIFIED PREMISE: The terminal-latch repair assumed exactly two failure reasons make every later call a refusal; the grant itself defines expiry as a third.
FIX: Centralize terminal-reason classification and include `grant_expired` (and `grant_revoked` if exposed through this session).

F5 | medium | src/core-runtime/reconstruct/observation-read-facade-server.ts:71 | Already-buffered calls outrun the terminal latch
FAILURE PATH: Send 32 admitted refusals, one terminal refusal, and N more calls in one stdin chunk → readline handles every line before stdout callbacks → the terminal call sets `isSpent`, but the N following calls are still handled → each produces an unmetered terminal reply before callback-driven close.
EVIDENCE: `observation-read-facade-server.ts:71-89` neither checks `isSpent` before dispatch nor stops input when it flips; `observation-read-facade.ts:543-576` returns another terminal response without incrementing exhausted meters. A Node readline probe handled all four sample frames before any callback. The new latch test invokes the session directly and never exercises shell closure.
FALSIFIED PREMISE: The shell repair assumed the terminal response callback would run before another input frame could be handled.
FIX: Mark the shell closing immediately when handling creates a spent session, pause/detach stdin, and discard subsequent buffered frames while the terminal response drains.

F6 | high | src/core-runtime/reconstruct/authored-artifact-reuse.ts:445 | Resume bypasses the new answer-claims boundary
FAILURE PATH: An opt-in run authored pre-fix claims whose cited cluster contains observation A but whose supporting ref names approved observation B → the run is resumed after the repair with identical inputs/model/flag → the reuse hash is unchanged because the new rule is parser code, not prompt text or a reuse-key field → `writeFreshAuthoredYamlDocument` returns the old artifact without calling `writeMaturationAnswerClaims` → the current direct-authority validator accepts it.
EVIDENCE: The fresh gate is only at `direct-call-directive-author.ts:3643-3684`; the reuse key contains the static prompt hash and existing boolean only at `authored-artifact-reuse.ts:359-376`; matching provenance returns directly at `authored-artifact-reuse.ts:445-472`; the run routes claims through that reusable wrapper at `run.ts:4063-4075`. `maturation-validation.ts:3438-3466` validates cluster existence/mode but not supporting-ref membership. A pure validator probe with an outside-cluster direct-authority ref returned `validation_status:"valid"` with no violations.
FALSIFIED PREMISE: The earlier reuse design assumed every change to author-output admissibility would rotate the prompt/input reuse identity; this runtime-only gate does not.
FIX: Add a claims-author output-contract version to the reuse match and bump it for this boundary so pre-fix claims cannot be reused.

CLAIMS REFUTED: “commit() after the stdout callback means the crash window only under-records” — F2; “observationIdsServed admits whole observations only” — F1.

CLAIMS CONFIRMED: `chargeRejectedCall`’s removed postcondition was algebraically redundant with its synchronous exact-cost precheck at `observation-read-grant.ts:576-585`; validating a nonempty receipt token followed by strict equality proves the expected token is that same nonempty string at `observation-read-facade.ts:795-797`; preparation removes and verifies absence of preexisting receipts at `observation-read-facade.ts:139-155`, while a racing reappearance is independently rejected by token binding; fresh-path OFF behavior remains all-source resolution, as pinned by `answer-claims-evidence-boundary.test.ts:172-183`; the shared ASCII pattern makes schema code-point and runtime UTF-16 lengths equal for every admissible id.

CHECKED AND CLEAN: All five consolidation removals preserve their defended property: expected-token validity follows from nonempty-file-token plus strict equality; the rejected-call postcondition repeated the same expression and could not catch NaN/negative inputs either; the inline rest destructure removes exactly `observation_read_facade`, with other keys pinned at `observation-read-pull.test.ts:436-448`; the whole-list membership rejection precedes the all-source resolver at `direct-call-directive-author.ts:3675-3729`; and the shared `+` pattern subsumes both removed blank-id declarations. The failure history correctly retains eight leading entries plus the latest and increments only for overwritten intermediates at `observation-read-facade.ts:585-596`; nested served-record validation rejects malformed records at `:806-814`. Lifecycle trace: death before facade opening leaves no receipt and runtime derives an empty served set; death after the opening write leaves a valid zero-served receipt; death after a sequential served/refused mutation but before its callback leaves the previous receipt, under-recording that page or failure/meter; after its callback, atomic temp-write/rename leaves the current state; a sequential terminal callback commits before normal close; missing, torn, malformed, or wrong-token read-back becomes an empty served set. Exceptions are F2-F5. Negative-control sensitivity: reinserting eager receipt writing inside `callTool` would fail the byte-unchanged assertion at `observation-read-facade.test.ts:645`, but moving server commit ordering or introducing pipelining would not; deleting the call-limit latch branch fails `:663`, while deleting the fetch-budget branch leaves that test green. The untracked claims test genuinely catches removal of the fresh ON gate and protects OFF behavior, but has no resume arm for F6.
