---
deliberation_status: performed
participation:
  expected_lenses:
    - logic
    - coverage
    - semantics
  received_lenses:
    - logic
    - coverage
    - semantics
  missing_or_failed_lenses: []
  run_status: full
  synthesis_run_status: full
---
# Synthesize

## Consensus
- issue-003 (high): The window-close predicate now - windowStartedAt < WINDOW_MS in notification-batcher.ts mixes two different time origins — windowStartedAt stamped from the internal Date.now() wall clock versus caller-supplied now, with enqueuedAt as a third unbound origin — and no concept expressed in the system designates which time source is the authority. Deliberation resolved the issue with severity HIGH confirmed and logic's severity abstention discharged from in-session evidence; the required closure is to designate exactly one time authority and declare the single-time-source contract in the doc comment.
  - root cause: The API design is asymmetric — flush receives time as a caller parameter while enqueue stamps windowStartedAt from ambient Date.now() and enqueuedAt arrives as unvalidated caller input — with no injected clock or shared single-time-source concept designated anywhere in the component.
  - materiality: The affected purpose is correctness and verifiability of window-close behavior under the declared review goals and the session's explicitly requested time-source-semantics focus, and the defect breaks both arms. Correctness: any caller or test whose time source for flush(now) differs from the internal Date.now() wall clock — fake timer, monotonic-derived value, cached timestamp — is within the exported contract, and cross-origin skew then makes the window-open predicate permanently true (flush returns [] forever while notifications accumulate indefinitely) or closes the window prematurely; the permanent-[] arm is in-target maximal and silent because it is indistinguishable from the documented 'window still open'. Verifiability: logic verified in-boundary that no caller-controllable input assignment determines the close predicate, since windowStartedAt binds to ambient Date.now() outside the API input space, so window behavior cannot be verified deterministically and a supported execution path cannot achieve the declared batching purpose. Deliberation confirmed HIGH from in-session evidence: under bounded_partial closure the exported flush(now) signature itself defines the supported caller (possibility-based materiality criterion), so the divergent-time-source caller is within-contract by derivation rather than empirical caller observation; logic's abstention (insufficient_evidence) was discharged on that contract framing plus the in-boundary verifiability proof and the requested time-source focus, coverage accepted the calibration refinement, both rebuttal slots expired unused, and no disagreement remains.
  - action: Designate one time authority: either inject a clock in the constructor and derive windowStartedAt, enqueuedAt defaults, and the flush judgment from it, or pass the same source to enqueue as to flush (enqueue(item, now)); in both arms declare the single-time-source contract in the doc comment. Problem framing classifies this fix_now with closure obligation must_close_in_target because the split-authority condition blocks the session's explicitly requested time-source focus and deterministic verification now; fix_now is the only projected action candidate. Ordering matters twice: deliberation resolved this as one decision unit with issue-008 (duplicates), so the two should close together; and shared_cause_candidate dep-001 (rel-004) ties the absent time-authority concept to issue-010, so one injected clock that stamps enqueuedAt internally and drives windowStartedAt and the flush judgment resolves both issues without making them the same root — which favors the injected-clock arm.
- issue-008 (high): The exported flush(now) signature in notification-batcher.ts asserts caller-owned current-time authority while windowStartedAt is always stamped from the system clock at enqueue, and no contract text declares the required clock-domain identity, so the window-close predicate (now - windowStartedAt < WINDOW_MS) silently violates the declared contract 'Flushes when the window has closed' for any caller honoring the visible signature with a non-system clock: with now far below real epoch milliseconds the window never closes, with now above it the window closes immediately, and no error surfaces either way. Deliberation resolved with severity HIGH confirmed, all three lenses accepting the ledgered root, no remaining disagreement, and the issue adjudicated identically with issue-003 as one decision unit per duplicates rel-003.
  - root cause: The time authority was split at the design point: enqueue independently stamps windowStartedAt from Date.now() while flush externalizes the clock as a caller parameter, and no contract text declares the required clock-domain identity.
  - materiality: The affected purpose is correctness of the declared flush contract under the session's explicitly requested time-source semantics focus. The failure condition is recorded contract-level per the E2 condition adopted in deliberation: any caller honoring the visible signature contract with a non-system clock — frozen or fake test clock, simulated or logical time. The impact is never-flush or always-flush behavior with no surfaced error, so that caller cannot achieve the declared batching purpose at all. Severity HIGH holds because the supported-caller premise was discharged in-session as a corollary of the signature-as-contract adjudication owned by semantics: within a bounded review the supported caller is defined by the published API surface, and the absence of a clock-domain restriction claim is itself the undocumented-meaning defect; logic's insufficient_evidence abstention was discharged on exactly this ground (E1-E4) and coverage independently kept high from its own observation of the same predicate.
  - action: Unify the time authority by one of two arms: drop the now parameter and use Date.now() inside flush as well, or accept the clock externally everywhere — enqueue(item, now) or a constructor-injected clock — and stamp windowStartedAt from that same source; in either arm, document the single clock domain on the class contract. Problem framing classifies this fix_now with closure obligation must_close_in_target and timing current_blocker, since the silent contract violation blocks the session's requested time-source focus now; the single action candidate is fix_now. Ordering matters: deliberation resolved issue-008 identically with issue-003 as one decision unit per duplicates rel-003, so the authority arm should be chosen once and applied to both issues together rather than as divergent edits, and the contract-documentation step depends on which arm is chosen.
- issue-001 (medium): The documented flush() carry-over invariant in notification-batcher.ts — 'notifications enqueued between the window close check and this reset are carried into the next window' — is logically incompatible with the realized reset semantics: pending.clear() unconditionally removes every entry, digests are built from the same live map that is then cleared, and no path snapshots or re-enqueues, so the claim set is unsatisfiable in every execution where the carry-over antecedent holds. All three lenses accept the ledgered root and keep severity medium; deliberation status is no-deliberation-needed, and closure means making exactly one of the two ledgered claims true.
  - root cause: The flush() reset was implemented as whole-map pending.clear() plus windowStartedAt nulling with no snapshot/swap step that could realize the documented carry-over, so the comment-declared invariant never had a realizing mechanism.
  - materiality: The affected purpose is contract-vs-behavior consistency of the documented flush() carry-over invariant under the correctness and regression_risk review goals, and this issue is a false safety claim inside that contract: notifications the doc says are carried are in fact included in the current digest or silently dropped. Today the claim is a dead claim describing a nonexistent mechanism, but the failure condition arms in any execution model where the carry-over antecedent becomes reachable — reentrancy, a future await inserted between the close check and the reset, shared-state usage — entered by a maintainer trusting the documented guarantee, at which point the impossible promise becomes silent notification loss, changing trust in the documented contract for the declared review purpose.
  - action: Make exactly one claim true: either realize the carry-over by swapping the map before building digests — build digests from the old map so late enqueues land in the fresh map — or correct the comment to state actual behavior (in-between enqueues are impossible in single-threaded synchronous use and would be included in the current digest or dropped, not carried). Problem framing classifies this fix_now with closure obligation must_close_before_next_stage because the silent-loss event arms only when the execution model evolves (async/await insertion, reentrancy), gating the next evolution step; projected action candidates are fix_before_release and fix_now. Ordering matters twice: issue-004 and issue-009 carry the same contradiction (duplicates noted by all lenses), so the realize-vs-correct choice should be adjudicated once for the issue-001/004/009 decision unit; and shared_cause_candidate dep-002 (rel-005) ties the destructive in-place reset to issue-005, so decoupling digest construction from wholesale state destruction — the snapshot/swap arm — addresses both issues without making them the same root.
- issue-004 (medium): The comment-declared carry-over in the flush() reset path of notification-batcher.ts — 'notifications enqueued between the window-close judgment and the reset are carried into the next window' — has no corresponding implementation element: pending.clear() unconditionally empties the whole map, no path separates digest snapshot from differential preservation, and in current synchronous single-threaded execution the described interleaving cannot occur at all, so the comment states an unrealizable behavior as contract. All three lenses accept the ledgered root and keep severity medium; deliberation status is no-deliberation-needed, and closure means aligning doc and behavior.
  - root cause: An implementation concept matching the documented carry-over — a distinction between digest snapshot and differential preservation — was never present in the reset path, so the comment states an unrealizable behavior as contract.
  - materiality: The affected purpose is contract-vs-behavior consistency of the flush reset contract under the regression_risk and maintainability review goals, and this issue plants a false guarantee exactly where future evolution will rely on it: the failure condition is a maintainer trusting the comment and making flush async or inserting an await between the close judgment and the reset. At that point notifications enqueued in between are dropped without warning instead of carried over — the documented carry-over guarantee inverts into silent notification drop — weakening trust in the documented contract for the declared review purpose; that async-evolution regression risk is what justifies medium, kept by all three lenses.
  - action: Align doc and behavior by making one side true: either remove only the entries emitted as digests — snapshot then differential preservation — implementing the carry as documented, or correct the comment to actual behavior (everything is cleared; in-between enqueues are impossible in synchronous execution); the raising lens marked the comment correction as the minimum in-target closure. Problem framing classifies this fix_now with closure obligation must_close_before_next_stage because the documented guarantee inverts into silent loss when a maintainer makes flush async while trusting the comment, gating the next evolution step; projected action candidates are fix_before_release and fix_now. Ordering matters: per the problem framing this is the coverage-absence framing of the same root defect as issue-001 and issue-009 (duplicate links, context only), so the implement-vs-correct choice should be decided once for that decision unit rather than as divergent per-issue edits.
- issue-005 (medium): flush() in notification-batcher.ts destroys all pending state — pending.clear() and windowStartedAt = null — before the caller can confirm delivery of the returned digests, making the component structurally at-most-once without declaring it: any delivery failure after flush returns loses the whole window's notifications with no recovery path. Coverage raised it alone, semantics supports the declare-the-contract arm, logic is not_applicable by boundary routing; severity medium is kept, the issue is material, and deliberation status is no-deliberation-needed.
  - root cause: The notification lifecycle was designed one-way with state destruction ahead of delivery confirmation: flush clears pending and nulls the window before returning digests, and no post-flushed lifecycle concept (ack, commit, reinjection, dead-letter) exists in the component.
  - materiality: The affected purpose is reliability of notification delivery semantics within the declared correctness and regression_risk goals. The failure condition is any delivery failure after flush returns — a process crash or send API error while the caller delivers the returned digests — because state destruction precedes delivery confirmation. When that fires, whole-window notification loss is unrecoverable and the at-most-once property is undeclared, meaningfully weakening trust and decision quality for the declared purpose; coverage judged the undeclared at-most-once substantive for the regression_risk goal and kept severity medium.
  - action: The proposed action keeps both arms offered by the raising lens: introduce a two-phase interface (peek/commit separating digest production from state removal) or a failure-reinjection API to represent lifecycle completion; or, if at-most-once is the intended semantics, declare that contract explicitly in the doc comment — semantics' narrow stance supports this declare-the-contract arm as a documented-meaning gap and defers the lifecycle-design arm to the raising lens. Problem framing classifies this needs_decision with closure obligation must_close_before_next_stage and timing next_step_blocker: the loss path is reachable in current operation but does not block the declared happy path, and the product-semantics decision must be settled before consumers depend on delivery guarantees. Action candidates are fix_before_release or accept_risk. Ordering matters via dep-002 (rel-005): issue-001 shares the destructive in-place reset — pending.clear() plus windowStartedAt = null with no snapshot/swap step and no commit/ack boundary — as a shared_cause_candidate, so decoupling digest construction from wholesale state destruction addresses both issues without making them the same root.
- issue-009 (medium): The flush() reset comment in notification-batcher.ts asserts a carry-over mechanism the code does not implement: digests are built by iterating the live pending map and pending.clear() then unconditionally drops every entry, so an interleaved enqueue would be silently lost, not carried over — and because flush is synchronous single-threaded JS, the described interleaving cannot occur at all, making the comment a vacuous scenario presented as a handled case. All three lenses (logic, coverage, semantics) accept the ledgered root and keep severity medium; deliberation status is no-deliberation-needed.
  - root cause: The comment encodes an intended concurrency contract (carry-over of late enqueues) that was never realized in the chosen reset strategy of building digests from the live pending map and then clearing the whole map.
  - materiality: This issue is material against the declared regression_risk and maintainability review goals: the comment documents a reset/carry-over contract the code does not honor, so maintainers trusting the documented guarantee inherit a false safety claim. The danger arms precisely when flush evolves — if flush becomes async or an await lands before the reset, evolution guided by the documented contract produces silent notification loss, which is exactly the failure mode the comment claims is already handled, meaningfully weakening trust in the result for the declared review purpose.
  - action: The ledgered action menu offers two arms: either correct the comment to state actual behavior (everything pending at flush time is emitted and cleared; no carry-over path exists; flush is synchronous so no interleaving is possible), or implement the stated contract by snapshotting the digested entries and deleting only those keys, leaving later enqueues in pending. Problem framing classifies this fix_now with closure obligation must_close_before_next_stage, since contract truth gates any future async/await evolution of flush. Ordering matters: duplicate links rel-001/rel-002 tie this issue to issue-001 and issue-004 as the same root defect, and the logic stance recommends adjudicating issue-001/004/009 as one decision unit so the comment correction and the reset-strategy choice land once and consistently rather than as three divergent edits.
- issue-010 (medium): PendingNotification.enqueuedAt in notification-batcher.ts is documented as a batcher-owned event-derived value ('epoch ms when the notification entered the batch') but is modeled as unverified independent caller input: enqueue never stamps it, nothing validates it, and no consumer in the component reads it — the digest uses only userId and body. This is the derived-value-as-independent-input pattern. Applicable lenses (coverage, semantics) accept the ledgered root and keep severity medium; logic is not_applicable by boundary routing; deliberation status is no-deliberation-needed, and closure requires choosing the field's contract among the ledgered arms.
  - root cause: enqueuedAt was placed in the caller-facing input type instead of being stamped by the component that owns the enqueue event, so its documented event-derived meaning ('epoch ms when the notification entered the batch') has no enforcement point and no consumer.
  - materiality: The declared purpose is verifiability and maintainability of the PendingNotification contract within the bounded target, and this issue defeats both: because the documented event-derived meaning has no enforcement point and no consumer, any caller-supplied value — correct or drifted — is indistinguishable, so contract truth is unverifiable and drift is unbounded, depending entirely on caller discipline. The type contract additionally misleads readers into assuming per-notification time semantics that batching decisions do not actually have.
  - action: The ledgered action menu offers three arms: have enqueue stamp the timestamp itself (accept {userId, body} and construct the pending record internally from the unified clock), or remove the field from the contract if genuinely unused, or re-document it as caller-asserted creation time that does not affect batching. Problem framing classifies this needs_decision with closure obligation must_close_before_next_stage: the field's contract must be chosen among these arms before further evolution. Dependency ordering matters: dep-001 (shared_cause_candidate with issue-003, via rel-004) identifies one missing time-authority concept behind both issues — a single injected clock that stamps enqueuedAt internally and drives windowStartedAt and the flush judgment would resolve both without merging them — so the stamp-internally arm should ride issue-003's single-clock remediation rather than introducing a second time mechanism.

## Conditional Consensus
- See material issue entries whose problem framing or deliberation status is narrowed or unresolved.

## Disagreement
- none

## Deliberation Decision
- issue-003: resolved
- issue-008: resolved
- issue-001: no-deliberation-needed
- issue-004: no-deliberation-needed
- issue-005: no-deliberation-needed
- issue-009: no-deliberation-needed
- issue-010: no-deliberation-needed

## Axiology-Proposed Additional Perspectives
- none

## Purpose Alignment Verification
- issue-003: Correctness and verifiability of window-close behavior (declared review goals; requested time-source semantics focus)
- issue-008: Correctness of the declared contract 'Flushes when the window has closed' under the session's time-source semantics focus Source finding context: Correctness of the declared flush contract under the session's time-source semantics focus
- issue-001: Contract-vs-behavior consistency of the documented flush() carry-over invariant (correctness, regression_risk review goals)
- issue-004: Contract-vs-behavior consistency of the flush reset contract (regression_risk, maintainability review goals)
- issue-005: Reliability of notification delivery semantics within the declared correctness and regression_risk goals
- issue-009: Contract-vs-behavior consistency of the documented reset/carry-over semantics (regression_risk, maintainability review goals)
- issue-010: Verifiability and maintainability of the PendingNotification contract within the bounded target

## Final Review Result
7 material issue(s) require attention. Highest-priority issue: issue-003 (high) — The window-close predicate now - windowStartedAt < WINDOW_MS in notification-batcher.ts mixes two different time origins — windowStartedAt stamped from the internal Date.now() wall clock versus caller-supplied now, with enqueuedAt as a third unbound origin — and no concept expressed in the system designates which time source is the authority. Deliberation resolved the issue with severity HIGH confirmed and logic's severity abstention discharged from in-session evidence; the required closure is to designate exactly one time authority and declare the single-time-source contract in the doc comment. Controlled deliberation did not leave a blocking unresolved disagreement for the highest-priority issue.

## Boundary Notes
- none

## Immediate Actions Required
- issue-003 (high): fix_now
- issue-008 (high): fix_now
- issue-001 (medium): fix_before_release, fix_now
- issue-004 (medium): fix_before_release, fix_now
- issue-005 (medium): fix_before_release, accept_risk
- issue-009 (medium): fix_before_release, fix_now
- issue-010 (medium): fix_before_release, accept_risk

## Recommendations
- issue-002 (low): flush()'s documented equivalence 'an empty array means the window is still open' is false in the no-window state: the windowStartedAt === null guard also returns [], so realized behavior is the strictly weaker disjunction. Source finding context: notification-batcher.ts flush() return-value contract Source finding context: notification-batcher.ts:28 (claim C4) vs notification-batcher.ts:31 (no-window guard returns []) Source finding context: C4 asserts that an empty array means the window is still open, but flush also returns [] when windowStartedAt === null, a reachable state (fresh instance, or immediately after any successful flush) in which no window exists; the documented equivalence is false in that state and realized behavior is the strictly weaker disjunction. Source finding context: Logic lens found the doc equivalence over-universal versus the no-window guard; rated low because the state invariant lets callers distinguish the two [] cases via pendingUserCount(), so the defect is surface-only. Source finding context: Weaken the doc claim to 'an empty array means the window is still open or no window has started', or return a discriminated result instead of overloading []. Source finding context: round1/logic.md#finding-l3
- issue-006 (low): windowStartedAt is one global value started by any user's first enqueue while collection and digest are per-user, so the effective batch window of a user who joins shortly before the global close converges to zero. Source finding context: notification-batcher.ts window granularity (global windowStartedAt) Source finding context: notification-batcher.ts line 92 (global windowStartedAt), line 96 (any user's first enqueue starts window), lines 91 and 110 (per-user collection/digest) Source finding context: window 개념에 per-user 하위 세분화가 부재하다: windowStartedAt은 전역 단일 값이고 어느 사용자든 첫 enqueue가 시작시키는 반면 수집·digest는 per-user여서, 윈도 종료 직전 합류한 사용자의 배치 창은 0에 수렴한다 (surface-only). Source finding context: Coverage lens surface-only absence: per-user window (or an explicit contract that a global window is intended) is missing, so late-joining users get a near-zero batch window, diverging from the per-user digest batching purpose. Source finding context: per-user window를 도입하거나, 전역 윈도가 의도임을 계약(doc comment)에 명시한다. Source finding context: round1/coverage.md#cov-4
- issue-007 (low): The time dimension is lost in the digest output: enqueuedAt is declared but unused by the digest body join, so after flush no consumer can reconstruct when each notification entered the batch. Source finding context: notification-batcher.ts digest output (time dimension) Source finding context: notification-batcher.ts line 85 (enqueuedAt declared) vs line 111 (digest body join does not use it) Source finding context: digest 산출물에서 시간 차원이 소실된다: enqueuedAt은 선언만 되고 digest 생성에서 사용되지 않아, flush 이후에는 각 알림이 언제 적재됐는지 재구성할 수 없다 (surface-only). Source finding context: Coverage lens surface-only absence: the temporal evidence of each notification is dropped from the artifact, leaving no history for time-dependent values after flush. Source finding context: digest 생성 시 enqueuedAt을 산출물에 포함하거나, 시간 차원이 불필요하다면 enqueuedAt 선언을 제거해 계약을 실제 사용과 일치시킨다. Source finding context: round1/coverage.md#cov-5
- issue-011 (low): flush names an unconditional operation but behaves as a conditional poll that returns [] and does nothing while the window is open; the contract text ('Callers poll this') and the name disagree inside the same declaration. Source finding context: notification-batcher.ts flush() naming vs poll behavior Source finding context: notification-batcher.ts lines 101-107 Source finding context: flush names an unconditional operation (conventional meaning: force pending contents out now) but behaves as a conditional poll that returns [] and does nothing while the window is open; the doc comment ('Callers poll this') admits the poll semantics, so the contract text and the name disagree inside the same declaration (surface-only). Source finding context: Semantics lens surface-only naming mismatch between conventional API vocabulary and realized conditional-poll behavior. Source finding context: Rename to poll() / tryFlush() / flushIfClosed(), or keep flush as a true force-flush and move the window check to the caller side. Source finding context: round1/semantics.md#semantics-4
- issue-012 (low): [] is a homonym for two distinct states — no active window (windowStartedAt === null) and window open but not yet closed — while the doc names only the second; a polling caller cannot distinguish 'nothing was ever enqueued' from 'wait longer'. Source finding context: notification-batcher.ts flush() empty-array return signal Source finding context: notification-batcher.ts line 104 (doc: an empty array means window still open), lines 106-107 (two distinct early returns) Source finding context: [] is a homonym for two distinct states — no active window (windowStartedAt === null) and window open but not yet closed — while the doc names only the second; a polling caller cannot distinguish 'nothing was ever enqueued' from 'wait longer' (surface-only). Source finding context: Semantics lens surface-only finding: two component states share one return signal and the documented meaning is narrower than the behavior. Source finding context: Document both meanings of [], or expose the state distinctly (e.g., return null when no window is active, or add a windowOpen() accessor). Source finding context: round1/semantics.md#semantics-5

## Unique Finding Tagging
- issue-002 (low): flush()'s documented equivalence 'an empty array means the window is still open' is false in the no-window state: the windowStartedAt === null guard also returns [], so realized behavior is the strictly weaker disjunction. Source finding context: notification-batcher.ts flush() return-value contract Source finding context: notification-batcher.ts:28 (claim C4) vs notification-batcher.ts:31 (no-window guard returns []) Source finding context: C4 asserts that an empty array means the window is still open, but flush also returns [] when windowStartedAt === null, a reachable state (fresh instance, or immediately after any successful flush) in which no window exists; the documented equivalence is false in that state and realized behavior is the strictly weaker disjunction. Source finding context: Logic lens found the doc equivalence over-universal versus the no-window guard; rated low because the state invariant lets callers distinguish the two [] cases via pendingUserCount(), so the defect is surface-only. Source finding context: Weaken the doc claim to 'an empty array means the window is still open or no window has started', or return a discriminated result instead of overloading []. Source finding context: round1/logic.md#finding-l3
- issue-006 (low): windowStartedAt is one global value started by any user's first enqueue while collection and digest are per-user, so the effective batch window of a user who joins shortly before the global close converges to zero. Source finding context: notification-batcher.ts window granularity (global windowStartedAt) Source finding context: notification-batcher.ts line 92 (global windowStartedAt), line 96 (any user's first enqueue starts window), lines 91 and 110 (per-user collection/digest) Source finding context: window 개념에 per-user 하위 세분화가 부재하다: windowStartedAt은 전역 단일 값이고 어느 사용자든 첫 enqueue가 시작시키는 반면 수집·digest는 per-user여서, 윈도 종료 직전 합류한 사용자의 배치 창은 0에 수렴한다 (surface-only). Source finding context: Coverage lens surface-only absence: per-user window (or an explicit contract that a global window is intended) is missing, so late-joining users get a near-zero batch window, diverging from the per-user digest batching purpose. Source finding context: per-user window를 도입하거나, 전역 윈도가 의도임을 계약(doc comment)에 명시한다. Source finding context: round1/coverage.md#cov-4
- issue-007 (low): The time dimension is lost in the digest output: enqueuedAt is declared but unused by the digest body join, so after flush no consumer can reconstruct when each notification entered the batch. Source finding context: notification-batcher.ts digest output (time dimension) Source finding context: notification-batcher.ts line 85 (enqueuedAt declared) vs line 111 (digest body join does not use it) Source finding context: digest 산출물에서 시간 차원이 소실된다: enqueuedAt은 선언만 되고 digest 생성에서 사용되지 않아, flush 이후에는 각 알림이 언제 적재됐는지 재구성할 수 없다 (surface-only). Source finding context: Coverage lens surface-only absence: the temporal evidence of each notification is dropped from the artifact, leaving no history for time-dependent values after flush. Source finding context: digest 생성 시 enqueuedAt을 산출물에 포함하거나, 시간 차원이 불필요하다면 enqueuedAt 선언을 제거해 계약을 실제 사용과 일치시킨다. Source finding context: round1/coverage.md#cov-5
- issue-011 (low): flush names an unconditional operation but behaves as a conditional poll that returns [] and does nothing while the window is open; the contract text ('Callers poll this') and the name disagree inside the same declaration. Source finding context: notification-batcher.ts flush() naming vs poll behavior Source finding context: notification-batcher.ts lines 101-107 Source finding context: flush names an unconditional operation (conventional meaning: force pending contents out now) but behaves as a conditional poll that returns [] and does nothing while the window is open; the doc comment ('Callers poll this') admits the poll semantics, so the contract text and the name disagree inside the same declaration (surface-only). Source finding context: Semantics lens surface-only naming mismatch between conventional API vocabulary and realized conditional-poll behavior. Source finding context: Rename to poll() / tryFlush() / flushIfClosed(), or keep flush as a true force-flush and move the window check to the caller side. Source finding context: round1/semantics.md#semantics-4
- issue-012 (low): [] is a homonym for two distinct states — no active window (windowStartedAt === null) and window open but not yet closed — while the doc names only the second; a polling caller cannot distinguish 'nothing was ever enqueued' from 'wait longer'. Source finding context: notification-batcher.ts flush() empty-array return signal Source finding context: notification-batcher.ts line 104 (doc: an empty array means window still open), lines 106-107 (two distinct early returns) Source finding context: [] is a homonym for two distinct states — no active window (windowStartedAt === null) and window open but not yet closed — while the doc names only the second; a polling caller cannot distinguish 'nothing was ever enqueued' from 'wait longer' (surface-only). Source finding context: Semantics lens surface-only finding: two component states share one return signal and the documented meaning is narrower than the behavior. Source finding context: Document both meanings of [], or expose the state distinctly (e.g., return null when no window is active, or add a windowOpen() accessor). Source finding context: round1/semantics.md#semantics-5

## Shared Phenomenon Summary
- none
