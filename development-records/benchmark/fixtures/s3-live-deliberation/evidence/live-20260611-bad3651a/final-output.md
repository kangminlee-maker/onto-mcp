---
session_id: 20260611-bad3651a
process: review
target: "notification-batcher.ts"
domain: none
date: 2026-06-11
---

## 9-Lens Review Result

### Review Target
- `notification-batcher.ts`

### Verification Context
- Domain: none
- Review mode: core-axis
- Execution realization: direct-call
- Host runtime: openai
- Artifact generation realization: live
- Semantic quality evidence: not_evaluated (real_semantic_path_only)
- Finding ledger: `.onto/review/20260611-bad3651a/finding-ledger.yaml`
- Issue ledger: `.onto/review/20260611-bad3651a/issue-ledger.yaml`
- Problem framing: `.onto/review/20260611-bad3651a/problem-framing.yaml`
- Controlled deliberation: `.onto/review/20260611-bad3651a/deliberation.md`
- Source artifact: `.onto/review/20260611-bad3651a/synthesis-ledger.yaml`
- Synthesis projection: `.onto/review/20260611-bad3651a/synthesis.md`
- Execution status: completed

### Domain Selection
- Explicit no-domain token was provided; runtime will run without domain documents.

### Final Review Result
#### Review Basis
- Execution status: completed
- Deliberation status: performed
- Participating lenses: 3/3
- Degraded lenses: none
- Halt reason: none

#### Synthesis Summary
8 material issue(s) require attention. Highest-priority issue: issue-001 (high) — Window start and window close are judged on two different time authorities: enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now, and no same-clock precondition is declared. The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 internal-clock window start, R3 caller-clock close check} is jointly satisfiable only under the undeclared premise that the caller's now comes from the same clock as Date.now(), so the documented flush close obligation is violable over the full declared input domain of flush(now). All three lenses accepted the recorded root and kept severity high; the required fix is to unify the time authority and declare the clock-source contract. Controlled deliberation did not leave a blocking unresolved disagreement for the highest-priority issue.

#### Classification Summary
- Highest severity: high
- Severity counts: blocker=0, high=4, medium=4, low=0, info=0
- Finding count: 13
- Root-cause issue count: 8
- Material issue count: 8
- Non-material finding count: 0

#### Material Issues
- issue-001 (high)
  - affected purpose: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability)
  - failure condition: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources
  - impact: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading
  - evidence: `round1/logic.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:27-28`, `notification-batcher.ts:30-32`
  - source lenses: logic
  - action candidates: fix_now
  - problem definition: Window start and window close are judged on two different time authorities — enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now — with no declared same-clock precondition, so the documented flush close obligation is violable over the full declared input domain of flush(now).
  - problem framing: symptom / current_blocker / fix_now / must_close_in_target / observed

- issue-002 (high)
  - affected purpose: correctness (no silent notification loss) and regression_risk (declared review goals)
  - failure condition: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue
  - impact: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing
  - evidence: `round1/logic.md#finding-2`, `notification-batcher.ts:32`, `notification-batcher.ts:34-36`, `notification-batcher.ts:37-39`
  - source lenses: logic
  - action candidates: fix_before_release, accept_risk
  - problem definition: The documented check-to-reset preservation property has no implementing mechanism — the guarantee is written at item granularity while the only reset operates at window granularity (whole-map pending.clear()), so the documented invariant is false today and loss activation is refactor-conditional, with no target-internal evidence locating the defect on the comment side versus the code side.
  - problem framing: independent_issue / next_step_blocker / needs_decision / must_close_before_next_stage / observed

- issue-004 (high)
  - affected purpose: correctness review goal — window-closure contract of the enqueue/flush pair / verifiability review goal — trustworthiness of the documented entry-time record
  - failure condition: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) / any caller supplying an inaccurate, stale, or fabricated enqueuedAt, whose divergence from actual entry time is unbounded and undetectable within the target Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target
  - impact: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases / downstream consumers (ordering, age-based expiry, audit) that trust the documented entry-time semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp coexists with no designated canonical source Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source
  - evidence: `round1/coverage.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `round1/coverage.md#finding-3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:19-24`
  - source lenses: coverage
  - action candidates: fix_now
  - problem definition: The design never designates a single canonical time authority for the batcher; the enqueue/flush clock split (internal Date.now() vs caller-supplied now) and the inert, caller-trusted enqueuedAt field beside the internally stamped windowStartedAt are two manifestations of that one missing designation, structurally unified at the line-20 Date.now() re-measurement and falsifiable by one injected clock owning every timestamp in the file.
  - problem framing: root_cause / current_blocker / fix_now / must_close_in_target / inferred

- issue-006 (high)
  - affected purpose: correctness (declared review goal) — window-duration semantics of the flush contract
  - failure condition: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent
  - impact: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly
  - evidence: `round1/semantics.md#f1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `notification-batcher.ts:8`
  - source lenses: semantics
  - action candidates: fix_now
  - problem definition: Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract; the expression now - windowStartedAt subtracts an internally sourced Date.now() value from a caller-supplied number whose time source is never declared, so the window-duration semantics are unguaranteed over the declared input domain and the file's caller-supplied epoch-ms precedent (enqueuedAt) makes a divergent caller clock an in-contract scenario.
  - problem framing: symptom / current_blocker / fix_now / must_close_in_target / observed

- issue-003 (medium)
  - affected purpose: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals)
  - failure condition: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely
  - impact: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics
  - evidence: `round1/logic.md#finding-3`, `notification-batcher.ts:28`, `notification-batcher.ts:31`, `notification-batcher.ts:40`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - problem definition: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single [] output while the doc defines only one meaning for it, so the documented meaning of [] is false on every idle-state flush(now) call with declared-legal inputs — a today-exercised, clock-independent contract falsity with interpretive-only standalone harm.
  - problem framing: independent_issue / next_step_blocker / fix_now / must_close_before_next_stage / observed

- issue-005 (medium)
  - affected purpose: contract-vs-behavior consistency and regression_risk (declared review goals)
  - failure condition: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset
  - impact: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites
  - evidence: `round1/coverage.md#finding-2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:32`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - problem definition: The reset mechanism was written at window granularity while the documented carry-over guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code, so the declared sub-behavior is absent from the system.
  - problem framing: independent_issue / next_step_blocker / needs_decision / must_close_before_next_stage / observed

- issue-007 (medium)
  - affected purpose: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset
  - failure condition: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism
  - impact: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated
  - evidence: `round1/semantics.md#f2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:34`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk
  - problem definition: The documented check-to-reset preservation property has no implementing mechanism — pending.clear() guarantees the complementary property (pending becomes empty) at the same program point, so implement-or-retract is the complete fix space — and the comment describes a scenario the current synchronous code neither encounters nor handles; a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss.
  - problem framing: independent_issue / next_step_blocker / needs_decision / must_close_before_next_stage / observed

- issue-008 (medium)
  - affected purpose: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals
  - failure condition: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time
  - impact: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps
  - evidence: `round1/semantics.md#f3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:20`
  - source lenses: semantics
  - action candidates: follow_up, accept_risk
  - problem definition: A derivable value (windowStartedAt) is modeled as an independent input from a second clock while its source field (enqueuedAt) is kept inert, leaving two un-unified authorities for the one meaning "time the notification entered the batch"; the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source.
  - problem framing: symptom / planned_follow_up / needs_decision / may_close_during_next_stage / observed

#### Synthesized Material Issue Explanations
- issue-001 (high): Window start and window close are judged on two different time authorities: enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now, and no same-clock precondition is declared. The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 internal-clock window start, R3 caller-clock close check} is jointly satisfiable only under the undeclared premise that the caller's now comes from the same clock as Date.now(), so the documented flush close obligation is violable over the full declared input domain of flush(now). All three lenses accepted the recorded root and kept severity high; the required fix is to unify the time authority and declare the clock-source contract.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: Window start and window close are judged on two different time authorities — enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now — and no same-clock precondition is declared, so the claim set {C5, C6, R1, R3} is jointly satisfiable only under an undeclared premise and the documented flush close obligation is violable over the full declared input domain of flush(now). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:27-28, notification-batcher.ts:30-32 Source finding context: The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 "window start sampled from internal Date.now()", R3 "window close judged against caller-supplied now"} is jointly satisfiable only under an undeclared necessary premise that the caller's now is sampled from the same clock as Date.now(); over the full declared input domain of flush(now: number) the set is not satisfiable. Source finding context: The logic lens found an inter-claim contradiction: enqueue binds the window-start time authority to the process wall clock while flush judges window close against an arbitrary caller-supplied number, and the contract never declares a same-clock precondition. This makes the documented flush close obligation (C5/C6) violable with declared-legal inputs, directly against the review intent focus on time-source semantics and the correctness/verifiability goals. Source finding context: Unify the time authority: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() in flush, or (c) inject a single clock function at construction used by both methods. In all variants, declare the clock-source contract on the API doc. Source finding context: round1/logic.md#finding-1 Source finding context: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability) Source finding context: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources Source finding context: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading Source finding context: window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition Source finding context: Split time authority is introduced: window start is bound to the process wall clock at enqueue while the close check is parameterized on a caller clock value. Source finding context: No same-clock precondition is declared on flush(now), so the allowed input domain includes clock values that diverge from the window-start authority. Source finding context: For a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] which the contract defines as "window still open" — the documented close obligation is violated.
  - affected purpose: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability)
  - failure condition: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources
  - impact: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading
  - root hypothesis: Window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition, so the flush close obligation is violable with declared-legal inputs.
  - evidence: `round1/logic.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:27-28`, `notification-batcher.ts:30-32`, `finding-ledger.yaml#finding-001`, `finding-001.cause-001`, `notification-batcher.ts:30`, `finding-001.cause-002`, `notification-batcher.ts:26-30`, `finding-001.cause-003`, `notification-batcher.ts:31-32`, `issue-stance-matrix.yaml#stances.issue-001.logic`, `issue-ledger.yaml#issue-001`, `issue-stance-matrix.yaml#stances.issue-001.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-001.semantics`, `round1/semantics.md#f1`, `notification-batcher.ts:8`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_now
  - materiality: The issue directly weakens the declared review goals of correctness and verifiability of the flush contract. The failure path is occupiable today with declared-legal inputs: any flush caller whose now is not sampled from the same wall clock that enqueue used (deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources) triggers it. The impact is that digests are never emitted for the affected window (a perpetual [] that the contract says means "window still open") or are emitted before the window elapses, so the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading. This presently occupiable failure path is the panel's high-severity calibration anchor.
  - root cause: The chain starts where the split time authority is introduced: enqueue binds the window-start time (windowStartedAt) to the process wall clock via Date.now(), while the close check in flush is parameterized on a caller-supplied clock value, and no same-clock precondition is declared anywhere on flush(now). Every downstream behavior follows from this split — without a single designated clock, window start and window close can be measured against diverging timelines, which is exactly what makes the close obligation violable. Deliberation framed this issue as a symptom of the broader issue-004 root (a single undesignated time authority for the window lifecycle), which generalizes but does not contradict this clock-split manifestation.
  - causal path: Step 1 (finding-001.cause-001): the split time authority is introduced — window start is bound to the process wall clock at enqueue while the close check takes a caller clock value. Step 2 (finding-001.cause-002, enabled by step 1): because no same-clock precondition is declared on flush(now), the allowed input domain includes clock values that diverge from the window-start authority. Step 3 (finding-001.cause-003, caused by step 2): for a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] — which the contract defines as "window still open" — and the documented close obligation is violated. One question stays open beyond the evidence: whether any production caller currently supplies a non-Date.now() clock is outside the bounded single-file target. Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics all support, accept the recorded root, and keep high severity, with no remaining disagreement.
  - action: Unify the time authority so window start and window close are judged on the same clock, choosing one of three variants: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() inside flush, or (c) inject a single clock function at construction that both methods use. In all variants, declare the clock-source contract on the API doc. Closure class is fix_now with closure obligation must_close_in_target: this is a current correctness blocker because the failure path is occupiable with declared-legal inputs today. Ordering matters in two ways recorded upstream: this clock-split defect enables issue-003's persistent misreading of [] (rel-005), and the generalizing single-time-authority root is tracked in issue-004, so the single-time-authority fix here is the unit that discharges this issue inside the target.

- issue-002 (high): The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point — the code guarantees the negation of the comment's guarantee (C7 vs R5). Today, under the synchronous realization, the documented invariant is false and the guarantee is vacuous and unverifiable; under any async/concurrent/re-entrant realization the behavior becomes silent notification loss, the exact failure the comment claims to exclude. Deliberation adopted a direction-neutral root (the documented preservation property has no implementing mechanism) and fixed severity at medium in one decision for the duplicate cluster issue-002/issue-005/issue-007; closure requires an explicit implement-or-retract decision before the next stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point; the code guarantees the negation of the comment's guarantee, so the documented preservation property is vacuous today and becomes silent notification loss under any async/concurrent realization. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-38, notification-batcher.ts:39 Source finding context: C7 ("Notifications enqueued between the window close check and this reset are carried into the next window") and R5 (flush reset unconditionally clears the entire pending map) cannot both hold in any execution where such an enqueue occurs; the code guarantees the negation of the comment's guarantee. Source finding context: The logic lens found a necessary/necessary inter-claim conflict: the reset comment declares a preservation guarantee for check-to-reset enqueues while pending.clear() makes the complementary property (pending becomes empty) necessary at the same program point. Under the current synchronous realization the guarantee is vacuous and unverifiable; under any async/concurrent realization the actual behavior is silent notification loss, the exact failure the comment claims to exclude — directly against the contract-vs-behavior consistency focus of the review. Source finding context: Make the comment true or make it honest: delete only the flushed entries (snapshot digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window, or rewrite the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. Source finding context: round1/logic.md#finding-2 Source finding context: correctness (no silent notification loss) and regression_risk (declared review goals) Source finding context: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue Source finding context: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing Source finding context: the reset clears the whole pending map instead of removing only the entries that were flushed into the digest Source finding context: Flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Source finding context: Any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Source finding context: The comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with the discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated.
  - affected purpose: correctness (no silent notification loss) and regression_risk (declared review goals)
  - failure condition: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue
  - impact: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing
  - root hypothesis: The flush reset clears the whole pending map instead of removing only the entries that were flushed into the digest, making the documented check-to-reset carry-over guarantee unsatisfiable whenever such an enqueue occurs.
  - evidence: `round1/logic.md#finding-2`, `notification-batcher.ts:32`, `notification-batcher.ts:34-36`, `notification-batcher.ts:37-39`, `finding-ledger.yaml#finding-002`, `finding-002.cause-001`, `notification-batcher.ts:39`, `finding-002.cause-002`, `finding-002.cause-003`, `notification-batcher.ts:37-38`, `issue-stance-matrix.yaml#stances.issue-002.logic`, `issue-ledger.yaml#issue-002`, `issue-stance-matrix.yaml#stances.issue-002.coverage`, `round1/coverage.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-002.semantics`, `round1/semantics.md#f2`, `notification-batcher.ts:34`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, accept_risk
  - materiality: The issue weakens the declared correctness goal (no silent notification loss) and the regression_risk goal. The failure condition is any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue. The impact is silent notification loss while the in-code documentation asserts the opposite guarantee, so future maintainers relying on the documented invariant will ship the loss without noticing. Severity was resolved at medium rather than high under the panel's impact-reachability calibration: the check-to-reset antecedent is unoccupiable with declared-legal inputs in the bounded synchronous target (unlike issue-001's input-reachable clock split), so the present harm is a false documented invariant — a verifiability/maintainability harm — with loss activation conditional on a future refactor.
  - root cause: The deliberated, direction-neutral root adopted by all three lenses is that the documented check-to-reset preservation property has no implementing mechanism: the guarantee is written at item granularity while the only reset operates at window granularity (whole-map pending.clear()). This is the starting point because the contradiction is structural — as long as the only reset is an unconditional whole-map clear, no execution where a check-to-reset enqueue occurs can satisfy the comment. Which side is defective (a missing carve-out in the reset versus a comment encoding unbuilt intent) is unverifiable within the bounded target, which is exactly why the root is stated direction-neutrally and the implement-or-retract action space is preserved.
  - causal path: Step 1 (finding-002.cause-001): flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Step 2 (finding-002.cause-002, caused by step 1): any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Step 3 (finding-002.cause-003, caused by step 2): the comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with that discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated. Beyond the evidence, whether any concurrent or async caller exists today is outside the bounded single-file target. Deliberation resolved the panel: logic conceded severity from high to medium by adopting the impact-reachability calibration and accepted the direction-neutral root as more faithful to the symmetric C7-vs-R5 result; coverage moved from the clear()-side-defective hypothesis to the same restatement; semantics' remaining blocker — one severity decision across the duplicate cluster — was discharged by fixing issue-002, issue-005, and issue-007 at medium in this single decision, with no remaining disagreement.
  - action: Make the comment true or make it honest. Either implement the documented property — delete only the flushed entries (snapshot the digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window — or retract it by rewriting the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. The ordering dependency is explicit in the framing: closure is gated by this implement-or-retract decision (closure_class needs_decision), because which side is defective is unverifiable within the bounded target, and the issue must close before the next stage (must_close_before_next_stage) — it blocks any async or re-entrant refactor of flush rather than current operation. The recorded action candidates are fix_before_release and accept_risk.

- issue-004 (high): The batcher never designates a single canonical time authority, and that one missing designation produces two coupled defects: the enqueue/flush clock split (window start stamped from internal Date.now() while window close is decided against a caller-supplied now with no declared time base) and the inert enqueuedAt field (declared as the batch-entry record yet never stamped or read by enqueue, which re-measures entry time from its own clock at line 20). Deliberation resolved this as the session's root issue at high severity with all three lenses accepting the full merged root; the fix is one designated clock owning every timestamp in the file, and it must close in target now.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: coverage, semantics, logic
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: No single time authority is designated anywhere in the batcher: the window lifecycle is governed by two parallel, unreconciled clocks (Date.now() in enqueue vs caller-injected now in flush), and the entry-time record is declared on enqueuedAt yet never stamped or read by the entry-owning method, which re-measures entry time from its own clock — one undesignated-authority root with two surface manifestations whose fixes are coupled. Source finding context: NotificationBatcher window-closure time base (enqueue/flush pair) Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window lifecycle is governed by two parallel, unreconciled time sources — an internal wall clock (Date.now() in enqueue) and a caller-injected now (flush) — and the system designates no single time authority nor declares any contract that the two share one time base; window closure can therefore fail in both directions (never closes, or closes immediately). Source finding context: The coverage lens checked the time-authority dimension of the window lifecycle and found a dimension deficit in an element that exists: time is managed in two places with no single source of authority designated. The request summary explicitly scopes time-source semantics, and the review-target-profile obligations classify a visible runtime-contract failure inside the bounded target as material against the correctness goal; this mismatch is fully visible within the file. Source finding context: Designate a single time authority: (a) inject time everywhere via a constructor-injected clock used by both enqueue and flush, or (b) own time internally by having flush() take no parameter and use Date.now(). If the injected-now polling API must stay, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a minimum stopgap. Source finding context: round1/coverage.md#finding-1 Source finding context: correctness review goal — window-closure contract of the enqueue/flush pair Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases Source finding context: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) without designating which clock is canonical Source finding context: The design splits time authority across the API boundary: testability-style injection in flush, hard-coded Date.now() in enqueue, with no canonical clock designated. Source finding context: flush externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock. Source finding context: The closure predicate now - windowStartedAt subtracts values from two unreconciled time bases, so window closure can fail in both directions (never closes, or closes immediately). Source finding context: PendingNotification.enqueuedAt entry-time record Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:19-24 Source finding context: The field contract declares enqueuedAt as "epoch ms when the notification entered the batch", but the method that owns batch entry (enqueue) neither stamps nor validates it; the entry-time record is caller-trusted, and entry time is managed in two parallel places (per-item enqueuedAt, per-window windowStartedAt via Date.now()) with no single authority designated. Source finding context: The coverage lens found that the temporal audit dimension exists nominally on the data shape but lacks the enforcement mechanism: the field doc asserts a fact about an event only enqueue can witness, yet enqueue never writes or checks the value. This matters under the verifiability review goal — any downstream consumer trusting the documented semantics builds on a record no component guarantees. Source finding context: Assign stamping authority to the entry owner: have enqueue set enqueuedAt itself (from the single clock chosen in the finding-005 fix), changing the input type to omit the field (Omit<PendingNotification, "enqueuedAt">); or, if callers must supply it, change the doc to "caller-asserted creation time" and stop claiming it records batch entry. Source finding context: round1/coverage.md#finding-3 Source finding context: verifiability review goal — trustworthiness of the documented entry-time record Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source Source finding context: the data shape and the behavior were specified independently; the contract names an event-derived value without designating which component has authority to derive it Source finding context: The data shape and the behavior were specified independently: the contract names an event-derived value (batch-entry time) without designating which component has authority to derive it. Source finding context: The field's semantics are declared on the interface but the stamping responsibility was never assigned to the entry-owning method: enqueue neither writes nor checks item.enqueuedAt. Source finding context: The entry-time record is caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority.
  - affected purpose: correctness review goal — window-closure contract of the enqueue/flush pair / verifiability review goal — trustworthiness of the documented entry-time record
  - failure condition: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) / any caller supplying an inaccurate, stale, or fabricated enqueuedAt, whose divergence from actual entry time is unbounded and undetectable within the target Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target
  - impact: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases / downstream consumers (ordering, age-based expiry, audit) that trust the documented entry-time semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp coexists with no designated canonical source Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source
  - root hypothesis: The design never designates a single canonical time authority for the batcher; the enqueue/flush clock split (internal Date.now() vs caller-supplied now) and the inert, caller-trusted enqueuedAt field beside the internally stamped windowStartedAt are two manifestations of that one missing designation. Falsifiable: a fix that injects one clock owning every timestamp in the file should dissolve both defects; if either survives such a fix, the common-root claim is wrong.
  - evidence: `round1/coverage.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `round1/coverage.md#finding-3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:19-24`, `finding-ledger.yaml#finding-005`, `finding-ledger.yaml#finding-007`, `rel-007`, `finding-relation-graph.yaml#rel-007`, `finding-005.cause-001`, `notification-batcher.ts:30`, `finding-005.cause-002`, `finding-005.cause-003`, `notification-batcher.ts:32`, `finding-007.cause-001`, `finding-007.cause-002`, `finding-007.cause-003`, `issue-stance-matrix.yaml#stances.issue-004.logic`, `issue-ledger.yaml#issue-004`, `round1/logic.md#finding-1`, `round1/logic.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-004.coverage`, `issue-stance-matrix.yaml#stances.issue-004.semantics`, `round1/semantics.md#f1`, `round1/semantics.md#f3`, `issue-ledger.yaml#dep-001`, `rel-006`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_now
  - materiality: The issue weakens both declared review purposes at once. Against the correctness goal, the window-closure contract of the enqueue/flush pair is input-reachable today: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) makes the closure subtraction mix two unreconciled time bases, so window closure fails in both directions — never closes (notifications never delivered) or closes immediately. Against the verifiability goal, the documented entry-time record is caller-trusted with unbounded, undetectable divergence from actual entry time, so downstream consumers (ordering, age-based expiry, audit) build on a record no component guarantees while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source. High severity holds via the input-reachable clock-split manifestation and was never contested by any lens.
  - root cause: The accepted root is that the design never designates a single canonical time authority for the batcher. It is the starting point because both surface defects are downstream of the same missing designation rather than of each other: the clock split exists because flush externalizes time as a bare number while enqueue hard-codes Date.now() with no canonical clock declared, and the inert enqueuedAt exists because the contract names an event-derived value without designating which component has authority to derive it. The two legs are structurally unified at the line-20 Date.now() re-measurement, which is simultaneously the internal window-start authority and the divergent second authority in both legs. The root is inferred from the two observed manifestations and is falsifiable as stated: one injected clock owning every timestamp in the file (including enqueuedAt stamping by the entry owner) should dissolve both defects; if either survives such a fix, the common-root claim is wrong. Per deliberation, the clock-split leg carries a formal logic contradiction (finding-001) while the enqueuedAt leg stands on coverage/semantics observational evidence — an evidence-scope annotation, not counter-evidence against the merge.
  - causal path: Two causal paths, both raised by the coverage lens, converge on the missing designation. Finding-005: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) with no canonical clock designated; flush therefore externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock; the closure predicate now - windowStartedAt then subtracts values from two unreconciled time bases, so window closure can fail in both directions (lines 20, 30-32). Finding-007: the data shape and the behavior were specified independently — the contract names batch-entry time without designating who derives it (lines 8-9); the stamping responsibility was never assigned to the entry-owning method, so enqueue neither writes nor checks item.enqueuedAt (lines 19-24); the entry-time record is therefore caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority. Relation rel-007 merges these two paths into this issue as two manifestations of one root.
  - action: Designate a single time authority owning every timestamp in the file: inject a constructor-supplied clock used by both enqueue and flush (or have flush own time internally via Date.now()), and assign enqueuedAt stamping to the entry owner from that same clock — for example by changing the input type to omit the field — or, if callers must supply it, re-document it as caller-asserted creation time and stop claiming it records batch entry. At minimum, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a stopgap. Ordering matters in two ways: the two manifestations have coupled fixes, so they must be closed by the same single-clock designation rather than patched separately, and because this is the session's root issue, the same fix closes the symptom issues (issue-001/issue-006 clock-split statements) in target. The dep-001/rel-006 shared-cause context with issue-008 is preserved as cross-issue dependency context — the chosen clock should also govern that issue's resolution — but it is dependency context, not merge evidence. Closure obligation is fix_now, must close in target.

- issue-006 (high): Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the closure expression now - windowStartedAt subtracts an internally sourced Date.now() value (stamped in enqueue at line 20) from a caller-supplied number whose time source is never declared (flush, lines 30-32). Because the file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), a divergent caller clock is an in-contract scenario, not a hypothetical. All three lenses support the issue with the root accepted and high severity kept; deliberation was skipped as no-deliberation-needed (consistent stances). It is a symptom of the issue-004 undesignated-time-authority root and closes in target via the same single-clock fix, now.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the expression now - windowStartedAt subtracts an internally sourced Date.now() value from a caller-supplied number whose time source is never declared, so the window-duration semantics are not guaranteed by the contract, and the file's caller-supplied epoch-ms precedent (enqueuedAt) makes a divergent caller clock an in-contract scenario. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: The expression now - this.windowStartedAt subtracts an internally sourced Date.now() wall-clock value from a caller-supplied now whose time source is never declared; the name now does not mean "same clock as windowStartedAt", so the window-duration semantics are not guaranteed by the contract. Source finding context: The semantics lens found a definition-explicitness failure: two distinct time concepts — "internal wall-clock now" and "caller-supplied now" — share one name without a unifying contract. The file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), so a caller-side clock distinct from the batcher's internal clock is an in-contract scenario, not a hypothetical; the declared review goal (correctness) is violated by a contract gap. Source finding context: Establish a single time authority: (a) drop the parameter and use Date.now() inside flush, (b) make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or (c) inject a clock dependency used by both methods. In all cases, document the required time source of now (e.g., "epoch ms from the same clock that produced enqueuedAt"). Source finding context: round1/semantics.md#f1 Source finding context: correctness (declared review goal) — window-duration semantics of the flush contract Source finding context: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent Source finding context: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly Source finding context: split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority Source finding context: Window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. Source finding context: The contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Source finding context: The cross-clock subtraction is semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly.
  - affected purpose: correctness (declared review goal) — window-duration semantics of the flush contract
  - failure condition: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent
  - impact: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly
  - root hypothesis: Split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority, making the cross-clock subtraction semantically meaningless.
  - evidence: `round1/semantics.md#f1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `notification-batcher.ts:8`, `finding-ledger.yaml#finding-009`, `finding-009.cause-001`, `finding-009.cause-002`, `notification-batcher.ts:26-29`, `finding-009.cause-003`, `notification-batcher.ts:32`, `notification-batcher.ts:28`, `issue-stance-matrix.yaml#stances.issue-006.logic`, `issue-ledger.yaml#issue-006`, `round1/logic.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-006.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-006.semantics`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_now
  - materiality: The declared correctness review goal is violated at the contract level: the window-duration semantics of flush are not guaranteed over the declared input domain, because the contract documents only the return-value meaning and never the parameter's time source. The failure condition is in-contract today — a caller supplying now from performance.now() (monotonic, origin near process start) or an injected test clock — given the file's caller-supplied epoch-ms precedent. The impact is silent in both directions: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open", and notifications are never delivered; with a skewed or mocked clock the window can close instantly. High severity was kept by all lenses (duplicate cluster with issue-001 via rel-002).
  - root cause: The accepted root, taken as recorded, is the split time authority introduced at line 20 versus line 30: window start is bound to the internal Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. This is the starting point because every observable consequence follows from that split — once the two ends of the window lifecycle answer to different undeclared clocks, the cross-clock subtraction is semantically meaningless regardless of any particular caller behavior. Within the session, this issue is classified as a symptom: the same split generalizes to the issue-004 undesignated-time-authority root, for the same reason as issue-001.
  - causal path: The path runs in three steps on semantics finding-009. First, window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority (lines 20, 30-32). Second — the enabling step — the contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt, line 8) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Third, the cross-clock subtraction becomes semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly. Logic confirms this as the same unsatisfiable claim set it proved in finding-001 (undeclared same-clock premise), and coverage confirms it as the same missing-single-time-authority deficit it raised as finding-005, so the semantic, formal, and coverage statements all pick out one defect.
  - action: Establish a single time authority: drop the parameter and use Date.now() inside flush, make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or inject a clock dependency used by both methods; in all cases document the required time source of now (for example "epoch ms from the same clock that produced enqueuedAt"). The dependency that matters is that this issue is a symptom of the issue-004 root, so it should not receive an isolated patch: the single-clock fix that closes issue-001/issue-004 closes this in target, and the chosen authority must be the same one designated there. Closure obligation is fix_now, must close in target, because the failure path is reachable with declared-legal inputs today.

- issue-003 (medium): The doc defines [] to mean "window still open" (C6), but flush also returns [] in the reachable state windowStartedAt === null in which no window is open, so one return value carries two undistinguished meanings and the definition contradicts the producing rule (R2) in that state. The documented meaning of [] is therefore false on every idle-state flush(now) call with declared-legal inputs — a today-exercised, clock-independent contract falsity whose standalone harm is interpretive-only. Deliberation adjudicated severity at medium; the fix is to either widen the documented meaning of [] or make the two states distinguishable in the signature, keeping C6 consistent with the choice.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: logic, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The doc defines [] to mean "window still open", but flush also returns [] in the reachable state windowStartedAt === null in which no window is open; one return value carries two undistinguished meanings, and under finding-001's clock-skew condition the misreading turns from a transient ambiguity into a permanently misleading caller-facing reading. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: C6 defines the output [] to mean "window still open", but R2 returns [] in the reachable state windowStartedAt === null, in which no window is open; the definition and the producing rule contradict in that state. Source finding context: The logic lens found a definition-vs-rule contradiction (possible-as-necessary modality error): the doc asserts a necessary equivalence ([] iff window still open) while the implementation also produces [] from the reachable no-window state (initial value and after every successful flush). The practical reading coincides today via the state invariant, holding severity at medium, but the declared definition is false in the null state and the misreading becomes persistent under finding-001's clock-skew condition. Source finding context: Widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open") or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"), and keep C6 consistent with whichever is chosen. Source finding context: round1/logic.md#finding-3 Source finding context: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals) Source finding context: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely Source finding context: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics Source finding context: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single output value while the doc defines only one meaning for it Source finding context: Two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Source finding context: The doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict.
  - affected purpose: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals)
  - failure condition: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely
  - impact: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics
  - root hypothesis: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single [] output while the doc defines only one meaning for it, so the definition contradicts the producing rule in the reachable no-window state.
  - evidence: `round1/logic.md#finding-3`, `notification-batcher.ts:28`, `notification-batcher.ts:31`, `notification-batcher.ts:40`, `finding-ledger.yaml#finding-003`, `finding-003.cause-001`, `notification-batcher.ts:32`, `finding-003.cause-002`, `notification-batcher.ts:16`, `issue-stance-matrix.yaml#stances.issue-003.logic`, `issue-ledger.yaml#issue-003`, `issue-stance-matrix.yaml#stances.issue-003.coverage`, `issue-stance-matrix.yaml#stances.issue-003.semantics`, `round1/semantics.md#f5`, `notification-batcher.ts:31-32`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - materiality: The issue weakens the declared goals of verifiability of the caller-facing contract and correctness of caller-side state interpretation. The failure condition is a caller or monitor branching on the documented meaning of [] in the no-window state, or operation under finding-001's clock-skew condition where the misleading reading persists indefinitely. The impact is misclassification of batcher state by polling callers (idle read as batching) and tests written against the documented meaning validating the wrong semantics. Severity is medium: the falsity is exercised today with declared-legal inputs and is independent of the clock defect, but the standalone harm is interpretive-only with no delivery failure, capping it below high; the persistent-misreading escalation under clock skew is priced in issue-001/issue-006, not here.
  - root cause: The chain starts where flush conflates two distinct states — "no window started" and "window open, not yet closed" — into a single [] output while the doc defines only one meaning for that value. This conflation is the root because the doc asserts a necessary equivalence ([] iff window still open) that the producing rule cannot honor: the implementation also yields [] from the reachable no-window state (the initial value and the state after every successful flush), a possible-as-necessary modality error. All lenses accepted this recorded root as-is; deliberation only adjudicated the deferred severity calibration.
  - causal path: Step 1 (finding-003.cause-001): two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Step 2 (finding-003.cause-002, caused by step 1): the doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict. Nothing was left unresolved beyond the evidence. Deliberation (logic and semantics participating) resolved the deferred medium-vs-low calibration at medium: logic answered semantics' falsifying question with a today-reachable harm path that does not route through the clock defect (idle-state flush returns [] while its documented meaning is false at that moment) and conceded the double-counting point that the permanent-misreading escalation belongs to issue-001; semantics then moved low to medium on rubric consistency, since a today-exercised contract falsity cannot grade below the refactor-conditional cluster (issue-002/005/007) held at medium. Both participating lenses' final positions are medium with no remaining disagreement.
  - action: Close the homonym in one of two ways and keep C6 consistent with whichever is chosen: widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open"), or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"). Either option closes the issue without a product-intent decision. Closure class is fix_now with obligation must_close_before_next_stage: it must close before verification-stage work because tests written against the current documented meaning would validate the wrong semantics. The severity escalation under clock skew is handled by the issue-001/issue-006 time-authority fix, so this issue's action is the independent definitional repair; the recorded action candidates are fix_before_release and fix_now.

- issue-005 (medium): The comment at lines 37-38 declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize that guarantee: the reset is a whole-map clear() that removes every entry unconditionally, so the declared sub-behavior is absent from the system. The harm is latent in the current single-threaded synchronous runtime and is activated by exactly the async/concurrent refactor the comment invites. Deliberation resolved the issue at medium severity (the single cluster decision for issue-002/issue-005/issue-007), and it must close before that next stage through one implement-or-retract decision.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: coverage, logic
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize the guarantee; the whole-map clear() removes every entry unconditionally, so the declared sub-behavior is absent from the system. Source finding context: NotificationBatcher.flush reset semantics vs documented carry-over contract Source finding context: notification-batcher.ts:37-39 Source finding context: The comment declares that notifications enqueued between the window-close check and the reset "are carried into the next window", but no mechanism implementing carry-over exists; the actual reset (this.pending.clear()) deletes the entire map, so any such notification would be silently dropped — the declared sub-behavior is absent from the system. Source finding context: The coverage lens found a declared sub-behavior with no implementing mechanism: the system contains no selective-removal, snapshot-then-diff, or re-queue mechanism that could realize the documented carry-over guarantee. The review goals explicitly include contract-vs-behavior consistency and regression_risk; the comment invites a future async/concurrent refactor to rely on a safety property the mechanism does not provide. Source finding context: Make behavior and contract converge: (a) implement the guarantee by snapshotting the digested keys/items and deleting only those, or replacing the map with a fresh one after capturing a reference; or (b) retract the contract by rewriting the comment to state the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). Source finding context: round1/coverage.md#finding-2 Source finding context: contract-vs-behavior consistency and regression_risk (declared review goals) Source finding context: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset Source finding context: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites Source finding context: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code Source finding context: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code. Source finding context: The reset is implemented as a whole-map clear() rather than removal of only the flushed items, so every entry — including any hypothetical late arrival — is removed unconditionally. Source finding context: The declared carry-over guarantee (lines 37-38) is contradicted by the reset: no item present but not yet digested can be "carried into the next window".
  - affected purpose: contract-vs-behavior consistency and regression_risk (declared review goals)
  - failure condition: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset
  - impact: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites
  - root hypothesis: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code.
  - evidence: `round1/coverage.md#finding-2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:32`, `finding-ledger.yaml#finding-006`, `finding-006.cause-001`, `finding-006.cause-002`, `notification-batcher.ts:39`, `finding-006.cause-003`, `notification-batcher.ts:37-38`, `issue-stance-matrix.yaml#stances.issue-005.logic`, `issue-ledger.yaml#issue-005`, `round1/logic.md#finding-2`, `notification-batcher.ts:34-36`, `issue-stance-matrix.yaml#stances.issue-005.coverage`, `issue-stance-matrix.yaml#stances.issue-005.semantics`, `round1/semantics.md#f2`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals explicitly include contract-vs-behavior consistency and regression_risk, and this issue weakens both: the documentation claims a carry-over safety property the mechanism does not provide, and the comment invites a future async/concurrent refactor (for example awaiting a send inside the digest loop) to rely on that property while enqueue can interleave between the close check and the reset. The impact is silent notification loss while documentation claims the opposite. Severity is medium per the impact-reachability calibration: the loss antecedent is modification-conditional — unoccupiable with declared-legal inputs in the bounded synchronous target — unlike the input-reachable clock split, so it blocks the next stage rather than current operation.
  - root cause: The accepted root, taken as recorded, is a granularity mismatch at the point of specification: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity, and the item-granularity concept ("late-enqueued notification") has no representation in the code. This is the starting point because every downstream fact follows from it — a reset conceived per-window can only be a whole-map operation, so no code path could ever distinguish a late-enqueued item from a flushed one, making the documented item-level carry-over unimplementable as written rather than merely unimplemented.
  - causal path: The causal path runs in three observed steps on coverage finding-006. First, the window/item granularity mismatch leaves the late-enqueued-notification concept unrepresented in the code (lines 37-39). Second, because the reset is implemented as a whole-map clear() rather than removal of only the flushed items, every entry — including any hypothetical late arrival — is removed unconditionally (line 39). Third, the declared carry-over guarantee at lines 37-38 is therefore contradicted by the reset: no item present but not yet digested can be "carried into the next window". All three lenses support this path — logic established the same contradiction formally as its finding-002 (the documented invariant is necessarily false in every execution where its antecedent holds), and semantics confirmed the ghost concept reading as its finding-010 — so the absence is directly observed, not inferred. Whether and when flush will actually be refactored to async or called concurrently remains outside the bounded single-file target.
  - action: Make behavior and contract converge through one explicit decision with two candidate paths. Either implement the guarantee before release — snapshot the digested keys/items and delete only those, or swap in a fresh map after capturing a reference — or accept the risk and retract the contract by rewriting the comment to the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). The ordering constraint is that this implement-or-retract decision must close before any async/concurrent refactor of flush, because that refactor is precisely the failure condition; the closure obligation is must_close_before_next_stage. The decision should also stay coherent with the rel-003/rel-004 duplicate cluster (issue-002/issue-007), whose severity was fixed at medium in the same single decision.

- issue-007 (medium): The reset comment in notification-batcher.ts promises that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the map and no snapshot separates entries being digested from later arrivals — the documented preservation property has no implementing mechanism. In the current synchronous code the described interleaving cannot even occur, so the comment is a false documented invariant today, and a later async/re-entrant refactor that relies on it inherits silent notification loss. Severity is medium, set by the single cluster severity decision shared with issue-002 and issue-005, and the issue closes through one implement-or-retract decision that must land before the next refactor stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: logic, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The reset comment states that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() removes all entries unconditionally and no snapshot distinguishes entries being digested from entries that arrived after the check; the comment describes a scenario the current synchronous code neither encounters nor handles, and a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment states that notifications enqueued between the window-close check and the reset "are carried into the next window", but this.pending.clear() unconditionally removes every entry; no carry-over behavior exists in the code. Source finding context: The semantics lens found a contract-vs-behavior inconsistency: the comment's claim requires that some entries survive the reset, but clear() removes all entries present at call time, and the digest loop iterates the same live map so a pre-clear entry is digested in the current flush — the opposite of "carried into the next window". This counts against the declared review goals (correctness, maintainability, regression_risk). Source finding context: Either implement the stated semantics (snapshot the keys/entries to digest, delete only those keys, leaving later arrivals in place) or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Source finding context: round1/semantics.md#f2 Source finding context: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset Source finding context: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism Source finding context: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated Source finding context: the comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code Source finding context: The reset comment declares a carry-over contract for notifications enqueued between the window-close check and the reset. Source finding context: this.pending.clear() removes all entries unconditionally and no snapshot distinguishes "entries being digested" from "entries that arrived after the check" — the contract has no implementing mechanism. Source finding context: In the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles; a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss.
  - affected purpose: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset
  - failure condition: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism
  - impact: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated
  - root hypothesis: The comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code.
  - evidence: `round1/semantics.md#f2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:34`, `finding-ledger.yaml#finding-010`, `finding-010.cause-001`, `notification-batcher.ts:37-38`, `finding-010.cause-002`, `notification-batcher.ts:39`, `finding-010.cause-003`, `notification-batcher.ts:32`, `issue-stance-matrix.yaml#stances.issue-007.logic`, `issue-ledger.yaml#issue-007`, `round1/logic.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-007.coverage`, `round1/coverage.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-007.semantics`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk
  - materiality: The issue counts against three declared review goals — correctness, maintainability, and regression_risk — through a direct contract-vs-behavior inconsistency in the flush reset. The failure condition is modification-conditional: a maintainer makes flush async or re-entrant and relies on the documented carry-over invariant, at which point the described interleaving becomes reachable while the code provides no implementing mechanism. The impact is silent notification loss inherited from a documented invariant the code does not provide; even today the comment describes a scenario the code neither encounters nor handles as stated, which is exactly the kind of misleading contract the declared goals exist to catch.
  - root cause: Deliberation adopted a direction-neutral root for family coherence with issue-002: the documented check-to-reset preservation property has no implementing mechanism, and no target-internal evidence locates the defect on the comment side (unbuilt intent) versus the code side (missing carve-out in the reset) — both intent facts are unverifiable in the bounded target. This is the starting point because every downstream observation follows from the contract/mechanism gap, and logic's necessary/necessary strengthening shows it is not a mere omission: pending.clear() guarantees the complementary property (pending becomes empty) at the same program point where the comment promises survival, so implement-or-retract is the complete fix space.
  - causal path: The causal chain from finding-010 (semantics) runs in three steps. First, the reset comment (notification-batcher.ts:37-38) declares a carry-over contract for notifications enqueued between the window-close check and the reset. Second, this.pending.clear() (line 39) removes all entries unconditionally and no snapshot distinguishes entries being digested from entries that arrived after the check, so the contract has no implementing mechanism — the digest loop iterates the same live map, the opposite of "carried into the next window". Third, in the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles, and a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss. One thing stays beyond the evidence: whether flush will actually become async or re-entrant in real usage is outside the bounded single-file target.
  - action: The accepted fix space is exactly two-sided (implement-or-retract): either implement the stated semantics — snapshot the keys/entries to digest and delete only those keys, leaving later arrivals in place — or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Logic's necessary/necessary observation confirms no third option exists, because the code actively guarantees the complementary property. Ordering matters: the issue is framed as a next-step blocker with closure obligation must_close_before_next_stage, so the decision must land before any async/re-entrant refactor of flush; it is also the third member of the rel-003/rel-004 duplicate cluster, so the choice should stay coherent with the single cluster decision covering issue-002 and issue-005. The projected action candidates are fix_before_release or accept_risk.

- issue-008 (medium): One meaning — "time the notification entered the batch" — exists twice in notification-batcher.ts: as the caller-supplied, documented enqueuedAt field in the exported interface and as the internal Date.now() capture in enqueue(). The two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Severity is medium as recorded; the issue is a symptom of the missing time-authority designation it shares with issue-004 (dep-001/rel-006), kept distinct because the two defects affect different purposes and are independently fixable. Closure needs one product-intent decision: does enqueuedAt drive windowing, or is it payload-only metadata.
  - issue stance agreement: 2/3
  - agreed or narrowed lenses: coverage, semantics
  - issue stance disagreement: 1/3
  - disagreeing stance lenses: logic
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: coverage, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The meaning "time the notification entered the batch" exists twice — as the caller-supplied enqueuedAt field and as the internal Date.now() capture in enqueue() — and the two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,20 Source finding context: The semantics lens found a ghost sub-area and an un-unified synonym pair: a required, documented field in an exported interface asserts behavioral relevance yet is never referenced after declaration, while windowStartedAt — semantically "the first pending notification's batch-entry time" — is modeled as an independent input from a second clock instead of being derived from the field that names the meaning. Source finding context: Pick one source of truth: derive the window anchor from pending items' enqueuedAt (e.g., window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. Source finding context: round1/semantics.md#f3 Source finding context: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals Source finding context: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time Source finding context: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps Source finding context: a derivable value (windowStartedAt) is modeled as an independent input while its source field (enqueuedAt) is kept inert Source finding context: The interface contract declares enqueuedAt as the batch-entry timestamp (required, documented field). Source finding context: enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock — two authorities for one meaning. Source finding context: When a caller backfills or replays notifications (enqueuedAt in the past), the window silently anchors to processing time instead of the declared entry time; the field's name and the system's behavior diverge.
  - affected purpose: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals
  - failure condition: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time
  - impact: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps
  - root hypothesis: A derivable value (windowStartedAt) is modeled as an independent input from a second clock while its source field (enqueuedAt) is kept inert, leaving two authorities for the one meaning "time the notification entered the batch".
  - evidence: `round1/semantics.md#f3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:20`, `finding-ledger.yaml#finding-011`, `finding-011.cause-001`, `finding-011.cause-002`, `finding-011.cause-003`, `issue-stance-matrix.yaml#stances.issue-008.logic`, `issue-ledger.yaml#issue-008`, `round1/logic.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-008.coverage`, `round1/coverage.md#finding-3`, `rel-006`, `issue-stance-matrix.yaml#stances.issue-008.semantics`, `issue-ledger.yaml#dep-001`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: follow_up, accept_risk
  - materiality: The issue weakens the declared goals of correctness and verifiability — callers are misled into believing enqueuedAt drives windowing — and of regression_risk, because duplicate time sources enable undetected drift between the two timestamps. The failure condition is in-contract given the field's documentation: a caller backfills or replays notifications with enqueuedAt in the past, and the window silently anchors to processing time instead of the declared entry time. There is no current delivery failure; the harm is a misleading interface contract plus drift enablement, which is why the framing places it as a planned follow-up rather than a release blocker.
  - root cause: The recorded root was accepted as-is with no deliberation needed: a derivable value (windowStartedAt) is modeled as an independent input from a second clock while its source field (enqueuedAt) is kept inert, leaving two authorities for the one meaning "time the notification entered the batch". This is the starting point because both observable symptoms — the inert documented field and the divergent window anchor — are direct consequences of never designating a single authority for entry time. The surface facts are logic-verified (enqueuedAt is consumed by no rule in the class body; a second internally sampled timestamp anchors the window); logic took a surface_only / insufficient_evidence position as explicit deference, since no declared claim formally ties windowStartedAt to enqueuedAt, and the applicable lenses (coverage, semantics) agree on the root and on medium severity.
  - causal path: The causal chain from finding-011 (semantics) runs in three steps. First, the interface contract declares enqueuedAt as the batch-entry timestamp — a required, documented field (notification-batcher.ts:8-9). Second, enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock (line 20), creating two authorities for one meaning. Third, when a caller backfills or replays notifications with enqueuedAt in the past, the window silently anchors to processing time instead of the declared entry time, so the field's name and the system's behavior diverge. Nothing in the chain remains unresolved beyond the cited evidence.
  - action: Pick one source of truth: either derive the window anchor from pending items' enqueuedAt (e.g., the window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. The dependency that matters is the shared cause with issue-004 (dep-001/rel-006, the undesignated entry-time authority): closure requires the product-intent decision on whether enqueuedAt drives windowing, and the issue may close alongside the coupled issue-004 single-authority fix during the next stage (closure obligation may_close_during_next_stage). The projected action candidates are follow_up or accept_risk.

#### Non-Material Findings
- none

#### Action Candidates
- issue-001: fix_now
  - rationale: All three lenses support with root accepted and high severity kept (no-deliberation-needed, consistent_stances); the {C5, C6, R1, R3} contradiction is established directly from observed code and doc claims, so judgment_state is observed. Classified symptom rather than root_cause because the resolved issue-004 root (single undesignated time authority) was accepted by all lenses as generalizing this clock-split manifestation; rel-005 additionally records that this defect enables issue-003's persistent misreading. The failure path is occupiable with declared-legal inputs today (the panel's high-severity calibration anchor), so it is a current correctness blocker that must close in the target via the single-time-authority fix. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:27-28, notification-batcher.ts:30-32 Source finding context: The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 "window start sampled from internal Date.now()", R3 "window close judged against caller-supplied now"} is jointly satisfiable only under an undeclared necessary premise that the caller's now is sampled from the same clock as Date.now(); over the full declared input domain of flush(now: number) the set is not satisfiable. Source finding context: The logic lens found an inter-claim contradiction: enqueue binds the window-start time authority to the process wall clock while flush judges window close against an arbitrary caller-supplied number, and the contract never declares a same-clock precondition. This makes the documented flush close obligation (C5/C6) violable with declared-legal inputs, directly against the review intent focus on time-source semantics and the correctness/verifiability goals. Source finding context: Unify the time authority: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() in flush, or (c) inject a single clock function at construction used by both methods. In all variants, declare the clock-source contract on the API doc. Source finding context: round1/logic.md#finding-1 Source finding context: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability) Source finding context: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources Source finding context: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading Source finding context: window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition Source finding context: Split time authority is introduced: window start is bound to the process wall clock at enqueue while the close check is parameterized on a caller clock value. Source finding context: No same-clock precondition is declared on flush(now), so the allowed input domain includes clock values that diverge from the window-start authority. Source finding context: For a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] which the contract defines as "window still open" — the documented close obligation is violated.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-002: fix_before_release, accept_risk
  - rationale: Resolved at medium with the direction-neutral root adopted by all three lenses under the impact-reachability calibration. The contract-vs-behavior falsity is directly observed (comment versus unconditional clear() at the same program point), but the loss antecedent is unoccupiable with declared-legal inputs in the bounded synchronous target, so it blocks the async/re-entrant refactor stage rather than current operation. Independent of the time-authority root; duplicate cluster with issue-005/issue-007 (rel-003/rel-004) decided at medium in one decision. Closure is gated by the implement-or-retract decision the resolution explicitly preserved, because which side is defective is unverifiable within the bounded target. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-38, notification-batcher.ts:39 Source finding context: C7 ("Notifications enqueued between the window close check and this reset are carried into the next window") and R5 (flush reset unconditionally clears the entire pending map) cannot both hold in any execution where such an enqueue occurs; the code guarantees the negation of the comment's guarantee. Source finding context: The logic lens found a necessary/necessary inter-claim conflict: the reset comment declares a preservation guarantee for check-to-reset enqueues while pending.clear() makes the complementary property (pending becomes empty) necessary at the same program point. Under the current synchronous realization the guarantee is vacuous and unverifiable; under any async/concurrent realization the actual behavior is silent notification loss, the exact failure the comment claims to exclude — directly against the contract-vs-behavior consistency focus of the review. Source finding context: Make the comment true or make it honest: delete only the flushed entries (snapshot digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window, or rewrite the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. Source finding context: round1/logic.md#finding-2 Source finding context: correctness (no silent notification loss) and regression_risk (declared review goals) Source finding context: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue Source finding context: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing Source finding context: the reset clears the whole pending map instead of removing only the entries that were flushed into the digest Source finding context: Flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Source finding context: Any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Source finding context: The comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with the discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-004: fix_now
  - rationale: Deliberation resolved this as the session's root issue — all three lenses accepted the full merged undesignated-time-authority root spanning the clock-split manifestation (issue-001/issue-006) and the inert-enqueuedAt manifestation (issue-008), with logic's original narrowing recorded as an evidence-scope annotation, not counter-evidence. The root designation itself is an inference from the two observed manifestations (falsifiable by one injected clock), hence inferred. High severity holds via the input-reachable clock-split manifestation, which can break window closure in both directions inside the bounded target, so it is a current correctness blocker whose coupled single-time-authority fix must close in target. The dep-001/rel-006 shared-cause context with issue-008 is preserved as dependency context, not merge evidence. Source finding context: NotificationBatcher window-closure time base (enqueue/flush pair) Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window lifecycle is governed by two parallel, unreconciled time sources — an internal wall clock (Date.now() in enqueue) and a caller-injected now (flush) — and the system designates no single time authority nor declares any contract that the two share one time base; window closure can therefore fail in both directions (never closes, or closes immediately). Source finding context: The coverage lens checked the time-authority dimension of the window lifecycle and found a dimension deficit in an element that exists: time is managed in two places with no single source of authority designated. The request summary explicitly scopes time-source semantics, and the review-target-profile obligations classify a visible runtime-contract failure inside the bounded target as material against the correctness goal; this mismatch is fully visible within the file. Source finding context: Designate a single time authority: (a) inject time everywhere via a constructor-injected clock used by both enqueue and flush, or (b) own time internally by having flush() take no parameter and use Date.now(). If the injected-now polling API must stay, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a minimum stopgap. Source finding context: round1/coverage.md#finding-1 Source finding context: correctness review goal — window-closure contract of the enqueue/flush pair Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases Source finding context: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) without designating which clock is canonical Source finding context: The design splits time authority across the API boundary: testability-style injection in flush, hard-coded Date.now() in enqueue, with no canonical clock designated. Source finding context: flush externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock. Source finding context: The closure predicate now - windowStartedAt subtracts values from two unreconciled time bases, so window closure can fail in both directions (never closes, or closes immediately). Source finding context: PendingNotification.enqueuedAt entry-time record Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:19-24 Source finding context: The field contract declares enqueuedAt as "epoch ms when the notification entered the batch", but the method that owns batch entry (enqueue) neither stamps nor validates it; the entry-time record is caller-trusted, and entry time is managed in two parallel places (per-item enqueuedAt, per-window windowStartedAt via Date.now()) with no single authority designated. Source finding context: The coverage lens found that the temporal audit dimension exists nominally on the data shape but lacks the enforcement mechanism: the field doc asserts a fact about an event only enqueue can witness, yet enqueue never writes or checks the value. This matters under the verifiability review goal — any downstream consumer trusting the documented semantics builds on a record no component guarantees. Source finding context: Assign stamping authority to the entry owner: have enqueue set enqueuedAt itself (from the single clock chosen in the finding-005 fix), changing the input type to omit the field (Omit<PendingNotification, "enqueuedAt">); or, if callers must supply it, change the doc to "caller-asserted creation time" and stop claiming it records batch entry. Source finding context: round1/coverage.md#finding-3 Source finding context: verifiability review goal — trustworthiness of the documented entry-time record Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source Source finding context: the data shape and the behavior were specified independently; the contract names an event-derived value without designating which component has authority to derive it Source finding context: The data shape and the behavior were specified independently: the contract names an event-derived value (batch-entry time) without designating which component has authority to derive it. Source finding context: The field's semantics are declared on the interface but the stamping responsibility was never assigned to the entry-owning method: enqueue neither writes nor checks item.enqueuedAt. Source finding context: The entry-time record is caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-006: fix_now
  - rationale: Duplicate cluster with issue-001 via rel-002 — the semantics statement of the same clock-split defect; all three lenses support with root accepted and high severity kept (no-deliberation-needed). Classified symptom of the issue-004 undesignated-time-authority root for the same reason as issue-001. The cross-clock subtraction is directly observed at lines 20 and 30-32 and the failure path is in-contract today (silent in both directions — perpetual [] read as "window still open", or instant close), so it is a current correctness blocker; the single-clock fix that closes issue-001/issue-004 closes this in target. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: The expression now - this.windowStartedAt subtracts an internally sourced Date.now() wall-clock value from a caller-supplied now whose time source is never declared; the name now does not mean "same clock as windowStartedAt", so the window-duration semantics are not guaranteed by the contract. Source finding context: The semantics lens found a definition-explicitness failure: two distinct time concepts — "internal wall-clock now" and "caller-supplied now" — share one name without a unifying contract. The file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), so a caller-side clock distinct from the batcher's internal clock is an in-contract scenario, not a hypothetical; the declared review goal (correctness) is violated by a contract gap. Source finding context: Establish a single time authority: (a) drop the parameter and use Date.now() inside flush, (b) make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or (c) inject a clock dependency used by both methods. In all cases, document the required time source of now (e.g., "epoch ms from the same clock that produced enqueuedAt"). Source finding context: round1/semantics.md#f1 Source finding context: correctness (declared review goal) — window-duration semantics of the flush contract Source finding context: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent Source finding context: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly Source finding context: split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority Source finding context: Window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. Source finding context: The contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Source finding context: The cross-clock subtraction is semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-003: fix_before_release, fix_now
  - rationale: Both participating lenses converged at medium after deliberation adjudicated the medium-vs-low calibration — a today-exercised contract falsity cannot grade below the refactor-conditional cluster held at medium. The persistent-misreading escalation under clock skew is priced in issue-001/issue-006, not here, so the issue stands as an independent definitional defect. The homonym is directly observed from the doc and the reachable no-window return path. Standalone harm is interpretive (monitors misclassify idle as batching; tests written against the documented meaning validate the wrong semantics), so it must close before verification-stage work; either widening the documented meaning or distinguishing the states in the signature closes it without a product-intent decision. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: C6 defines the output [] to mean "window still open", but R2 returns [] in the reachable state windowStartedAt === null, in which no window is open; the definition and the producing rule contradict in that state. Source finding context: The logic lens found a definition-vs-rule contradiction (possible-as-necessary modality error): the doc asserts a necessary equivalence ([] iff window still open) while the implementation also produces [] from the reachable no-window state (initial value and after every successful flush). The practical reading coincides today via the state invariant, holding severity at medium, but the declared definition is false in the null state and the misreading becomes persistent under finding-001's clock-skew condition. Source finding context: Widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open") or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"), and keep C6 consistent with whichever is chosen. Source finding context: round1/logic.md#finding-3 Source finding context: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals) Source finding context: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely Source finding context: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics Source finding context: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single output value while the doc defines only one meaning for it Source finding context: Two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Source finding context: The doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-005: fix_before_release, accept_risk
  - rationale: Member of the rel-003/rel-004 duplicate cluster with issue-002/issue-007; the cluster severity was fixed at medium in a single decision under the impact-reachability calibration, with logic withdrawing its raise to high. Coverage's absence framing is canonical here — the declared item-granularity sub-behavior is unrepresented in the system — hence impact_kind completeness. The absence is directly observed against the whole-map clear(); harm activates only under the async/concurrent refactor the comment itself invites, so it blocks that next stage rather than current operation and closes through the same implement-or-retract decision before that stage. Source finding context: NotificationBatcher.flush reset semantics vs documented carry-over contract Source finding context: notification-batcher.ts:37-39 Source finding context: The comment declares that notifications enqueued between the window-close check and the reset "are carried into the next window", but no mechanism implementing carry-over exists; the actual reset (this.pending.clear()) deletes the entire map, so any such notification would be silently dropped — the declared sub-behavior is absent from the system. Source finding context: The coverage lens found a declared sub-behavior with no implementing mechanism: the system contains no selective-removal, snapshot-then-diff, or re-queue mechanism that could realize the documented carry-over guarantee. The review goals explicitly include contract-vs-behavior consistency and regression_risk; the comment invites a future async/concurrent refactor to rely on a safety property the mechanism does not provide. Source finding context: Make behavior and contract converge: (a) implement the guarantee by snapshotting the digested keys/items and deleting only those, or replacing the map with a fresh one after capturing a reference; or (b) retract the contract by rewriting the comment to state the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). Source finding context: round1/coverage.md#finding-2 Source finding context: contract-vs-behavior consistency and regression_risk (declared review goals) Source finding context: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset Source finding context: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites Source finding context: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code Source finding context: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code. Source finding context: The reset is implemented as a whole-map clear() rather than removal of only the flushed items, so every entry — including any hypothetical late arrival — is removed unconditionally. Source finding context: The declared carry-over guarantee (lines 37-38) is contradicted by the reset: no item present but not yet digested can be "carried into the next window".
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-007: fix_before_release, accept_risk
  - rationale: Third member of the rel-003/rel-004 duplicate cluster; medium per the single cluster severity decision, with logic's raise withdrawn under the impact-reachability calibration and the direction-neutral root adopted for family coherence with issue-002 (both intent facts are unverifiable in the bounded target). This issue's distinct emphasis is the future maintainer inheriting silent loss from a false documented invariant under an async/re-entrant refactor, hence maintainability_evolvability. The contract falsity is directly observed; activation is refactor-conditional, so it blocks the next refactor stage and closes through the same implement-or-retract decision before that stage. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment states that notifications enqueued between the window-close check and the reset "are carried into the next window", but this.pending.clear() unconditionally removes every entry; no carry-over behavior exists in the code. Source finding context: The semantics lens found a contract-vs-behavior inconsistency: the comment's claim requires that some entries survive the reset, but clear() removes all entries present at call time, and the digest loop iterates the same live map so a pre-clear entry is digested in the current flush — the opposite of "carried into the next window". This counts against the declared review goals (correctness, maintainability, regression_risk). Source finding context: Either implement the stated semantics (snapshot the keys/entries to digest, delete only those keys, leaving later arrivals in place) or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Source finding context: round1/semantics.md#f2 Source finding context: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset Source finding context: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism Source finding context: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated Source finding context: the comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code Source finding context: The reset comment declares a carry-over contract for notifications enqueued between the window-close check and the reset. Source finding context: this.pending.clear() removes all entries unconditionally and no snapshot distinguishes "entries being digested" from "entries that arrived after the check" — the contract has no implementing mechanism. Source finding context: In the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles; a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-008: follow_up, accept_risk
  - rationale: Kept distinct from issue-004 (different affected purposes, independently fixable defects) but classified symptom because the resolved issue-004 root names the inert enqueuedAt field as the second manifestation of the missing time-authority designation (dep-001/rel-006 shared-cause context). The surface facts are directly observed and logic-verified (field consumed by no rule, second internally sampled timestamp); logic's surface_only / insufficient_evidence position is explicit deference, and the applicable lenses agree on root and medium severity. No current delivery failure — the harm is a misleading interface contract and undetected-drift enablement, with backfill/replay as the in-contract failure path — so planned follow-up; closure needs the product-intent decision whether enqueuedAt drives windowing or is payload-only metadata, and may close alongside the coupled issue-004 single-authority fix during the next stage. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,20 Source finding context: The meaning "time the notification entered the batch" exists twice — as the caller-supplied enqueuedAt field and as the internal Date.now() capture in enqueue() — and the two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Source finding context: The semantics lens found a ghost sub-area and an un-unified synonym pair: a required, documented field in an exported interface asserts behavioral relevance yet is never referenced after declaration, while windowStartedAt — semantically "the first pending notification's batch-entry time" — is modeled as an independent input from a second clock instead of being derived from the field that names the meaning. Source finding context: Pick one source of truth: derive the window anchor from pending items' enqueuedAt (e.g., window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. Source finding context: round1/semantics.md#f3 Source finding context: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals Source finding context: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time Source finding context: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps Source finding context: a derivable value (windowStartedAt) is modeled as an independent input while its source field (enqueuedAt) is kept inert Source finding context: The interface contract declares enqueuedAt as the batch-entry timestamp (required, documented field). Source finding context: enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock — two authorities for one meaning. Source finding context: When a caller backfills or replays notifications (enqueuedAt in the past), the window silently anchors to processing time instead of the declared entry time; the field's name and the system's behavior diverge.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`

### Consensus (3/3, core-axis mode)
- issue-001 (high): Window start and window close are judged on two different time authorities: enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now, and no same-clock precondition is declared. The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 internal-clock window start, R3 caller-clock close check} is jointly satisfiable only under the undeclared premise that the caller's now comes from the same clock as Date.now(), so the documented flush close obligation is violable over the full declared input domain of flush(now). All three lenses accepted the recorded root and kept severity high; the required fix is to unify the time authority and declare the clock-source contract.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: Window start and window close are judged on two different time authorities — enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now — and no same-clock precondition is declared, so the claim set {C5, C6, R1, R3} is jointly satisfiable only under an undeclared premise and the documented flush close obligation is violable over the full declared input domain of flush(now). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:27-28, notification-batcher.ts:30-32 Source finding context: The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 "window start sampled from internal Date.now()", R3 "window close judged against caller-supplied now"} is jointly satisfiable only under an undeclared necessary premise that the caller's now is sampled from the same clock as Date.now(); over the full declared input domain of flush(now: number) the set is not satisfiable. Source finding context: The logic lens found an inter-claim contradiction: enqueue binds the window-start time authority to the process wall clock while flush judges window close against an arbitrary caller-supplied number, and the contract never declares a same-clock precondition. This makes the documented flush close obligation (C5/C6) violable with declared-legal inputs, directly against the review intent focus on time-source semantics and the correctness/verifiability goals. Source finding context: Unify the time authority: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() in flush, or (c) inject a single clock function at construction used by both methods. In all variants, declare the clock-source contract on the API doc. Source finding context: round1/logic.md#finding-1 Source finding context: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability) Source finding context: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources Source finding context: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading Source finding context: window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition Source finding context: Split time authority is introduced: window start is bound to the process wall clock at enqueue while the close check is parameterized on a caller clock value. Source finding context: No same-clock precondition is declared on flush(now), so the allowed input domain includes clock values that diverge from the window-start authority. Source finding context: For a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] which the contract defines as "window still open" — the documented close obligation is violated.
  - affected purpose: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability)
  - failure condition: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources
  - impact: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading
  - root hypothesis: Window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition, so the flush close obligation is violable with declared-legal inputs.
  - evidence: `round1/logic.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:27-28`, `notification-batcher.ts:30-32`, `finding-ledger.yaml#finding-001`, `finding-001.cause-001`, `notification-batcher.ts:30`, `finding-001.cause-002`, `notification-batcher.ts:26-30`, `finding-001.cause-003`, `notification-batcher.ts:31-32`, `issue-stance-matrix.yaml#stances.issue-001.logic`, `issue-ledger.yaml#issue-001`, `issue-stance-matrix.yaml#stances.issue-001.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-001.semantics`, `round1/semantics.md#f1`, `notification-batcher.ts:8`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_now
  - materiality: The issue directly weakens the declared review goals of correctness and verifiability of the flush contract. The failure path is occupiable today with declared-legal inputs: any flush caller whose now is not sampled from the same wall clock that enqueue used (deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources) triggers it. The impact is that digests are never emitted for the affected window (a perpetual [] that the contract says means "window still open") or are emitted before the window elapses, so the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading. This presently occupiable failure path is the panel's high-severity calibration anchor.
  - root cause: The chain starts where the split time authority is introduced: enqueue binds the window-start time (windowStartedAt) to the process wall clock via Date.now(), while the close check in flush is parameterized on a caller-supplied clock value, and no same-clock precondition is declared anywhere on flush(now). Every downstream behavior follows from this split — without a single designated clock, window start and window close can be measured against diverging timelines, which is exactly what makes the close obligation violable. Deliberation framed this issue as a symptom of the broader issue-004 root (a single undesignated time authority for the window lifecycle), which generalizes but does not contradict this clock-split manifestation.
  - causal path: Step 1 (finding-001.cause-001): the split time authority is introduced — window start is bound to the process wall clock at enqueue while the close check takes a caller clock value. Step 2 (finding-001.cause-002, enabled by step 1): because no same-clock precondition is declared on flush(now), the allowed input domain includes clock values that diverge from the window-start authority. Step 3 (finding-001.cause-003, caused by step 2): for a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] — which the contract defines as "window still open" — and the documented close obligation is violated. One question stays open beyond the evidence: whether any production caller currently supplies a non-Date.now() clock is outside the bounded single-file target. Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics all support, accept the recorded root, and keep high severity, with no remaining disagreement.
  - action: Unify the time authority so window start and window close are judged on the same clock, choosing one of three variants: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() inside flush, or (c) inject a single clock function at construction that both methods use. In all variants, declare the clock-source contract on the API doc. Closure class is fix_now with closure obligation must_close_in_target: this is a current correctness blocker because the failure path is occupiable with declared-legal inputs today. Ordering matters in two ways recorded upstream: this clock-split defect enables issue-003's persistent misreading of [] (rel-005), and the generalizing single-time-authority root is tracked in issue-004, so the single-time-authority fix here is the unit that discharges this issue inside the target.

- issue-002 (high): The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point — the code guarantees the negation of the comment's guarantee (C7 vs R5). Today, under the synchronous realization, the documented invariant is false and the guarantee is vacuous and unverifiable; under any async/concurrent/re-entrant realization the behavior becomes silent notification loss, the exact failure the comment claims to exclude. Deliberation adopted a direction-neutral root (the documented preservation property has no implementing mechanism) and fixed severity at medium in one decision for the duplicate cluster issue-002/issue-005/issue-007; closure requires an explicit implement-or-retract decision before the next stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point; the code guarantees the negation of the comment's guarantee, so the documented preservation property is vacuous today and becomes silent notification loss under any async/concurrent realization. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-38, notification-batcher.ts:39 Source finding context: C7 ("Notifications enqueued between the window close check and this reset are carried into the next window") and R5 (flush reset unconditionally clears the entire pending map) cannot both hold in any execution where such an enqueue occurs; the code guarantees the negation of the comment's guarantee. Source finding context: The logic lens found a necessary/necessary inter-claim conflict: the reset comment declares a preservation guarantee for check-to-reset enqueues while pending.clear() makes the complementary property (pending becomes empty) necessary at the same program point. Under the current synchronous realization the guarantee is vacuous and unverifiable; under any async/concurrent realization the actual behavior is silent notification loss, the exact failure the comment claims to exclude — directly against the contract-vs-behavior consistency focus of the review. Source finding context: Make the comment true or make it honest: delete only the flushed entries (snapshot digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window, or rewrite the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. Source finding context: round1/logic.md#finding-2 Source finding context: correctness (no silent notification loss) and regression_risk (declared review goals) Source finding context: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue Source finding context: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing Source finding context: the reset clears the whole pending map instead of removing only the entries that were flushed into the digest Source finding context: Flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Source finding context: Any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Source finding context: The comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with the discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated.
  - affected purpose: correctness (no silent notification loss) and regression_risk (declared review goals)
  - failure condition: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue
  - impact: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing
  - root hypothesis: The flush reset clears the whole pending map instead of removing only the entries that were flushed into the digest, making the documented check-to-reset carry-over guarantee unsatisfiable whenever such an enqueue occurs.
  - evidence: `round1/logic.md#finding-2`, `notification-batcher.ts:32`, `notification-batcher.ts:34-36`, `notification-batcher.ts:37-39`, `finding-ledger.yaml#finding-002`, `finding-002.cause-001`, `notification-batcher.ts:39`, `finding-002.cause-002`, `finding-002.cause-003`, `notification-batcher.ts:37-38`, `issue-stance-matrix.yaml#stances.issue-002.logic`, `issue-ledger.yaml#issue-002`, `issue-stance-matrix.yaml#stances.issue-002.coverage`, `round1/coverage.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-002.semantics`, `round1/semantics.md#f2`, `notification-batcher.ts:34`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, accept_risk
  - materiality: The issue weakens the declared correctness goal (no silent notification loss) and the regression_risk goal. The failure condition is any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue. The impact is silent notification loss while the in-code documentation asserts the opposite guarantee, so future maintainers relying on the documented invariant will ship the loss without noticing. Severity was resolved at medium rather than high under the panel's impact-reachability calibration: the check-to-reset antecedent is unoccupiable with declared-legal inputs in the bounded synchronous target (unlike issue-001's input-reachable clock split), so the present harm is a false documented invariant — a verifiability/maintainability harm — with loss activation conditional on a future refactor.
  - root cause: The deliberated, direction-neutral root adopted by all three lenses is that the documented check-to-reset preservation property has no implementing mechanism: the guarantee is written at item granularity while the only reset operates at window granularity (whole-map pending.clear()). This is the starting point because the contradiction is structural — as long as the only reset is an unconditional whole-map clear, no execution where a check-to-reset enqueue occurs can satisfy the comment. Which side is defective (a missing carve-out in the reset versus a comment encoding unbuilt intent) is unverifiable within the bounded target, which is exactly why the root is stated direction-neutrally and the implement-or-retract action space is preserved.
  - causal path: Step 1 (finding-002.cause-001): flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Step 2 (finding-002.cause-002, caused by step 1): any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Step 3 (finding-002.cause-003, caused by step 2): the comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with that discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated. Beyond the evidence, whether any concurrent or async caller exists today is outside the bounded single-file target. Deliberation resolved the panel: logic conceded severity from high to medium by adopting the impact-reachability calibration and accepted the direction-neutral root as more faithful to the symmetric C7-vs-R5 result; coverage moved from the clear()-side-defective hypothesis to the same restatement; semantics' remaining blocker — one severity decision across the duplicate cluster — was discharged by fixing issue-002, issue-005, and issue-007 at medium in this single decision, with no remaining disagreement.
  - action: Make the comment true or make it honest. Either implement the documented property — delete only the flushed entries (snapshot the digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window — or retract it by rewriting the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. The ordering dependency is explicit in the framing: closure is gated by this implement-or-retract decision (closure_class needs_decision), because which side is defective is unverifiable within the bounded target, and the issue must close before the next stage (must_close_before_next_stage) — it blocks any async or re-entrant refactor of flush rather than current operation. The recorded action candidates are fix_before_release and accept_risk.

- issue-004 (high): The batcher never designates a single canonical time authority, and that one missing designation produces two coupled defects: the enqueue/flush clock split (window start stamped from internal Date.now() while window close is decided against a caller-supplied now with no declared time base) and the inert enqueuedAt field (declared as the batch-entry record yet never stamped or read by enqueue, which re-measures entry time from its own clock at line 20). Deliberation resolved this as the session's root issue at high severity with all three lenses accepting the full merged root; the fix is one designated clock owning every timestamp in the file, and it must close in target now.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: coverage, semantics, logic
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: No single time authority is designated anywhere in the batcher: the window lifecycle is governed by two parallel, unreconciled clocks (Date.now() in enqueue vs caller-injected now in flush), and the entry-time record is declared on enqueuedAt yet never stamped or read by the entry-owning method, which re-measures entry time from its own clock — one undesignated-authority root with two surface manifestations whose fixes are coupled. Source finding context: NotificationBatcher window-closure time base (enqueue/flush pair) Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window lifecycle is governed by two parallel, unreconciled time sources — an internal wall clock (Date.now() in enqueue) and a caller-injected now (flush) — and the system designates no single time authority nor declares any contract that the two share one time base; window closure can therefore fail in both directions (never closes, or closes immediately). Source finding context: The coverage lens checked the time-authority dimension of the window lifecycle and found a dimension deficit in an element that exists: time is managed in two places with no single source of authority designated. The request summary explicitly scopes time-source semantics, and the review-target-profile obligations classify a visible runtime-contract failure inside the bounded target as material against the correctness goal; this mismatch is fully visible within the file. Source finding context: Designate a single time authority: (a) inject time everywhere via a constructor-injected clock used by both enqueue and flush, or (b) own time internally by having flush() take no parameter and use Date.now(). If the injected-now polling API must stay, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a minimum stopgap. Source finding context: round1/coverage.md#finding-1 Source finding context: correctness review goal — window-closure contract of the enqueue/flush pair Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases Source finding context: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) without designating which clock is canonical Source finding context: The design splits time authority across the API boundary: testability-style injection in flush, hard-coded Date.now() in enqueue, with no canonical clock designated. Source finding context: flush externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock. Source finding context: The closure predicate now - windowStartedAt subtracts values from two unreconciled time bases, so window closure can fail in both directions (never closes, or closes immediately). Source finding context: PendingNotification.enqueuedAt entry-time record Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:19-24 Source finding context: The field contract declares enqueuedAt as "epoch ms when the notification entered the batch", but the method that owns batch entry (enqueue) neither stamps nor validates it; the entry-time record is caller-trusted, and entry time is managed in two parallel places (per-item enqueuedAt, per-window windowStartedAt via Date.now()) with no single authority designated. Source finding context: The coverage lens found that the temporal audit dimension exists nominally on the data shape but lacks the enforcement mechanism: the field doc asserts a fact about an event only enqueue can witness, yet enqueue never writes or checks the value. This matters under the verifiability review goal — any downstream consumer trusting the documented semantics builds on a record no component guarantees. Source finding context: Assign stamping authority to the entry owner: have enqueue set enqueuedAt itself (from the single clock chosen in the finding-005 fix), changing the input type to omit the field (Omit<PendingNotification, "enqueuedAt">); or, if callers must supply it, change the doc to "caller-asserted creation time" and stop claiming it records batch entry. Source finding context: round1/coverage.md#finding-3 Source finding context: verifiability review goal — trustworthiness of the documented entry-time record Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source Source finding context: the data shape and the behavior were specified independently; the contract names an event-derived value without designating which component has authority to derive it Source finding context: The data shape and the behavior were specified independently: the contract names an event-derived value (batch-entry time) without designating which component has authority to derive it. Source finding context: The field's semantics are declared on the interface but the stamping responsibility was never assigned to the entry-owning method: enqueue neither writes nor checks item.enqueuedAt. Source finding context: The entry-time record is caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority.
  - affected purpose: correctness review goal — window-closure contract of the enqueue/flush pair / verifiability review goal — trustworthiness of the documented entry-time record
  - failure condition: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) / any caller supplying an inaccurate, stale, or fabricated enqueuedAt, whose divergence from actual entry time is unbounded and undetectable within the target Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target
  - impact: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases / downstream consumers (ordering, age-based expiry, audit) that trust the documented entry-time semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp coexists with no designated canonical source Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source
  - root hypothesis: The design never designates a single canonical time authority for the batcher; the enqueue/flush clock split (internal Date.now() vs caller-supplied now) and the inert, caller-trusted enqueuedAt field beside the internally stamped windowStartedAt are two manifestations of that one missing designation. Falsifiable: a fix that injects one clock owning every timestamp in the file should dissolve both defects; if either survives such a fix, the common-root claim is wrong.
  - evidence: `round1/coverage.md#finding-1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `round1/coverage.md#finding-3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:19-24`, `finding-ledger.yaml#finding-005`, `finding-ledger.yaml#finding-007`, `rel-007`, `finding-relation-graph.yaml#rel-007`, `finding-005.cause-001`, `notification-batcher.ts:30`, `finding-005.cause-002`, `finding-005.cause-003`, `notification-batcher.ts:32`, `finding-007.cause-001`, `finding-007.cause-002`, `finding-007.cause-003`, `issue-stance-matrix.yaml#stances.issue-004.logic`, `issue-ledger.yaml#issue-004`, `round1/logic.md#finding-1`, `round1/logic.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-004.coverage`, `issue-stance-matrix.yaml#stances.issue-004.semantics`, `round1/semantics.md#f1`, `round1/semantics.md#f3`, `issue-ledger.yaml#dep-001`, `rel-006`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_now
  - materiality: The issue weakens both declared review purposes at once. Against the correctness goal, the window-closure contract of the enqueue/flush pair is input-reachable today: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) makes the closure subtraction mix two unreconciled time bases, so window closure fails in both directions — never closes (notifications never delivered) or closes immediately. Against the verifiability goal, the documented entry-time record is caller-trusted with unbounded, undetectable divergence from actual entry time, so downstream consumers (ordering, age-based expiry, audit) build on a record no component guarantees while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source. High severity holds via the input-reachable clock-split manifestation and was never contested by any lens.
  - root cause: The accepted root is that the design never designates a single canonical time authority for the batcher. It is the starting point because both surface defects are downstream of the same missing designation rather than of each other: the clock split exists because flush externalizes time as a bare number while enqueue hard-codes Date.now() with no canonical clock declared, and the inert enqueuedAt exists because the contract names an event-derived value without designating which component has authority to derive it. The two legs are structurally unified at the line-20 Date.now() re-measurement, which is simultaneously the internal window-start authority and the divergent second authority in both legs. The root is inferred from the two observed manifestations and is falsifiable as stated: one injected clock owning every timestamp in the file (including enqueuedAt stamping by the entry owner) should dissolve both defects; if either survives such a fix, the common-root claim is wrong. Per deliberation, the clock-split leg carries a formal logic contradiction (finding-001) while the enqueuedAt leg stands on coverage/semantics observational evidence — an evidence-scope annotation, not counter-evidence against the merge.
  - causal path: Two causal paths, both raised by the coverage lens, converge on the missing designation. Finding-005: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) with no canonical clock designated; flush therefore externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock; the closure predicate now - windowStartedAt then subtracts values from two unreconciled time bases, so window closure can fail in both directions (lines 20, 30-32). Finding-007: the data shape and the behavior were specified independently — the contract names batch-entry time without designating who derives it (lines 8-9); the stamping responsibility was never assigned to the entry-owning method, so enqueue neither writes nor checks item.enqueuedAt (lines 19-24); the entry-time record is therefore caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority. Relation rel-007 merges these two paths into this issue as two manifestations of one root.
  - action: Designate a single time authority owning every timestamp in the file: inject a constructor-supplied clock used by both enqueue and flush (or have flush own time internally via Date.now()), and assign enqueuedAt stamping to the entry owner from that same clock — for example by changing the input type to omit the field — or, if callers must supply it, re-document it as caller-asserted creation time and stop claiming it records batch entry. At minimum, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a stopgap. Ordering matters in two ways: the two manifestations have coupled fixes, so they must be closed by the same single-clock designation rather than patched separately, and because this is the session's root issue, the same fix closes the symptom issues (issue-001/issue-006 clock-split statements) in target. The dep-001/rel-006 shared-cause context with issue-008 is preserved as cross-issue dependency context — the chosen clock should also govern that issue's resolution — but it is dependency context, not merge evidence. Closure obligation is fix_now, must close in target.

- issue-006 (high): Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the closure expression now - windowStartedAt subtracts an internally sourced Date.now() value (stamped in enqueue at line 20) from a caller-supplied number whose time source is never declared (flush, lines 30-32). Because the file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), a divergent caller clock is an in-contract scenario, not a hypothetical. All three lenses support the issue with the root accepted and high severity kept; deliberation was skipped as no-deliberation-needed (consistent stances). It is a symptom of the issue-004 undesignated-time-authority root and closes in target via the same single-clock fix, now.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: coverage, logic, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the expression now - windowStartedAt subtracts an internally sourced Date.now() value from a caller-supplied number whose time source is never declared, so the window-duration semantics are not guaranteed by the contract, and the file's caller-supplied epoch-ms precedent (enqueuedAt) makes a divergent caller clock an in-contract scenario. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: The expression now - this.windowStartedAt subtracts an internally sourced Date.now() wall-clock value from a caller-supplied now whose time source is never declared; the name now does not mean "same clock as windowStartedAt", so the window-duration semantics are not guaranteed by the contract. Source finding context: The semantics lens found a definition-explicitness failure: two distinct time concepts — "internal wall-clock now" and "caller-supplied now" — share one name without a unifying contract. The file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), so a caller-side clock distinct from the batcher's internal clock is an in-contract scenario, not a hypothetical; the declared review goal (correctness) is violated by a contract gap. Source finding context: Establish a single time authority: (a) drop the parameter and use Date.now() inside flush, (b) make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or (c) inject a clock dependency used by both methods. In all cases, document the required time source of now (e.g., "epoch ms from the same clock that produced enqueuedAt"). Source finding context: round1/semantics.md#f1 Source finding context: correctness (declared review goal) — window-duration semantics of the flush contract Source finding context: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent Source finding context: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly Source finding context: split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority Source finding context: Window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. Source finding context: The contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Source finding context: The cross-clock subtraction is semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly.
  - affected purpose: correctness (declared review goal) — window-duration semantics of the flush contract
  - failure condition: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent
  - impact: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly
  - root hypothesis: Split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority, making the cross-clock subtraction semantically meaningless.
  - evidence: `round1/semantics.md#f1`, `notification-batcher.ts:20`, `notification-batcher.ts:30-32`, `notification-batcher.ts:8`, `finding-ledger.yaml#finding-009`, `finding-009.cause-001`, `finding-009.cause-002`, `notification-batcher.ts:26-29`, `finding-009.cause-003`, `notification-batcher.ts:32`, `notification-batcher.ts:28`, `issue-stance-matrix.yaml#stances.issue-006.logic`, `issue-ledger.yaml#issue-006`, `round1/logic.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-006.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-006.semantics`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_now
  - materiality: The declared correctness review goal is violated at the contract level: the window-duration semantics of flush are not guaranteed over the declared input domain, because the contract documents only the return-value meaning and never the parameter's time source. The failure condition is in-contract today — a caller supplying now from performance.now() (monotonic, origin near process start) or an injected test clock — given the file's caller-supplied epoch-ms precedent. The impact is silent in both directions: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open", and notifications are never delivered; with a skewed or mocked clock the window can close instantly. High severity was kept by all lenses (duplicate cluster with issue-001 via rel-002).
  - root cause: The accepted root, taken as recorded, is the split time authority introduced at line 20 versus line 30: window start is bound to the internal Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. This is the starting point because every observable consequence follows from that split — once the two ends of the window lifecycle answer to different undeclared clocks, the cross-clock subtraction is semantically meaningless regardless of any particular caller behavior. Within the session, this issue is classified as a symptom: the same split generalizes to the issue-004 undesignated-time-authority root, for the same reason as issue-001.
  - causal path: The path runs in three steps on semantics finding-009. First, window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority (lines 20, 30-32). Second — the enabling step — the contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt, line 8) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Third, the cross-clock subtraction becomes semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly. Logic confirms this as the same unsatisfiable claim set it proved in finding-001 (undeclared same-clock premise), and coverage confirms it as the same missing-single-time-authority deficit it raised as finding-005, so the semantic, formal, and coverage statements all pick out one defect.
  - action: Establish a single time authority: drop the parameter and use Date.now() inside flush, make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or inject a clock dependency used by both methods; in all cases document the required time source of now (for example "epoch ms from the same clock that produced enqueuedAt"). The dependency that matters is that this issue is a symptom of the issue-004 root, so it should not receive an isolated patch: the single-clock fix that closes issue-001/issue-004 closes this in target, and the chosen authority must be the same one designated there. Closure obligation is fix_now, must close in target, because the failure path is reachable with declared-legal inputs today.

- issue-003 (medium): The doc defines [] to mean "window still open" (C6), but flush also returns [] in the reachable state windowStartedAt === null in which no window is open, so one return value carries two undistinguished meanings and the definition contradicts the producing rule (R2) in that state. The documented meaning of [] is therefore false on every idle-state flush(now) call with declared-legal inputs — a today-exercised, clock-independent contract falsity whose standalone harm is interpretive-only. Deliberation adjudicated severity at medium; the fix is to either widen the documented meaning of [] or make the two states distinguishable in the signature, keeping C6 consistent with the choice.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: logic, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The doc defines [] to mean "window still open", but flush also returns [] in the reachable state windowStartedAt === null in which no window is open; one return value carries two undistinguished meanings, and under finding-001's clock-skew condition the misreading turns from a transient ambiguity into a permanently misleading caller-facing reading. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: C6 defines the output [] to mean "window still open", but R2 returns [] in the reachable state windowStartedAt === null, in which no window is open; the definition and the producing rule contradict in that state. Source finding context: The logic lens found a definition-vs-rule contradiction (possible-as-necessary modality error): the doc asserts a necessary equivalence ([] iff window still open) while the implementation also produces [] from the reachable no-window state (initial value and after every successful flush). The practical reading coincides today via the state invariant, holding severity at medium, but the declared definition is false in the null state and the misreading becomes persistent under finding-001's clock-skew condition. Source finding context: Widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open") or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"), and keep C6 consistent with whichever is chosen. Source finding context: round1/logic.md#finding-3 Source finding context: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals) Source finding context: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely Source finding context: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics Source finding context: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single output value while the doc defines only one meaning for it Source finding context: Two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Source finding context: The doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict.
  - affected purpose: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals)
  - failure condition: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely
  - impact: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics
  - root hypothesis: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single [] output while the doc defines only one meaning for it, so the definition contradicts the producing rule in the reachable no-window state.
  - evidence: `round1/logic.md#finding-3`, `notification-batcher.ts:28`, `notification-batcher.ts:31`, `notification-batcher.ts:40`, `finding-ledger.yaml#finding-003`, `finding-003.cause-001`, `notification-batcher.ts:32`, `finding-003.cause-002`, `notification-batcher.ts:16`, `issue-stance-matrix.yaml#stances.issue-003.logic`, `issue-ledger.yaml#issue-003`, `issue-stance-matrix.yaml#stances.issue-003.coverage`, `issue-stance-matrix.yaml#stances.issue-003.semantics`, `round1/semantics.md#f5`, `notification-batcher.ts:31-32`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - materiality: The issue weakens the declared goals of verifiability of the caller-facing contract and correctness of caller-side state interpretation. The failure condition is a caller or monitor branching on the documented meaning of [] in the no-window state, or operation under finding-001's clock-skew condition where the misleading reading persists indefinitely. The impact is misclassification of batcher state by polling callers (idle read as batching) and tests written against the documented meaning validating the wrong semantics. Severity is medium: the falsity is exercised today with declared-legal inputs and is independent of the clock defect, but the standalone harm is interpretive-only with no delivery failure, capping it below high; the persistent-misreading escalation under clock skew is priced in issue-001/issue-006, not here.
  - root cause: The chain starts where flush conflates two distinct states — "no window started" and "window open, not yet closed" — into a single [] output while the doc defines only one meaning for that value. This conflation is the root because the doc asserts a necessary equivalence ([] iff window still open) that the producing rule cannot honor: the implementation also yields [] from the reachable no-window state (the initial value and the state after every successful flush), a possible-as-necessary modality error. All lenses accepted this recorded root as-is; deliberation only adjudicated the deferred severity calibration.
  - causal path: Step 1 (finding-003.cause-001): two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Step 2 (finding-003.cause-002, caused by step 1): the doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict. Nothing was left unresolved beyond the evidence. Deliberation (logic and semantics participating) resolved the deferred medium-vs-low calibration at medium: logic answered semantics' falsifying question with a today-reachable harm path that does not route through the clock defect (idle-state flush returns [] while its documented meaning is false at that moment) and conceded the double-counting point that the permanent-misreading escalation belongs to issue-001; semantics then moved low to medium on rubric consistency, since a today-exercised contract falsity cannot grade below the refactor-conditional cluster (issue-002/005/007) held at medium. Both participating lenses' final positions are medium with no remaining disagreement.
  - action: Close the homonym in one of two ways and keep C6 consistent with whichever is chosen: widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open"), or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"). Either option closes the issue without a product-intent decision. Closure class is fix_now with obligation must_close_before_next_stage: it must close before verification-stage work because tests written against the current documented meaning would validate the wrong semantics. The severity escalation under clock skew is handled by the issue-001/issue-006 time-authority fix, so this issue's action is the independent definitional repair; the recorded action candidates are fix_before_release and fix_now.

- issue-005 (medium): The comment at lines 37-38 declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize that guarantee: the reset is a whole-map clear() that removes every entry unconditionally, so the declared sub-behavior is absent from the system. The harm is latent in the current single-threaded synchronous runtime and is activated by exactly the async/concurrent refactor the comment invites. Deliberation resolved the issue at medium severity (the single cluster decision for issue-002/issue-005/issue-007), and it must close before that next stage through one implement-or-retract decision.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: coverage, logic
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize the guarantee; the whole-map clear() removes every entry unconditionally, so the declared sub-behavior is absent from the system. Source finding context: NotificationBatcher.flush reset semantics vs documented carry-over contract Source finding context: notification-batcher.ts:37-39 Source finding context: The comment declares that notifications enqueued between the window-close check and the reset "are carried into the next window", but no mechanism implementing carry-over exists; the actual reset (this.pending.clear()) deletes the entire map, so any such notification would be silently dropped — the declared sub-behavior is absent from the system. Source finding context: The coverage lens found a declared sub-behavior with no implementing mechanism: the system contains no selective-removal, snapshot-then-diff, or re-queue mechanism that could realize the documented carry-over guarantee. The review goals explicitly include contract-vs-behavior consistency and regression_risk; the comment invites a future async/concurrent refactor to rely on a safety property the mechanism does not provide. Source finding context: Make behavior and contract converge: (a) implement the guarantee by snapshotting the digested keys/items and deleting only those, or replacing the map with a fresh one after capturing a reference; or (b) retract the contract by rewriting the comment to state the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). Source finding context: round1/coverage.md#finding-2 Source finding context: contract-vs-behavior consistency and regression_risk (declared review goals) Source finding context: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset Source finding context: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites Source finding context: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code Source finding context: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code. Source finding context: The reset is implemented as a whole-map clear() rather than removal of only the flushed items, so every entry — including any hypothetical late arrival — is removed unconditionally. Source finding context: The declared carry-over guarantee (lines 37-38) is contradicted by the reset: no item present but not yet digested can be "carried into the next window".
  - affected purpose: contract-vs-behavior consistency and regression_risk (declared review goals)
  - failure condition: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset
  - impact: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites
  - root hypothesis: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code.
  - evidence: `round1/coverage.md#finding-2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:32`, `finding-ledger.yaml#finding-006`, `finding-006.cause-001`, `finding-006.cause-002`, `notification-batcher.ts:39`, `finding-006.cause-003`, `notification-batcher.ts:37-38`, `issue-stance-matrix.yaml#stances.issue-005.logic`, `issue-ledger.yaml#issue-005`, `round1/logic.md#finding-2`, `notification-batcher.ts:34-36`, `issue-stance-matrix.yaml#stances.issue-005.coverage`, `issue-stance-matrix.yaml#stances.issue-005.semantics`, `round1/semantics.md#f2`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals explicitly include contract-vs-behavior consistency and regression_risk, and this issue weakens both: the documentation claims a carry-over safety property the mechanism does not provide, and the comment invites a future async/concurrent refactor (for example awaiting a send inside the digest loop) to rely on that property while enqueue can interleave between the close check and the reset. The impact is silent notification loss while documentation claims the opposite. Severity is medium per the impact-reachability calibration: the loss antecedent is modification-conditional — unoccupiable with declared-legal inputs in the bounded synchronous target — unlike the input-reachable clock split, so it blocks the next stage rather than current operation.
  - root cause: The accepted root, taken as recorded, is a granularity mismatch at the point of specification: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity, and the item-granularity concept ("late-enqueued notification") has no representation in the code. This is the starting point because every downstream fact follows from it — a reset conceived per-window can only be a whole-map operation, so no code path could ever distinguish a late-enqueued item from a flushed one, making the documented item-level carry-over unimplementable as written rather than merely unimplemented.
  - causal path: The causal path runs in three observed steps on coverage finding-006. First, the window/item granularity mismatch leaves the late-enqueued-notification concept unrepresented in the code (lines 37-39). Second, because the reset is implemented as a whole-map clear() rather than removal of only the flushed items, every entry — including any hypothetical late arrival — is removed unconditionally (line 39). Third, the declared carry-over guarantee at lines 37-38 is therefore contradicted by the reset: no item present but not yet digested can be "carried into the next window". All three lenses support this path — logic established the same contradiction formally as its finding-002 (the documented invariant is necessarily false in every execution where its antecedent holds), and semantics confirmed the ghost concept reading as its finding-010 — so the absence is directly observed, not inferred. Whether and when flush will actually be refactored to async or called concurrently remains outside the bounded single-file target.
  - action: Make behavior and contract converge through one explicit decision with two candidate paths. Either implement the guarantee before release — snapshot the digested keys/items and delete only those, or swap in a fresh map after capturing a reference — or accept the risk and retract the contract by rewriting the comment to the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). The ordering constraint is that this implement-or-retract decision must close before any async/concurrent refactor of flush, because that refactor is precisely the failure condition; the closure obligation is must_close_before_next_stage. The decision should also stay coherent with the rel-003/rel-004 duplicate cluster (issue-002/issue-007), whose severity was fixed at medium in the same single decision.

- issue-007 (medium): The reset comment in notification-batcher.ts promises that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the map and no snapshot separates entries being digested from later arrivals — the documented preservation property has no implementing mechanism. In the current synchronous code the described interleaving cannot even occur, so the comment is a false documented invariant today, and a later async/re-entrant refactor that relies on it inherits silent notification loss. Severity is medium, set by the single cluster severity decision shared with issue-002 and issue-005, and the issue closes through one implement-or-retract decision that must land before the next refactor stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: logic, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The reset comment states that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() removes all entries unconditionally and no snapshot distinguishes entries being digested from entries that arrived after the check; the comment describes a scenario the current synchronous code neither encounters nor handles, and a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment states that notifications enqueued between the window-close check and the reset "are carried into the next window", but this.pending.clear() unconditionally removes every entry; no carry-over behavior exists in the code. Source finding context: The semantics lens found a contract-vs-behavior inconsistency: the comment's claim requires that some entries survive the reset, but clear() removes all entries present at call time, and the digest loop iterates the same live map so a pre-clear entry is digested in the current flush — the opposite of "carried into the next window". This counts against the declared review goals (correctness, maintainability, regression_risk). Source finding context: Either implement the stated semantics (snapshot the keys/entries to digest, delete only those keys, leaving later arrivals in place) or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Source finding context: round1/semantics.md#f2 Source finding context: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset Source finding context: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism Source finding context: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated Source finding context: the comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code Source finding context: The reset comment declares a carry-over contract for notifications enqueued between the window-close check and the reset. Source finding context: this.pending.clear() removes all entries unconditionally and no snapshot distinguishes "entries being digested" from "entries that arrived after the check" — the contract has no implementing mechanism. Source finding context: In the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles; a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss.
  - affected purpose: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset
  - failure condition: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism
  - impact: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated
  - root hypothesis: The comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code.
  - evidence: `round1/semantics.md#f2`, `notification-batcher.ts:37-39`, `notification-batcher.ts:34`, `finding-ledger.yaml#finding-010`, `finding-010.cause-001`, `notification-batcher.ts:37-38`, `finding-010.cause-002`, `notification-batcher.ts:39`, `finding-010.cause-003`, `notification-batcher.ts:32`, `issue-stance-matrix.yaml#stances.issue-007.logic`, `issue-ledger.yaml#issue-007`, `round1/logic.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-007.coverage`, `round1/coverage.md#finding-2`, `issue-stance-matrix.yaml#stances.issue-007.semantics`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk
  - materiality: The issue counts against three declared review goals — correctness, maintainability, and regression_risk — through a direct contract-vs-behavior inconsistency in the flush reset. The failure condition is modification-conditional: a maintainer makes flush async or re-entrant and relies on the documented carry-over invariant, at which point the described interleaving becomes reachable while the code provides no implementing mechanism. The impact is silent notification loss inherited from a documented invariant the code does not provide; even today the comment describes a scenario the code neither encounters nor handles as stated, which is exactly the kind of misleading contract the declared goals exist to catch.
  - root cause: Deliberation adopted a direction-neutral root for family coherence with issue-002: the documented check-to-reset preservation property has no implementing mechanism, and no target-internal evidence locates the defect on the comment side (unbuilt intent) versus the code side (missing carve-out in the reset) — both intent facts are unverifiable in the bounded target. This is the starting point because every downstream observation follows from the contract/mechanism gap, and logic's necessary/necessary strengthening shows it is not a mere omission: pending.clear() guarantees the complementary property (pending becomes empty) at the same program point where the comment promises survival, so implement-or-retract is the complete fix space.
  - causal path: The causal chain from finding-010 (semantics) runs in three steps. First, the reset comment (notification-batcher.ts:37-38) declares a carry-over contract for notifications enqueued between the window-close check and the reset. Second, this.pending.clear() (line 39) removes all entries unconditionally and no snapshot distinguishes entries being digested from entries that arrived after the check, so the contract has no implementing mechanism — the digest loop iterates the same live map, the opposite of "carried into the next window". Third, in the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles, and a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss. One thing stays beyond the evidence: whether flush will actually become async or re-entrant in real usage is outside the bounded single-file target.
  - action: The accepted fix space is exactly two-sided (implement-or-retract): either implement the stated semantics — snapshot the keys/entries to digest and delete only those keys, leaving later arrivals in place — or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Logic's necessary/necessary observation confirms no third option exists, because the code actively guarantees the complementary property. Ordering matters: the issue is framed as a next-step blocker with closure obligation must_close_before_next_stage, so the decision must land before any async/re-entrant refactor of flush; it is also the third member of the rel-003/rel-004 duplicate cluster, so the choice should stay coherent with the single cluster decision covering issue-002 and issue-005. The projected action candidates are fix_before_release or accept_risk.

- issue-008 (medium): One meaning — "time the notification entered the batch" — exists twice in notification-batcher.ts: as the caller-supplied, documented enqueuedAt field in the exported interface and as the internal Date.now() capture in enqueue(). The two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Severity is medium as recorded; the issue is a symptom of the missing time-authority designation it shares with issue-004 (dep-001/rel-006), kept distinct because the two defects affect different purposes and are independently fixable. Closure needs one product-intent decision: does enqueuedAt drive windowing, or is it payload-only metadata.
  - issue stance agreement: 2/3
  - agreed or narrowed lenses: coverage, semantics
  - issue stance disagreement: 1/3
  - disagreeing stance lenses: logic
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 2/2 deliberation participants
  - accepted lenses: coverage, semantics
  - remaining disagreement: 0/2 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The meaning "time the notification entered the batch" exists twice — as the caller-supplied enqueuedAt field and as the internal Date.now() capture in enqueue() — and the two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,20 Source finding context: The semantics lens found a ghost sub-area and an un-unified synonym pair: a required, documented field in an exported interface asserts behavioral relevance yet is never referenced after declaration, while windowStartedAt — semantically "the first pending notification's batch-entry time" — is modeled as an independent input from a second clock instead of being derived from the field that names the meaning. Source finding context: Pick one source of truth: derive the window anchor from pending items' enqueuedAt (e.g., window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. Source finding context: round1/semantics.md#f3 Source finding context: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals Source finding context: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time Source finding context: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps Source finding context: a derivable value (windowStartedAt) is modeled as an independent input while its source field (enqueuedAt) is kept inert Source finding context: The interface contract declares enqueuedAt as the batch-entry timestamp (required, documented field). Source finding context: enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock — two authorities for one meaning. Source finding context: When a caller backfills or replays notifications (enqueuedAt in the past), the window silently anchors to processing time instead of the declared entry time; the field's name and the system's behavior diverge.
  - affected purpose: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals
  - failure condition: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time
  - impact: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps
  - root hypothesis: A derivable value (windowStartedAt) is modeled as an independent input from a second clock while its source field (enqueuedAt) is kept inert, leaving two authorities for the one meaning "time the notification entered the batch".
  - evidence: `round1/semantics.md#f3`, `notification-batcher.ts:8-9`, `notification-batcher.ts:20`, `finding-ledger.yaml#finding-011`, `finding-011.cause-001`, `finding-011.cause-002`, `finding-011.cause-003`, `issue-stance-matrix.yaml#stances.issue-008.logic`, `issue-ledger.yaml#issue-008`, `round1/logic.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-008.coverage`, `round1/coverage.md#finding-3`, `rel-006`, `issue-stance-matrix.yaml#stances.issue-008.semantics`, `issue-ledger.yaml#dep-001`, `.onto/review/20260611-bad3651a/issue-ledger.yaml`, `.onto/review/20260611-bad3651a/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: follow_up, accept_risk
  - materiality: The issue weakens the declared goals of correctness and verifiability — callers are misled into believing enqueuedAt drives windowing — and of regression_risk, because duplicate time sources enable undetected drift between the two timestamps. The failure condition is in-contract given the field's documentation: a caller backfills or replays notifications with enqueuedAt in the past, and the window silently anchors to processing time instead of the declared entry time. There is no current delivery failure; the harm is a misleading interface contract plus drift enablement, which is why the framing places it as a planned follow-up rather than a release blocker.
  - root cause: The recorded root was accepted as-is with no deliberation needed: a derivable value (windowStartedAt) is modeled as an independent input from a second clock while its source field (enqueuedAt) is kept inert, leaving two authorities for the one meaning "time the notification entered the batch". This is the starting point because both observable symptoms — the inert documented field and the divergent window anchor — are direct consequences of never designating a single authority for entry time. The surface facts are logic-verified (enqueuedAt is consumed by no rule in the class body; a second internally sampled timestamp anchors the window); logic took a surface_only / insufficient_evidence position as explicit deference, since no declared claim formally ties windowStartedAt to enqueuedAt, and the applicable lenses (coverage, semantics) agree on the root and on medium severity.
  - causal path: The causal chain from finding-011 (semantics) runs in three steps. First, the interface contract declares enqueuedAt as the batch-entry timestamp — a required, documented field (notification-batcher.ts:8-9). Second, enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock (line 20), creating two authorities for one meaning. Third, when a caller backfills or replays notifications with enqueuedAt in the past, the window silently anchors to processing time instead of the declared entry time, so the field's name and the system's behavior diverge. Nothing in the chain remains unresolved beyond the cited evidence.
  - action: Pick one source of truth: either derive the window anchor from pending items' enqueuedAt (e.g., the window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. The dependency that matters is the shared cause with issue-004 (dep-001/rel-006, the undesignated entry-time authority): closure requires the product-intent decision on whether enqueuedAt drives windowing, and the issue may close alongside the coupled issue-004 single-authority fix during the next stage (closure obligation may_close_during_next_stage). The projected action candidates are follow_up or accept_risk.

### Conditional Consensus
- issue-001 (no-deliberation-needed): Window start and window close are judged on two different time authorities: enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now, and no same-clock precondition is declared. The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 internal-clock window start, R3 caller-clock close check} is jointly satisfiable only under the undeclared premise that the caller's now comes from the same clock as Date.now(), so the documented flush close obligation is violable over the full declared input domain of flush(now). All three lenses accepted the recorded root and kept severity high; the required fix is to unify the time authority and declare the clock-source contract.
- issue-006 (no-deliberation-needed): Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the closure expression now - windowStartedAt subtracts an internally sourced Date.now() value (stamped in enqueue at line 20) from a caller-supplied number whose time source is never declared (flush, lines 30-32). Because the file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), a divergent caller clock is an in-contract scenario, not a hypothetical. All three lenses support the issue with the root accepted and high severity kept; deliberation was skipped as no-deliberation-needed (consistent stances). It is a symptom of the issue-004 undesignated-time-authority root and closes in target via the same single-clock fix, now.
- issue-008 (no-deliberation-needed): One meaning — "time the notification entered the batch" — exists twice in notification-batcher.ts: as the caller-supplied, documented enqueuedAt field in the exported interface and as the internal Date.now() capture in enqueue(). The two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Severity is medium as recorded; the issue is a symptom of the missing time-authority designation it shares with issue-004 (dep-001/rel-006), kept distinct because the two defects affect different purposes and are independently fixable. Closure needs one product-intent decision: does enqueuedAt drive windowing, or is it payload-only metadata.

### Disagreement
- none

### Axiology-Proposed Additional Perspectives
- none

### Purpose Alignment Verification
- issue-001: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability)
- issue-002: correctness (no silent notification loss) and regression_risk (declared review goals)
- issue-004: correctness review goal — window-closure contract of the enqueue/flush pair / verifiability review goal — trustworthiness of the documented entry-time record
- issue-006: correctness (declared review goal) — window-duration semantics of the flush contract
- issue-003: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals)
- issue-005: contract-vs-behavior consistency and regression_risk (declared review goals)
- issue-007: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset
- issue-008: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals

### Boundary Notes
- logic evidence gap: C6 (definition, notification-batcher.ts:28): for flush callers, "an empty array means 'window still open'".
- logic evidence gap: R3 (implementation rule, notification-batcher.ts:32): window close is judged by `now - windowStartedAt >= WINDOW_MS` with caller-supplied `now`.
- coverage evidence gap: claim: "The window lifecycle is governed by two parallel, unreconciled time sources — an internal wall clock (Date.now() in enqueue) and a caller-injected `now` (flush) — and the system designates no single time authority nor declares any contract that the two share one time base; window closure can therefore fail i.

### Immediate Actions Required
- issue-001 (high)
  - issue: Window start and window close are judged on two different time authorities: enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now, and no same-clock precondition is declared. The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 internal-clock window start, R3 caller-clock close check} is jointly satisfiable only under the undeclared premise that the caller's now comes from the same clock as Date.now(), so the documented flush close obligation is violable over the full declared input domain of flush(now). All three lenses accepted the recorded root and kept severity high; the required fix is to unify the time authority and declare the clock-source contract.
  - target: Window start and window close are judged on two different time authorities — enqueue binds windowStartedAt to the internal Date.now() wall clock while flush evaluates closure against an arbitrary caller-supplied now — and no same-clock precondition is declared, so the claim set {C5, C6, R1, R3} is jointly satisfiable only under an undeclared premise and the documented flush close obligation is violable over the full declared input domain of flush(now). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:27-28, notification-batcher.ts:30-32 Source finding context: The claim set {C5 "flushes when the window has closed", C6 "[] means window still open", R1 "window start sampled from internal Date.now()", R3 "window close judged against caller-supplied now"} is jointly satisfiable only under an undeclared necessary premise that the caller's now is sampled from the same clock as Date.now(); over the full declared input domain of flush(now: number) the set is not satisfiable. Source finding context: The logic lens found an inter-claim contradiction: enqueue binds the window-start time authority to the process wall clock while flush judges window close against an arbitrary caller-supplied number, and the contract never declares a same-clock precondition. This makes the documented flush close obligation (C5/C6) violable with declared-legal inputs, directly against the review intent focus on time-source semantics and the correctness/verifiability goals. Source finding context: Unify the time authority: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() in flush, or (c) inject a single clock function at construction used by both methods. In all variants, declare the clock-source contract on the API doc. Source finding context: round1/logic.md#finding-1 Source finding context: correctness and verifiability of the flush contract (declared review goals: correctness, verifiability) Source finding context: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources Source finding context: digests are never emitted for the affected window (perpetual [] read as "window still open") or are emitted before the window elapses; the 30s batching contract is silently broken and the documented [] meaning becomes permanently misleading Source finding context: window-start and window-close use different time authorities (internal Date.now() vs caller-supplied now) with no declared same-clock precondition Source finding context: Split time authority is introduced: window start is bound to the process wall clock at enqueue while the close check is parameterized on a caller clock value. Source finding context: No same-clock precondition is declared on flush(now), so the allowed input domain includes clock values that diverge from the window-start authority. Source finding context: For a lagging caller clock the close predicate stays false after the documented 30s window has elapsed, so flush returns [] which the contract defines as "window still open" — the documented close obligation is violated.
  - failure condition: any flush caller whose now is not sampled from the same wall clock that enqueue used: deterministic-clock tests, monotonic-time-derived values, cross-process or skewed clock sources
  - candidates: fix_now
  - rationale: Unify the time authority so window start and window close are judged on the same clock, choosing one of three variants: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() inside flush, or (c) inject a single clock function at construction that both methods use. In all variants, declare the clock-source contract on the API doc. Closure class is fix_now with closure obligation must_close_in_target: this is a current correctness blocker because the failure path is occupiable with declared-legal inputs today. Ordering matters in two ways recorded upstream: this clock-split defect enables issue-003's persistent misreading of [] (rel-005), and the generalizing single-time-authority root is tracked in issue-004, so the single-time-authority fix here is the unit that discharges this issue inside the target.
  - remediation: Unify the time authority so window start and window close are judged on the same clock, choosing one of three variants: (a) parameterize enqueue on the same clock (enqueue(item, now)), (b) drop the now parameter and use Date.now() inside flush, or (c) inject a single clock function at construction that both methods use. In all variants, declare the clock-source contract on the API doc. Closure class is fix_now with closure obligation must_close_in_target: this is a current correctness blocker because the failure path is occupiable with declared-legal inputs today. Ordering matters in two ways recorded upstream: this clock-split defect enables issue-003's persistent misreading of [] (rel-005), and the generalizing single-time-authority root is tracked in issue-004, so the single-time-authority fix here is the unit that discharges this issue inside the target.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-002 (high)
  - issue: The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point — the code guarantees the negation of the comment's guarantee (C7 vs R5). Today, under the synchronous realization, the documented invariant is false and the guarantee is vacuous and unverifiable; under any async/concurrent/re-entrant realization the behavior becomes silent notification loss, the exact failure the comment claims to exclude. Deliberation adopted a direction-neutral root (the documented preservation property has no implementing mechanism) and fixed severity at medium in one decision for the duplicate cluster issue-002/issue-005/issue-007; closure requires an explicit implement-or-retract decision before the next stage.
  - target: The reset comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the entire map at the same program point; the code guarantees the negation of the comment's guarantee, so the documented preservation property is vacuous today and becomes silent notification loss under any async/concurrent realization. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-38, notification-batcher.ts:39 Source finding context: C7 ("Notifications enqueued between the window close check and this reset are carried into the next window") and R5 (flush reset unconditionally clears the entire pending map) cannot both hold in any execution where such an enqueue occurs; the code guarantees the negation of the comment's guarantee. Source finding context: The logic lens found a necessary/necessary inter-claim conflict: the reset comment declares a preservation guarantee for check-to-reset enqueues while pending.clear() makes the complementary property (pending becomes empty) necessary at the same program point. Under the current synchronous realization the guarantee is vacuous and unverifiable; under any async/concurrent realization the actual behavior is silent notification loss, the exact failure the comment claims to exclude — directly against the contract-vs-behavior consistency focus of the review. Source finding context: Make the comment true or make it honest: delete only the flushed entries (snapshot digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window, or rewrite the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. Source finding context: round1/logic.md#finding-2 Source finding context: correctness (no silent notification loss) and regression_risk (declared review goals) Source finding context: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue Source finding context: silent notification loss while the in-code documentation asserts the opposite guarantee; future maintainers relying on the documented invariant will ship the loss without noticing Source finding context: the reset clears the whole pending map instead of removing only the entries that were flushed into the digest Source finding context: Flush resets state by unconditionally clearing the entire pending map rather than deleting only the flushed entries. Source finding context: Any notification present in pending at reset time but not part of the produced digests is discarded, never preserved. Source finding context: The comment's guarantee that check-to-reset enqueues are "carried into the next window" is unsatisfiable together with the discard behavior whenever such an enqueue occurs; under the current synchronous realization the guarantee is vacuous and unverifiable as stated.
  - failure condition: any realization in which enqueue can interleave with flush between the close check and clear(): an async refactor of flush, concurrent callers, or re-entrant enqueue
  - candidates: fix_before_release, accept_risk
  - rationale: Make the comment true or make it honest. Either implement the documented property — delete only the flushed entries (snapshot the digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window — or retract it by rewriting the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. The ordering dependency is explicit in the framing: closure is gated by this implement-or-retract decision (closure_class needs_decision), because which side is defective is unverifiable within the bounded target, and the issue must close before the next stage (must_close_before_next_stage) — it blocks any async or re-entrant refactor of flush rather than current operation. The recorded action candidates are fix_before_release and accept_risk.
  - remediation: Make the comment true or make it honest. Either implement the documented property — delete only the flushed entries (snapshot the digested keys/items, then delete those specific entries) so post-check arrivals genuinely persist into the next window — or retract it by rewriting the comment to state that the reset drops everything currently pending and that the implementation relies on synchronous execution to keep the check-to-reset interval empty. The ordering dependency is explicit in the framing: closure is gated by this implement-or-retract decision (closure_class needs_decision), because which side is defective is unverifiable within the bounded target, and the issue must close before the next stage (must_close_before_next_stage) — it blocks any async or re-entrant refactor of flush rather than current operation. The recorded action candidates are fix_before_release and accept_risk.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-004 (high)
  - issue: The batcher never designates a single canonical time authority, and that one missing designation produces two coupled defects: the enqueue/flush clock split (window start stamped from internal Date.now() while window close is decided against a caller-supplied now with no declared time base) and the inert enqueuedAt field (declared as the batch-entry record yet never stamped or read by enqueue, which re-measures entry time from its own clock at line 20). Deliberation resolved this as the session's root issue at high severity with all three lenses accepting the full merged root; the fix is one designated clock owning every timestamp in the file, and it must close in target now.
  - target: No single time authority is designated anywhere in the batcher: the window lifecycle is governed by two parallel, unreconciled clocks (Date.now() in enqueue vs caller-injected now in flush), and the entry-time record is declared on enqueuedAt yet never stamped or read by the entry-owning method, which re-measures entry time from its own clock — one undesignated-authority root with two surface manifestations whose fixes are coupled. Source finding context: NotificationBatcher window-closure time base (enqueue/flush pair) Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window lifecycle is governed by two parallel, unreconciled time sources — an internal wall clock (Date.now() in enqueue) and a caller-injected now (flush) — and the system designates no single time authority nor declares any contract that the two share one time base; window closure can therefore fail in both directions (never closes, or closes immediately). Source finding context: The coverage lens checked the time-authority dimension of the window lifecycle and found a dimension deficit in an element that exists: time is managed in two places with no single source of authority designated. The request summary explicitly scopes time-source semantics, and the review-target-profile obligations classify a visible runtime-contract failure inside the bounded target as material against the correctness goal; this mismatch is fully visible within the file. Source finding context: Designate a single time authority: (a) inject time everywhere via a constructor-injected clock used by both enqueue and flush, or (b) own time internally by having flush() take no parameter and use Date.now(). If the injected-now polling API must stay, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a minimum stopgap. Source finding context: round1/coverage.md#finding-1 Source finding context: correctness review goal — window-closure contract of the enqueue/flush pair Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: window closure fails in both directions — never closes (notifications never delivered) or closes immediately — because the closure subtraction mixes two unreconciled time bases Source finding context: the design splits time authority across the API boundary (testability-style injection in flush, hard-coded Date.now() in enqueue) without designating which clock is canonical Source finding context: The design splits time authority across the API boundary: testability-style injection in flush, hard-coded Date.now() in enqueue, with no canonical clock designated. Source finding context: flush externalizes its time source as a bare number parameter with no time-base contract while windowStartedAt is stamped from the internal wall clock. Source finding context: The closure predicate now - windowStartedAt subtracts values from two unreconciled time bases, so window closure can fail in both directions (never closes, or closes immediately). Source finding context: PendingNotification.enqueuedAt entry-time record Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:19-24 Source finding context: The field contract declares enqueuedAt as "epoch ms when the notification entered the batch", but the method that owns batch entry (enqueue) neither stamps nor validates it; the entry-time record is caller-trusted, and entry time is managed in two parallel places (per-item enqueuedAt, per-window windowStartedAt via Date.now()) with no single authority designated. Source finding context: The coverage lens found that the temporal audit dimension exists nominally on the data shape but lacks the enforcement mechanism: the field doc asserts a fact about an event only enqueue can witness, yet enqueue never writes or checks the value. This matters under the verifiability review goal — any downstream consumer trusting the documented semantics builds on a record no component guarantees. Source finding context: Assign stamping authority to the entry owner: have enqueue set enqueuedAt itself (from the single clock chosen in the finding-005 fix), changing the input type to omit the field (Omit<PendingNotification, "enqueuedAt">); or, if callers must supply it, change the doc to "caller-asserted creation time" and stop claiming it records batch entry. Source finding context: round1/coverage.md#finding-3 Source finding context: verifiability review goal — trustworthiness of the documented entry-time record Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target Source finding context: downstream consumers (ordering, age-based expiry, audit) that trust the documented semantics build on a record no component guarantees, while a second parallel entry-adjacent timestamp (windowStartedAt) coexists with no designated canonical source Source finding context: the data shape and the behavior were specified independently; the contract names an event-derived value without designating which component has authority to derive it Source finding context: The data shape and the behavior were specified independently: the contract names an event-derived value (batch-entry time) without designating which component has authority to derive it. Source finding context: The field's semantics are declared on the interface but the stamping responsibility was never assigned to the entry-owning method: enqueue neither writes nor checks item.enqueuedAt. Source finding context: The entry-time record is caller-trusted while the class records its own entry-adjacent timestamp from Date.now(), leaving two parallel time records with no single designated authority.
  - failure condition: any caller supplying now from a different time base than Date.now() (performance.now(), seconds instead of ms, or a simulated test clock) / any caller supplying an inaccurate, stale, or fabricated enqueuedAt, whose divergence from actual entry time is unbounded and undetectable within the target Source finding context: any caller supplying now from a different time base than Date.now(): performance.now() (monotonic, starts near 0), seconds instead of ms, or a simulated test clock Source finding context: any caller supplying an inaccurate, stale, or fabricated enqueuedAt; divergence between recorded and actual entry time is unbounded and undetectable within the target
  - candidates: fix_now
  - rationale: Designate a single time authority owning every timestamp in the file: inject a constructor-supplied clock used by both enqueue and flush (or have flush own time internally via Date.now()), and assign enqueuedAt stamping to the entry owner from that same clock — for example by changing the input type to omit the field — or, if callers must supply it, re-document it as caller-asserted creation time and stop claiming it records batch entry. At minimum, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a stopgap. Ordering matters in two ways: the two manifestations have coupled fixes, so they must be closed by the same single-clock designation rather than patched separately, and because this is the session's root issue, the same fix closes the symptom issues (issue-001/issue-006 clock-split statements) in target. The dep-001/rel-006 shared-cause context with issue-008 is preserved as cross-issue dependency context — the chosen clock should also govern that issue's resolution — but it is dependency context, not merge evidence. Closure obligation is fix_now, must close in target.
  - remediation: Designate a single time authority owning every timestamp in the file: inject a constructor-supplied clock used by both enqueue and flush (or have flush own time internally via Date.now()), and assign enqueuedAt stamping to the entry owner from that same clock — for example by changing the input type to omit the field — or, if callers must supply it, re-document it as caller-asserted creation time and stop claiming it records batch entry. At minimum, document the required time base on flush(now) ("epoch ms from the same clock as Date.now()") as a stopgap. Ordering matters in two ways: the two manifestations have coupled fixes, so they must be closed by the same single-clock designation rather than patched separately, and because this is the session's root issue, the same fix closes the symptom issues (issue-001/issue-006 clock-split statements) in target. The dep-001/rel-006 shared-cause context with issue-008 is preserved as cross-issue dependency context — the chosen clock should also govern that issue's resolution — but it is dependency context, not merge evidence. Closure obligation is fix_now, must close in target.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-006 (high)
  - issue: Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the closure expression now - windowStartedAt subtracts an internally sourced Date.now() value (stamped in enqueue at line 20) from a caller-supplied number whose time source is never declared (flush, lines 30-32). Because the file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), a divergent caller clock is an in-contract scenario, not a hypothetical. All three lenses support the issue with the root accepted and high severity kept; deliberation was skipped as no-deliberation-needed (consistent stances). It is a symptom of the issue-004 undesignated-time-authority root and closes in target via the same single-clock fix, now.
  - target: Two distinct time concepts — internal wall-clock now and caller-supplied now — share one name without a unifying contract: the expression now - windowStartedAt subtracts an internally sourced Date.now() value from a caller-supplied number whose time source is never declared, so the window-duration semantics are not guaranteed by the contract, and the file's caller-supplied epoch-ms precedent (enqueuedAt) makes a divergent caller clock an in-contract scenario. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: The expression now - this.windowStartedAt subtracts an internally sourced Date.now() wall-clock value from a caller-supplied now whose time source is never declared; the name now does not mean "same clock as windowStartedAt", so the window-duration semantics are not guaranteed by the contract. Source finding context: The semantics lens found a definition-explicitness failure: two distinct time concepts — "internal wall-clock now" and "caller-supplied now" — share one name without a unifying contract. The file already establishes caller-supplied epoch-ms timestamps (enqueuedAt), so a caller-side clock distinct from the batcher's internal clock is an in-contract scenario, not a hypothetical; the declared review goal (correctness) is violated by a contract gap. Source finding context: Establish a single time authority: (a) drop the parameter and use Date.now() inside flush, (b) make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or (c) inject a clock dependency used by both methods. In all cases, document the required time source of now (e.g., "epoch ms from the same clock that produced enqueuedAt"). Source finding context: round1/semantics.md#f1 Source finding context: correctness (declared review goal) — window-duration semantics of the flush contract Source finding context: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent Source finding context: with a monotonic now the subtraction is hugely negative so flush returns [] forever, which the doc defines as "window still open" — notifications are never delivered; with a skewed or mocked clock the window can also close instantly Source finding context: split time authority introduced at line 20 versus line 30: window start bound to the internal Date.now() authority while window close is decided against an external now with no declared authority Source finding context: Window start is bound to the Date.now() authority inside enqueue() while window close is decided against an external now with no declared authority. Source finding context: The contract documents only the return-value meaning, not the parameter's time source, and the file establishes caller-side epoch-ms clocks (enqueuedAt) as an in-contract scenario, so a caller clock distinct from the internal clock is declared-legal. Source finding context: The cross-clock subtraction is semantically meaningless: with a monotonic now flush returns [] forever (documented as "window still open") and notifications are never delivered; with a skewed or mocked clock the window can close instantly.
  - failure condition: a caller supplies now from a different clock than the internal Date.now() capture: performance.now() (monotonic, origin near process start) or an injected test clock — an in-contract scenario given the file's caller-supplied epoch-ms precedent
  - candidates: fix_now
  - rationale: Establish a single time authority: drop the parameter and use Date.now() inside flush, make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or inject a clock dependency used by both methods; in all cases document the required time source of now (for example "epoch ms from the same clock that produced enqueuedAt"). The dependency that matters is that this issue is a symptom of the issue-004 root, so it should not receive an isolated patch: the single-clock fix that closes issue-001/issue-004 closes this in target, and the chosen authority must be the same one designated there. Closure obligation is fix_now, must close in target, because the failure path is reachable with declared-legal inputs today.
  - remediation: Establish a single time authority: drop the parameter and use Date.now() inside flush, make both sides caller-supplied (enqueue(item, now) or use item.enqueuedAt), or inject a clock dependency used by both methods; in all cases document the required time source of now (for example "epoch ms from the same clock that produced enqueuedAt"). The dependency that matters is that this issue is a symptom of the issue-004 root, so it should not receive an isolated patch: the single-clock fix that closes issue-001/issue-004 closes this in target, and the chosen authority must be the same one designated there. Closure obligation is fix_now, must close in target, because the failure path is reachable with declared-legal inputs today.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-003 (medium)
  - issue: The doc defines [] to mean "window still open" (C6), but flush also returns [] in the reachable state windowStartedAt === null in which no window is open, so one return value carries two undistinguished meanings and the definition contradicts the producing rule (R2) in that state. The documented meaning of [] is therefore false on every idle-state flush(now) call with declared-legal inputs — a today-exercised, clock-independent contract falsity whose standalone harm is interpretive-only. Deliberation adjudicated severity at medium; the fix is to either widen the documented meaning of [] or make the two states distinguishable in the signature, keeping C6 consistent with the choice.
  - target: The doc defines [] to mean "window still open", but flush also returns [] in the reachable state windowStartedAt === null in which no window is open; one return value carries two undistinguished meanings, and under finding-001's clock-skew condition the misreading turns from a transient ambiguity into a permanently misleading caller-facing reading. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: C6 defines the output [] to mean "window still open", but R2 returns [] in the reachable state windowStartedAt === null, in which no window is open; the definition and the producing rule contradict in that state. Source finding context: The logic lens found a definition-vs-rule contradiction (possible-as-necessary modality error): the doc asserts a necessary equivalence ([] iff window still open) while the implementation also produces [] from the reachable no-window state (initial value and after every successful flush). The practical reading coincides today via the state invariant, holding severity at medium, but the declared definition is false in the null state and the misreading becomes persistent under finding-001's clock-skew condition. Source finding context: Widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open") or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"), and keep C6 consistent with whichever is chosen. Source finding context: round1/logic.md#finding-3 Source finding context: verifiability of the caller-facing contract; correctness of caller-side state interpretation (declared review goals) Source finding context: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely Source finding context: misclassification of batcher state by polling callers; tests written against the documented meaning validate the wrong semantics Source finding context: flush conflates two distinct states ("no window started" and "window open, not yet closed") into a single output value while the doc defines only one meaning for it Source finding context: Two early returns map the distinct states windowStartedAt === null and now - windowStartedAt < WINDOW_MS to the same output []. Source finding context: The doc definition assigns [] the single meaning "window still open", which is false in the reachable no-window state — definition and producing rule contradict.
  - failure condition: a caller or monitor branches on the documented meaning of [] in the no-window state, or operates under finding-001's clock-skew condition where the misleading reading persists indefinitely
  - candidates: fix_before_release, fix_now
  - rationale: Close the homonym in one of two ways and keep C6 consistent with whichever is chosen: widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open"), or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"). Either option closes the issue without a product-intent decision. Closure class is fix_now with obligation must_close_before_next_stage: it must close before verification-stage work because tests written against the current documented meaning would validate the wrong semantics. The severity escalation under clock skew is handled by the issue-001/issue-006 time-authority fix, so this issue's action is the independent definitional repair; the recorded action candidates are fix_before_release and fix_now.
  - remediation: Close the homonym in one of two ways and keep C6 consistent with whichever is chosen: widen the documented meaning ("[] means there is nothing to flush yet: no window has started, or the window is still open"), or make the states distinguishable in the signature (e.g. return null for "no window" vs [] for "window open"). Either option closes the issue without a product-intent decision. Closure class is fix_now with obligation must_close_before_next_stage: it must close before verification-stage work because tests written against the current documented meaning would validate the wrong semantics. The severity escalation under clock skew is handled by the issue-001/issue-006 time-authority fix, so this issue's action is the independent definitional repair; the recorded action candidates are fix_before_release and fix_now.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-005 (medium)
  - issue: The comment at lines 37-38 declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize that guarantee: the reset is a whole-map clear() that removes every entry unconditionally, so the declared sub-behavior is absent from the system. The harm is latent in the current single-threaded synchronous runtime and is activated by exactly the async/concurrent refactor the comment invites. Deliberation resolved the issue at medium severity (the single cluster decision for issue-002/issue-005/issue-007), and it must close before that next stage through one implement-or-retract decision.
  - target: The comment declares that notifications enqueued between the window-close check and the reset are carried into the next window, but no selective-removal, snapshot-then-diff, or re-queue mechanism exists that could realize the guarantee; the whole-map clear() removes every entry unconditionally, so the declared sub-behavior is absent from the system. Source finding context: NotificationBatcher.flush reset semantics vs documented carry-over contract Source finding context: notification-batcher.ts:37-39 Source finding context: The comment declares that notifications enqueued between the window-close check and the reset "are carried into the next window", but no mechanism implementing carry-over exists; the actual reset (this.pending.clear()) deletes the entire map, so any such notification would be silently dropped — the declared sub-behavior is absent from the system. Source finding context: The coverage lens found a declared sub-behavior with no implementing mechanism: the system contains no selective-removal, snapshot-then-diff, or re-queue mechanism that could realize the documented carry-over guarantee. The review goals explicitly include contract-vs-behavior consistency and regression_risk; the comment invites a future async/concurrent refactor to rely on a safety property the mechanism does not provide. Source finding context: Make behavior and contract converge: (a) implement the guarantee by snapshotting the digested keys/items and deleting only those, or replacing the map with a fresh one after capturing a reference; or (b) retract the contract by rewriting the comment to state the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). Source finding context: round1/coverage.md#finding-2 Source finding context: contract-vs-behavior consistency and regression_risk (declared review goals) Source finding context: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset Source finding context: silent notification loss while documentation claims the opposite; the harm is latent in the current single-threaded synchronous runtime but is activated by exactly the refactor the comment invites Source finding context: the reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code Source finding context: The reset mechanism was written at window granularity while the documented guarantee is written at item granularity; the item-granularity concept ("late-enqueued notification") has no representation in the code. Source finding context: The reset is implemented as a whole-map clear() rather than removal of only the flushed items, so every entry — including any hypothetical late arrival — is removed unconditionally. Source finding context: The declared carry-over guarantee (lines 37-38) is contradicted by the reset: no item present but not yet digested can be "carried into the next window".
  - failure condition: a future async/concurrent refactor (e.g., awaiting a send inside the digest loop) relies on the documented carry-over safety property while enqueue can interleave between the close check and the reset
  - candidates: fix_before_release, accept_risk
  - rationale: Make behavior and contract converge through one explicit decision with two candidate paths. Either implement the guarantee before release — snapshot the digested keys/items and delete only those, or swap in a fresh map after capturing a reference — or accept the risk and retract the contract by rewriting the comment to the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). The ordering constraint is that this implement-or-retract decision must close before any async/concurrent refactor of flush, because that refactor is precisely the failure condition; the closure obligation is must_close_before_next_stage. The decision should also stay coherent with the rel-003/rel-004 duplicate cluster (issue-002/issue-007), whose severity was fixed at medium in the same single decision.
  - remediation: Make behavior and contract converge through one explicit decision with two candidate paths. Either implement the guarantee before release — snapshot the digested keys/items and delete only those, or swap in a fresh map after capturing a reference — or accept the risk and retract the contract by rewriting the comment to the true semantics ("the entire pending map is dropped at flush; flush must not be interleaved with enqueue"). The ordering constraint is that this implement-or-retract decision must close before any async/concurrent refactor of flush, because that refactor is precisely the failure condition; the closure obligation is must_close_before_next_stage. The decision should also stay coherent with the rel-003/rel-004 duplicate cluster (issue-002/issue-007), whose severity was fixed at medium in the same single decision.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-007 (medium)
  - issue: The reset comment in notification-batcher.ts promises that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() unconditionally empties the map and no snapshot separates entries being digested from later arrivals — the documented preservation property has no implementing mechanism. In the current synchronous code the described interleaving cannot even occur, so the comment is a false documented invariant today, and a later async/re-entrant refactor that relies on it inherits silent notification loss. Severity is medium, set by the single cluster severity decision shared with issue-002 and issue-005, and the issue closes through one implement-or-retract decision that must land before the next refactor stage.
  - target: The reset comment states that notifications enqueued between the window-close check and the reset are carried into the next window, but pending.clear() removes all entries unconditionally and no snapshot distinguishes entries being digested from entries that arrived after the check; the comment describes a scenario the current synchronous code neither encounters nor handles, and a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment states that notifications enqueued between the window-close check and the reset "are carried into the next window", but this.pending.clear() unconditionally removes every entry; no carry-over behavior exists in the code. Source finding context: The semantics lens found a contract-vs-behavior inconsistency: the comment's claim requires that some entries survive the reset, but clear() removes all entries present at call time, and the digest loop iterates the same live map so a pre-clear entry is digested in the current flush — the opposite of "carried into the next window". This counts against the declared review goals (correctness, maintainability, regression_risk). Source finding context: Either implement the stated semantics (snapshot the keys/entries to digest, delete only those keys, leaving later arrivals in place) or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Source finding context: round1/semantics.md#f2 Source finding context: correctness, maintainability, and regression_risk (declared review goals) — contract-vs-behavior consistency of the flush reset Source finding context: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism Source finding context: silent notification loss inherited from a documented invariant the code does not provide; today the comment describes a scenario the code neither encounters nor handles as stated Source finding context: the comment encodes an intended-but-unbuilt behavior: a carry-over contract with no implementing mechanism in the code Source finding context: The reset comment declares a carry-over contract for notifications enqueued between the window-close check and the reset. Source finding context: this.pending.clear() removes all entries unconditionally and no snapshot distinguishes "entries being digested" from "entries that arrived after the check" — the contract has no implementing mechanism. Source finding context: In the current synchronous, single-threaded execution the described interleaving cannot occur, so the comment describes a scenario the code neither encounters nor handles; a later async/re-entrant refactor relying on the documented invariant inherits silent notification loss.
  - failure condition: a maintainer later makes flush async or re-entrant and relies on the documented carry-over invariant; the described interleaving then becomes reachable while the code provides no implementing mechanism
  - candidates: fix_before_release, accept_risk
  - rationale: The accepted fix space is exactly two-sided (implement-or-retract): either implement the stated semantics — snapshot the keys/entries to digest and delete only those keys, leaving later arrivals in place — or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Logic's necessary/necessary observation confirms no third option exists, because the code actively guarantees the complementary property. Ordering matters: the issue is framed as a next-step blocker with closure obligation must_close_before_next_stage, so the decision must land before any async/re-entrant refactor of flush; it is also the third member of the rel-003/rel-004 duplicate cluster, so the choice should stay coherent with the single cluster decision covering issue-002 and issue-005. The projected action candidates are fix_before_release or accept_risk.
  - remediation: The accepted fix space is exactly two-sided (implement-or-retract): either implement the stated semantics — snapshot the keys/entries to digest and delete only those keys, leaving later arrivals in place — or rewrite the comment to state the actual behavior ("all entries pending at flush time are digested and cleared; nothing is carried over"). Logic's necessary/necessary observation confirms no third option exists, because the code actively guarantees the complementary property. Ordering matters: the issue is framed as a next-step blocker with closure obligation must_close_before_next_stage, so the decision must land before any async/re-entrant refactor of flush; it is also the third member of the rel-003/rel-004 duplicate cluster, so the choice should stay coherent with the single cluster decision covering issue-002 and issue-005. The projected action candidates are fix_before_release or accept_risk.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-008 (medium)
  - issue: One meaning — "time the notification entered the batch" — exists twice in notification-batcher.ts: as the caller-supplied, documented enqueuedAt field in the exported interface and as the internal Date.now() capture in enqueue(). The two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Severity is medium as recorded; the issue is a symptom of the missing time-authority designation it shares with issue-004 (dep-001/rel-006), kept distinct because the two defects affect different purposes and are independently fixable. Closure needs one product-intent decision: does enqueuedAt drive windowing, or is it payload-only metadata.
  - target: The meaning "time the notification entered the batch" exists twice — as the caller-supplied enqueuedAt field and as the internal Date.now() capture in enqueue() — and the two are never unified: the batcher never reads enqueuedAt, so the interface advertises a timestamp contract with no behavioral meaning while the window anchors to a divergent second source. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,20 Source finding context: The semantics lens found a ghost sub-area and an un-unified synonym pair: a required, documented field in an exported interface asserts behavioral relevance yet is never referenced after declaration, while windowStartedAt — semantically "the first pending notification's batch-entry time" — is modeled as an independent input from a second clock instead of being derived from the field that names the meaning. Source finding context: Pick one source of truth: derive the window anchor from pending items' enqueuedAt (e.g., window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. Source finding context: round1/semantics.md#f3 Source finding context: correctness and verifiability (callers misled into believing enqueuedAt drives windowing) and regression_risk (duplicate time sources enable drift) — declared review goals Source finding context: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time Source finding context: the field's name and the system's behavior diverge: the interface advertises a timestamp contract with no behavioral meaning, and the duplicate source enables undetected drift between the two timestamps Source finding context: a derivable value (windowStartedAt) is modeled as an independent input while its source field (enqueuedAt) is kept inert Source finding context: The interface contract declares enqueuedAt as the batch-entry timestamp (required, documented field). Source finding context: enqueue() ignores item.enqueuedAt and re-measures entry time from its own clock — two authorities for one meaning. Source finding context: When a caller backfills or replays notifications (enqueuedAt in the past), the window silently anchors to processing time instead of the declared entry time; the field's name and the system's behavior diverge.
  - failure condition: a caller backfills or replays notifications with enqueuedAt in the past — an in-contract use given the field's documentation; the window then silently anchors to processing time instead of the declared entry time
  - candidates: follow_up, accept_risk
  - rationale: Pick one source of truth: either derive the window anchor from pending items' enqueuedAt (e.g., the window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. The dependency that matters is the shared cause with issue-004 (dep-001/rel-006, the undesignated entry-time authority): closure requires the product-intent decision on whether enqueuedAt drives windowing, and the issue may close alongside the coupled issue-004 single-authority fix during the next stage (closure obligation may_close_during_next_stage). The projected action candidates are follow_up or accept_risk.
  - remediation: Pick one source of truth: either derive the window anchor from pending items' enqueuedAt (e.g., the window starts at the first item's enqueuedAt), or remove enqueuedAt from the interface (scope permitting) / explicitly document that it is payload-only metadata that does not affect windowing. The dependency that matters is the shared cause with issue-004 (dep-001/rel-006, the undesignated entry-time authority): closure requires the product-intent decision on whether enqueuedAt drives windowing, and the issue may close alongside the coupled issue-004 single-authority fix during the next stage (closure obligation may_close_during_next_stage). The projected action candidates are follow_up or accept_risk.
  - verification: verify the remediation against the failure condition with a focused check before closing

### Recommendations
- none

### Unique Finding Tagging
- none

### Individual Lens Findings
- logic: `.onto/review/20260611-bad3651a/round1/logic.md`
- coverage: `.onto/review/20260611-bad3651a/round1/coverage.md`
- semantics: `.onto/review/20260611-bad3651a/round1/semantics.md`
