---
session_id: 20260611-d9760486
process: review
target: "notification-batcher.ts"
domain: none
date: 2026-06-11
---

## 9-Lens Review Result

### Review Target
- `notification-batcher.ts`

### Verification Context
- Domain: none
- Review mode: core-axis
- Execution realization: direct-call
- Host runtime: openai
- Artifact generation realization: live
- Semantic quality evidence: not_evaluated (real_semantic_path_only)
- Finding ledger: `.onto/review/20260611-d9760486/finding-ledger.yaml`
- Issue ledger: `.onto/review/20260611-d9760486/issue-ledger.yaml`
- Problem framing: `.onto/review/20260611-d9760486/problem-framing.yaml`
- Controlled deliberation: `.onto/review/20260611-d9760486/deliberation.md`
- Source artifact: `.onto/review/20260611-d9760486/synthesis-ledger.yaml`
- Synthesis projection: `.onto/review/20260611-d9760486/synthesis.md`
- Execution status: completed

### Domain Selection
- Explicit no-domain token was provided; runtime will run without domain documents.

### Final Review Result
#### Review Basis
- Execution status: completed
- Deliberation status: performed
- Participating lenses: 3/3
- Degraded lenses: none
- Halt reason: none

#### Synthesis Summary
7 material issue(s) require attention. Highest-priority issue: issue-001 (high) — The window-closed predicate in flush subtracts an internally Date.now()-sampled anchor (set at enqueue, line 20) from a caller-supplied now (line 30), so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. This is a high-severity, material correctness defect that must be closed in the target by binding the whole window lifecycle to one declared time authority. Controlled deliberation did not leave a blocking unresolved disagreement for the highest-priority issue.

#### Classification Summary
- Highest severity: high
- Severity counts: blocker=0, high=2, medium=5, low=5, info=0
- Finding count: 13
- Root-cause issue count: 12
- Material issue count: 7
- Non-material finding count: 5

#### Material Issues
- issue-001 (high)
  - affected purpose: Declared review goal `correctness` and the flush contract C4 ('flushes when the window has closed') under the requested time-source-semantics focus. Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area.
  - failure condition: Any permitted caller whose flush(now) value comes from a clock other than Date.now() — test clock, monotonic clock, replayed or skewed clock — since the signature flush(now: number) carries no time-base constraint. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number.
  - impact: With a smaller-magnitude (e.g. monotonic) now the window never closes and flush returns [] forever; with a larger-epoch now it flushes immediately, violating the 30s window rule — the core batching contract cannot be trusted for permitted inputs. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract.
  - evidence: `round1/logic.md#finding-l1`
  - source lenses: logic
  - action candidates: fix_now
  - problem definition: Time authority is split across the batcher's two mutating entry points — the window anchor is sampled from ambient Date.now() at enqueue (line 20) while window closure is computed from the caller-injected now at flush (line 30) — with no declared contract binding the two clocks, so the window-closed predicate (contract C4) rests on an undeclared shared-clock condition and is violated for permitted inputs.
  - problem framing: symptom / current_blocker / fix_now / must_close_in_target / inferred

- issue-005 (high)
  - affected purpose: Declared review goal `correctness` for window-closure decisions under the requested time-source-semantics focus. Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics.
  - failure condition: Whenever the caller's now comes from a different epoch or skewed source than Date.now() (test clock, monotonic clock, mocked scheduler time), the closure predicate becomes a cross-clock subtraction. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor.
  - impact: A cross-clock subtraction makes the window close never, early, or instantly, violating the declared correctness goal inside the bounded target. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior.
  - evidence: `round1/coverage.md#finding-1`
  - source lenses: coverage
  - action candidates: fix_now
  - problem definition: The contract contains no clock-authority concept — no clock parameter on enqueue and no documented requirement on the source of flush's now (absence across lines 18-32) — so window-closure decisions mix two undeclared, free-running time authorities that are free to diverge.
  - problem framing: root_cause / current_blocker / fix_now / must_close_in_target / observed

- issue-002 (medium)
  - affected purpose: Review goals `correctness` and `regression_risk`: the documented loss-safety (carry-over) contract of flush's reset step. Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee).
  - failure condition: Any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush. Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush.
  - impact: The documented carry-over guarantee has no implementing mechanism; maintainers and callers relying on it assume loss-free behavior that the code contradicts, weakening trust in the documented contract. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior.
  - evidence: `round1/logic.md#finding-l2`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - problem definition: The documented carry-over guarantee has no implementing mechanism: no code path snapshots window membership at the close instant, so the guaranteed interval is empty under synchronous execution and the only materially reachable behavior is emit-all-then-clear, contradicting the written contract at lines 37-38.
  - problem framing: independent_issue / next_step_blocker / fix_now / must_close_in_target / inferred

- issue-006 (medium)
  - affected purpose: Review goals `correctness` and `regression_risk`: loss-free handling of pending notifications across the full lifecycle. Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths.
  - failure condition: Process shutdown, poller failure, or batcher decommission while items are pending — the future poll that would release them never happens. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state.
  - impact: Pending notifications are silently dropped with no contract acknowledging it; silent loss is unacceptable per the artifact's stated delivery purpose. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it.
  - evidence: `round1/coverage.md#finding-2`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - problem definition: The pending-notification lifecycle has no termination segment: the exported surface contains only enqueue, window-gated flush, and pendingUserCount, so no unconditional exit path exists for pending items and they are silently lost at shutdown, poller failure, or batcher decommission.
  - problem framing: independent_issue / next_step_blocker / needs_decision / must_close_before_next_stage / inferred

- issue-007 (medium)
  - affected purpose: Review goals `correctness` and `regression_risk`: bounded resource behavior of the pending store in a long-lived process. Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling.
  - failure condition: A stalled or slow external poll loop while enqueues continue — memory consumption is proportional to enqueue rate times the gap between successful polls. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process.
  - impact: Unbounded memory growth in a long-lived process, with no contract surface to detect, reject, or shed load. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract.
  - evidence: `round1/coverage.md#finding-3`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - problem definition: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation (absence across lines 14-48): enqueue appends unconditionally while the sole reducer (flush) is poll- and window-gated, so the pending store grows without bound whenever polling stalls or slows while enqueues continue.
  - problem framing: independent_issue / next_step_blocker / needs_decision / must_close_before_next_stage / inferred

- issue-010 (medium)
  - affected purpose: Review goals `correctness`, `regression_risk`, and `maintainability`: the truthfulness of the inline reset contract that future maintainers will rely on. Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract.
  - failure condition: A maintainer assumes loss-free carry-over per the comment; or a future async/re-entrant refactor makes the interleaving point reachable, where clear() silently drops the interleaved items. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable.
  - impact: The comment licenses an assumption of loss-free behavior that the code inverts — a material regression trap inside the bounded target. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter.
  - evidence: `round1/semantics.md#finding-1`
  - source lenses: semantics
  - action candidates: fix_before_release, fix_now
  - problem definition: The carry-over comment (lines 37-38) asserts a behavioral guarantee over an interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while this.pending.clear() would unconditionally discard such items if the point ever became reachable — the documented contract is false as written and inverts the actual failure mode.
  - problem framing: independent_issue / next_step_blocker / fix_now / must_close_in_target / observed

- issue-011 (medium)
  - affected purpose: Review goals `correctness` and `maintainability` under the requested time-source-semantics focus: the documented 30-second window semantics (WINDOW_MS) and the documented meaning of the enqueuedAt contract field. Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning.
  - failure condition: A caller using injected, fake, or skewed time for flush's now — the closure subtraction at line 32 becomes a cross-clock artifact — and any consumer trusting enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue.
  - impact: The window never closes, closes immediately, or stays open indefinitely — the named 30-second contract is unkeepable across clocks — and the only per-item time fact carries a documented meaning that no behavior establishes or verifies. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior.
  - evidence: `round1/semantics.md#finding-2`, `round1/semantics.md#finding-3`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk, fix_now
  - problem definition: The batcher owns no time fact: the window anchor, the closure input, and the documented batch-entry timestamp are all sourced from ambient or caller-chosen clocks that the batcher neither establishes nor validates, so neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert — time ownership is undesigned in the class.
  - problem framing: root_cause / next_step_blocker / needs_decision / must_close_in_target / inferred

#### Synthesized Material Issue Explanations
- issue-001 (high): The window-closed predicate in flush subtracts an internally Date.now()-sampled anchor (set at enqueue, line 20) from a caller-supplied now (line 30), so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. This is a high-severity, material correctness defect that must be closed in the target by binding the whole window lifecycle to one declared time authority.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The window-closed predicate subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window-closed predicate compares a caller-supplied clock (`now`) against an internally sampled ambient clock (`Date.now()`); the rule set is jointly satisfiable only under an undeclared necessary condition that both values come from the same epoch-ms clock, so C4 ('flushes when the window has closed') is violated for permitted inputs. Source finding context: Logic lens found an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush): a necessary shared-clock constraint is treated as merely possible, directly hitting the requested time-source-semantics focus and the correctness review goal. Residual contradiction persists after semantic disambiguation of `now`, so logic holds the primary finding. Source finding context: Use one time authority for the whole window lifecycle: either thread the caller clock through both entry points (enqueue(item, now) or use item.enqueuedAt to start the window), or drop the now parameter and use Date.now() inside flush (providing the test time-injection seam via an injected clock). At minimum document that now must be epoch ms from the same clock as Date.now(). Source finding context: round1/logic.md#finding-l1 Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract. Source finding context: The design splits time authority across the two mutating entry points — window start from the ambient clock at enqueue (line 20), window closure from injected time at flush (line 30) — with no claim in the target declaring or enforcing the required inter-clock identity. Source finding context: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. Source finding context: Time authority is split across enqueue (ambient Date.now(), line 20) and flush (caller-injected now, line 30) and no declared contract binds the two clocks, so the closure rule's truth depends on an undeclared necessary condition.
  - affected purpose: Declared review goal `correctness` and the flush contract C4 ('flushes when the window has closed') under the requested time-source-semantics focus. Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area.
  - failure condition: Any permitted caller whose flush(now) value comes from a clock other than Date.now() — test clock, monotonic clock, replayed or skewed clock — since the signature flush(now: number) carries no time-base constraint. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number.
  - impact: With a smaller-magnitude (e.g. monotonic) now the window never closes and flush returns [] forever; with a larger-epoch now it flushes immediately, violating the 30s window rule — the core batching contract cannot be trusted for permitted inputs. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract.
  - root hypothesis: Time authority is split across the two mutating entry points — the window anchor is sampled from ambient Date.now() at enqueue (line 20) while window closure is computed from the caller-injected now at flush (line 30) — and no declared contract binds the two clocks, so the closure rule's truth rests on an undeclared necessary condition. If binding both operands of the closure predicate to one declared clock authority removes the contradiction for all permitted inputs, the hypothesis holds; if the contradiction persists, it is refuted.
  - evidence: `round1/logic.md#finding-l1`, `finding-ledger.yaml#finding-001`, `finding-001.cause-001`, `finding-001.cause-002`, `issue-stance-matrix.yaml#stances.issue-001.logic`, `issue-ledger.yaml#issue-001`, `issue-stance-matrix.yaml#stances.issue-001.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-001.semantics`, `round1/semantics.md#finding-2`, `issue-ledger.yaml#dep-001`, `rel-001`, `issue-ledger.yaml#dep-002`, `rel-002`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_now
  - materiality: The issue strikes the declared review goal `correctness` on the core flush contract C4 inside the explicitly requested time-source-semantics focus. The signature flush(now: number) carries no time-base constraint, so any permitted caller whose now comes from a clock other than Date.now() — a test clock, monotonic clock, or replayed/skewed clock — triggers the failure: with a smaller-magnitude now the window never closes and flush returns [] forever, while with a larger-epoch now it flushes immediately. Either way the declared 30s digest behavior is unachievable for permitted inputs, so the primary batching contract cannot be trusted.
  - root cause: Time authority is split across the two mutating entry points: the window anchor is sampled from ambient Date.now() at enqueue (line 20) while window closure is computed from the caller-injected now at flush (line 30), and no declared contract binds the two clocks. The closure rule's truth therefore rests on an undeclared necessary condition — that both values come from the same epoch-ms clock — which the contract treats as merely possible. This is the starting point because every downstream symptom (wrong closure, never/early/instant flush) follows directly from the two operands of the closure predicate living on free-running, unbound time sources; the hypothesis carries its own falsification test: binding both closure operands to one declared clock authority must remove the contradiction for all permitted inputs, and root confidence is high.
  - causal path: The logic lens's finding-001 chain has two steps over evidence round1/logic.md#finding-l1. Step finding-001.cause-001: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. That step is caused by the root step finding-001.cause-002: time authority is split across enqueue and flush with no declared contract binding the two clocks, so the closure rule depends on an undeclared necessary condition. The lens derived this as an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush); the residual contradiction persists after semantic disambiguation of `now`, leaving nothing unresolved beyond evidence. Coverage independently located the same absent inter-clock binding at the identical anchors, and semantics reached the same terminal cause from the naming side, corroborating the chain.
  - action: Adopt a single time authority for the whole window lifecycle — either thread the caller clock through both entry points (enqueue(item, now) or anchor the window from item.enqueuedAt) or use one injected/ambient clock for both — and declare the time-base constraint on the interface. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters because of the shared-cause dependencies: dep-001 links this issue to issue-005 (the missing time-source authority concept, coverage side) and dep-002 links it to issue-011 (no single time authority plus a false shared-timebase name, semantics side); all three rest on the same absent inter-clock binding, and this issue is framed as the symptom of that shared cause, so designating the single clock authority once closes the contradiction here rather than patching the predicate in isolation. Deliberation was skipped (consistent_stances): all three lenses support the issue, accept the single-clock-authority root hypothesis, and keep high severity, with no remaining disagreement or follow-up evidence required.

- issue-005 (high): Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue (line 20) and caller-supplied now at flush (line 30) — and no single-origin time-source authority concept exists anywhere in the file. This is a high-severity, material correctness defect framed as the root cause shared with issue-001 and issue-011: designate one time authority for window anchoring and closure and document it on the interface.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue and caller-supplied now at flush — with no single-origin time-source authority concept anywhere in the file. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: Window-closure decisions mix two undeclared time authorities (internal Date.now() at enqueue vs caller-supplied now at flush) with no single time-source authority concept. Source finding context: Coverage lens flagged a dimensional deficit: one time-dependent value (window age) is parallel-managed across two time sources with no single-origin authority designation anywhere in the file; the request summary explicitly scopes time-source semantics into this review. Source finding context: Designate a single time authority; smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied time source (enqueue(item, now) or an injected clock used by both methods), and document on flush that now must come from that same authority. Source finding context: round1/coverage.md#finding-1 Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior. Source finding context: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32). Source finding context: The window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. Source finding context: The closure predicate (line 32) is a cross-clock subtraction: windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). Source finding context: No clock-authority concept exists in the contract — nothing binds the caller's time source to the internal anchor's source, so the two authorities are free to diverge.
  - affected purpose: Declared review goal `correctness` for window-closure decisions under the requested time-source-semantics focus. Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics.
  - failure condition: Whenever the caller's now comes from a different epoch or skewed source than Date.now() (test clock, monotonic clock, mocked scheduler time), the closure predicate becomes a cross-clock subtraction. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor.
  - impact: A cross-clock subtraction makes the window close never, early, or instantly, violating the declared correctness goal inside the bounded target. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior.
  - root hypothesis: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32), so the two time authorities are free to diverge. If designating a single time authority for both entry points eliminates the cross-clock subtraction, the hypothesis holds.
  - evidence: `round1/coverage.md#finding-1`, `finding-ledger.yaml#finding-005`, `finding-005.cause-001`, `finding-005.cause-002`, `finding-005.cause-003`, `issue-stance-matrix.yaml#stances.issue-005.logic`, `issue-ledger.yaml#issue-005`, `finding-relation-graph.yaml#rel-001`, `round1/logic.md#finding-l1`, `issue-stance-matrix.yaml#stances.issue-005.coverage`, `issue-stance-matrix.yaml#stances.issue-005.semantics`, `round1/semantics.md#finding-2`, `issue-ledger.yaml#dep-001`, `rel-001`, `issue-ledger.yaml#dep-003`, `rel-003`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_now
  - materiality: The issue weakens the declared review goal `correctness` for window-closure decisions under the explicitly requested time-source-semantics focus. Whenever the caller's now comes from a different epoch or skewed source than Date.now() — a test clock, monotonic clock, or mocked scheduler time — the closure predicate at line 32 becomes a cross-clock subtraction, making the window close never, early, or instantly. The core window contract thus fails under permitted caller behavior inside the bounded target, which is why it stands as a current_blocker rather than a latent concern.
  - root cause: The contract lacks any clock-authority concept: enqueue takes no clock parameter and nothing documents a requirement on the source of flush's now (absence across lines 18-32), so the two time authorities are free to diverge. This absence is the starting point — it is directly verifiable across the file (judgment state: observed), and the problem framing classifies this issue as the root_cause on which issue-001's closure contradiction rests (dep-001) and which issue-011 reaches from the naming side (dep-003). The hypothesis is falsifiable: if designating a single time authority for both entry points eliminates the cross-clock subtraction, it holds; root confidence is high.
  - causal path: The coverage lens's finding-005 chain has three steps over evidence round1/coverage.md#finding-1, running from symptom back to the root step finding-005.cause-003. Step finding-005.cause-001: the window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. That is caused by finding-005.cause-002: the closure predicate (line 32) is a cross-clock subtraction — windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). That in turn is caused by the root step finding-005.cause-003: no clock-authority concept exists in the contract, so nothing binds the caller's time source to the internal anchor's source and the two authorities are free to diverge. Coverage framed this as a dimensional deficit — one time-dependent value (window age) parallel-managed across two time sources with no single-origin authority designation — squarely inside the requested time-source-semantics scope; logic confirms the same absent binding is the premise of its closure contradiction (rel-001), and semantics reached the identical absence through naming analysis (rel-003: WINDOW_MS cannot denote 30 seconds of any single clock). Nothing is unresolved beyond evidence.
  - action: Designate a single time authority for window anchoring and closure — a shared caller-supplied time source threaded through both methods (the smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied source, e.g. enqueue(item, now)) or an injected clock used by both methods — and document on flush that its now must come from that same authority. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters: this issue is the shared cause behind issue-001's closure contradiction (dep-001) and converges with issue-011's missing-single-time-authority and false shared-timebase naming finding (dep-003), so resolving it first closes the common root for all three rather than treating each symptom separately. Deliberation was skipped (consistent_stances): all three lenses support the missing clock-authority concept, accept the root hypothesis, and keep high severity; the shared-cause dependencies are agreed dependency context, not stance conflicts, and no follow-up evidence is required.

- issue-002 (medium): The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: under synchronous execution the guaranteed interval admits no enqueues (the claim is vacuous), and in the only materially reachable reading items enqueued after the window-close instant are emitted in the current digest and wiped by clear(), never carried over. This is a medium-severity, material consistency defect: align the comment with the behavior, or implement the guarantee.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: the guaranteed interval is empty under synchronous execution, and in the only materially reachable reading items are emitted in the current digest and cleared, never carried over. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-40, notification-batcher.ts:34-36 Source finding context: The comment claims notifications enqueued between the window-close check and the reset are carried into the next window, but under synchronous execution that interval admits no enqueues (the claim is vacuous), and under the only materially reachable reading (enqueued after the window-close instant, before the flush poll) such items are emitted in the current digest and cleared, never carried over. The documented guarantee and the implemented behavior are not jointly satisfiable. Source finding context: Logic lens found C7 (carry-over guarantee, lines 37-38) and C8 (emit-all-then-clear, lines 34-36,39) jointly unsatisfiable for any item present at flush time: an obligation is asserted over an interval the execution model makes empty, masking a false guarantee in the reachable interpretation — a contract-vs-behavior contradiction under the correctness goal. Source finding context: Make the code and the claim agree: either implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). Source finding context: round1/logic.md#finding-l2 Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee). Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior. Source finding context: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only while content is taken wholesale at emission time, so the promised carry-over has no mechanism that could implement it. Source finding context: The comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. Source finding context: No mechanism snapshots window membership at the close instant — timing and content are decoupled (close check gates when, the loop takes whatever is present) — making the obligated carry-over state unreachable.
  - affected purpose: Review goals `correctness` and `regression_risk`: the documented loss-safety (carry-over) contract of flush's reset step. Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee).
  - failure condition: Any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush. Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush.
  - impact: The documented carry-over guarantee has no implementing mechanism; maintainers and callers relying on it assume loss-free behavior that the code contradicts, weakening trust in the documented contract. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior.
  - root hypothesis: No code path snapshots window membership at the close instant — the close check (line 32) gates timing only while emission takes pending content wholesale — so the promised carry-over has no mechanism that could implement it. If filtering digest membership by the window-close instant (or snapshotting membership) makes the documented guarantee exhibitable, the hypothesis holds.
  - evidence: `round1/logic.md#finding-l2`, `finding-ledger.yaml#finding-002`, `finding-002.cause-001`, `finding-002.cause-002`, `issue-stance-matrix.yaml#stances.issue-002.logic`, `issue-ledger.yaml#issue-002`, `issue-stance-matrix.yaml#stances.issue-002.coverage`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-002.semantics`, `round1/semantics.md#finding-1`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - materiality: The issue weakens the declared review goals `correctness` and `regression_risk` on the documented loss-safety (carry-over) contract of flush's reset step. The failure condition is any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush — and any future async/re-entrant refactor of flush. Because the documented guarantee has no implementing mechanism, maintainers and callers relying on it assume loss-free behavior that the code contradicts, so the written contract actively misleads about loss behavior even though the happy path still functions; this undermines trust in the documented contract rather than blocking current execution (next_step_blocker).
  - root cause: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only, while the digest loop takes pending content wholesale at emission time. Timing and content are decoupled, so the obligated carry-over state is unreachable — the promised guarantee has no mechanism that could implement it. This is the starting point because the contradiction between C7 (carry-over guarantee) and C8 (emit-all-then-clear) is not a wording slip but the absence of any membership-snapshotting mechanism; the hypothesis is falsifiable — if filtering digest membership by the window-close instant (or snapshotting membership) makes the documented guarantee exhibitable, it holds — and root confidence is high.
  - causal path: The logic lens's finding-002 chain has two steps over evidence round1/logic.md#finding-l2. Step finding-002.cause-001: the comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. That step is caused by the root step finding-002.cause-002: no mechanism snapshots window membership at the close instant — the close check gates when, the loop takes whatever is present — making the obligated carry-over state unreachable. The lens derived C7 (lines 37-38) and C8 (lines 34-36, 39) as jointly unsatisfiable for any item present at flush time: an obligation asserted over an interval the synchronous execution model makes empty, masking a false guarantee in the reachable interpretation. Coverage corroborates that the guarantee has no implementing concept and no evidence counterpart (flush destroys state without persisting any record), and semantics holds the recorded duplicate of the surface finding, confirming that clear() at line 39 would invert the promised outcome if the interval ever became reachable. Nothing is unresolved beyond evidence.
  - action: Make the code and the claim agree, in one of two ways: implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state the actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). The projected action classes are fix_before_release and fix_now, with closure obligation must_close_in_target; the fix is local to the target file. No cross-issue ordering constraint applies: the root (no snapshot mechanism) is self-contained — rel-004 records duplication with issue-010, not a shared root — so this issue is framed as an independent_issue and can be closed on its own. Deliberation was skipped (consistent_stances): logic, coverage, and semantics unanimously support the contradiction and its no-snapshot-mechanism root, accept the hypothesis, and keep medium severity, with no remaining disagreement or follow-up evidence required.

- issue-006 (medium): The pending-notification lifecycle has no termination segment: the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/dispose member, so pending items are unreachable at shutdown, poller stop, or decommission and are silently lost. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires adding a terminal operation plus a documented caller shutdown obligation before the next stage relies on loss-free delivery.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The pending-notification lifecycle covers enqueue and window-gated flush but has no termination segment (drain/close/dispose); pending items are unreachable at shutdown or poller stop and are silently lost. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:30-32,44-47 Source finding context: Coverage lens ghost-sub-area check: lifecycle termination is implied by the batching domain but entirely absent — the only exit path for pending items is a future poll that satisfies the window predicate, a correctness and regression risk inside the declared review goal. Source finding context: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }> that emits digests for all pending items regardless of WINDOW_MS and resets state; document that callers must invoke it on shutdown. Source finding context: round1/coverage.md#finding-2 Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it. Source finding context: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member. Source finding context: At process shutdown, poller failure, or decommission, pending notifications are silently dropped because the poll that would flush them never happens. Source finding context: Every pending item's only exit is a future external poll of flush that also satisfies the WINDOW_MS predicate (lines 28, 32). Source finding context: The API has no terminal-lifecycle operation (drain/close/dispose) anywhere in the class body, so no unconditional exit path exists.
  - affected purpose: Review goals `correctness` and `regression_risk`: loss-free handling of pending notifications across the full lifecycle. Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths.
  - failure condition: Process shutdown, poller failure, or batcher decommission while items are pending — the future poll that would release them never happens. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state.
  - impact: Pending notifications are silently dropped with no contract acknowledging it; silent loss is unacceptable per the artifact's stated delivery purpose. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it.
  - root hypothesis: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member — so no unconditional exit path exists for pending items. Adding a terminal operation that emits regardless of WINDOW_MS removes the silent-loss mode; if loss persists with such an operation, the hypothesis is refuted.
  - evidence: `round1/coverage.md#finding-2`, `finding-ledger.yaml#finding-006`, `finding-006.cause-001`, `finding-006.cause-002`, `finding-006.cause-003`, `issue-stance-matrix.yaml#stances.issue-006.logic`, `issue-ledger.yaml#issue-006`, `issue-stance-matrix.yaml#stances.issue-006.coverage`, `issue-stance-matrix.yaml#stances.issue-006.semantics`, `issue-ledger.yaml#dep-004`, `rel-005`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals are correctness and regression_risk for loss-free handling of pending notifications across the full lifecycle. At process shutdown, poller failure, or batcher decommission with non-empty pending state, the future poll that would release those items never happens, so pending notifications are silently dropped with no contract surface acknowledging or mitigating the loss. Silent data loss directly contradicts the artifact's stated delivery purpose, which makes the issue material against the declared goals rather than a stylistic gap.
  - root cause: The chain starts at finding-006.cause-003: no terminal-lifecycle concept exists in the API — the exported surface is exactly enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member — so no unconditional exit path exists for pending items. This is the root because every downstream effect follows once it holds, and it carries a sound refutation condition accepted by the logic lens: if silent loss persisted even with a terminal operation that emits regardless of WINDOW_MS, the hypothesis would be refuted. The semantics lens independently verified the structural premise by inventorying the surface and finding no member whose name or doc denotes termination, drain, or disposal. Root confidence is high.
  - causal path: Because no terminal operation exists anywhere in the class body (finding-006.cause-003), every pending item's only exit is a future external poll of flush that must also satisfy the WINDOW_MS predicate at lines 28 and 32 (finding-006.cause-002); therefore, at process shutdown, poller failure, or decommission, the poll that would flush pending items never happens and they are silently dropped (finding-006.cause-001). This three-step causes chain, evidenced throughout by round1/coverage.md#finding-2, leads exactly to the observed issue: a lifecycle covering enqueue and window-gated flush but lacking the termination segment the batching domain implies. Deliberation was skipped (reason_code consistent_stances) with unanimous support/accepts/keeps; nothing was narrowed and no disagreement remains, and all lenses agree the shared mid-path cause with issue-007 (dep-004, rel-005) is a distinct-root relation rather than the same root.
  - action: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }>, that emits digests for all pending items regardless of WINDOW_MS and resets state, and document that callers must invoke it on shutdown. This is a contract-extension decision (closure_class needs_decision) and a next_step_blocker that must close before any next stage relies on loss-free delivery (must_close_before_next_stage). Ordering note: dep-004 records that issue-006 and issue-007 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but have distinct roots and distinct mitigations, so this terminal operation can be decided and landed independently of issue-007's capacity/overflow policy. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave silent loss unacknowledged against the artifact's stated delivery purpose.

- issue-007 (medium): The pending store grows without bound: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, enqueue appends unconditionally, and the sole reducer (flush) is poll- and window-gated. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires selecting a documented capacity bound and an explicit overflow policy, surfaced to the caller, before the next stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The pending store grows without bound — no max-size, overflow policy, or backpressure concept exists, while the only reducer (flush) is poll- and window-gated. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:21-23,32 Source finding context: Coverage lens ghost-sub-area check: the batching domain implies a capacity/overflow sub-area (bounded buffers, drop/force-flush policies) that is entirely absent; combined with the unguarded poll dependency this is a regression and correctness risk within the declared review goal. Source finding context: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Source finding context: round1/coverage.md#finding-3 Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract. Source finding context: No capacity concept exists in the contract or implementation (absence across lines 14-48): enqueue has no size check or rejection path and nothing in the file mentions a bound. Source finding context: Pending storage grows without bound when the poller stalls while enqueues continue. Source finding context: The sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28). Source finding context: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, so enqueue appends unconditionally for any number of users and items.
  - affected purpose: Review goals `correctness` and `regression_risk`: bounded resource behavior of the pending store in a long-lived process. Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling.
  - failure condition: A stalled or slow external poll loop while enqueues continue — memory consumption is proportional to enqueue rate times the gap between successful polls. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process.
  - impact: Unbounded memory growth in a long-lived process, with no contract surface to detect, reject, or shed load. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract.
  - root hypothesis: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation (absence across lines 14-48): enqueue appends unconditionally for any number of users and items. A documented capacity bound with an explicit overflow policy removes the unbounded-growth mode; if growth persists under a bound, the hypothesis is refuted.
  - evidence: `round1/coverage.md#finding-3`, `finding-ledger.yaml#finding-007`, `finding-007.cause-001`, `finding-007.cause-002`, `finding-007.cause-003`, `issue-stance-matrix.yaml#stances.issue-007.logic`, `issue-ledger.yaml#issue-007`, `issue-stance-matrix.yaml#stances.issue-007.coverage`, `issue-stance-matrix.yaml#stances.issue-007.semantics`, `issue-ledger.yaml#dep-004`, `rel-005`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals are correctness and regression_risk for bounded resource behavior of the pending store in a long-lived process. With a stalled or slow external poll loop while enqueues continue, memory consumption grows proportional to enqueue rate times the gap between successful polls, and the contract offers no surface to detect, reject, or shed load. Unbounded memory growth with no detection or overflow policy is an operational failure mode squarely inside the declared goals, which makes the issue material.
  - root cause: The chain starts at finding-007.cause-003: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation (absence across lines 14-48) — enqueue has no size check or rejection path and nothing in the file mentions a bound, so enqueue appends unconditionally for any number of users and items. This is the root because unbounded growth follows necessarily once it holds, and it carries a well-formed refutation test accepted by the logic lens: if growth persisted under a documented capacity bound, the hypothesis would be refuted. The semantics lens independently confirmed the absence on the inventoried surface — no name, doc, or contract element denotes capacity, overflow, or backpressure, and the docs make no boundedness promise. Root confidence is high.
  - causal path: Because no capacity, overflow, or backpressure concept exists (finding-007.cause-003), enqueue appends unconditionally while the sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28) (finding-007.cause-002); therefore, whenever the poller stalls or slows while enqueues continue, pending storage grows without bound (finding-007.cause-001). This three-step causes chain, evidenced throughout by round1/coverage.md#finding-3, leads exactly to the observed issue: a batching domain that implies a capacity/overflow sub-area which is entirely absent. Deliberation was skipped (reason_code consistent_stances) with unanimous support/accepts/keeps; nothing was narrowed and no disagreement remains, and the lenses agree the shared poll-gated-reducer cause with issue-006 (dep-004, rel-005) does not make the two issues share a root.
  - action: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Selecting the bound and the policy is a contract decision (closure_class needs_decision) and a next_step_blocker that must close before the next stage (must_close_before_next_stage). Ordering note: dep-004 records that issue-007 and issue-006 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but name distinct missing concepts with distinct mitigations (capacity/overflow policy vs terminal-lifecycle operation), so this fix can be decided and landed independently of issue-006's drain operation. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave an undetectable unbounded-growth mode in long-lived processes.

- issue-010 (medium): The inline comment at notification-batcher.ts lines 37-38 promises that notifications enqueued between the window-close check (line 32) and the reset (line 39) are carried into the next window, but this.pending.clear() at line 39 unconditionally discards every entry regardless of when it was enqueued. The documented carry-over contract is false as written and inverts the actual failure mode: the only conditions under which the promise would matter are exactly the conditions under which clear() silently drops the items. The comment and the mechanism must be aligned — either re-document the true synchronous semantics or implement real carry-over.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match the behavior of this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: Semantics lens found the documented contract false as written: the guarantee describes an interleaving point that synchronous flush makes unreachable, and under any future condition where it became reachable clear() would silently drop — the opposite of the documented carry-over; a regression trap under the declared correctness/regression_risk/ maintainability goals. Source finding context: Make the comment state the true semantics (flush is synchronous; nothing can interleave; if it ever becomes async, interleaved enqueues would be dropped by clear() — preserve them explicitly), or implement real carry-over by iterating a snapshot and deleting only flushed entries so the comment becomes true. Source finding context: round1/semantics.md#finding-1 Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter. Source finding context: The comment asserts a behavioral guarantee about a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while clear() inverts the promised outcome if it ever became reachable. Source finding context: The carry-over comment does not match this.pending.clear(), which unconditionally discards every entry regardless of when it was enqueued. Source finding context: The guarantee was written over an unreachable interleaving point (synchronous flush, no await/callback/iterator hand-off between lines 32 and 39) with no implementing mechanism, so it is vacuous today and inverted under any future reachability.
  - affected purpose: Review goals `correctness`, `regression_risk`, and `maintainability`: the truthfulness of the inline reset contract that future maintainers will rely on. Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract.
  - failure condition: A maintainer assumes loss-free carry-over per the comment; or a future async/re-entrant refactor makes the interleaving point reachable, where clear() silently drops the interleaved items. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable.
  - impact: The comment licenses an assumption of loss-free behavior that the code inverts — a material regression trap inside the bounded target. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter.
  - root hypothesis: The carry-over comment asserts a behavioral guarantee over a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while this.pending.clear() inverts the promised outcome if it ever became reachable; aligning comment and mechanism (snapshot or truthful re-documentation) resolves the issue.
  - evidence: `round1/semantics.md#finding-1`, `finding-ledger.yaml#finding-010`, `finding-010.cause-001`, `finding-010.cause-002`, `issue-stance-matrix.yaml#stances.issue-010.logic`, `issue-ledger.yaml#issue-010`, `finding-relation-graph.yaml#rel-004`, `round1/logic.md#finding-l2`, `issue-stance-matrix.yaml#stances.issue-010.coverage`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-010.semantics`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, fix_now
  - materiality: This is material under the declared correctness, regression_risk, and maintainability goals because the comment is the inline reset contract that future maintainers will rely on, and it licenses an assumption of loss-free carry-over that the code inverts. The guarantee is unexhibitable today (synchronous flush makes the described interleaving unreachable) and inverted (silent drop) under the only conditions where it would matter: a maintainer trusting the documented carry-over, or a future async/re-entrant refactor that makes the interleaving point reachable. That makes it a regression trap inside the bounded target, not a cosmetic doc nit.
  - root cause: The accepted root cause (finding-010.cause-002) is that the guarantee was authored over an interleaving point that the synchronous execution model makes unreachable — there is no await, callback, or iterator hand-off between the close check at line 32 and the reset at line 39 — and with no implementing mechanism behind it. This is the starting point of the problem because the visible comment-vs-clear() mismatch is only the surface symptom: a behavioral promise was written about a hypothetical execution window that cannot occur today, so the promise is vacuous now and, since clear() unconditionally discards, becomes an inverted promise under any future reachability. Root confidence is high and all lenses accepted this derivation.
  - causal path: The causal path has two steps from the semantics lens (round1 finding-1). Step finding-010.cause-001 records the observable defect: the carry-over comment does not match this.pending.clear(), which discards every entry unconditionally. Step finding-010.cause-002 explains why that mismatch exists and is marked as the root step: the guarantee was written over an unreachable interleaving point with no mechanism to implement it, so it is vacuous today and inverted under any future reachability. cause-002 causes cause-001 — a contract asserted over a non-existent execution point cannot be matched by the reset mechanism, which produces exactly the false-as-written, failure-mode-inverting comment stated in the issue. Logic's independent finding-l2 (C7 and C8 jointly unsatisfiable for any item present at flush time, duplication recorded by rel-004) and coverage's verifiability analysis (no mechanism or persisted record could implement or evidence the promised carry-over) converge on the same chain.
  - action: Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics unanimously support the claim, accept the root hypothesis, and keep medium severity, with no remaining disagreement and no follow-up evidence required. The required action offers two closure paths: (a) make the comment state the true semantics — flush is synchronous, nothing can interleave, and if flush ever becomes async, interleaved enqueues would be dropped by clear() and must be preserved explicitly; or (b) implement real carry-over by iterating a snapshot and deleting only flushed entries, making the comment true. Framing is fix_now with must_close_in_target, so the fix is local to the target and should land before the next change builds on the false contract. Ordering note: rel-004 records this as a duplicate surface of issue-002 without licensing a merge, so coordinate the fix so one comment/mechanism alignment closes both surfaces consistently.

- issue-011 (medium): Time ownership is undesigned in the batcher. windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32) with no contract binding the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock; and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher. Neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. The accepted resolution is to designate one batcher-owned time authority for the window lifecycle and have the batcher stamp enqueuedAt from that same authority at the enqueue site, or re-document the field to its true provenance.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now (lines 30-32) with no contract binding the two clocks, and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher — time ownership is undesigned in the class, so neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32); the name now implies the same timebase but no contract binds the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock. Source finding context: Semantics lens found `now` is a homonym across two unrelated clocks: the parameter name semantically asserts one shared notion of now while the class holds no single time authority — directly violating the request focus on time-source semantics and the correctness goal. Source finding context: Pick one time authority: either accept now everywhere (add a now or injected-clock argument to enqueue and set windowStartedAt from it) or use Date.now() everywhere and drop the now parameter from flush; if the parameter is kept, document the precondition that now must come from the same epoch-ms clock as Date.now(). Source finding context: round1/semantics.md#finding-2 Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: The class holds no single time authority: line 20 hardcodes Date.now() while line 30 delegates time to the caller, and no statement of the shared-clock precondition exists on the interface. Source finding context: WINDOW_MS does not denote 30 seconds of any single clock: the closure subtraction mixes the internal wall-clock anchor with whatever value the caller passes as now. Source finding context: No single time authority exists in the class and the parameter name now falsely asserts a shared timebase, so nothing binds the two operands of the closure subtraction to one clock. Source finding context: notification-batcher.ts:8-9,19-24 Source finding context: enqueuedAt is documented as 'Epoch ms when the notification entered the batch' (line 8), but enqueue (lines 19-24) neither assigns nor validates it and no code in the target reads it; the value is caller-supplied before the notification has entered any batch, so the name/doc asserts a fact only the batcher could establish. Source finding context: Semantics lens found the field's documented meaning diverges from its actual provenance: batch-entry semantics are assigned to a value constructed before enqueue, the per-item batch-entry time is captured nowhere, and the documented meaning is verified by no behavior; ontological note: PendingNotification is a phase-named sortal applied to pre-enqueue (not yet pending) values. Source finding context: Make the batcher own the timestamp: have enqueue accept {userId, body} and stamp enqueuedAt itself from the single time authority chosen for finding-011, storing the enriched record internally; or if caller-supplied time is intentional, rename/re-document the field to its true provenance (e.g. createdAt) and either use it or remove it from this contract. Source finding context: round1/semantics.md#finding-3 Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior. Source finding context: The field documentation assigns batch-entry semantics to a value constructed by the caller before enqueue is invoked; the only component that could truthfully populate it (the batcher, at the enqueue site) does not. Source finding context: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher; the only entry-time fact recorded is windowStartedAt, a different concept. Source finding context: Batch-entry semantics were assigned to a caller-constructed pre-enqueue value that the caller cannot truthfully populate as documented, and the batcher never stamps it at the enqueue site.
  - affected purpose: Review goals `correctness` and `maintainability` under the requested time-source-semantics focus: the documented 30-second window semantics (WINDOW_MS) and the documented meaning of the enqueuedAt contract field. Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning.
  - failure condition: A caller using injected, fake, or skewed time for flush's now — the closure subtraction at line 32 becomes a cross-clock artifact — and any consumer trusting enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue.
  - impact: The window never closes, closes immediately, or stays open indefinitely — the named 30-second contract is unkeepable across clocks — and the only per-item time fact carries a documented meaning that no behavior establishes or verifies. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior.
  - root hypothesis: The batcher owns no time fact: the window anchor, the closure input, and the documented batch-entry timestamp are all sourced from ambient or caller-chosen clocks that the batcher neither establishes nor validates. Designating a single batcher-owned time authority — and stamping enqueuedAt at the enqueue site from that authority — resolves both findings; if clock unification alone leaves finding-012 intact without the stamping change, the common root is refuted.
  - evidence: `round1/semantics.md#finding-2`, `round1/semantics.md#finding-3`, `finding-ledger.yaml#finding-011`, `finding-ledger.yaml#finding-012`, `rel-006`, `finding-relation-graph.yaml#rel-006`, `finding-011.cause-001`, `finding-011.cause-002`, `finding-012.cause-001`, `finding-012.cause-002`, `issue-stance-matrix.yaml#stances.issue-011.logic`, `issue-ledger.yaml#issue-011`, `finding-relation-graph.yaml#rel-002`, `round1/logic.md#finding-l1`, `round1/logic.md#finding-l4`, `issue-stance-matrix.yaml#stances.issue-011.coverage`, `round1/coverage.md#finding-1`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-011.semantics`, `issue-ledger.yaml#dep-002`, `rel-002`, `issue-ledger.yaml#dep-003`, `rel-003`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk, fix_now
  - materiality: This is material under the declared correctness and maintainability goals in the explicitly requested time-source-semantics focus area. If a caller uses an injected, fake, or skewed clock for flush's now, the closure subtraction at line 32 becomes a cross-clock artifact: the window never closes, closes immediately, or stays open indefinitely, so the named 30-second contract is unkeepable across clocks. Simultaneously, the only per-item time fact in the contract, enqueuedAt, carries a documented meaning that no behavior establishes or verifies, so any consumer trusting it as actual batch-entry time (metrics, ordering, expiry) is misled while callers populate it pre-enqueue.
  - root cause: The accepted compound root is that the batcher owns no time fact: the window anchor, the closure input, and the documented batch-entry timestamp are all sourced from ambient or caller-chosen clocks that the batcher neither establishes nor validates. It is the starting point because both surface findings reduce to that absent authority — finding-011's root step (finding-011.cause-002: no single time authority, and the parameter name "now" falsely asserts a shared timebase) and finding-012's root step (finding-012.cause-002: batch-entry semantics assigned to a caller-constructed pre-enqueue value the batcher never stamps). Root confidence is deliberately medium because the hypothesis carries a built-in refutation condition accepted by all lenses: if clock unification alone left finding-012 intact without the enqueue-site stamping change, the common root would be refuted. Framing classifies this issue as root_cause with judgment inferred, per rel-006 pairing both findings under the compound root.
  - causal path: Two parallel chains converge on the compound root. For finding-011: WINDOW_MS does not denote 30 seconds of any single clock because the closure subtraction mixes the internal wall-clock anchor with whatever the caller passes as now (cause-001), and that mixing exists because no single time authority lives in the class while the name "now" falsely asserts a shared timebase (cause-002, root). For finding-012: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher (cause-001), because batch-entry semantics were assigned to a caller-constructed pre-enqueue value the caller cannot truthfully populate and the batcher never stamps at the enqueue site (cause-002, root). rel-006 pairs the two findings under the same batcher-owns-no-time-fact root, and dep-002/dep-003 record it as a shared cause with issue-001 (the undeclared necessary condition of the closure rule, rel-002) and issue-005 (the missing clock-authority concept, rel-003) — the same absent clock binding observed from the logic, coverage, and semantics angles.
  - action: Deliberation was skipped as no-deliberation-needed (consistent_stances): all three lenses support the claim, accept the compound root including its refutation condition, and keep medium severity, with nothing left to deliberate. The required action is to designate one batcher-owned time authority for the window lifecycle and have the batcher establish per-item enqueuedAt from that same authority at the enqueue site; alternatively, if caller-supplied time is intentional, rename/re-document the field to its true provenance and either use it or remove it from the contract. Dependency and ordering matter: closure is needs_decision with must_close_in_target, and the single-time-authority decision is instantiated by the in-target fixes of issues 001 and 005 (dep-002/dep-003), so the authority should be chosen once there and applied here. Per the accepted refutation condition, clock unification alone is insufficient — the enqueue-site stamping (or the re-documentation alternative) must accompany it to close finding-012.

#### Non-Material Findings
- issue-003 (low)
  - affected purpose: Verifiability and maintainability of the flush return-value protocol documentation.
  - failure condition: The no-window state (windowStartedAt === null): nothing enqueued yet, or immediately after a flush.
  - impact: A polling caller cannot distinguish 'still batching' from 'idle' via the documented protocol; the stated rule is formally false in a reachable state, though behaviorally benign for pure polling loops.
  - evidence: `round1/logic.md#finding-l3`
  - source lenses: logic
  - action candidates: follow_up
  - problem definition: The flush return-value protocol overloads [] across two distinct reachable states (no window exists vs window still open) while the documentation at line 28 asserts a single meaning, making the documented rule false in the reachable no-window state (windowStartedAt === null).
  - problem framing: independent_issue / planned_follow_up / document_only / may_close_during_next_stage / observed

- issue-004 (low)
  - affected purpose: Correctness and maintainability of the PendingNotification interface definition under the time-source-semantics focus.
  - failure condition: Any caller that populates enqueuedAt with a value other than Date.now() at the moment of the enqueue call — permitted by the declared contract.
  - impact: Two definitions of one instant may silently diverge; the divergence is latent (no current reader of enqueuedAt) but undermines the documented meaning of the contract field.
  - evidence: `round1/logic.md#finding-l4`
  - source lenses: logic
  - action candidates: follow_up, accept_risk
  - problem definition: Two recordings purport to denote the batch-entry instant — the caller-supplied enqueuedAt field (lines 8-9) and the internally sampled windowStartedAt (line 20) — with no reconciliation rule or designated authority, so the field's documented definition holds only under the undeclared convention that callers pass Date.now() at call time and is latently false for permitted inputs.
  - problem framing: symptom / planned_follow_up / needs_decision / may_close_during_next_stage / inferred

- issue-008 (low)
  - affected purpose: Review goal `verifiability`: reconstructability of past batch state and verifiability of the carry-over comment.
  - failure condition: Any post-hoc attempt to verify what a flushed window contained or when items were delivered — state is destroyed at flush with nothing persisted.
  - impact: Past windows cannot be reconstructed and the documented carry-over assertion can never be checked against evidence.
  - evidence: `round1/coverage.md#finding-4`
  - source lenses: coverage
  - action candidates: follow_up, accept_risk
  - problem definition: The temporal axis is declared on the input side (enqueuedAt) but has no output or history counterpart — the field is never read, digests carry only bodies, and flush destroys state without persisting any record — so past batch state cannot be reconstructed and the carry-over comment can never be checked against evidence.
  - problem framing: independent_issue / planned_follow_up / needs_decision / planned_later / observed

- issue-009 (low)
  - affected purpose: Review goals `verifiability` and `regression_risk`: operational detectability of the poll-stall failure mode.
  - failure condition: Polling stops or slows while items age in pending — the existing metric reports user breadth only and cannot reveal volume or staleness.
  - impact: The system's characteristic failure mode (items aging indefinitely) is invisible to operators through the provided monitoring surface.
  - evidence: `round1/coverage.md#finding-5`
  - source lenses: coverage
  - action candidates: follow_up
  - problem definition: Monitoring concept distribution is skewed to the one dimension — user breadth via pendingUserCount — that is formally invariant to pending-item volume and age, so the contract's characteristic poll-stall failure mode (items aging indefinitely) is undetectable through the provided monitoring surface.
  - problem framing: independent_issue / planned_follow_up / carry_forward / planned_later / inferred

- issue-012 (low)
  - affected purpose: Review goal `maintainability`: name-vs-behavior fit of the flush API surface and its return-value documentation.
  - failure condition: Readers or callers relying on the conventional meaning of flush, the documented single meaning of [], or strict window-bounded digest membership.
  - impact: Names and docs overpromise relative to behavior, inviting incorrect caller assumptions, though the behavior itself is consistent for polling use.
  - evidence: `round1/semantics.md#finding-4`
  - source lenses: semantics
  - action candidates: follow_up
  - problem definition: The flush name and its documentation claim stricter semantics than the behavior provides — unconditional force-out vs window-gated conditional poll, a single documented meaning for [] vs two guard states, and window-bounded digest membership vs poll-time membership — inviting incorrect caller assumptions although the behavior is internally consistent for polling use.
  - problem framing: independent_issue / planned_follow_up / document_only / may_close_during_next_stage / observed

#### Action Candidates
- issue-001: fix_now
  - rationale: High-severity material candidate with unanimous support/accepts/keeps stances and a no-deliberation-needed resolution. Classified symptom rather than root_cause because the closure contradiction is the manifestation of the absent inter-clock binding carried as shared cause by issue-005 (dep-001) and issue-011 (dep-002). The violation is a deductive derivation over permitted inputs rather than a directly observed runtime failure (inferred); it breaks the declared correctness goal on the core C4 flush contract inside the requested time-source-semantics focus (current_blocker), and binding both closure operands to one declared clock authority is achievable within the single-file target (fix_now, must_close_in_target). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window-closed predicate compares a caller-supplied clock (`now`) against an internally sampled ambient clock (`Date.now()`); the rule set is jointly satisfiable only under an undeclared necessary condition that both values come from the same epoch-ms clock, so C4 ('flushes when the window has closed') is violated for permitted inputs. Source finding context: Logic lens found an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush): a necessary shared-clock constraint is treated as merely possible, directly hitting the requested time-source-semantics focus and the correctness review goal. Residual contradiction persists after semantic disambiguation of `now`, so logic holds the primary finding. Source finding context: Use one time authority for the whole window lifecycle: either thread the caller clock through both entry points (enqueue(item, now) or use item.enqueuedAt to start the window), or drop the now parameter and use Date.now() inside flush (providing the test time-injection seam via an injected clock). At minimum document that now must be epoch ms from the same clock as Date.now(). Source finding context: round1/logic.md#finding-l1 Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract. Source finding context: The design splits time authority across the two mutating entry points — window start from the ambient clock at enqueue (line 20), window closure from injected time at flush (line 30) — with no claim in the target declaring or enforcing the required inter-clock identity. Source finding context: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. Source finding context: Time authority is split across enqueue (ambient Date.now(), line 20) and flush (caller-injected now, line 30) and no declared contract binds the two clocks, so the closure rule's truth depends on an undeclared necessary condition.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-005: fix_now
  - rationale: High-severity material candidate, unanimous stances, no-deliberation-needed. Classified root_cause because the absent clock-authority concept is the shared cause (dep-001, dep-003) on which issue-001's closure contradiction rests and which issue-011 reaches from the naming side. The absence is directly verifiable across the file (observed). A cross-clock subtraction can make the window close never, early, or instantly, breaking the declared correctness goal under the requested time-source-semantics focus (current_blocker, correctness); designating a single time authority for anchoring and closure is in-target work (fix_now, must_close_in_target). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: Window-closure decisions mix two undeclared time authorities (internal Date.now() at enqueue vs caller-supplied now at flush) with no single time-source authority concept. Source finding context: Coverage lens flagged a dimensional deficit: one time-dependent value (window age) is parallel-managed across two time sources with no single-origin authority designation anywhere in the file; the request summary explicitly scopes time-source semantics into this review. Source finding context: Designate a single time authority; smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied time source (enqueue(item, now) or an injected clock used by both methods), and document on flush that now must come from that same authority. Source finding context: round1/coverage.md#finding-1 Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior. Source finding context: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32). Source finding context: The window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. Source finding context: The closure predicate (line 32) is a cross-clock subtraction: windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). Source finding context: No clock-authority concept exists in the contract — nothing binds the caller's time source to the internal anchor's source, so the two authorities are free to diverge.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-002: fix_before_release, fix_now
  - rationale: Medium-severity material candidate with unanimous stances, resolved no-deliberation-needed. The joint unsatisfiability of C7 and C8 is a formal derivation (inferred) over a doc-vs-behavior disagreement (consistency). The root (no snapshot mechanism) is self-contained — rel-004 records duplication with issue-010, not a shared root, and the contract licenses merges only via same_root_candidate — so independent_issue. Aligning comment and behavior is local to the target (fix_now, must_close_in_target); the happy path still functions, so it undermines trust in the documented contract rather than blocking current execution (next_step_blocker). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-40, notification-batcher.ts:34-36 Source finding context: The comment claims notifications enqueued between the window-close check and the reset are carried into the next window, but under synchronous execution that interval admits no enqueues (the claim is vacuous), and under the only materially reachable reading (enqueued after the window-close instant, before the flush poll) such items are emitted in the current digest and cleared, never carried over. The documented guarantee and the implemented behavior are not jointly satisfiable. Source finding context: Logic lens found C7 (carry-over guarantee, lines 37-38) and C8 (emit-all-then-clear, lines 34-36,39) jointly unsatisfiable for any item present at flush time: an obligation is asserted over an interval the execution model makes empty, masking a false guarantee in the reachable interpretation — a contract-vs-behavior contradiction under the correctness goal. Source finding context: Make the code and the claim agree: either implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). Source finding context: round1/logic.md#finding-l2 Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee). Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior. Source finding context: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only while content is taken wholesale at emission time, so the promised carry-over has no mechanism that could implement it. Source finding context: The comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. Source finding context: No mechanism snapshots window membership at the close instant — timing and content are decoupled (close check gates when, the loop takes whatever is present) — making the obligated carry-over state unreachable.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-006: fix_before_release, accept_risk
  - rationale: Medium-severity material candidate, unanimous stances, no-deliberation-needed. The silent-loss conclusion is a valid inference from the exported surface, not a contradiction of any declared claim (inferred); the defect is a missing lifecycle termination segment (completeness). dep-004 records a shared mid-path cause with issue-007 (poll-and-window-gated sole reducer) but the roots and mitigations are distinct, so independent_issue. Closure requires choosing and adding a terminal operation plus a documented caller shutdown obligation — a contract-extension decision (needs_decision) that must land before any next stage relies on loss-free delivery (next_step_blocker, must_close_before_next_stage). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:30-32,44-47 Source finding context: The pending-notification lifecycle covers enqueue and window-gated flush but has no termination segment (drain/close/dispose); pending items are unreachable at shutdown or poller stop and are silently lost. Source finding context: Coverage lens ghost-sub-area check: lifecycle termination is implied by the batching domain but entirely absent — the only exit path for pending items is a future poll that satisfies the window predicate, a correctness and regression risk inside the declared review goal. Source finding context: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }> that emits digests for all pending items regardless of WINDOW_MS and resets state; document that callers must invoke it on shutdown. Source finding context: round1/coverage.md#finding-2 Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it. Source finding context: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member. Source finding context: At process shutdown, poller failure, or decommission, pending notifications are silently dropped because the poll that would flush them never happens. Source finding context: Every pending item's only exit is a future external poll of flush that also satisfies the WINDOW_MS predicate (lines 28, 32). Source finding context: The API has no terminal-lifecycle operation (drain/close/dispose) anywhere in the class body, so no unconditional exit path exists.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-007: fix_before_release, accept_risk
  - rationale: Medium-severity material candidate, unanimous stances, no-deliberation-needed. Unbounded growth follows necessarily from unconditional append plus the poll-and-window-gated sole reducer (inferred); the defect is an absent capacity/overflow/backpressure concept (completeness). Distinct root and mitigation from issue-006 despite the shared mid-path cause recorded by dep-004, so independent_issue. Closure requires selecting a documented capacity bound and an explicit overflow policy — reject, drop-oldest, or force-flush — and surfacing overflow to the caller (needs_decision, next_step_blocker, must_close_before_next_stage). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:21-23,32 Source finding context: The pending store grows without bound — no max-size, overflow policy, or backpressure concept exists, while the only reducer (flush) is poll- and window-gated. Source finding context: Coverage lens ghost-sub-area check: the batching domain implies a capacity/overflow sub-area (bounded buffers, drop/force-flush policies) that is entirely absent; combined with the unguarded poll dependency this is a regression and correctness risk within the declared review goal. Source finding context: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Source finding context: round1/coverage.md#finding-3 Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract. Source finding context: No capacity concept exists in the contract or implementation (absence across lines 14-48): enqueue has no size check or rejection path and nothing in the file mentions a bound. Source finding context: Pending storage grows without bound when the poller stalls while enqueues continue. Source finding context: The sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28). Source finding context: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, so enqueue appends unconditionally for any number of users and items.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-010: fix_before_release, fix_now
  - rationale: Medium-severity material candidate, unanimous stances, no-deliberation-needed. The comment-vs-clear() mismatch is directly readable at lines 37-39 (observed) and is a documented-contract consistency defect that licenses a false loss-free assumption — a regression trap for maintainers and any future async refactor. rel-004 records it as a duplicate surface of issue-002 without licensing a merge (relation is duplicates, not same_root_candidate), so independent_issue. Aligning the comment with the mechanism (or vice versa) is local to the target (fix_now, must_close_in_target); it endangers maintenance and future change rather than current execution (next_step_blocker). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match the behavior of this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: Semantics lens found the documented contract false as written: the guarantee describes an interleaving point that synchronous flush makes unreachable, and under any future condition where it became reachable clear() would silently drop — the opposite of the documented carry-over; a regression trap under the declared correctness/regression_risk/ maintainability goals. Source finding context: Make the comment state the true semantics (flush is synchronous; nothing can interleave; if it ever becomes async, interleaved enqueues would be dropped by clear() — preserve them explicitly), or implement real carry-over by iterating a snapshot and deleting only flushed entries so the comment becomes true. Source finding context: round1/semantics.md#finding-1 Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter. Source finding context: The comment asserts a behavioral guarantee about a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while clear() inverts the promised outcome if it ever became reachable. Source finding context: The carry-over comment does not match this.pending.clear(), which unconditionally discards every entry regardless of when it was enqueued. Source finding context: The guarantee was written over an unreachable interleaving point (synchronous flush, no await/callback/iterator hand-off between lines 32 and 39) with no implementing mechanism, so it is vacuous today and inverted under any future reachability.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-011: fix_before_release, accept_risk, fix_now
  - rationale: Medium-severity material candidate, unanimous stances, no-deliberation-needed, with medium root confidence and an accepted built-in refutation condition — clock unification alone leaving finding-012 intact would refute the common root (inferred). Classified root_cause because rel-006 pairs findings 011 and 012 under the compound batcher-owns-no-time-fact root and dep-002/dep-003 record it as shared cause with issue-001 and issue-005. Impact lands on the declared correctness goal for the named 30-second window semantics. Closure requires the single-time-authority designation plus the stamp-enqueuedAt-or-redocument choice (needs_decision); that decision is instantiated by the in-target fixes of issues 001/005 (must_close_in_target), while its own residual bite beyond those highs is medium (next_step_blocker). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32); the name now implies the same timebase but no contract binds the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock. Source finding context: Semantics lens found `now` is a homonym across two unrelated clocks: the parameter name semantically asserts one shared notion of now while the class holds no single time authority — directly violating the request focus on time-source semantics and the correctness goal. Source finding context: Pick one time authority: either accept now everywhere (add a now or injected-clock argument to enqueue and set windowStartedAt from it) or use Date.now() everywhere and drop the now parameter from flush; if the parameter is kept, document the precondition that now must come from the same epoch-ms clock as Date.now(). Source finding context: round1/semantics.md#finding-2 Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: The class holds no single time authority: line 20 hardcodes Date.now() while line 30 delegates time to the caller, and no statement of the shared-clock precondition exists on the interface. Source finding context: WINDOW_MS does not denote 30 seconds of any single clock: the closure subtraction mixes the internal wall-clock anchor with whatever value the caller passes as now. Source finding context: No single time authority exists in the class and the parameter name now falsely asserts a shared timebase, so nothing binds the two operands of the closure subtraction to one clock. Source finding context: notification-batcher.ts:8-9,19-24 Source finding context: enqueuedAt is documented as 'Epoch ms when the notification entered the batch' (line 8), but enqueue (lines 19-24) neither assigns nor validates it and no code in the target reads it; the value is caller-supplied before the notification has entered any batch, so the name/doc asserts a fact only the batcher could establish. Source finding context: Semantics lens found the field's documented meaning diverges from its actual provenance: batch-entry semantics are assigned to a value constructed before enqueue, the per-item batch-entry time is captured nowhere, and the documented meaning is verified by no behavior; ontological note: PendingNotification is a phase-named sortal applied to pre-enqueue (not yet pending) values. Source finding context: Make the batcher own the timestamp: have enqueue accept {userId, body} and stamp enqueuedAt itself from the single time authority chosen for finding-011, storing the enriched record internally; or if caller-supplied time is intentional, rename/re-document the field to its true provenance (e.g. createdAt) and either use it or remove it from this contract. Source finding context: round1/semantics.md#finding-3 Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior. Source finding context: The field documentation assigns batch-entry semantics to a value constructed by the caller before enqueue is invoked; the only component that could truthfully populate it (the batcher, at the enqueue site) does not. Source finding context: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher; the only entry-time fact recorded is windowStartedAt, a different concept. Source finding context: Batch-entry semantics were assigned to a caller-constructed pre-enqueue value that the caller cannot truthfully populate as documented, and the batcher never stamps it at the enqueue site.
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-003: follow_up
  - rationale: Low severity, non-material per the material-severity candidate boundary; unanimous stances; skipped from deliberation as non_material_issue. The doc rule's falsity in the windowStartedAt === null state is directly traceable in the code path (observed) and is a doc-protocol inconsistency that is behaviorally benign for pure polling callers. No relation connects finding-003 to any other finding (independent_issue). Narrowing the documentation to cover both empty-return states closes it (document_only) and can ride along the material doc corrections (planned_follow_up, may_close_during_next_stage). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: The documented interpretation rule 'an empty array means window still open' is contradicted by the no-window state: when windowStartedAt is null (nothing enqueued, or just flushed), flush returns [] although no window exists, so the conditional rule 'empty implies window still open' is false in a reachable state. Source finding context: Logic lens found the doc-level conditional C5 (empty return implies open window) violated by the reachable no-window state at line 31 — a formal contract-vs-behavior inconsistency in the return-value protocol, kept surface-only per severity (behaviorally benign for pure polling loops). Source finding context: Either narrow the doc ('an empty array means there is nothing ready to flush — the window is still open or no notifications are pending') or split the protocol (e.g. null for no-window vs [] for window-open) if callers need to distinguish the states. Source finding context: round1/logic.md#finding-l3
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-004: follow_up, accept_risk
  - rationale: Low severity, non-material, latent — no current reader of enqueuedAt exists; unanimous stances; skipped as non_material_issue. The definitional failure arises only under permitted-but-unexercised caller inputs (inferred) and is a contract-field consistency defect. Classified symptom because the recorded semantics stance states the deeper time-ownership root is carried separately by issue-011, even though no graph relation licenses a merge. The pick-one-authority action is a decision (make enqueuedAt load-bearing vs remove it) that resolves naturally alongside issue-011's authority designation (needs_decision, planned_follow_up, may_close_during_next_stage). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:20 Source finding context: C2 defines enqueuedAt as 'Epoch ms when the notification entered the batch', yet enqueue records the same event instant independently via Date.now() and never consults enqueuedAt anywhere; the definition holds only under the undeclared necessary condition that callers pass Date.now() at call time, so the model permits states in which the field's definition is false. Source finding context: Logic lens found two recordings purporting to denote the first-enqueue instant (caller-supplied enqueuedAt vs internally sampled windowStartedAt) with no reconciliation rule — a definitional inconsistency in time-source semantics; latent rather than active since no behavior currently reads the field, kept surface-only per severity. Source finding context: Pick one authority for the batch-entry instant: use item.enqueuedAt to start the window (making the field load-bearing, aligning with the finding-001 single-authority fix), or remove the field from the required interface if the batcher is the time authority; document the chosen authority on the interface. Source finding context: round1/logic.md#finding-l4
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-008: follow_up, accept_risk
  - rationale: Low severity, non-material; unanimous stances; skipped as non_material_issue. Both premises — enqueuedAt is read by nothing and flush persists nothing — are directly checkable in the target (observed). The deficit is scoped to the declared verifiability goal, i.e. reconstructability and auditability of delivered batches (governance_value). No relation connects finding-008 to any other finding (independent_issue). The remedy admits two distinct paths (remove the contract field vs give it a consumer and report flush metadata), so needs_decision, scheduled as planned_follow_up with planned_later obligation under the bounded_partial closure level. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,35,37-38 Source finding context: enqueuedAt is a documented contract field but is never read; digests carry only bodies and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment is unverifiable. Source finding context: Coverage lens dimensional-deficit check: the temporal axis is declared at input but absent on the output/history side — declared temporality with no history/evidence counterpart, bearing on the declared verifiability goal; kept surface-level per severity. Source finding context: Either remove enqueuedAt from the contract or use it (order digest lines by it, include timestamps in the digest, or expose flushed-batch metadata); at minimum have flush return or report enough metadata (window start, item counts) that delivered batches are verifiable. Source finding context: round1/coverage.md#finding-4
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-009: follow_up
  - rationale: Low severity, non-material; unanimous stances; skipped as non_material_issue. Undetectability is a valid inference from the metric's type — for any fixed pendingUserCount there exist states with arbitrarily many or arbitrarily old pending items (inferred). The gap is missing monitoring dimensions (pending-item count, oldest-pending-age), an operability gap rather than a correctness defect (completeness). No relation connects finding-009 to any other finding (independent_issue). Adding the accessors is concrete backlog work carried beyond this review (carry_forward, planned_follow_up, planned_later). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28,44-47 Source finding context: Monitoring covers only the user-count dimension (pendingUserCount); there is no pending-item count and no oldest-pending-age metric, so a stalled poll loop — the failure mode the poll-driven contract makes possible — is undetectable. Source finding context: Coverage lens observability-dimension check: concept distribution in the monitoring sub-area is skewed toward the one dimension (user breadth) that cannot detect the characteristic poll-stall failure mode; kept surface-level per severity. Source finding context: Add pendingItemCount() and an oldest-age accessor (which would also give enqueuedAt a consumer, see finding-008) so operators can alert on aging pending notifications. Source finding context: round1/coverage.md#finding-5
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`
- issue-012: follow_up
  - rationale: Low severity, non-material; unanimous stances; skipped as non_material_issue. The name/doc overpromise is directly readable from the surface inventory (observed), and the behavior itself is internally consistent for polling use, so the impact is name-vs-behavior fit of the API surface (maintainability_evolvability). No relation connects finding-013 to any other finding (independent_issue). Re-documenting the conditional-poll semantics, the two empty-return guard states, and the late-item absorption rule closes it without behavior change (document_only) and can ride along the related doc corrections (planned_follow_up, may_close_during_next_stage). Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:26-32 Source finding context: flush conventionally means 'force out pending content', but this method is a conditional poll that usually returns []; the doc 'an empty array means window still open' (line 28) is also inaccurate because [] is equally returned when no window exists at all (line 31), and items enqueued after the window elapsed but before the poll are absorbed into the closing digest, loosening the documented 'when the batch window closes' semantics (lines 2-3). Source finding context: Semantics lens surface naming/doc finding: the name and docs claim stricter semantics than the behavior provides (unconditional flush vs conditional poll; single meaning for [] vs two guard states; window-bounded membership vs poll-time membership); kept surface-level per severity. Source finding context: Rename to poll-style (pollDigests, flushIfWindowClosed) or document the conditional behavior; correct the doc to 'an empty array means no closed window is ready (no window started, or window still open)'; either state that late items ride the closing digest or filter membership by window if strict windowing is intended. Source finding context: round1/semantics.md#finding-4
  - derivation refs: `issue-ledger.yaml`, `problem-framing.yaml`

### Consensus (3/3, core-axis mode)
- issue-001 (high): The window-closed predicate in flush subtracts an internally Date.now()-sampled anchor (set at enqueue, line 20) from a caller-supplied now (line 30), so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. This is a high-severity, material correctness defect that must be closed in the target by binding the whole window lifecycle to one declared time authority.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The window-closed predicate subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window-closed predicate compares a caller-supplied clock (`now`) against an internally sampled ambient clock (`Date.now()`); the rule set is jointly satisfiable only under an undeclared necessary condition that both values come from the same epoch-ms clock, so C4 ('flushes when the window has closed') is violated for permitted inputs. Source finding context: Logic lens found an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush): a necessary shared-clock constraint is treated as merely possible, directly hitting the requested time-source-semantics focus and the correctness review goal. Residual contradiction persists after semantic disambiguation of `now`, so logic holds the primary finding. Source finding context: Use one time authority for the whole window lifecycle: either thread the caller clock through both entry points (enqueue(item, now) or use item.enqueuedAt to start the window), or drop the now parameter and use Date.now() inside flush (providing the test time-injection seam via an injected clock). At minimum document that now must be epoch ms from the same clock as Date.now(). Source finding context: round1/logic.md#finding-l1 Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract. Source finding context: The design splits time authority across the two mutating entry points — window start from the ambient clock at enqueue (line 20), window closure from injected time at flush (line 30) — with no claim in the target declaring or enforcing the required inter-clock identity. Source finding context: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. Source finding context: Time authority is split across enqueue (ambient Date.now(), line 20) and flush (caller-injected now, line 30) and no declared contract binds the two clocks, so the closure rule's truth depends on an undeclared necessary condition.
  - affected purpose: Declared review goal `correctness` and the flush contract C4 ('flushes when the window has closed') under the requested time-source-semantics focus. Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area.
  - failure condition: Any permitted caller whose flush(now) value comes from a clock other than Date.now() — test clock, monotonic clock, replayed or skewed clock — since the signature flush(now: number) carries no time-base constraint. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number.
  - impact: With a smaller-magnitude (e.g. monotonic) now the window never closes and flush returns [] forever; with a larger-epoch now it flushes immediately, violating the 30s window rule — the core batching contract cannot be trusted for permitted inputs. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract.
  - root hypothesis: Time authority is split across the two mutating entry points — the window anchor is sampled from ambient Date.now() at enqueue (line 20) while window closure is computed from the caller-injected now at flush (line 30) — and no declared contract binds the two clocks, so the closure rule's truth rests on an undeclared necessary condition. If binding both operands of the closure predicate to one declared clock authority removes the contradiction for all permitted inputs, the hypothesis holds; if the contradiction persists, it is refuted.
  - evidence: `round1/logic.md#finding-l1`, `finding-ledger.yaml#finding-001`, `finding-001.cause-001`, `finding-001.cause-002`, `issue-stance-matrix.yaml#stances.issue-001.logic`, `issue-ledger.yaml#issue-001`, `issue-stance-matrix.yaml#stances.issue-001.coverage`, `round1/coverage.md#finding-1`, `issue-stance-matrix.yaml#stances.issue-001.semantics`, `round1/semantics.md#finding-2`, `issue-ledger.yaml#dep-001`, `rel-001`, `issue-ledger.yaml#dep-002`, `rel-002`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_now
  - materiality: The issue strikes the declared review goal `correctness` on the core flush contract C4 inside the explicitly requested time-source-semantics focus. The signature flush(now: number) carries no time-base constraint, so any permitted caller whose now comes from a clock other than Date.now() — a test clock, monotonic clock, or replayed/skewed clock — triggers the failure: with a smaller-magnitude now the window never closes and flush returns [] forever, while with a larger-epoch now it flushes immediately. Either way the declared 30s digest behavior is unachievable for permitted inputs, so the primary batching contract cannot be trusted.
  - root cause: Time authority is split across the two mutating entry points: the window anchor is sampled from ambient Date.now() at enqueue (line 20) while window closure is computed from the caller-injected now at flush (line 30), and no declared contract binds the two clocks. The closure rule's truth therefore rests on an undeclared necessary condition — that both values come from the same epoch-ms clock — which the contract treats as merely possible. This is the starting point because every downstream symptom (wrong closure, never/early/instant flush) follows directly from the two operands of the closure predicate living on free-running, unbound time sources; the hypothesis carries its own falsification test: binding both closure operands to one declared clock authority must remove the contradiction for all permitted inputs, and root confidence is high.
  - causal path: The logic lens's finding-001 chain has two steps over evidence round1/logic.md#finding-l1. Step finding-001.cause-001: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. That step is caused by the root step finding-001.cause-002: time authority is split across enqueue and flush with no declared contract binding the two clocks, so the closure rule depends on an undeclared necessary condition. The lens derived this as an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush); the residual contradiction persists after semantic disambiguation of `now`, leaving nothing unresolved beyond evidence. Coverage independently located the same absent inter-clock binding at the identical anchors, and semantics reached the same terminal cause from the naming side, corroborating the chain.
  - action: Adopt a single time authority for the whole window lifecycle — either thread the caller clock through both entry points (enqueue(item, now) or anchor the window from item.enqueuedAt) or use one injected/ambient clock for both — and declare the time-base constraint on the interface. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters because of the shared-cause dependencies: dep-001 links this issue to issue-005 (the missing time-source authority concept, coverage side) and dep-002 links it to issue-011 (no single time authority plus a false shared-timebase name, semantics side); all three rest on the same absent inter-clock binding, and this issue is framed as the symptom of that shared cause, so designating the single clock authority once closes the contradiction here rather than patching the predicate in isolation. Deliberation was skipped (consistent_stances): all three lenses support the issue, accept the single-clock-authority root hypothesis, and keep high severity, with no remaining disagreement or follow-up evidence required.

- issue-005 (high): Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue (line 20) and caller-supplied now at flush (line 30) — and no single-origin time-source authority concept exists anywhere in the file. This is a high-severity, material correctness defect framed as the root cause shared with issue-001 and issue-011: designate one time authority for window anchoring and closure and document it on the interface.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue and caller-supplied now at flush — with no single-origin time-source authority concept anywhere in the file. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: Window-closure decisions mix two undeclared time authorities (internal Date.now() at enqueue vs caller-supplied now at flush) with no single time-source authority concept. Source finding context: Coverage lens flagged a dimensional deficit: one time-dependent value (window age) is parallel-managed across two time sources with no single-origin authority designation anywhere in the file; the request summary explicitly scopes time-source semantics into this review. Source finding context: Designate a single time authority; smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied time source (enqueue(item, now) or an injected clock used by both methods), and document on flush that now must come from that same authority. Source finding context: round1/coverage.md#finding-1 Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior. Source finding context: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32). Source finding context: The window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. Source finding context: The closure predicate (line 32) is a cross-clock subtraction: windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). Source finding context: No clock-authority concept exists in the contract — nothing binds the caller's time source to the internal anchor's source, so the two authorities are free to diverge.
  - affected purpose: Declared review goal `correctness` for window-closure decisions under the requested time-source-semantics focus. Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics.
  - failure condition: Whenever the caller's now comes from a different epoch or skewed source than Date.now() (test clock, monotonic clock, mocked scheduler time), the closure predicate becomes a cross-clock subtraction. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor.
  - impact: A cross-clock subtraction makes the window close never, early, or instantly, violating the declared correctness goal inside the bounded target. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior.
  - root hypothesis: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32), so the two time authorities are free to diverge. If designating a single time authority for both entry points eliminates the cross-clock subtraction, the hypothesis holds.
  - evidence: `round1/coverage.md#finding-1`, `finding-ledger.yaml#finding-005`, `finding-005.cause-001`, `finding-005.cause-002`, `finding-005.cause-003`, `issue-stance-matrix.yaml#stances.issue-005.logic`, `issue-ledger.yaml#issue-005`, `finding-relation-graph.yaml#rel-001`, `round1/logic.md#finding-l1`, `issue-stance-matrix.yaml#stances.issue-005.coverage`, `issue-stance-matrix.yaml#stances.issue-005.semantics`, `round1/semantics.md#finding-2`, `issue-ledger.yaml#dep-001`, `rel-001`, `issue-ledger.yaml#dep-003`, `rel-003`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_now
  - materiality: The issue weakens the declared review goal `correctness` for window-closure decisions under the explicitly requested time-source-semantics focus. Whenever the caller's now comes from a different epoch or skewed source than Date.now() — a test clock, monotonic clock, or mocked scheduler time — the closure predicate at line 32 becomes a cross-clock subtraction, making the window close never, early, or instantly. The core window contract thus fails under permitted caller behavior inside the bounded target, which is why it stands as a current_blocker rather than a latent concern.
  - root cause: The contract lacks any clock-authority concept: enqueue takes no clock parameter and nothing documents a requirement on the source of flush's now (absence across lines 18-32), so the two time authorities are free to diverge. This absence is the starting point — it is directly verifiable across the file (judgment state: observed), and the problem framing classifies this issue as the root_cause on which issue-001's closure contradiction rests (dep-001) and which issue-011 reaches from the naming side (dep-003). The hypothesis is falsifiable: if designating a single time authority for both entry points eliminates the cross-clock subtraction, it holds; root confidence is high.
  - causal path: The coverage lens's finding-005 chain has three steps over evidence round1/coverage.md#finding-1, running from symptom back to the root step finding-005.cause-003. Step finding-005.cause-001: the window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. That is caused by finding-005.cause-002: the closure predicate (line 32) is a cross-clock subtraction — windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). That in turn is caused by the root step finding-005.cause-003: no clock-authority concept exists in the contract, so nothing binds the caller's time source to the internal anchor's source and the two authorities are free to diverge. Coverage framed this as a dimensional deficit — one time-dependent value (window age) parallel-managed across two time sources with no single-origin authority designation — squarely inside the requested time-source-semantics scope; logic confirms the same absent binding is the premise of its closure contradiction (rel-001), and semantics reached the identical absence through naming analysis (rel-003: WINDOW_MS cannot denote 30 seconds of any single clock). Nothing is unresolved beyond evidence.
  - action: Designate a single time authority for window anchoring and closure — a shared caller-supplied time source threaded through both methods (the smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied source, e.g. enqueue(item, now)) or an injected clock used by both methods — and document on flush that its now must come from that same authority. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters: this issue is the shared cause behind issue-001's closure contradiction (dep-001) and converges with issue-011's missing-single-time-authority and false shared-timebase naming finding (dep-003), so resolving it first closes the common root for all three rather than treating each symptom separately. Deliberation was skipped (consistent_stances): all three lenses support the missing clock-authority concept, accept the root hypothesis, and keep high severity; the shared-cause dependencies are agreed dependency context, not stance conflicts, and no follow-up evidence is required.

- issue-002 (medium): The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: under synchronous execution the guaranteed interval admits no enqueues (the claim is vacuous), and in the only materially reachable reading items enqueued after the window-close instant are emitted in the current digest and wiped by clear(), never carried over. This is a medium-severity, material consistency defect: align the comment with the behavior, or implement the guarantee.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: logic
  - issue statement: The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: the guaranteed interval is empty under synchronous execution, and in the only materially reachable reading items are emitted in the current digest and cleared, never carried over. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-40, notification-batcher.ts:34-36 Source finding context: The comment claims notifications enqueued between the window-close check and the reset are carried into the next window, but under synchronous execution that interval admits no enqueues (the claim is vacuous), and under the only materially reachable reading (enqueued after the window-close instant, before the flush poll) such items are emitted in the current digest and cleared, never carried over. The documented guarantee and the implemented behavior are not jointly satisfiable. Source finding context: Logic lens found C7 (carry-over guarantee, lines 37-38) and C8 (emit-all-then-clear, lines 34-36,39) jointly unsatisfiable for any item present at flush time: an obligation is asserted over an interval the execution model makes empty, masking a false guarantee in the reachable interpretation — a contract-vs-behavior contradiction under the correctness goal. Source finding context: Make the code and the claim agree: either implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). Source finding context: round1/logic.md#finding-l2 Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee). Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior. Source finding context: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only while content is taken wholesale at emission time, so the promised carry-over has no mechanism that could implement it. Source finding context: The comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. Source finding context: No mechanism snapshots window membership at the close instant — timing and content are decoupled (close check gates when, the loop takes whatever is present) — making the obligated carry-over state unreachable.
  - affected purpose: Review goals `correctness` and `regression_risk`: the documented loss-safety (carry-over) contract of flush's reset step. Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee).
  - failure condition: Any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush. Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush.
  - impact: The documented carry-over guarantee has no implementing mechanism; maintainers and callers relying on it assume loss-free behavior that the code contradicts, weakening trust in the documented contract. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior.
  - root hypothesis: No code path snapshots window membership at the close instant — the close check (line 32) gates timing only while emission takes pending content wholesale — so the promised carry-over has no mechanism that could implement it. If filtering digest membership by the window-close instant (or snapshotting membership) makes the documented guarantee exhibitable, the hypothesis holds.
  - evidence: `round1/logic.md#finding-l2`, `finding-ledger.yaml#finding-002`, `finding-002.cause-001`, `finding-002.cause-002`, `issue-stance-matrix.yaml#stances.issue-002.logic`, `issue-ledger.yaml#issue-002`, `issue-stance-matrix.yaml#stances.issue-002.coverage`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-002.semantics`, `round1/semantics.md#finding-1`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: logic
  - action candidates: fix_before_release, fix_now
  - materiality: The issue weakens the declared review goals `correctness` and `regression_risk` on the documented loss-safety (carry-over) contract of flush's reset step. The failure condition is any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush — and any future async/re-entrant refactor of flush. Because the documented guarantee has no implementing mechanism, maintainers and callers relying on it assume loss-free behavior that the code contradicts, so the written contract actively misleads about loss behavior even though the happy path still functions; this undermines trust in the documented contract rather than blocking current execution (next_step_blocker).
  - root cause: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only, while the digest loop takes pending content wholesale at emission time. Timing and content are decoupled, so the obligated carry-over state is unreachable — the promised guarantee has no mechanism that could implement it. This is the starting point because the contradiction between C7 (carry-over guarantee) and C8 (emit-all-then-clear) is not a wording slip but the absence of any membership-snapshotting mechanism; the hypothesis is falsifiable — if filtering digest membership by the window-close instant (or snapshotting membership) makes the documented guarantee exhibitable, it holds — and root confidence is high.
  - causal path: The logic lens's finding-002 chain has two steps over evidence round1/logic.md#finding-l2. Step finding-002.cause-001: the comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. That step is caused by the root step finding-002.cause-002: no mechanism snapshots window membership at the close instant — the close check gates when, the loop takes whatever is present — making the obligated carry-over state unreachable. The lens derived C7 (lines 37-38) and C8 (lines 34-36, 39) as jointly unsatisfiable for any item present at flush time: an obligation asserted over an interval the synchronous execution model makes empty, masking a false guarantee in the reachable interpretation. Coverage corroborates that the guarantee has no implementing concept and no evidence counterpart (flush destroys state without persisting any record), and semantics holds the recorded duplicate of the surface finding, confirming that clear() at line 39 would invert the promised outcome if the interval ever became reachable. Nothing is unresolved beyond evidence.
  - action: Make the code and the claim agree, in one of two ways: implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state the actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). The projected action classes are fix_before_release and fix_now, with closure obligation must_close_in_target; the fix is local to the target file. No cross-issue ordering constraint applies: the root (no snapshot mechanism) is self-contained — rel-004 records duplication with issue-010, not a shared root — so this issue is framed as an independent_issue and can be closed on its own. Deliberation was skipped (consistent_stances): logic, coverage, and semantics unanimously support the contradiction and its no-snapshot-mechanism root, accept the hypothesis, and keep medium severity, with no remaining disagreement or follow-up evidence required.

- issue-006 (medium): The pending-notification lifecycle has no termination segment: the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/dispose member, so pending items are unreachable at shutdown, poller stop, or decommission and are silently lost. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires adding a terminal operation plus a documented caller shutdown obligation before the next stage relies on loss-free delivery.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The pending-notification lifecycle covers enqueue and window-gated flush but has no termination segment (drain/close/dispose); pending items are unreachable at shutdown or poller stop and are silently lost. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:30-32,44-47 Source finding context: Coverage lens ghost-sub-area check: lifecycle termination is implied by the batching domain but entirely absent — the only exit path for pending items is a future poll that satisfies the window predicate, a correctness and regression risk inside the declared review goal. Source finding context: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }> that emits digests for all pending items regardless of WINDOW_MS and resets state; document that callers must invoke it on shutdown. Source finding context: round1/coverage.md#finding-2 Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it. Source finding context: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member. Source finding context: At process shutdown, poller failure, or decommission, pending notifications are silently dropped because the poll that would flush them never happens. Source finding context: Every pending item's only exit is a future external poll of flush that also satisfies the WINDOW_MS predicate (lines 28, 32). Source finding context: The API has no terminal-lifecycle operation (drain/close/dispose) anywhere in the class body, so no unconditional exit path exists.
  - affected purpose: Review goals `correctness` and `regression_risk`: loss-free handling of pending notifications across the full lifecycle. Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths.
  - failure condition: Process shutdown, poller failure, or batcher decommission while items are pending — the future poll that would release them never happens. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state.
  - impact: Pending notifications are silently dropped with no contract acknowledging it; silent loss is unacceptable per the artifact's stated delivery purpose. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it.
  - root hypothesis: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member — so no unconditional exit path exists for pending items. Adding a terminal operation that emits regardless of WINDOW_MS removes the silent-loss mode; if loss persists with such an operation, the hypothesis is refuted.
  - evidence: `round1/coverage.md#finding-2`, `finding-ledger.yaml#finding-006`, `finding-006.cause-001`, `finding-006.cause-002`, `finding-006.cause-003`, `issue-stance-matrix.yaml#stances.issue-006.logic`, `issue-ledger.yaml#issue-006`, `issue-stance-matrix.yaml#stances.issue-006.coverage`, `issue-stance-matrix.yaml#stances.issue-006.semantics`, `issue-ledger.yaml#dep-004`, `rel-005`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals are correctness and regression_risk for loss-free handling of pending notifications across the full lifecycle. At process shutdown, poller failure, or batcher decommission with non-empty pending state, the future poll that would release those items never happens, so pending notifications are silently dropped with no contract surface acknowledging or mitigating the loss. Silent data loss directly contradicts the artifact's stated delivery purpose, which makes the issue material against the declared goals rather than a stylistic gap.
  - root cause: The chain starts at finding-006.cause-003: no terminal-lifecycle concept exists in the API — the exported surface is exactly enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member — so no unconditional exit path exists for pending items. This is the root because every downstream effect follows once it holds, and it carries a sound refutation condition accepted by the logic lens: if silent loss persisted even with a terminal operation that emits regardless of WINDOW_MS, the hypothesis would be refuted. The semantics lens independently verified the structural premise by inventorying the surface and finding no member whose name or doc denotes termination, drain, or disposal. Root confidence is high.
  - causal path: Because no terminal operation exists anywhere in the class body (finding-006.cause-003), every pending item's only exit is a future external poll of flush that must also satisfy the WINDOW_MS predicate at lines 28 and 32 (finding-006.cause-002); therefore, at process shutdown, poller failure, or decommission, the poll that would flush pending items never happens and they are silently dropped (finding-006.cause-001). This three-step causes chain, evidenced throughout by round1/coverage.md#finding-2, leads exactly to the observed issue: a lifecycle covering enqueue and window-gated flush but lacking the termination segment the batching domain implies. Deliberation was skipped (reason_code consistent_stances) with unanimous support/accepts/keeps; nothing was narrowed and no disagreement remains, and all lenses agree the shared mid-path cause with issue-007 (dep-004, rel-005) is a distinct-root relation rather than the same root.
  - action: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }>, that emits digests for all pending items regardless of WINDOW_MS and resets state, and document that callers must invoke it on shutdown. This is a contract-extension decision (closure_class needs_decision) and a next_step_blocker that must close before any next stage relies on loss-free delivery (must_close_before_next_stage). Ordering note: dep-004 records that issue-006 and issue-007 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but have distinct roots and distinct mitigations, so this terminal operation can be decided and landed independently of issue-007's capacity/overflow policy. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave silent loss unacknowledged against the artifact's stated delivery purpose.

- issue-007 (medium): The pending store grows without bound: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, enqueue appends unconditionally, and the sole reducer (flush) is poll- and window-gated. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires selecting a documented capacity bound and an explicit overflow policy, surfaced to the caller, before the next stage.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: coverage
  - issue statement: The pending store grows without bound — no max-size, overflow policy, or backpressure concept exists, while the only reducer (flush) is poll- and window-gated. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:21-23,32 Source finding context: Coverage lens ghost-sub-area check: the batching domain implies a capacity/overflow sub-area (bounded buffers, drop/force-flush policies) that is entirely absent; combined with the unguarded poll dependency this is a regression and correctness risk within the declared review goal. Source finding context: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Source finding context: round1/coverage.md#finding-3 Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract. Source finding context: No capacity concept exists in the contract or implementation (absence across lines 14-48): enqueue has no size check or rejection path and nothing in the file mentions a bound. Source finding context: Pending storage grows without bound when the poller stalls while enqueues continue. Source finding context: The sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28). Source finding context: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, so enqueue appends unconditionally for any number of users and items.
  - affected purpose: Review goals `correctness` and `regression_risk`: bounded resource behavior of the pending store in a long-lived process. Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling.
  - failure condition: A stalled or slow external poll loop while enqueues continue — memory consumption is proportional to enqueue rate times the gap between successful polls. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process.
  - impact: Unbounded memory growth in a long-lived process, with no contract surface to detect, reject, or shed load. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract.
  - root hypothesis: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation (absence across lines 14-48): enqueue appends unconditionally for any number of users and items. A documented capacity bound with an explicit overflow policy removes the unbounded-growth mode; if growth persists under a bound, the hypothesis is refuted.
  - evidence: `round1/coverage.md#finding-3`, `finding-ledger.yaml#finding-007`, `finding-007.cause-001`, `finding-007.cause-002`, `finding-007.cause-003`, `issue-stance-matrix.yaml#stances.issue-007.logic`, `issue-ledger.yaml#issue-007`, `issue-stance-matrix.yaml#stances.issue-007.coverage`, `issue-stance-matrix.yaml#stances.issue-007.semantics`, `issue-ledger.yaml#dep-004`, `rel-005`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: coverage
  - action candidates: fix_before_release, accept_risk
  - materiality: The declared review goals are correctness and regression_risk for bounded resource behavior of the pending store in a long-lived process. With a stalled or slow external poll loop while enqueues continue, memory consumption grows proportional to enqueue rate times the gap between successful polls, and the contract offers no surface to detect, reject, or shed load. Unbounded memory growth with no detection or overflow policy is an operational failure mode squarely inside the declared goals, which makes the issue material.
  - root cause: The chain starts at finding-007.cause-003: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation (absence across lines 14-48) — enqueue has no size check or rejection path and nothing in the file mentions a bound, so enqueue appends unconditionally for any number of users and items. This is the root because unbounded growth follows necessarily once it holds, and it carries a well-formed refutation test accepted by the logic lens: if growth persisted under a documented capacity bound, the hypothesis would be refuted. The semantics lens independently confirmed the absence on the inventoried surface — no name, doc, or contract element denotes capacity, overflow, or backpressure, and the docs make no boundedness promise. Root confidence is high.
  - causal path: Because no capacity, overflow, or backpressure concept exists (finding-007.cause-003), enqueue appends unconditionally while the sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28) (finding-007.cause-002); therefore, whenever the poller stalls or slows while enqueues continue, pending storage grows without bound (finding-007.cause-001). This three-step causes chain, evidenced throughout by round1/coverage.md#finding-3, leads exactly to the observed issue: a batching domain that implies a capacity/overflow sub-area which is entirely absent. Deliberation was skipped (reason_code consistent_stances) with unanimous support/accepts/keeps; nothing was narrowed and no disagreement remains, and the lenses agree the shared poll-gated-reducer cause with issue-006 (dep-004, rel-005) does not make the two issues share a root.
  - action: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Selecting the bound and the policy is a contract decision (closure_class needs_decision) and a next_step_blocker that must close before the next stage (must_close_before_next_stage). Ordering note: dep-004 records that issue-007 and issue-006 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but name distinct missing concepts with distinct mitigations (capacity/overflow policy vs terminal-lifecycle operation), so this fix can be decided and landed independently of issue-006's drain operation. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave an undetectable unbounded-growth mode in long-lived processes.

- issue-010 (medium): The inline comment at notification-batcher.ts lines 37-38 promises that notifications enqueued between the window-close check (line 32) and the reset (line 39) are carried into the next window, but this.pending.clear() at line 39 unconditionally discards every entry regardless of when it was enqueued. The documented carry-over contract is false as written and inverts the actual failure mode: the only conditions under which the promise would matter are exactly the conditions under which clear() silently drops the items. The comment and the mechanism must be aligned — either re-document the true synchronous semantics or implement real carry-over.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match the behavior of this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: Semantics lens found the documented contract false as written: the guarantee describes an interleaving point that synchronous flush makes unreachable, and under any future condition where it became reachable clear() would silently drop — the opposite of the documented carry-over; a regression trap under the declared correctness/regression_risk/ maintainability goals. Source finding context: Make the comment state the true semantics (flush is synchronous; nothing can interleave; if it ever becomes async, interleaved enqueues would be dropped by clear() — preserve them explicitly), or implement real carry-over by iterating a snapshot and deleting only flushed entries so the comment becomes true. Source finding context: round1/semantics.md#finding-1 Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter. Source finding context: The comment asserts a behavioral guarantee about a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while clear() inverts the promised outcome if it ever became reachable. Source finding context: The carry-over comment does not match this.pending.clear(), which unconditionally discards every entry regardless of when it was enqueued. Source finding context: The guarantee was written over an unreachable interleaving point (synchronous flush, no await/callback/iterator hand-off between lines 32 and 39) with no implementing mechanism, so it is vacuous today and inverted under any future reachability.
  - affected purpose: Review goals `correctness`, `regression_risk`, and `maintainability`: the truthfulness of the inline reset contract that future maintainers will rely on. Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract.
  - failure condition: A maintainer assumes loss-free carry-over per the comment; or a future async/re-entrant refactor makes the interleaving point reachable, where clear() silently drops the interleaved items. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable.
  - impact: The comment licenses an assumption of loss-free behavior that the code inverts — a material regression trap inside the bounded target. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter.
  - root hypothesis: The carry-over comment asserts a behavioral guarantee over a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while this.pending.clear() inverts the promised outcome if it ever became reachable; aligning comment and mechanism (snapshot or truthful re-documentation) resolves the issue.
  - evidence: `round1/semantics.md#finding-1`, `finding-ledger.yaml#finding-010`, `finding-010.cause-001`, `finding-010.cause-002`, `issue-stance-matrix.yaml#stances.issue-010.logic`, `issue-ledger.yaml#issue-010`, `finding-relation-graph.yaml#rel-004`, `round1/logic.md#finding-l2`, `issue-stance-matrix.yaml#stances.issue-010.coverage`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-010.semantics`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, fix_now
  - materiality: This is material under the declared correctness, regression_risk, and maintainability goals because the comment is the inline reset contract that future maintainers will rely on, and it licenses an assumption of loss-free carry-over that the code inverts. The guarantee is unexhibitable today (synchronous flush makes the described interleaving unreachable) and inverted (silent drop) under the only conditions where it would matter: a maintainer trusting the documented carry-over, or a future async/re-entrant refactor that makes the interleaving point reachable. That makes it a regression trap inside the bounded target, not a cosmetic doc nit.
  - root cause: The accepted root cause (finding-010.cause-002) is that the guarantee was authored over an interleaving point that the synchronous execution model makes unreachable — there is no await, callback, or iterator hand-off between the close check at line 32 and the reset at line 39 — and with no implementing mechanism behind it. This is the starting point of the problem because the visible comment-vs-clear() mismatch is only the surface symptom: a behavioral promise was written about a hypothetical execution window that cannot occur today, so the promise is vacuous now and, since clear() unconditionally discards, becomes an inverted promise under any future reachability. Root confidence is high and all lenses accepted this derivation.
  - causal path: The causal path has two steps from the semantics lens (round1 finding-1). Step finding-010.cause-001 records the observable defect: the carry-over comment does not match this.pending.clear(), which discards every entry unconditionally. Step finding-010.cause-002 explains why that mismatch exists and is marked as the root step: the guarantee was written over an unreachable interleaving point with no mechanism to implement it, so it is vacuous today and inverted under any future reachability. cause-002 causes cause-001 — a contract asserted over a non-existent execution point cannot be matched by the reset mechanism, which produces exactly the false-as-written, failure-mode-inverting comment stated in the issue. Logic's independent finding-l2 (C7 and C8 jointly unsatisfiable for any item present at flush time, duplication recorded by rel-004) and coverage's verifiability analysis (no mechanism or persisted record could implement or evidence the promised carry-over) converge on the same chain.
  - action: Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics unanimously support the claim, accept the root hypothesis, and keep medium severity, with no remaining disagreement and no follow-up evidence required. The required action offers two closure paths: (a) make the comment state the true semantics — flush is synchronous, nothing can interleave, and if flush ever becomes async, interleaved enqueues would be dropped by clear() and must be preserved explicitly; or (b) implement real carry-over by iterating a snapshot and deleting only flushed entries, making the comment true. Framing is fix_now with must_close_in_target, so the fix is local to the target and should land before the next change builds on the false contract. Ordering note: rel-004 records this as a duplicate surface of issue-002 without licensing a merge, so coordinate the fix so one comment/mechanism alignment closes both surfaces consistently.

- issue-011 (medium): Time ownership is undesigned in the batcher. windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32) with no contract binding the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock; and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher. Neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. The accepted resolution is to designate one batcher-owned time authority for the window lifecycle and have the batcher stamp enqueuedAt from that same authority at the enqueue site, or re-document the field to its true provenance.
  - issue stance agreement: 3/3
  - agreed or narrowed lenses: logic, coverage, semantics
  - issue stance disagreement: 0/3
  - disagreeing stance lenses: none
  - not applicable lenses: none
  - insufficient evidence lenses: none
  - resolution accepted by: 3/3 deliberation participants
  - accepted lenses: logic, coverage, semantics
  - remaining disagreement: 0/3 deliberation participants
  - remaining disagreement lenses: none
  - raised by lenses: semantics
  - issue statement: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now (lines 30-32) with no contract binding the two clocks, and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher — time ownership is undesigned in the class, so neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32); the name now implies the same timebase but no contract binds the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock. Source finding context: Semantics lens found `now` is a homonym across two unrelated clocks: the parameter name semantically asserts one shared notion of now while the class holds no single time authority — directly violating the request focus on time-source semantics and the correctness goal. Source finding context: Pick one time authority: either accept now everywhere (add a now or injected-clock argument to enqueue and set windowStartedAt from it) or use Date.now() everywhere and drop the now parameter from flush; if the parameter is kept, document the precondition that now must come from the same epoch-ms clock as Date.now(). Source finding context: round1/semantics.md#finding-2 Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: The class holds no single time authority: line 20 hardcodes Date.now() while line 30 delegates time to the caller, and no statement of the shared-clock precondition exists on the interface. Source finding context: WINDOW_MS does not denote 30 seconds of any single clock: the closure subtraction mixes the internal wall-clock anchor with whatever value the caller passes as now. Source finding context: No single time authority exists in the class and the parameter name now falsely asserts a shared timebase, so nothing binds the two operands of the closure subtraction to one clock. Source finding context: notification-batcher.ts:8-9,19-24 Source finding context: enqueuedAt is documented as 'Epoch ms when the notification entered the batch' (line 8), but enqueue (lines 19-24) neither assigns nor validates it and no code in the target reads it; the value is caller-supplied before the notification has entered any batch, so the name/doc asserts a fact only the batcher could establish. Source finding context: Semantics lens found the field's documented meaning diverges from its actual provenance: batch-entry semantics are assigned to a value constructed before enqueue, the per-item batch-entry time is captured nowhere, and the documented meaning is verified by no behavior; ontological note: PendingNotification is a phase-named sortal applied to pre-enqueue (not yet pending) values. Source finding context: Make the batcher own the timestamp: have enqueue accept {userId, body} and stamp enqueuedAt itself from the single time authority chosen for finding-011, storing the enriched record internally; or if caller-supplied time is intentional, rename/re-document the field to its true provenance (e.g. createdAt) and either use it or remove it from this contract. Source finding context: round1/semantics.md#finding-3 Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior. Source finding context: The field documentation assigns batch-entry semantics to a value constructed by the caller before enqueue is invoked; the only component that could truthfully populate it (the batcher, at the enqueue site) does not. Source finding context: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher; the only entry-time fact recorded is windowStartedAt, a different concept. Source finding context: Batch-entry semantics were assigned to a caller-constructed pre-enqueue value that the caller cannot truthfully populate as documented, and the batcher never stamps it at the enqueue site.
  - affected purpose: Review goals `correctness` and `maintainability` under the requested time-source-semantics focus: the documented 30-second window semantics (WINDOW_MS) and the documented meaning of the enqueuedAt contract field. Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning.
  - failure condition: A caller using injected, fake, or skewed time for flush's now — the closure subtraction at line 32 becomes a cross-clock artifact — and any consumer trusting enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue.
  - impact: The window never closes, closes immediately, or stays open indefinitely — the named 30-second contract is unkeepable across clocks — and the only per-item time fact carries a documented meaning that no behavior establishes or verifies. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior.
  - root hypothesis: The batcher owns no time fact: the window anchor, the closure input, and the documented batch-entry timestamp are all sourced from ambient or caller-chosen clocks that the batcher neither establishes nor validates. Designating a single batcher-owned time authority — and stamping enqueuedAt at the enqueue site from that authority — resolves both findings; if clock unification alone leaves finding-012 intact without the stamping change, the common root is refuted.
  - evidence: `round1/semantics.md#finding-2`, `round1/semantics.md#finding-3`, `finding-ledger.yaml#finding-011`, `finding-ledger.yaml#finding-012`, `rel-006`, `finding-relation-graph.yaml#rel-006`, `finding-011.cause-001`, `finding-011.cause-002`, `finding-012.cause-001`, `finding-012.cause-002`, `issue-stance-matrix.yaml#stances.issue-011.logic`, `issue-ledger.yaml#issue-011`, `finding-relation-graph.yaml#rel-002`, `round1/logic.md#finding-l1`, `round1/logic.md#finding-l4`, `issue-stance-matrix.yaml#stances.issue-011.coverage`, `round1/coverage.md#finding-1`, `round1/coverage.md#finding-4`, `issue-stance-matrix.yaml#stances.issue-011.semantics`, `issue-ledger.yaml#dep-002`, `rel-002`, `issue-ledger.yaml#dep-003`, `rel-003`, `.onto/review/20260611-d9760486/issue-ledger.yaml`, `.onto/review/20260611-d9760486/problem-framing.yaml`
  - source lenses: semantics
  - action candidates: fix_before_release, accept_risk, fix_now
  - materiality: This is material under the declared correctness and maintainability goals in the explicitly requested time-source-semantics focus area. If a caller uses an injected, fake, or skewed clock for flush's now, the closure subtraction at line 32 becomes a cross-clock artifact: the window never closes, closes immediately, or stays open indefinitely, so the named 30-second contract is unkeepable across clocks. Simultaneously, the only per-item time fact in the contract, enqueuedAt, carries a documented meaning that no behavior establishes or verifies, so any consumer trusting it as actual batch-entry time (metrics, ordering, expiry) is misled while callers populate it pre-enqueue.
  - root cause: The accepted compound root is that the batcher owns no time fact: the window anchor, the closure input, and the documented batch-entry timestamp are all sourced from ambient or caller-chosen clocks that the batcher neither establishes nor validates. It is the starting point because both surface findings reduce to that absent authority — finding-011's root step (finding-011.cause-002: no single time authority, and the parameter name "now" falsely asserts a shared timebase) and finding-012's root step (finding-012.cause-002: batch-entry semantics assigned to a caller-constructed pre-enqueue value the batcher never stamps). Root confidence is deliberately medium because the hypothesis carries a built-in refutation condition accepted by all lenses: if clock unification alone left finding-012 intact without the enqueue-site stamping change, the common root would be refuted. Framing classifies this issue as root_cause with judgment inferred, per rel-006 pairing both findings under the compound root.
  - causal path: Two parallel chains converge on the compound root. For finding-011: WINDOW_MS does not denote 30 seconds of any single clock because the closure subtraction mixes the internal wall-clock anchor with whatever the caller passes as now (cause-001), and that mixing exists because no single time authority lives in the class while the name "now" falsely asserts a shared timebase (cause-002, root). For finding-012: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher (cause-001), because batch-entry semantics were assigned to a caller-constructed pre-enqueue value the caller cannot truthfully populate and the batcher never stamps at the enqueue site (cause-002, root). rel-006 pairs the two findings under the same batcher-owns-no-time-fact root, and dep-002/dep-003 record it as a shared cause with issue-001 (the undeclared necessary condition of the closure rule, rel-002) and issue-005 (the missing clock-authority concept, rel-003) — the same absent clock binding observed from the logic, coverage, and semantics angles.
  - action: Deliberation was skipped as no-deliberation-needed (consistent_stances): all three lenses support the claim, accept the compound root including its refutation condition, and keep medium severity, with nothing left to deliberate. The required action is to designate one batcher-owned time authority for the window lifecycle and have the batcher establish per-item enqueuedAt from that same authority at the enqueue site; alternatively, if caller-supplied time is intentional, rename/re-document the field to its true provenance and either use it or remove it from the contract. Dependency and ordering matter: closure is needs_decision with must_close_in_target, and the single-time-authority decision is instantiated by the in-target fixes of issues 001 and 005 (dep-002/dep-003), so the authority should be chosen once there and applied here. Per the accepted refutation condition, clock unification alone is insufficient — the enqueue-site stamping (or the re-documentation alternative) must accompany it to close finding-012.

### Conditional Consensus
- issue-001 (no-deliberation-needed): The window-closed predicate in flush subtracts an internally Date.now()-sampled anchor (set at enqueue, line 20) from a caller-supplied now (line 30), so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. This is a high-severity, material correctness defect that must be closed in the target by binding the whole window lifecycle to one declared time authority.
- issue-005 (no-deliberation-needed): Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue (line 20) and caller-supplied now at flush (line 30) — and no single-origin time-source authority concept exists anywhere in the file. This is a high-severity, material correctness defect framed as the root cause shared with issue-001 and issue-011: designate one time authority for window anchoring and closure and document it on the interface.
- issue-002 (no-deliberation-needed): The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: under synchronous execution the guaranteed interval admits no enqueues (the claim is vacuous), and in the only materially reachable reading items enqueued after the window-close instant are emitted in the current digest and wiped by clear(), never carried over. This is a medium-severity, material consistency defect: align the comment with the behavior, or implement the guarantee.
- issue-006 (no-deliberation-needed): The pending-notification lifecycle has no termination segment: the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/dispose member, so pending items are unreachable at shutdown, poller stop, or decommission and are silently lost. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires adding a terminal operation plus a documented caller shutdown obligation before the next stage relies on loss-free delivery.
- issue-007 (no-deliberation-needed): The pending store grows without bound: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, enqueue appends unconditionally, and the sole reducer (flush) is poll- and window-gated. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires selecting a documented capacity bound and an explicit overflow policy, surfaced to the caller, before the next stage.
- issue-010 (no-deliberation-needed): The inline comment at notification-batcher.ts lines 37-38 promises that notifications enqueued between the window-close check (line 32) and the reset (line 39) are carried into the next window, but this.pending.clear() at line 39 unconditionally discards every entry regardless of when it was enqueued. The documented carry-over contract is false as written and inverts the actual failure mode: the only conditions under which the promise would matter are exactly the conditions under which clear() silently drops the items. The comment and the mechanism must be aligned — either re-document the true synchronous semantics or implement real carry-over.
- issue-011 (no-deliberation-needed): Time ownership is undesigned in the batcher. windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32) with no contract binding the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock; and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher. Neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. The accepted resolution is to designate one batcher-owned time authority for the window lifecycle and have the batcher stamp enqueuedAt from that same authority at the enqueue site, or re-document the field to its true provenance.

### Disagreement
- none

### Axiology-Proposed Additional Perspectives
- none

### Purpose Alignment Verification
- issue-001: Declared review goal `correctness` and the flush contract C4 ('flushes when the window has closed') under the requested time-source-semantics focus. Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area.
- issue-005: Declared review goal `correctness` for window-closure decisions under the requested time-source-semantics focus. Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics.
- issue-002: Review goals `correctness` and `regression_risk`: the documented loss-safety (carry-over) contract of flush's reset step. Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee).
- issue-006: Review goals `correctness` and `regression_risk`: loss-free handling of pending notifications across the full lifecycle. Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths.
- issue-007: Review goals `correctness` and `regression_risk`: bounded resource behavior of the pending store in a long-lived process. Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling.
- issue-010: Review goals `correctness`, `regression_risk`, and `maintainability`: the truthfulness of the inline reset contract that future maintainers will rely on. Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract.
- issue-011: Review goals `correctness` and `maintainability` under the requested time-source-semantics focus: the documented 30-second window semantics (WINDOW_MS) and the documented meaning of the enqueuedAt contract field. Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning.

### Boundary Notes
- issue-003 evidence gap: The flush return-value protocol overloads [] across two distinct reachable states (no window exists vs window still open) while the documentation at line 28 asserts a single meaning, making the documented rule false in the reachable no-window state (windowStartedAt === null). The documented rule 'an empty array means window still open' (line 28) is false in the reachable no-window state (windowStartedAt === null), where flush also returns [] although no window exists. Source finding context: notification-batcher.ts Source finding context: notification...
- issue-004 evidence gap: Two recordings purport to denote the batch-entry instant — the caller-supplied enqueuedAt field (lines 8-9) and the internally sampled windowStartedAt (line 20) — with no reconciliation rule or designated authority, so the field's documented definition holds only under the undeclared convention that callers pass Date.now() at call time and is latently false for permitted inputs. enqueuedAt is defined as 'Epoch ms when the notification entered the batch' yet enqueue records the same instant independently via Date.now() and never consults enqueuedAt, so...
- issue-008 evidence gap: The temporal axis is declared on the input side (enqueuedAt) but has no output or history counterpart — the field is never read, digests carry only bodies, and flush destroys state without persisting any record — so past batch state cannot be reconstructed and the carry-over comment can never be checked against evidence. enqueuedAt is a documented contract field that is never read, and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment cannot be checked against evidence. Source fin...

### Immediate Actions Required
- issue-001 (high)
  - issue: The window-closed predicate in flush subtracts an internally Date.now()-sampled anchor (set at enqueue, line 20) from a caller-supplied now (line 30), so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. This is a high-severity, material correctness defect that must be closed in the target by binding the whole window lifecycle to one declared time authority.
  - target: The window-closed predicate subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so contract C4 ('flushes when the window has closed') is jointly satisfiable only under an undeclared shared-clock condition and is violated for permitted inputs. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20, notification-batcher.ts:30-32 Source finding context: The window-closed predicate compares a caller-supplied clock (`now`) against an internally sampled ambient clock (`Date.now()`); the rule set is jointly satisfiable only under an undeclared necessary condition that both values come from the same epoch-ms clock, so C4 ('flushes when the window has closed') is violated for permitted inputs. Source finding context: Logic lens found an inter-claim unsatisfiability (verdict: fail) between C3 (windowStartedAt = Date.now() at enqueue) and C4+C6 (closure computed from caller-supplied now at flush): a necessary shared-clock constraint is treated as merely possible, directly hitting the requested time-source-semantics focus and the correctness review goal. Residual contradiction persists after semantic disambiguation of `now`, so logic holds the primary finding. Source finding context: Use one time authority for the whole window lifecycle: either thread the caller clock through both entry points (enqueue(item, now) or use item.enqueuedAt to start the window), or drop the now parameter and use Date.now() inside flush (providing the test time-injection seam via an injected clock). At minimum document that now must be epoch ms from the same clock as Date.now(). Source finding context: round1/logic.md#finding-l1 Source finding context: Review goal `correctness` and contract C4 (flush on window close) in the declared time-source-semantics focus area. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number. Source finding context: Window closure becomes never, early, or instant; the declared 30s digest behavior is unachievable for permitted inputs, breaking trust in the primary batching contract. Source finding context: The design splits time authority across the two mutating entry points — window start from the ambient clock at enqueue (line 20), window closure from injected time at flush (line 30) — with no claim in the target declaring or enforcing the required inter-clock identity. Source finding context: flush's window-closed predicate (now - windowStartedAt >= WINDOW_MS) subtracts an internally Date.now()-sampled anchor from a caller-supplied now, so closure is wrong whenever the two values are not on one shared epoch-ms clock. Source finding context: Time authority is split across enqueue (ambient Date.now(), line 20) and flush (caller-injected now, line 30) and no declared contract binds the two clocks, so the closure rule's truth depends on an undeclared necessary condition.
  - failure condition: Any permitted caller whose flush(now) value comes from a clock other than Date.now() — test clock, monotonic clock, replayed or skewed clock — since the signature flush(now: number) carries no time-base constraint. Source finding context: Caller supplies flush(now) from any clock other than the Date.now() epoch-ms timeline; the declared contract permits any number.
  - candidates: fix_now
  - rationale: Adopt a single time authority for the whole window lifecycle — either thread the caller clock through both entry points (enqueue(item, now) or anchor the window from item.enqueuedAt) or use one injected/ambient clock for both — and declare the time-base constraint on the interface. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters because of the shared-cause dependencies: dep-001 links this issue to issue-005 (the missing time-source authority concept, coverage side) and dep-002 links it to issue-011 (no single time authority plus a false shared-timebase name, semantics side); all three rest on the same absent inter-clock binding, and this issue is framed as the symptom of that shared cause, so designating the single clock authority once closes the contradiction here rather than patching the predicate in isolation. Deliberation was skipped (consistent_stances): all three lenses support the issue, accept the single-clock-authority root hypothesis, and keep high severity, with no remaining disagreement or follow-up evidence required.
  - remediation: Adopt a single time authority for the whole window lifecycle — either thread the caller clock through both entry points (enqueue(item, now) or anchor the window from item.enqueuedAt) or use one injected/ambient clock for both — and declare the time-base constraint on the interface. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters because of the shared-cause dependencies: dep-001 links this issue to issue-005 (the missing time-source authority concept, coverage side) and dep-002 links it to issue-011 (no single time authority plus a false shared-timebase name, semantics side); all three rest on the same absent inter-clock binding, and this issue is framed as the symptom of that shared cause, so designating the single clock authority once closes the contradiction here rather than patching the predicate in isolation. Deliberation was skipped (consistent_stances): all three lenses support the issue, accept the single-clock-authority root hypothesis, and keep high severity, with no remaining disagreement or follow-up evidence required.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-005 (high)
  - issue: Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue (line 20) and caller-supplied now at flush (line 30) — and no single-origin time-source authority concept exists anywhere in the file. This is a high-severity, material correctness defect framed as the root cause shared with issue-001 and issue-011: designate one time authority for window anchoring and closure and document it on the interface.
  - target: Window-closure decisions mix two undeclared time authorities — internal Date.now() at enqueue and caller-supplied now at flush — with no single-origin time-source authority concept anywhere in the file. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: Window-closure decisions mix two undeclared time authorities (internal Date.now() at enqueue vs caller-supplied now at flush) with no single time-source authority concept. Source finding context: Coverage lens flagged a dimensional deficit: one time-dependent value (window age) is parallel-managed across two time sources with no single-origin authority designation anywhere in the file; the request summary explicitly scopes time-source semantics into this review. Source finding context: Designate a single time authority; smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied time source (enqueue(item, now) or an injected clock used by both methods), and document on flush that now must come from that same authority. Source finding context: round1/coverage.md#finding-1 Source finding context: Correctness of window-closure decisions; explicitly in-scope time-source semantics. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor. Source finding context: Window closes never, early, or instantly — the core window contract fails under permitted caller behavior. Source finding context: The contract lacks any clock-authority concept: no clock parameter on enqueue and no documented requirement on now's source (absence across lines 18-32). Source finding context: The window closes never, early, or instantly whenever the caller's now diverges from the Date.now() timeline, violating the declared correctness goal. Source finding context: The closure predicate (line 32) is a cross-clock subtraction: windowStartedAt is anchored to the internal wall clock (line 20) while now is caller-supplied (line 30). Source finding context: No clock-authority concept exists in the contract — nothing binds the caller's time source to the internal anchor's source, so the two authorities are free to diverge.
  - failure condition: Whenever the caller's now comes from a different epoch or skewed source than Date.now() (test clock, monotonic clock, mocked scheduler time), the closure predicate becomes a cross-clock subtraction. Source finding context: Caller-supplied now from a different epoch or skewed source than the Date.now() anchor.
  - candidates: fix_now
  - rationale: Designate a single time authority for window anchoring and closure — a shared caller-supplied time source threaded through both methods (the smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied source, e.g. enqueue(item, now)) or an injected clock used by both methods — and document on flush that its now must come from that same authority. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters: this issue is the shared cause behind issue-001's closure contradiction (dep-001) and converges with issue-011's missing-single-time-authority and false shared-timebase naming finding (dep-003), so resolving it first closes the common root for all three rather than treating each symptom separately. Deliberation was skipped (consistent_stances): all three lenses support the missing clock-authority concept, accept the root hypothesis, and keep high severity; the shared-cause dependencies are agreed dependency context, not stance conflicts, and no follow-up evidence is required.
  - remediation: Designate a single time authority for window anchoring and closure — a shared caller-supplied time source threaded through both methods (the smallest in-target fix: stop calling Date.now() in enqueue and anchor the window from the same caller-supplied source, e.g. enqueue(item, now)) or an injected clock used by both methods — and document on flush that its now must come from that same authority. The projected action class is fix_now with closure obligation must_close_in_target. Ordering matters: this issue is the shared cause behind issue-001's closure contradiction (dep-001) and converges with issue-011's missing-single-time-authority and false shared-timebase naming finding (dep-003), so resolving it first closes the common root for all three rather than treating each symptom separately. Deliberation was skipped (consistent_stances): all three lenses support the missing clock-authority concept, accept the root hypothesis, and keep high severity; the shared-cause dependencies are agreed dependency context, not stance conflicts, and no follow-up evidence is required.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-002 (medium)
  - issue: The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: under synchronous execution the guaranteed interval admits no enqueues (the claim is vacuous), and in the only materially reachable reading items enqueued after the window-close instant are emitted in the current digest and wiped by clear(), never carried over. This is a medium-severity, material consistency defect: align the comment with the behavior, or implement the guarantee.
  - target: The documented carry-over guarantee (lines 37-38) and the implemented emit-all-then-clear behavior (lines 34-36, 39) are not jointly satisfiable: the guaranteed interval is empty under synchronous execution, and in the only materially reachable reading items are emitted in the current digest and cleared, never carried over. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-40, notification-batcher.ts:34-36 Source finding context: The comment claims notifications enqueued between the window-close check and the reset are carried into the next window, but under synchronous execution that interval admits no enqueues (the claim is vacuous), and under the only materially reachable reading (enqueued after the window-close instant, before the flush poll) such items are emitted in the current digest and cleared, never carried over. The documented guarantee and the implemented behavior are not jointly satisfiable. Source finding context: Logic lens found C7 (carry-over guarantee, lines 37-38) and C8 (emit-all-then-clear, lines 34-36,39) jointly unsatisfiable for any item present at flush time: an obligation is asserted over an interval the execution model makes empty, masking a false guarantee in the reachable interpretation — a contract-vs-behavior contradiction under the correctness goal. Source finding context: Make the code and the claim agree: either implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). Source finding context: round1/logic.md#finding-l2 Source finding context: Correctness and regression risk of the documented flush reset contract (carry-over guarantee). Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush. Source finding context: Documented guarantee is unsatisfiable as implemented — items are emitted in the closing digest and wiped by clear(), so the contract misleads maintainers about loss behavior. Source finding context: No code path snapshots window membership at the close instant: the close check (line 32) gates timing only while content is taken wholesale at emission time, so the promised carry-over has no mechanism that could implement it. Source finding context: The comment promises carry-over into the next window, but the digest loop emits every pending entry with no timestamp filter and clear() unconditionally removes everything, so nothing is ever carried over. Source finding context: No mechanism snapshots window membership at the close instant — timing and content are decoupled (close check gates when, the loop takes whatever is present) — making the obligated carry-over state unreachable.
  - failure condition: Any item present in the pending map at flush time when the window has closed — in particular items enqueued after the window-close instant but before the caller polls flush. Source finding context: Items enqueued after the window-close instant but before the flush poll; any future async/re-entrant refactor of flush.
  - candidates: fix_before_release, fix_now
  - rationale: Make the code and the claim agree, in one of two ways: implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state the actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). The projected action classes are fix_before_release and fix_now, with closure obligation must_close_in_target; the fix is local to the target file. No cross-issue ordering constraint applies: the root (no snapshot mechanism) is self-contained — rel-004 records duplication with issue-010, not a shared root — so this issue is framed as an independent_issue and can be closed on its own. Deliberation was skipped (consistent_stances): logic, coverage, and semantics unanimously support the contradiction and its no-snapshot-mechanism root, accept the hypothesis, and keep medium severity, with no remaining disagreement or follow-up evidence required.
  - remediation: Make the code and the claim agree, in one of two ways: implement the guarantee by filtering the digest loop to items with enqueuedAt < windowStartedAt + WINDOW_MS and retaining later items in pending, or correct the comment to state the actual behavior (all pending notifications at flush time are emitted in this digest; nothing carries over). The projected action classes are fix_before_release and fix_now, with closure obligation must_close_in_target; the fix is local to the target file. No cross-issue ordering constraint applies: the root (no snapshot mechanism) is self-contained — rel-004 records duplication with issue-010, not a shared root — so this issue is framed as an independent_issue and can be closed on its own. Deliberation was skipped (consistent_stances): logic, coverage, and semantics unanimously support the contradiction and its no-snapshot-mechanism root, accept the hypothesis, and keep medium severity, with no remaining disagreement or follow-up evidence required.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-006 (medium)
  - issue: The pending-notification lifecycle has no termination segment: the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/dispose member, so pending items are unreachable at shutdown, poller stop, or decommission and are silently lost. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires adding a terminal operation plus a documented caller shutdown obligation before the next stage relies on loss-free delivery.
  - target: The pending-notification lifecycle covers enqueue and window-gated flush but has no termination segment (drain/close/dispose); pending items are unreachable at shutdown or poller stop and are silently lost. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:30-32,44-47 Source finding context: Coverage lens ghost-sub-area check: lifecycle termination is implied by the batching domain but entirely absent — the only exit path for pending items is a future poll that satisfies the window predicate, a correctness and regression risk inside the declared review goal. Source finding context: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }> that emits digests for all pending items regardless of WINDOW_MS and resets state; document that callers must invoke it on shutdown. Source finding context: round1/coverage.md#finding-2 Source finding context: Correctness and regression risk of notification delivery at end-of-life (shutdown/decommission) paths. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state. Source finding context: Silent data loss of pending notifications with no contract surface acknowledging or mitigating it. Source finding context: No terminal-lifecycle concept exists in the API — the exported surface contains only enqueue, window-gated flush, and pendingUserCount, with no drain/close/force-flush member. Source finding context: At process shutdown, poller failure, or decommission, pending notifications are silently dropped because the poll that would flush them never happens. Source finding context: Every pending item's only exit is a future external poll of flush that also satisfies the WINDOW_MS predicate (lines 28, 32). Source finding context: The API has no terminal-lifecycle operation (drain/close/dispose) anywhere in the class body, so no unconditional exit path exists.
  - failure condition: Process shutdown, poller failure, or batcher decommission while items are pending — the future poll that would release them never happens. Source finding context: Shutdown, poller stop, or decommission with non-empty pending state.
  - candidates: fix_before_release, accept_risk
  - rationale: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }>, that emits digests for all pending items regardless of WINDOW_MS and resets state, and document that callers must invoke it on shutdown. This is a contract-extension decision (closure_class needs_decision) and a next_step_blocker that must close before any next stage relies on loss-free delivery (must_close_before_next_stage). Ordering note: dep-004 records that issue-006 and issue-007 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but have distinct roots and distinct mitigations, so this terminal operation can be decided and landed independently of issue-007's capacity/overflow policy. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave silent loss unacknowledged against the artifact's stated delivery purpose.
  - remediation: Add a terminal operation, e.g. drain(): Array<{ userId: string; digest: string }>, that emits digests for all pending items regardless of WINDOW_MS and resets state, and document that callers must invoke it on shutdown. This is a contract-extension decision (closure_class needs_decision) and a next_step_blocker that must close before any next stage relies on loss-free delivery (must_close_before_next_stage). Ordering note: dep-004 records that issue-006 and issue-007 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but have distinct roots and distinct mitigations, so this terminal operation can be decided and landed independently of issue-007's capacity/overflow policy. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave silent loss unacknowledged against the artifact's stated delivery purpose.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-007 (medium)
  - issue: The pending store grows without bound: no capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, enqueue appends unconditionally, and the sole reducer (flush) is poll- and window-gated. All three lenses (logic, coverage, semantics) accepted this root and kept medium severity with no deliberation needed; closure requires selecting a documented capacity bound and an explicit overflow policy, surfaced to the caller, before the next stage.
  - target: The pending store grows without bound — no max-size, overflow policy, or backpressure concept exists, while the only reducer (flush) is poll- and window-gated. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:21-23,32 Source finding context: Coverage lens ghost-sub-area check: the batching domain implies a capacity/overflow sub-area (bounded buffers, drop/force-flush policies) that is entirely absent; combined with the unguarded poll dependency this is a regression and correctness risk within the declared review goal. Source finding context: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Source finding context: round1/coverage.md#finding-3 Source finding context: Correctness and regression risk of resource behavior under sustained enqueue with delayed or absent polling. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process. Source finding context: Unbounded pending growth — an operational failure mode with no detection or overflow policy in the contract. Source finding context: No capacity concept exists in the contract or implementation (absence across lines 14-48): enqueue has no size check or rejection path and nothing in the file mentions a bound. Source finding context: Pending storage grows without bound when the poller stalls while enqueues continue. Source finding context: The sole reducer of pending is flush, which is conditional on window age (line 32) and on an external caller polling (line 28). Source finding context: No capacity, overflow, or backpressure concept exists anywhere in the contract or implementation, so enqueue appends unconditionally for any number of users and items.
  - failure condition: A stalled or slow external poll loop while enqueues continue — memory consumption is proportional to enqueue rate times the gap between successful polls. Source finding context: Stalled or slow poller combined with ongoing enqueues in a long-lived process.
  - candidates: fix_before_release, accept_risk
  - rationale: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Selecting the bound and the policy is a contract decision (closure_class needs_decision) and a next_step_blocker that must close before the next stage (must_close_before_next_stage). Ordering note: dep-004 records that issue-007 and issue-006 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but name distinct missing concepts with distinct mitigations (capacity/overflow policy vs terminal-lifecycle operation), so this fix can be decided and landed independently of issue-006's drain operation. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave an undetectable unbounded-growth mode in long-lived processes.
  - remediation: Add a documented capacity bound (per user and/or total) with an explicit overflow policy — reject, drop-oldest, or force-flush on overflow — and surface overflow occurrences to the caller. Selecting the bound and the policy is a contract decision (closure_class needs_decision) and a next_step_blocker that must close before the next stage (must_close_before_next_stage). Ordering note: dep-004 records that issue-007 and issue-006 share the poll-and-window-gated sole-reducer mid-path cause (rel-005) but name distinct missing concepts with distinct mitigations (capacity/overflow policy vs terminal-lifecycle operation), so this fix can be decided and landed independently of issue-006's drain operation. Projected action candidates are fix_before_release and accept_risk; accepting the risk would leave an undetectable unbounded-growth mode in long-lived processes.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-010 (medium)
  - issue: The inline comment at notification-batcher.ts lines 37-38 promises that notifications enqueued between the window-close check (line 32) and the reset (line 39) are carried into the next window, but this.pending.clear() at line 39 unconditionally discards every entry regardless of when it was enqueued. The documented carry-over contract is false as written and inverts the actual failure mode: the only conditions under which the promise would matter are exactly the conditions under which clear() silently drops the items. The comment and the mechanism must be aligned — either re-document the true synchronous semantics or implement real carry-over.
  - target: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:37-39 Source finding context: The comment 'Notifications enqueued between the window close check and this reset are carried into the next window' (lines 37-38) does not match the behavior of this.pending.clear() (line 39), which would unconditionally discard such notifications; the documented contract is false as written and inverts the actual failure mode. Source finding context: Semantics lens found the documented contract false as written: the guarantee describes an interleaving point that synchronous flush makes unreachable, and under any future condition where it became reachable clear() would silently drop — the opposite of the documented carry-over; a regression trap under the declared correctness/regression_risk/ maintainability goals. Source finding context: Make the comment state the true semantics (flush is synchronous; nothing can interleave; if it ever becomes async, interleaved enqueues would be dropped by clear() — preserve them explicitly), or implement real carry-over by iterating a snapshot and deleting only flushed entries so the comment becomes true. Source finding context: round1/semantics.md#finding-1 Source finding context: Correctness, regression risk, and maintainability of the documented reset/carry-over contract. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable. Source finding context: Documented guarantee is unexhibitable today and inverted (silent drop) under the only conditions where it would matter. Source finding context: The comment asserts a behavioral guarantee about a hypothetical interleaving window between the close check (line 32) and the reset (line 39) that the synchronous execution model makes unreachable, while clear() inverts the promised outcome if it ever became reachable. Source finding context: The carry-over comment does not match this.pending.clear(), which unconditionally discards every entry regardless of when it was enqueued. Source finding context: The guarantee was written over an unreachable interleaving point (synchronous flush, no await/callback/iterator hand-off between lines 32 and 39) with no implementing mechanism, so it is vacuous today and inverted under any future reachability.
  - failure condition: A maintainer assumes loss-free carry-over per the comment; or a future async/re-entrant refactor makes the interleaving point reachable, where clear() silently drops the interleaved items. Source finding context: Maintainer reliance on the carry-over guarantee, or any future refactor that makes the described interleaving reachable.
  - candidates: fix_before_release, fix_now
  - rationale: Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics unanimously support the claim, accept the root hypothesis, and keep medium severity, with no remaining disagreement and no follow-up evidence required. The required action offers two closure paths: (a) make the comment state the true semantics — flush is synchronous, nothing can interleave, and if flush ever becomes async, interleaved enqueues would be dropped by clear() and must be preserved explicitly; or (b) implement real carry-over by iterating a snapshot and deleting only flushed entries, making the comment true. Framing is fix_now with must_close_in_target, so the fix is local to the target and should land before the next change builds on the false contract. Ordering note: rel-004 records this as a duplicate surface of issue-002 without licensing a merge, so coordinate the fix so one comment/mechanism alignment closes both surfaces consistently.
  - remediation: Deliberation was skipped as no-deliberation-needed (consistent_stances): logic, coverage, and semantics unanimously support the claim, accept the root hypothesis, and keep medium severity, with no remaining disagreement and no follow-up evidence required. The required action offers two closure paths: (a) make the comment state the true semantics — flush is synchronous, nothing can interleave, and if flush ever becomes async, interleaved enqueues would be dropped by clear() and must be preserved explicitly; or (b) implement real carry-over by iterating a snapshot and deleting only flushed entries, making the comment true. Framing is fix_now with must_close_in_target, so the fix is local to the target and should land before the next change builds on the false contract. Ordering note: rel-004 records this as a duplicate surface of issue-002 without licensing a merge, so coordinate the fix so one comment/mechanism alignment closes both surfaces consistently.
  - verification: verify the remediation against the failure condition with a focused check before closing
- issue-011 (medium)
  - issue: Time ownership is undesigned in the batcher. windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32) with no contract binding the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock; and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher. Neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. The accepted resolution is to designate one batcher-owned time authority for the window lifecycle and have the batcher stamp enqueuedAt from that same authority at the enqueue site, or re-document the field to its true provenance.
  - target: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now (lines 30-32) with no contract binding the two clocks, and enqueuedAt's documented batch-entry meaning (line 8) is delegated to callers pre-enqueue and never established, validated, or read by the batcher — time ownership is undesigned in the class, so neither WINDOW_MS nor enqueuedAt denotes what its name and documentation assert. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:20,30-32 Source finding context: windowStartedAt is written from the internal wall clock Date.now() (line 20) but compared against the caller-supplied now parameter (lines 30-32); the name now implies the same timebase but no contract binds the two clocks, so WINDOW_MS does not denote 30 seconds of any single clock. Source finding context: Semantics lens found `now` is a homonym across two unrelated clocks: the parameter name semantically asserts one shared notion of now while the class holds no single time authority — directly violating the request focus on time-source semantics and the correctness goal. Source finding context: Pick one time authority: either accept now everywhere (add a now or injected-clock argument to enqueue and set windowStartedAt from it) or use Date.now() everywhere and drop the now parameter from flush; if the parameter is kept, document the precondition that now must come from the same epoch-ms clock as Date.now(). Source finding context: round1/semantics.md#finding-2 Source finding context: Correctness of the documented 30-second window semantics (WINDOW_MS) in the time-source-semantics focus area. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Window-close semantics are undefined across clocks; the named 30-second contract is unkeepable. Source finding context: The class holds no single time authority: line 20 hardcodes Date.now() while line 30 delegates time to the caller, and no statement of the shared-clock precondition exists on the interface. Source finding context: WINDOW_MS does not denote 30 seconds of any single clock: the closure subtraction mixes the internal wall-clock anchor with whatever value the caller passes as now. Source finding context: No single time authority exists in the class and the parameter name now falsely asserts a shared timebase, so nothing binds the two operands of the closure subtraction to one clock. Source finding context: notification-batcher.ts:8-9,19-24 Source finding context: enqueuedAt is documented as 'Epoch ms when the notification entered the batch' (line 8), but enqueue (lines 19-24) neither assigns nor validates it and no code in the target reads it; the value is caller-supplied before the notification has entered any batch, so the name/doc asserts a fact only the batcher could establish. Source finding context: Semantics lens found the field's documented meaning diverges from its actual provenance: batch-entry semantics are assigned to a value constructed before enqueue, the per-item batch-entry time is captured nowhere, and the documented meaning is verified by no behavior; ontological note: PendingNotification is a phase-named sortal applied to pre-enqueue (not yet pending) values. Source finding context: Make the batcher own the timestamp: have enqueue accept {userId, body} and stamp enqueuedAt itself from the single time authority chosen for finding-011, storing the enriched record internally; or if caller-supplied time is intentional, rename/re-document the field to its true provenance (e.g. createdAt) and either use it or remove it from this contract. Source finding context: round1/semantics.md#finding-3 Source finding context: Correctness and maintainability of the enqueuedAt field contract and downstream consumers that would trust its documented meaning. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Documented fact is establishable only by the batcher, which never establishes or reads it — the contract is unverified by any behavior. Source finding context: The field documentation assigns batch-entry semantics to a value constructed by the caller before enqueue is invoked; the only component that could truthfully populate it (the batcher, at the enqueue site) does not. Source finding context: enqueuedAt is documented as the batch-entry instant but is never assigned, validated, or read by the batcher; the only entry-time fact recorded is windowStartedAt, a different concept. Source finding context: Batch-entry semantics were assigned to a caller-constructed pre-enqueue value that the caller cannot truthfully populate as documented, and the batcher never stamps it at the enqueue site.
  - failure condition: A caller using injected, fake, or skewed time for flush's now — the closure subtraction at line 32 becomes a cross-clock artifact — and any consumer trusting enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue. Source finding context: Caller time source differs from Date.now() — injected/fake clocks or skewed clocks. Source finding context: Consumers using enqueuedAt as actual batch-entry time (metrics, ordering, expiry) while callers populate it pre-enqueue.
  - candidates: fix_before_release, accept_risk, fix_now
  - rationale: Deliberation was skipped as no-deliberation-needed (consistent_stances): all three lenses support the claim, accept the compound root including its refutation condition, and keep medium severity, with nothing left to deliberate. The required action is to designate one batcher-owned time authority for the window lifecycle and have the batcher establish per-item enqueuedAt from that same authority at the enqueue site; alternatively, if caller-supplied time is intentional, rename/re-document the field to its true provenance and either use it or remove it from the contract. Dependency and ordering matter: closure is needs_decision with must_close_in_target, and the single-time-authority decision is instantiated by the in-target fixes of issues 001 and 005 (dep-002/dep-003), so the authority should be chosen once there and applied here. Per the accepted refutation condition, clock unification alone is insufficient — the enqueue-site stamping (or the re-documentation alternative) must accompany it to close finding-012.
  - remediation: Deliberation was skipped as no-deliberation-needed (consistent_stances): all three lenses support the claim, accept the compound root including its refutation condition, and keep medium severity, with nothing left to deliberate. The required action is to designate one batcher-owned time authority for the window lifecycle and have the batcher establish per-item enqueuedAt from that same authority at the enqueue site; alternatively, if caller-supplied time is intentional, rename/re-document the field to its true provenance and either use it or remove it from the contract. Dependency and ordering matter: closure is needs_decision with must_close_in_target, and the single-time-authority decision is instantiated by the in-target fixes of issues 001 and 005 (dep-002/dep-003), so the authority should be chosen once there and applied here. Per the accepted refutation condition, clock unification alone is insufficient — the enqueue-site stamping (or the re-documentation alternative) must accompany it to close finding-012.
  - verification: verify the remediation against the failure condition with a focused check before closing

### Recommendations
- issue-003 (low): The documented rule 'an empty array means window still open' (line 28) is false in the reachable no-window state (windowStartedAt === null), where flush also returns [] although no window exists. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: The documented interpretation rule 'an empty array means window still open' is contradicted by the no-window state: when windowStartedAt is null (nothing enqueued, or just flushed), flush returns [] although no window exists, so the conditional rule 'empty implies window still open' is false in a reachable state. Source finding context: Logic lens found the doc-level conditional C5 (empty return implies open window) violated by the reachable no-window state at line 31 — a formal contract-vs-behavior inconsistency in the return-value protocol, kept surface-only per severity (behaviorally benign for pure polling loops). Source finding context: Either narrow the doc ('an empty array means there is nothing ready to flush — the window is still open or no notifications are pending') or split the protocol (e.g. null for no-window vs [] for window-open) if callers need to distinguish the states. Source finding context: round1/logic.md#finding-l3
- issue-004 (low): enqueuedAt is defined as 'Epoch ms when the notification entered the batch' yet enqueue records the same instant independently via Date.now() and never consults enqueuedAt, so the model permits states in which the field's definition is false. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:20 Source finding context: C2 defines enqueuedAt as 'Epoch ms when the notification entered the batch', yet enqueue records the same event instant independently via Date.now() and never consults enqueuedAt anywhere; the definition holds only under the undeclared necessary condition that callers pass Date.now() at call time, so the model permits states in which the field's definition is false. Source finding context: Logic lens found two recordings purporting to denote the first-enqueue instant (caller-supplied enqueuedAt vs internally sampled windowStartedAt) with no reconciliation rule — a definitional inconsistency in time-source semantics; latent rather than active since no behavior currently reads the field, kept surface-only per severity. Source finding context: Pick one authority for the batch-entry instant: use item.enqueuedAt to start the window (making the field load-bearing, aligning with the finding-001 single-authority fix), or remove the field from the required interface if the batcher is the time authority; document the chosen authority on the interface. Source finding context: round1/logic.md#finding-l4
- issue-008 (low): enqueuedAt is a documented contract field that is never read, and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment cannot be checked against evidence. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,35,37-38 Source finding context: enqueuedAt is a documented contract field but is never read; digests carry only bodies and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment is unverifiable. Source finding context: Coverage lens dimensional-deficit check: the temporal axis is declared at input but absent on the output/history side — declared temporality with no history/evidence counterpart, bearing on the declared verifiability goal; kept surface-level per severity. Source finding context: Either remove enqueuedAt from the contract or use it (order digest lines by it, include timestamps in the digest, or expose flushed-batch metadata); at minimum have flush return or report enough metadata (window start, item counts) that delivered batches are verifiable. Source finding context: round1/coverage.md#finding-4
- issue-009 (low): Monitoring covers only the user-count dimension (pendingUserCount); there is no pending-item count and no oldest-pending-age metric, so a stalled poll loop — the failure mode the poll-driven contract makes possible — is undetectable. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28,44-47 Source finding context: Coverage lens observability-dimension check: concept distribution in the monitoring sub-area is skewed toward the one dimension (user breadth) that cannot detect the characteristic poll-stall failure mode; kept surface-level per severity. Source finding context: Add pendingItemCount() and an oldest-age accessor (which would also give enqueuedAt a consumer, see finding-008) so operators can alert on aging pending notifications. Source finding context: round1/coverage.md#finding-5
- issue-012 (low): flush conventionally means 'force out pending content' but the method is a conditional poll that usually returns []; the empty-return doc is inaccurate for the no-window state, and items enqueued after the window elapsed but before the poll are absorbed into the closing digest, loosening the documented window semantics. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:26-32 Source finding context: flush conventionally means 'force out pending content', but this method is a conditional poll that usually returns []; the doc 'an empty array means window still open' (line 28) is also inaccurate because [] is equally returned when no window exists at all (line 31), and items enqueued after the window elapsed but before the poll are absorbed into the closing digest, loosening the documented 'when the batch window closes' semantics (lines 2-3). Source finding context: Semantics lens surface naming/doc finding: the name and docs claim stricter semantics than the behavior provides (unconditional flush vs conditional poll; single meaning for [] vs two guard states; window-bounded membership vs poll-time membership); kept surface-level per severity. Source finding context: Rename to poll-style (pollDigests, flushIfWindowClosed) or document the conditional behavior; correct the doc to 'an empty array means no closed window is ready (no window started, or window still open)'; either state that late items ride the closing digest or filter membership by window if strict windowing is intended. Source finding context: round1/semantics.md#finding-4

### Unique Finding Tagging
- issue-003 (low): The documented rule 'an empty array means window still open' (line 28) is false in the reachable no-window state (windowStartedAt === null), where flush also returns [] although no window exists. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28, notification-batcher.ts:31 Source finding context: The documented interpretation rule 'an empty array means window still open' is contradicted by the no-window state: when windowStartedAt is null (nothing enqueued, or just flushed), flush returns [] although no window exists, so the conditional rule 'empty implies window still open' is false in a reachable state. Source finding context: Logic lens found the doc-level conditional C5 (empty return implies open window) violated by the reachable no-window state at line 31 — a formal contract-vs-behavior inconsistency in the return-value protocol, kept surface-only per severity (behaviorally benign for pure polling loops). Source finding context: Either narrow the doc ('an empty array means there is nothing ready to flush — the window is still open or no notifications are pending') or split the protocol (e.g. null for no-window vs [] for window-open) if callers need to distinguish the states. Source finding context: round1/logic.md#finding-l3
- issue-004 (low): enqueuedAt is defined as 'Epoch ms when the notification entered the batch' yet enqueue records the same instant independently via Date.now() and never consults enqueuedAt, so the model permits states in which the field's definition is false. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9, notification-batcher.ts:20 Source finding context: C2 defines enqueuedAt as 'Epoch ms when the notification entered the batch', yet enqueue records the same event instant independently via Date.now() and never consults enqueuedAt anywhere; the definition holds only under the undeclared necessary condition that callers pass Date.now() at call time, so the model permits states in which the field's definition is false. Source finding context: Logic lens found two recordings purporting to denote the first-enqueue instant (caller-supplied enqueuedAt vs internally sampled windowStartedAt) with no reconciliation rule — a definitional inconsistency in time-source semantics; latent rather than active since no behavior currently reads the field, kept surface-only per severity. Source finding context: Pick one authority for the batch-entry instant: use item.enqueuedAt to start the window (making the field load-bearing, aligning with the finding-001 single-authority fix), or remove the field from the required interface if the batcher is the time authority; document the chosen authority on the interface. Source finding context: round1/logic.md#finding-l4
- issue-008 (low): enqueuedAt is a documented contract field that is never read, and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment cannot be checked against evidence. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:8-9,35,37-38 Source finding context: enqueuedAt is a documented contract field but is never read; digests carry only bodies and flush leaves no record of what was delivered when, so past batch state cannot be reconstructed and the carry-over comment is unverifiable. Source finding context: Coverage lens dimensional-deficit check: the temporal axis is declared at input but absent on the output/history side — declared temporality with no history/evidence counterpart, bearing on the declared verifiability goal; kept surface-level per severity. Source finding context: Either remove enqueuedAt from the contract or use it (order digest lines by it, include timestamps in the digest, or expose flushed-batch metadata); at minimum have flush return or report enough metadata (window start, item counts) that delivered batches are verifiable. Source finding context: round1/coverage.md#finding-4
- issue-009 (low): Monitoring covers only the user-count dimension (pendingUserCount); there is no pending-item count and no oldest-pending-age metric, so a stalled poll loop — the failure mode the poll-driven contract makes possible — is undetectable. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:28,44-47 Source finding context: Coverage lens observability-dimension check: concept distribution in the monitoring sub-area is skewed toward the one dimension (user breadth) that cannot detect the characteristic poll-stall failure mode; kept surface-level per severity. Source finding context: Add pendingItemCount() and an oldest-age accessor (which would also give enqueuedAt a consumer, see finding-008) so operators can alert on aging pending notifications. Source finding context: round1/coverage.md#finding-5
- issue-012 (low): flush conventionally means 'force out pending content' but the method is a conditional poll that usually returns []; the empty-return doc is inaccurate for the no-window state, and items enqueued after the window elapsed but before the poll are absorbed into the closing digest, loosening the documented window semantics. Source finding context: notification-batcher.ts Source finding context: notification-batcher.ts:26-32 Source finding context: flush conventionally means 'force out pending content', but this method is a conditional poll that usually returns []; the doc 'an empty array means window still open' (line 28) is also inaccurate because [] is equally returned when no window exists at all (line 31), and items enqueued after the window elapsed but before the poll are absorbed into the closing digest, loosening the documented 'when the batch window closes' semantics (lines 2-3). Source finding context: Semantics lens surface naming/doc finding: the name and docs claim stricter semantics than the behavior provides (unconditional flush vs conditional poll; single meaning for [] vs two guard states; window-bounded membership vs poll-time membership); kept surface-level per severity. Source finding context: Rename to poll-style (pollDigests, flushIfWindowClosed) or document the conditional behavior; correct the doc to 'an empty array means no closed window is ready (no window started, or window still open)'; either state that late items ride the closing digest or filter membership by window if strict windowing is intended. Source finding context: round1/semantics.md#finding-4

### Individual Lens Findings
- logic: `.onto/review/20260611-d9760486/round1/logic.md`
- coverage: `.onto/review/20260611-d9760486/round1/coverage.md`
- semantics: `.onto/review/20260611-d9760486/round1/semantics.md`
