---
deliberation_status: performed
participation:
  expected_lenses:
    - axiology
    - coverage
    - evolution
    - logic
    - semantics
    - structure
  received_lenses:
    - axiology
    - coverage
    - evolution
    - logic
    - semantics
    - structure
  missing_or_failed_lenses: []
  run_status: full
  synthesis_run_status: full
---
# Synthesize

## Consensus
- issue-001 (high): manufacturing-bom-ontology.yaml은 헤더에서 'PLM/MES 통합의 개념 기준 문서'를 선언하지만, Part.rev, Part.current_eco, BomLine.scrap_rate, InspectionPlan.acceptance_criteria, std_time 등 핵심 속성 다수의 원본 권위를 외부 시스템(도면 관리대장, ECO 시스템, 생산계획팀 엑셀, 품질팀 SOP, 표준원가 시스템)에 두고 동기화 계약 없는 복사본만 보유한다. 이는 선언된 목적으로부터의 구조적 이탈이며, 6개 렌즈(axiology, coverage, evolution, logic, semantics, structure) 전원이 지지하고 severity high를 유지한 확정 이슈다. 권위 경계를 1급 개념으로 선언하거나 복사 속성을 외부 참조로 격하하는 fix_now 조치가 필요하다.
  - root cause: 온톨로지가 권위 경계(어떤 개념·값의 진실을 이 문서가 소유하는가)를 설계하지 않고, 현행 시스템·부서별 관리 실태를 그대로 전사(transcribe)하는 방식으로 작성됨
  - materiality: 기준 문서의 존재 목적은 PLM과 MES가 충돌할 때 어느 개념·값이 진실인지 판정하는 권위를 제공하는 것이다(review-value-alignment-criteria.yaml의 user-request-intent 기준). 그러나 본 문서는 판정 권위를 체계 없이 외부 시스템들에 분산시키고 복사 시점·갱신 규칙을 미정의로 남겨, 통합 시 양 시스템이 서로 다른 '원본'을 참조하는 상태를 오히려 공식화한다. PLM과 MES가 rev/ECO/scrap_rate/검사 기준 값에 대해 본 문서를 기준으로 삼으려 할 때, 외부 원본과 복사본이 불일치하는 모든 시점(주간 배치 사이, 분기 대사 사이, 엑셀 갱신 사이)에 충돌 판정 기능이 사라지고 각 시스템이 자체 값으로 동작하므로, 본 리뷰가 검증해야 할 '개념 정합성' 자체가 성립 불가능해진다. 거버넌스 가치(기준 문서의 판정 권위)를 직접 무너뜨리는 current_blocker로 분류되었다.
  - action: 확정 조치(fix_now, must_close_in_target): 각 속성에 대해 authority(원본 시스템)·동기화 방향·갱신 계약을 명시적 1급 개념(예: source_of_truth, sync_policy)으로 선언하거나, 복사 속성을 외부 참조로 격하하여 본 온톨로지가 권위를 갖는 개념 집합을 명확히 경계 짓는다. 동시에 '기준 문서'가 무엇에 대한 기준인지(개념 구조만인지, 값 권위까지인지)를 헤더에서 선언한다. 순서·의존성 측면에서 이 이슈는 issue-010/019/031/037/043 등 다수 권위 결함의 공통 상위 원인(issue_role: root_cause)이므로, 권위 경계 선언을 먼저 확정해야 하위 이슈들의 교정 방향이 결정된다. 헤더의 권위 범위 선언은 '값 권위까지 기대하는가'라는 미해결 질문에 대한 조직의 답을 명문화하는 단계이기도 하므로 속성별 조치보다 선행하는 것이 자연스럽다. 제안 방향은 evolution 렌즈의 교정 방향(원본 참조 선언 또는 즉시 동기화)과도 일치한다.
- issue-002 (high): manufacturing-bom-ontology.yaml의 integrity_rules[0]은 'BOM은 비순환이어야 한다'는 불변식을 선언하면서 같은 문장의 단서로 스크랩 재투입을 Assembly 자기 포함으로 모델링하도록 허용해, MES/MRP 전개가 의존하는 비순환성 보장을 정당화나 보상 규칙 없이 스스로 깨뜨린다. 6개 렌즈(axiology, coverage, evolution, logic, semantics, structure)가 모두 동일한 근본 원인과 high 심각도를 지지했으며, 스크랩 재투입을 별도 관계로 분리하고 비순환 규칙을 무예외로 복원하는 fix_now 조치가 필요하다.
  - root cause: 재생 원료 회수라는 물질 흐름 개념을 별도 관계로 분리하지 않고 기존 BomLine 구조에 욱여넣는 개념 절약의 오적용
  - materiality: 이 리뷰의 선언된 목적은 'BOM 개념 정합성과 제조 운영 위험 최소화'(user-request-intent)다. 비순환성은 MRP 소요량 전개, 원가 적산, 리드타임 계산 등 MES/PLM 핵심 연산이 종료된다는 약속인데, 자기 포함 Assembly를 공식 허용하면 이 약속을 기준 문서 차원에서 포기하게 된다. 실패 조건은 자기 포함 Assembly를 가진 BOM을 MES/MRP가 전개하는 모든 경우로, 전개 무한루프 또는 시스템별 임의 예외 처리로 인한 소요량 불일치가 발생한다. 기준 문서가 순환을 공식 허용하면 하위 시스템들이 각자 방어 로직을 발명하게 되어 통합 기준으로서의 신뢰와 운영 안전이 동시에 깨지므로, 이는 리뷰 기준이 명시한 '제조 운영 위험' 축의 한가운데에 있는 비정렬 트레이드오프다(value_type=tradeoff, alignment_direction=misaligned). 텍스트로 직접 확인되는 observed 결함이며 BOM 전개 종료라는 연산 정확성(correctness)을 현재 시점에 깨뜨리는 current_blocker다.
  - action: 조치는 두 부분이며 순서가 중요하다. 첫째, 스크랩 재투입을 BomLine 자기 참조가 아닌 별도 관계(예: reclaims_into 또는 by-product/co-product 개념)로 분리해 회수 물질 흐름에 전용 표현 수단을 먼저 제공한다 — 표현 수단 부재가 근본 원인이므로 이것이 선행되어야 예외 없는 비순환 규칙이 실현 가능해진다. 둘째, 그 위에서 BOM 비순환 규칙(integrity_rules[0])을 예외 없이 복원하고, 전개 연산이 재투입 관계를 어떻게 다루는지(전개 제외 또는 별도 정산)를 규칙으로 명시해 하위 시스템들이 각자 임의 방어 로직을 발명하지 않도록 한다. 문제 프레이밍상 closure_class는 fix_now, closure_obligation은 must_close_in_target이며, 이 이슈가 관련 결함 패밀리의 root_cause이므로 이 수정이 파생 결함들의 해소 전제가 된다.
- issue-003 (high): manufacturing-bom-ontology.yaml의 integrity_rules[2]는 'ECO.effective_date 이후 생산분은 신규 rev를 따른다'는 발효 의무와 'Part.rev 갱신은 도면 관리대장 반영 후 주간 배치로 동기화한다'는 단서를 한 규칙 안에 담아, 발효일과 동기화 시점 사이 최대 1주의 구 리비전 생산 허용 창을 문서 자체에 내장하고 있다. 6개 렌즈 전원이 support했고 deliberation은 계층화된 근본 원인(직접: 발효 판정 권위 미정의, 상류: issue-007이 추적하는 시간 축 개념 차원의 부재)으로 합의했다. severity high, fix_now, 대상 내 해소 필수.
  - root cause: rev의 권위를 도면 관리대장에 두고 온톨로지를 지연 복사본으로 설계하면서, 발효일 판정이라는 시간 의미론을 어느 권위가 소유하는지 정의하지 않음
  - materiality: 이 이슈는 리뷰 기준이 명시적으로 지목한 검토 축인 '변경관리 개념의 정합성'을 직접 약화시킨다. 기준 문서 자체가 위반 창을 보장하므로 judgment_state는 observed이며, 이 문서를 기준으로 통합한 PLM/MES는 ECO effective_date와 주간 배치 동기화 사이의 공백 기간에 생성·실행되는 생산 오더의 변경관리 위반을 구조적으로 탐지할 수 없다. 잘못된 리비전 생산은 리콜·폐기로 직결되는 대표적 제조 운영 위험이어서 impact_kind가 safety_risk로 분류되었고, 운영 편의(배치 동기화 비용 절감)를 위해 변경관리 보증을 희생하는 이 트레이드오프가 문서에서 인식·정당화되지 않았으며 보상 수단(공백 기간 중 ECO 직접 조회 의무, 발효 보류 상태 등)도 제시되지 않았다는 점이 materiality를 확정한다.
  - action: deliberation이 확정한 canonical 조치 순서: (1) 선행 필수 — effectivity 판정 권위를 ECO 엔티티로 명시 선언하고 Part.rev를 파생 캐시로 강등하며 공백 기간의 판정 규칙(ECO 우선)을 추가한다. (2) 후속 — 그 권위 선언을 기계적으로 판정 가능하게 하는 시간 축/effectivity 모델링(issue-007 범위)을 (1)의 완성 조건으로 배치한다. 두 교정은 경쟁 관계가 아니라 계층 관계이므로 순서가 의존성이다: 권위 선언 없이 시간 모델링만 하면 판정 주체가 여전히 모호하고, 시간 모델링 없이는 권위 선언이 기계 판정 가능한 형태로 완성되지 않는다. 옵션 (b) — rev 동기화 완료를 ECO status=applied의 필요조건으로 정의해 발효와 동기화의 순서를 강제 — 는 권위 선언의 등가 대안으로 병기한다. closure는 fix_now, must_close_in_target이다.
- issue-007 (high): BOM 구조와 시점 의존 값(qty_per, scrap_rate, rev)에 유효기간(effectivity)·이력 개념이 전혀 없어 과거/미래 시점의 BOM을 재구성할 수 없다. Source finding context: manufacturing-bom-ontology.yaml — BomLine, Part.rev, scrap_rate, std_time (시점 의존 값 전반) Source finding context: BomLine.attributes (qty_per, scrap_rate에 유효기간 없음); Part.rev note '주간 배치로 동기화'; ECO...
  - root cause: 온톨로지가 모든 시점 의존 값을 '현재 스냅샷 단일 값'으로 모델링하고 시간 축(effectivity/history) 차원을 개념 공간에서 누락함
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합의 개념 기준 문서로서 변경관리(ECO)와 BOM의 정합성 보장. Failure condition: ECO effective_date 전후에 걸친 생산 오더가 존재할 때, 해당 오더가 따라야 할 BOM 구성·rev를 온톨로지 개념만으로 결정할 수 없는 모든 경우. Impact: 통합 대상 시스템(PLM/MES)이 각자 임의의 시점 해석을 하게 되어 기준 문서 역할이 실패하고, 과거 생산분의 BOM 재구성·감사가 불가능해진다..
  - action: Preserved proposed action: BomLine에 effective_from/effective_to(또는 eco_in/eco_out 참조)를 추가하고, Part.rev를 단일 값이 아닌 리비전 이력(RevisionRecord: rev, effective_date, eco_ref)으로 승격한다. scrap_rate 등 계수에도 유효기간을 부여한다.. Action candidates: fix_now.
- issue-008 (high): PLM(설계 BOM)과 MES(제조 BOM)를 잇는 기준 문서임에도 EBOM/MBOM 구분, phantom assembly, 대체 BOM 등 BOM 뷰 범주가 누락되었다. Source finding context: manufacturing-bom-ontology.yaml — BOM 뷰 범주 (EBOM/MBOM 구분) Source finding context: entities: Assembly.bom_lines (단일 BOM 구조만 존재); ontology 헤더 'PLM/MES 통합의 개념 기준 문서' Source finding context: PLM은 설계 관점...
  - root cause: 온톨로지가 'BOM은 하나'라는 단일 관점 가정 위에 작성되어, 소비 시스템(PLM vs MES)별 BOM 사용 관점 차이(EBOM/MBOM)가 개념 공간에 반영되지 않았다.
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 두 시스템 간 BOM 개념 매핑의 기준 제공. Failure condition: 설계 구조와 제조 구조가 다른 품목(현실 제조에서 일반적)을 두 시스템이 본 온톨로지로 매핑하려 할 때. Impact: 기준 문서가 가장 빈번한 통합 충돌 지점(EBOM↔MBOM 변환)에 대해 침묵하므로, 통합 설계가 문서 밖 암묵 합의에 의존하게 된다..
  - action: Preserved proposed action: BOM에 view/usage 구분(engineering/manufacturing) 개념을 추가하거나, 단일 BOM 정책을 의도적으로 채택했다면 'EBOM=MBOM 단일 구조' 가정을 명시적 제약으로 문서화한다.. Action candidates: fix_now.
- issue-009 (high): ECO가 Part 단위 영향(affected_parts)만 표현하고 BOM 구조·라우팅 변경을 커버하지 못하며, 승인 증거 속성과 부결·취소 종결 상태가 없어 'approved' 상태가 감사 가능하지 않다. Source finding context: manufacturing-bom-ontology.yaml — ECO (변경관리 범위와 통제 증거) Source finding context: ECO.attributes (affected_parts: list of Part; status: [open, approved, applied]); relations '{ fro...
  - root cause: 변경관리 개념이 '도면 리비전 변경' 사용 사례 하나에서 출발해 설계되어, 변경 대상의 일반화(BOM 구조·공정)와 통제 행위 증거 차원(승인자·시각·근거)·종결 상태(부결·취소)가 개념화되지 않았다.
  - materiality: Upstream artifacts classify this as high. Affected purpose: 품목·BOM·라우팅 전반에 대한 변경관리 정합성 (요청문이 명시한 검토 목적). Failure condition: BOM 구성 변경 또는 라우팅 변경이 발생할 때(품목 자체는 불변), 그리고 ECO가 부결되거나 승인 책임을 추적해야 할 때. Impact: 변경관리 개념이 제조 변경의 상당 부분을 표현하지 못해, 기준 문서를 따르는 시스템에서 통제 밖 변경 경로가 구조적으로 허용된다..
  - action: Preserved proposed action: ECO의 영향 대상을 BomLine·Routing(또는 ChangeItem 개념)으로 확장하고, status에 rejected/cancelled를 추가하며, 승인 증거 속성(approved_by, approved_at, rationale)을 부여한다.. Action candidates: fix_now.
- issue-010 (high): 복수 장부에 걸쳐 관리된다고 명시된 값들(scrap_rate, 누적 std_time, rev)에 단일 원본(권위) 지정과 동기화 실패 시 우선순위 규칙이 없다. Source finding context: manufacturing-bom-ontology.yaml — scrap_rate, std_time, rev (병행 관리 값의 단일 권위 미지정) Source finding context: BomLine.scrap_rate note '생산계획팀 엑셀로 별도 관리되는 값을 복사해 둔다'; notes[0] '표준원가 시스템에도 수기 입력 필드가 있어 분기마다 대사한다...
  - root cause: 기존 운영 현실(엑셀 별도 관리, 수기 입력, 주간 배치)을 권위 재설계 없이 그대로 온톨로지에 기술하여, '현황 기술'과 '기준 규범'이 분리되지 않음
  - materiality: Upstream artifacts classify this as high. Affected purpose: 여러 시스템이 참조할 단일 개념 기준으로서의 권위 제공. Failure condition: 동기화 지연 구간(rev: 최대 1주, std_time: 최대 1분기) 또는 복사 누락 시 두 장부의 값이 다른 모든 경우. Impact: 기준 문서 자체가 사본/원본 혼재를 내장하고 있어, 통합 시스템이 잘못된 값을 권위로 채택해도 문서상 위반이 아니다 — 자재 소요·원가·생산 리비전 오류가 정당화된다..
  - action: Preserved proposed action: 각 병행 관리 값에 authority(원본 시스템) 지정, 동기화 주기·방향, 불일치 구간의 우선 규칙(예: 'rev는 도면 관리대장이 항상 우선, 온톨로지 값은 캐시')을 속성 또는 integrity_rule로 명시한다.. Action candidates: fix_now.
- issue-016 (high): 비순환 원칙에 자기포함 예외를 허용하여, DAG를 전제하는 PLM/MES/MRP 통합 및 향후 재활용 시나리오 확장 시 구조 충돌이 발생한다. Source finding context: manufacturing-bom-ontology.yaml > integrity_rules[0] (BOM 비순환 규칙의 자기포함 예외) Source finding context: integrity_rules[0]: "BOM은 비순환이어야 한다. 단, 재생 원료 회수 공정(스크랩 재투입)은 Assembly가 자기 하위에 자신을 포함하는 형태로 모델링한다." Source finding...
  - root cause: 스크랩 재투입이라는 물질 흐름 개념을 BOM 구성 관계(BomLine)와 분리하지 않고 같은 그래프에 욱여넣은 개념 설계 결정
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합의 개념 기준 문서로서 BOM 구조 불변식을 제공하는 역할. Failure condition: DAG 전제의 외부 시스템(MRP, BOM 전개, 원가 적산)에 자기 순환 BOM 데이터가 유입되는 통합 시점. Impact: 기준 문서가 허용한 구조를 통합 대상 시스템이 수용하지 못해, 시스템별 임의 예외 처리 또는 데이터 거부가 발생하고 기준 문서의 권위가 무너진다..
  - action: Preserved proposed action: 비순환 규칙을 예외 없이 유지하고, 스크랩 재투입은 별도 개념(예: ByProduct/RecoveredMaterial 산출 관계 또는 co-product 표기)으로 분리하여 BOM 그래프 밖에서 표현한다.. Action candidates: fix_now.
- issue-017 (high): BomLine에 유효기간(effectivity)·리비전 차원이 없어 ECO 기반 변경관리가 BOM 구조 수준에서 표현 불가능하고, 변경 시 과거 BOM의 연속성이 끊긴다. Source finding context: manufacturing-bom-ontology.yaml > BomLine (effectivity 부재) 및 ECO와의 연결 구조 Source finding context: BomLine.attributes(parent_ref, child_ref, qty_per, scrap_rate만 존재), ECO.attributes.affected_parts, i...
  - root cause: BOM을 시점 무관한 현재 상태 스냅샷으로 개념화하고, 변경관리(ECO)를 Part 메타데이터로만 부착한 설계 결정
  - materiality: Upstream artifacts classify this as high. Affected purpose: 품목·BOM·변경관리 개념 정합성을 보장하는 PLM/MES 통합 기준 문서 역할(요청 요지에 명시). Failure condition: ECO가 BOM 구성(qty 변경, 부품 교체)을 변경하는 모든 경우 — 제조업에서 가장 빈번한 변경 유형. Impact: 변경 전후 BOM의 동시 표현이 불가능해 effective_date 기반 생산 규칙이 집행 불가능한 선언으로 남고, MES가 시점별 BOM을 조회할 수 없어 통합 기준 문서로서의 핵심 용도가 실패한다..
  - action: Preserved proposed action: BomLine에 effective_from/effective_to(또는 eco_in/eco_out 참조)를 추가하여 BOM 변경을 행 추가로 표현하고, ECO가 Part가 아닌 BomLine 변경 집합을 가리키도록 관계를 확장한다.. Action candidates: fix_now.
- issue-018 (high): 외부 원본(도면 관리대장, ECO 시스템)의 변경이 주간 배치로만 반영되어, ECO effective_date와 rev 사이에 최대 1주의 불일치 창이 구조적으로 존재한다. Source finding context: manufacturing-bom-ontology.yaml > Part.rev / Part.current_eco 동기화 규칙 Source finding context: Part.rev note("도면 관리대장이 원본"), Part.current_eco note("ECO 시스템이 원본"), integrity_rules[2]("Part.rev 갱신은 .....
  - root cause: 외부 시스템이 원본인 값을 온톨로지에 복사 속성으로 보유하면서 동기화 지연을 허용한 권위(authority) 설계 결정
  - materiality: Upstream artifacts classify this as high. Affected purpose: ECO effective_date 이후 생산분이 신규 rev를 따른다는 변경관리 무결성 규칙(integrity_rules[2]). Failure condition: ECO effective_date와 주간 배치 사이 기간에 해당 품목의 생산 오더가 생성되는 경우. Impact: 구 리비전 도면으로 생산이 진행되어 품질·규격 부적합 리스크가 발생하고, 기준 문서의 무결성 규칙이 운영상 거짓이 되어 통합 시스템 간 신뢰 기준이 사라진다..
  - action: Preserved proposed action: rev/current_eco를 복사 저장하지 말고 원본 시스템 참조(조회 시점 해석)로 선언하거나, ECO 승인 이벤트 기반 즉시 동기화로 정책을 바꾸고 주간 배치는 대사 용도로 격하한다.. Action candidates: fix_now.
- issue-025 (high): 단일 규칙 내에서 'BOM은 비순환이어야 한다'(necessary 제약)와 '스크랩 재투입은 Assembly가 자기 하위에 자신을 포함하는 형태로 모델링한다'(자기순환 구조의 처방)가 동시 만족 불가능하다. [conflict_pair: integrity_rules[0]:89 ↔ integrity_rules[0]:89 / satisfiability_note: intra-claim — 동일 문장의 본문과 단서 조항이 직접 모순 / modality_note: necessary — 필수 불변식에 대해 그 불변식을 위반하는 구조를 처방(modality 구분 오류: 예외를...
  - root cause: 물질 흐름 현상(스크랩 재투입)을 별도 개념으로 분리하지 않고 BOM 구조 사이클로 표현하기로 한 모델링 결정이, 그 사이클을 금지하는 불변식과 같은 규칙 문장 안에 단서 조항으로 병합됨
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합의 개념 기준 문서로서 BOM 구조 불변식을 통합 대상 시스템에 일관되게 제공하는 것 (파일 헤더 2행의 선언 목적). Failure condition: 재생 원료 회수(스크랩 재투입)를 포함하는 품목이 하나라도 존재하는 순간, 처방된 모델링과 선언된 불변식이 동시에 성립할 수 없다. Impact: 통합 시스템들이 비순환 검증과 self-loop 허용 중 어느 쪽을 따를지 각자 선택하게 되어 기준 문서의 권위가 무력화되고, MRP 전개/원가 롤업의 종료 조건이 시스템별로 갈라진다.
  - action: Preserved proposed action: 비순환 규칙을 예외 없이 BomLine 그래프 전체에 적용하고, 스크랩 재투입은 BOM 구조 밖의 별도 개념(예: by-product/recovery flow 관계 또는 음수 산출 라인으로 BOM 전개에서 제외되는 명시 타입)으로 분리 모델링한다. 가장 작은 수정은 integrity_rules[0]의 단서 조항을 삭제하고 '스크랩 재투입은 BomLine이 아닌 별도 관계로 표현하며 BOM 전개 대상에서 제외한다'는 규칙으로 대체하는 것이다.. Action candidates: fix_now.
- issue-026 (high): AlternatePart 정의는 대체 관계의 대칭성을 무조건적(necessary)으로 선언하는데('A의 대체가 B이면 B의 대체도 A다'), direction 속성은 one_way를 허용하고 기본값으로 삼아 비대칭 인스턴스를 기본 상태로 만든다. 두 claim은 one_way 인스턴스에 대해 동시 만족 불가능하다. [conflict_pair: AlternatePart.definition:33 ↔ AlternatePart.direction:37-40 / satisfiability_note: inter-claim — 동일 엔티티 내 정의 claim과 속성 스키마 c...
  - root cause: v2.0 작성 시 대칭 관계로 출발한 정의 텍스트와 이후 추가된 방향성 속성(one_way 기본) 사이의 modality 정합화가 수행되지 않음 — 정의는 necessary 진술로 남고 속성은 그것을 부정하는 기본값을 도입
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합 시 대체 부품 판정 로직의 단일 기준 제공 (문서 목적 선언 2행, AlternatePart 32-40행). Failure condition: direction=one_way인 AlternatePart 인스턴스가 존재하고 둘 이상의 소비 시스템이 각각 정의와 속성 중 다른 쪽을 기준으로 구현한 경우. Impact: 역방향 대체 투입은 제조 현장에서 품질 부적합·사양 위반으로 직결될 수 있는 운영 위험이며, 어느 구현도 문서 위반이 아니라는 점에서 기준 문서가 위험을 만들어내는 형태가 된다.
  - action: Preserved proposed action: 정의 문장을 'direction=bidirectional인 경우 대칭이며, one_way는 primary→alternate 방향만 허용한다'로 수정하여 대칭성을 조건부(possible)로 격하하는 것이 최소 수정이다. 대안으로 보편 대칭이 실제 의도라면 direction 속성과 one_way 값을 제거한다. relations 목록의 alternate_of 단축 표기(86행)에도 선택한 방향 의미를 명시한다.. Action candidates: fix_now.
- issue-027 (high): 'ECO.effective_date 이후 생산분은 신규 rev를 따른다'는 obligatory 규칙이, 같은 문장이 인정하는 'Part.rev는 주간 배치로만 갱신된다'는 상태 전개 claim과 시간 전개 경로 위에서 충돌한다. effective_date부터 차주 배치 사이의 구간에서는 모델이 보유한 Part.rev로 이 의무를 만족시키는 것이 불가능하다. [conflict_pair: integrity_rules[2] 전반부:91 ↔ integrity_rules[2] 후반부:91 (보조: Part.rev:15) / satisfiability_note: intra...
  - root cause: rev의 권위 원본(도면 관리대장)과 온톨로지 내 표현(Part.rev)의 비동기 복제를 사실로 수용한 채, 의무 규칙은 권위 원본 시점 기준으로 작성되어 두 시간 축이 한 규칙 안에서 봉합되지 않음
  - materiality: Upstream artifacts classify this as high. Affected purpose: 변경관리(ECO)와 생산 실행 간 rev 정합성 보장 — 요청 요지의 '변경관리 개념의 정합성과 제조 운영 위험' 핵심 축. Failure condition: effective_date가 주간 배치 동기화보다 앞서는 모든 ECO에서 최대 1주의 지연 구간이 구조적으로 발생한다 (예외가 아니라 규칙 자체가 보장하는 정상 경로). Impact: 지연 구간 생산분의 rev 추적 기록이 규칙과 모순되거나 부재하게 되어, PLM/MES 통합의 변경관리 계약 자체를 이 문서가 보증할 수 없다.
  - action: Preserved proposed action: 의무 규칙의 판정 근거를 Part.rev 스냅숏에서 분리한다. 최소 수정은 '생산 시점 유효 rev는 Part.rev가 아니라 ECO(affected_parts, effective_date, status)로부터 도출한다'는 유효성(effectivity) 해석 규칙을 명시하고, Part.rev를 캐시/투영 값으로 격하하는 것이다. 그렇지 않다면 의무 발효 시점을 'effective_date와 rev 동기화 완료 중 늦은 시점'으로 재정의해 규칙과 상태 전개를 일치시킨다.. Action candidates: fix_now.
- issue-030 (high): AlternatePart의 정의는 대칭(양방향) 관계를 단언하지만 direction 속성은 기본값 one_way로 비대칭을 허용하여, 이름·정의와 실제 모델 의미가 정면으로 모순된다. Source finding context: manufacturing-bom-ontology.yaml > entities.AlternatePart Source finding context: AlternatePart.definition("A의 대체가 B이면 B의 대체도 A다") vs AlternatePart.attributes.direction(enum [one_way, bidirec...
  - root cause: 대칭 대체와 단방향 대체라는 두 개의 서로 다른 도메인 개념을 하나의 AlternatePart 엔티티에 합치면서, 정의 문장은 대칭 사례 기준으로, 속성은 단방향 사례 기준으로 각각 작성됨
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합의 개념 기준 문서로서 대체부품 의미의 단일 해석 제공. Failure condition: PLM과 MES가 각각 정의 문장과 direction 속성 중 다른 쪽을 구현 근거로 삼아 대체부품 허용 범위가 시스템 간 불일치하는 경우. Impact: 단방향 대체만 허용되는 부품이 역방향으로 투입될 수 있어 잘못된 자재 사용·품질 사고로 이어질 수 있고, 기준 문서가 모순을 내장하므로 어느 구현도 '기준 위반'으로 판정할 수 없게 된다.
  - action: Preserved proposed action: 정의 문장에서 무조건적 대칭 단언을 제거하고 'direction=bidirectional인 경우에만 대칭'으로 수정한다. relations의 alternate_of 단축 표기에도 방향 의미가 보존됨(또는 bidirectional 한정 단축임)을 명시한다.. Action candidates: fix_now.
- issue-031 (high): scrap_rate는 이름·의미상 공정(Operation) 수율에서 파생되는 계수인데, 외부 엑셀이 원본인 값을 BOM 라인에 독립 입력 복사본으로 모델링하여 파생값-입력 불일치와 원천 드리프트를 내장한다. Source finding context: manufacturing-bom-ontology.yaml > entities.BomLine.attributes.scrap_rate Source finding context: BomLine.scrap_rate note: "공정 불량 감안 계수. 생산계획팀 엑셀로 별도 관리되는 값을 복사해 둔다." Source find...
  - root cause: scrap_rate의 권위 소재(생산계획 엑셀)를 모델 밖에 둔 채, 온톨로지에는 동기화 계약 없는 복사 필드를 정의함
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합 시 자재 소요량 계산의 단일 진실 원천 확립. Failure condition: 생산계획팀 엑셀의 scrap_rate가 갱신되었으나 BomLine 복사본이 갱신되지 않은 상태에서 MES/MRP가 BomLine 값으로 소요량을 산출하는 경우. Impact: 자재 부족 또는 과잉 발주가 발생하며, 기준 문서 자체가 복사 패턴을 승인하고 있어 통합 설계 단계에서 이 위험이 교정되지 않고 그대로 이관된다.
  - action: Preserved proposed action: scrap_rate의 원천 권위를 모델에서 선언한다. 최소 수정으로는 type을 ref 또는 derived로 바꾸고 원천(생산계획 scrap 마스터)과 동기화 계약(주기·방향·충돌 시 우선순위)을 명시하거나, 공정 귀속 계수라면 Operation/Routing 쪽 속성으로 이동시킨다.. Action candidates: fix_now.
- issue-032 (high): 'BOM 구성 관계'라는 이름으로 '공정 재투입(자재 흐름)'이라는 다른 의미의 개념을 표현하게 하여, BomLine의 의미(상위 품목이 하위 품목을 얼마나 쓰는지)와 실제 사용(재생 루프)이 불일치하는 동음이의 사용을 제도화한다. Source finding context: manufacturing-bom-ontology.yaml > integrity_rules[0] Source finding context: integrity_rules[0]: "BOM은 비순환이어야 한다. 단, 재생 원료 회수 공정(스크랩 재투입)은 Assembly가 자기 하위에 자신을 포함하...
  - root cause: 공정 차원의 자재 회수 흐름(시간·공정 종속 개념)을 표현할 별도 개념이 없어, 가장 가까운 기존 구조인 BomLine 자기참조를 의미 전용(轉用)함
  - materiality: Upstream artifacts classify this as high. Affected purpose: BOM 전개·소요량·원가 계산이 의존하는 'BomLine=정적 구성 관계' 의미의 일관성. Failure condition: MES/MRP가 자기참조 BomLine을 포함한 BOM을 전개할 때(무한 루프 방어 로직의 유무·방식에 따라 결과가 달라지는 모든 경우). Impact: 전개 결과·소요량·원가가 구현마다 달라지거나 시스템이 루프에서 실패하며, 통합 기준 문서가 이 모호성을 규칙으로 승격시켜 두 시스템이 서로 다른 루프 처리 의미를 갖게 된다.
  - action: Preserved proposed action: 재생 원료 회수는 BomLine이 아닌 별도 개념(예: ByproductRecovery 또는 Operation에 귀속되는 회수 산출 관계)으로 분리하고, BOM 비순환 규칙은 예외 없이 유지한다.. Action candidates: fix_now.
- issue-033 (high): 'rev'라는 이름은 품목의 현행 유효 리비전을 의미하지만, 실제 의미는 '최대 1주 지연된 마지막 동기화 시점의 리비전'이어서 이름과 실제 의미가 불일치하며, integrity_rules[2]의 두 문장이 이 불일치를 규칙 내부 모순으로 노출한다. Source finding context: manufacturing-bom-ontology.yaml > entities.Part.attributes.rev, current_eco 및 integrity_rules[2] Source finding context: Part.rev note "도면 관리대장이 원본", Par...
  - root cause: 유효 리비전이라는 파생 개념(ECO effective_date로부터 도출)을 독립 복사 필드 Part.rev로 모델링하고, 동기화 지연(주간 배치)을 규칙에 명문화함
  - materiality: Upstream artifacts classify this as high. Affected purpose: ECO 변경관리 규칙(effective_date 이후 신규 rev 적용)의 실효성과 변경 적용 시점의 정확성. Failure condition: ECO effective_date와 주간 배치 동기화 사이의 기간에 MES가 Part.rev를 현행 리비전으로 신뢰하여 생산 오더를 생성하는 경우. Impact: 구 리비전 도면 기준 생산이 최대 1주간 규칙상 '정상'으로 통과하며, 변경관리 개념의 정합성이라는 검토 목적 자체가 충족되지 않는다.
  - action: Preserved proposed action: 최소 수정: rev의 의미를 'last_synced_rev'로 정직하게 개명하거나, note에 '유효 rev 판정은 ECO.effective_date와 결합해 도출해야 하며 Part.rev 단독 참조 금지'를 명시한다. 더 나은 수정: 유효 rev를 ECO로부터의 파생 투영으로 정의하고 Part.rev 독립 필드를 제거한다.. Action candidates: fix_now.
- issue-040 (high): ECO에서 BomLine/Routing으로 가는 변경 전파 경로가 relations와 엔티티 속성 어디에도 없어, 설계변경이 BOM 구조·공정에 미치는 영향을 그래프로 추적할 수 없다. Source finding context: manufacturing-bom-ontology.yaml entities.ECO / relations 블록 (변경관리 연결 구조) Source finding context: manufacturing-bom-ontology.yaml:64-70, 79-86 (ECO.affected_parts는 Part만 참조, relations에 ECO→P...
  - root cause: 변경관리 모델링이 ECO→Part 입자(품목 단위)에서 멈췄고, 변경의 실제 효과 단위인 BOM 구성/라우팅 수준의 effectivity 개념이 도입되지 않아 ECO에서 BomLine/Routing으로 가는 변경 전파 경로가 그래프에 존재하지 않는다.
  - materiality: Upstream artifacts classify this as high. Affected purpose: PLM/MES 통합의 개념 기준 문서로서 변경관리(ECO) 개념의 정합성 보장. Failure condition: ECO가 BOM 구성이나 공정 변경을 수반할 때(수량 변경, 부품 교체, 공정 추가), 그래프 상에서 변경 대상 BomLine/Routing을 식별할 수 없어 effective_date 기준 신·구 BOM 적용을 시스템이 판정하지 못한다 Source finding context: ECO가 BOM 구성이나 공정 변경을 수반할 때(수량 변경, 부품 교체, 공정 추가), 그래프 상에서 변경 대상 BomLine/Routing을 식별할 수 없어 effective_date 기준 신·구 BOM 적용을 시스템이 판정하지 못한다.. Impact: 통합 대상 시스템이 변경 전파를 각자 암묵 규칙(part_no 매칭 등)으로 구현하게 되어, 신·구 BOM 혼용 생산·구버전 라우팅 오더 생성 같은 제조 운영 위험이 기준 문서 수준에서 차단되지 않는다. Source finding context: 통합 대상 시스템(PLM, MES)이 변경 전파를 각자 암묵 규칙으로 구현하게 되어, 신·구 BOM 혼용 생산·구버전 라우팅으로의 오더...
  - action: Preserved proposed action: ECO에 affected_bom_lines 또는 BomLine/Routing 수준의 effectivity 참조를 추가하거나, BomLine·Routing에 effective_from_eco 속성과 대응 relation을 추가해 ECO→BOM/공정 경로를 닫는다.. Action candidates: fix_now.
- issue-004 (medium): 정의는 대칭성을 선언하고 속성 기본값은 one_way를 선언하는 모순을 기준 문서가 미해결로 보유하여, 통합 표준으로서 대체품 의미론을 판정하는 목적을 수행하지 못한다. Source finding context: manufacturing-bom-ontology.yaml AlternatePart (definition vs direction 속성) Source finding context: manufacturing-bom-ontology.yaml: AlternatePart.definition 'A의 대체가 B이면 B의 대체도 A다' vs direction enum...
  - root cause: v2.0 개정 과정에서 direction 속성을 추가하면서 구 대칭 정의 문장을 갱신하지 않은 정의-속성 동기화 누락(경계 내 증거로는 정의와 속성의 공존 자체가 시작 원인)
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품목(대체품) 개념의 정합성 (user-request-intent). Failure condition: PLM과 MES가 본 문서를 각자 해석하여 대체품 전개 로직을 구현하고, one_way 의도였던 대체쌍이 한쪽 시스템에서 양방향으로 적용되는 경우. Impact: 기준 문서가 모순을 내장하면 의미론 판정 권위가 사라지고, 잘못된 방향의 대체 투입은 품질·안전 위험으로 직결된다..
  - action: Preserved proposed action: 정의를 방향성 중립으로 수정하고('대체 가능 관계이며 방향성은 direction이 결정한다') 대칭 주장 문장을 삭제하거나, bidirectional을 기본값으로 삼을 도메인 근거가 있다면 정의와 기본값을 일치시킨다. relations의 alternate_of 단축 표기에도 방향 의미를 명시한다.. Action candidates: fix_now.
- issue-005 (medium): 단위 환산이라는 수량 무결성의 핵심 결정을 현장 임의 판단에 위임하는 현행 관행을, 기준 문서가 교정 대상이 아닌 전제로 당연시하여 수록하고 있다. Source finding context: manufacturing-bom-ontology.yaml notes[1] (uom 환산의 현장 위임) Source finding context: manufacturing-bom-ontology.yaml: notes[1] 'uom 변환(kg↔ea 등)은 품목별 환산계수 마스터가 없고, 필요 시 현장에서 환산한다.' + Part.uom enum [ea, kg, m] (가치 판단...
  - root cause: 환산계수를 소유할 권위 시스템이 조직에 부재한 현실을, 온톨로지가 결함으로 선언하는 대신 전제로 수용함
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 제조 운영 위험의 식별과 통제 (user-request-intent). Failure condition: kg 관리 자재가 ea 기준 BOM 라인에 투입되는 등 단위가 다른 품목 간 수량 결정이 필요한 모든 생산·재고 처리 시점. Impact: 통합 기준 문서가 비통제 환산을 전제로 수용하면 PLM/MES 어느 쪽도 환산계수 마스터를 소유할 책임을 지지 않게 되어 공백이 영속화된다..
  - action: Preserved proposed action: 품목별 환산계수(UomConversion) 개념을 추가하거나, 추가하지 않을 경우 notes의 해당 항목을 '알려진 공백/위험'으로 재분류하여 기준 문서가 현장 임의 환산을 승인하지 않음을 명시한다.. Action candidates: fix_before_release, follow_up.
- issue-011 (medium): Shared-root issue across finding-011, finding-020: 폐쇄형 uom enum과 환산계수 부재(020)는 finding-011과 같은 뿌리 — 단위·환산을 1급 개념으로 승격하지 않은 설계 결정 — 에서 나온 결함이다. Source finding context: manufacturing-bom-ontology.yaml — UoM 변환 개념 Source finding context: notes[1] 'uom 변환(kg↔ea 등)은 품목별 환산계수 마스터가 없고, 필요 시 현장에서 환산한다'; Part.uom enum [ea, k...
  - root cause: 폐쇄형 uom enum과 환산계수 부재(020)는 finding-011과 같은 뿌리 — 단위·환산을 1급 개념으로 승격하지 않은 설계 결정 — 에서 나온 결함이다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: MES 측 자재 소요·생산 실적 계산의 결정성 / 품목 개념의 정합성과 BOM 수량(qty_per)의 시스템 간 일관 해석(PLM/MES 통합 기준 역할). Failure condition: parent와 child의 uom이 다른 BomLine이 존재하는 모든 경우(enum이 3종이므로 구조적으로 발생 가능) / 3종 외 단위를 쓰는 신규 품목 등록 시(enum 수정 필요), 또는 kg 관리 자재가 ea 기준 BOM에 투입되어 환산이 필요한 모든 경우. Impact: BOM 수량 의미가 품목 쌍마다 비결정적이 되어, MES 구현이 기준 문서로부터 소요량 계산 규칙을 도출할 수 없다. / 확장마다 기준 문서 자체의 스키마 변경이 발생하고, 환산이 모델 밖 현장 판단에 의존하여 동일 BOM이 시스템마다 다른 수량으로 해석될 수 있다..
  - action: Preserved proposed action: UomConversion 개념(part_ref, from_uom, to_uom, factor, 유효기간)을 추가하고, BomLine 단위 불일치 시 환산계수 존재를 요구하는 integrity_rule을 더한다. / uom을 참조형 단위 마스터(UomCode 엔티티 또는 개방형 코드 목록)로 바꾸고, 품목별 환산계수(UomConversion: part_ref, from_uom, to_uom, factor) 개념을 추가한다.. Action candidates: fix_before_release, follow_up.
- issue-012 (medium): Part에 lifecycle 상태 개념이 전혀 없고 Routing lifecycle은 draft/released에서 끝나, 단종 품목 발주·구공정 생산을 모델이 표현·차단할 수 없다. Source finding context: manufacturing-bom-ontology.yaml — lifecycle 차원 (Part 상태, Routing 종결 상태) Source finding context: Part.attributes (status/lifecycle 속성 없음); Routing.status enum [draft, released] Source finding...
  - root cause: 온톨로지가 '생산 개시(draft→released)' 전반부 lifecycle만 모델링하고 종료·대체 구간(단종, 폐기, 개정 대체)을 개념화하지 않았다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품목·라우팅의 도입부터 종료까지의 제조 운영 통제 (요청문의 '제조 운영 위험' 축). Failure condition: 품목 단종 또는 라우팅 개정이 발생한 이후 — 구 라우팅이 여전히 released이고 단종 품목이 여전히 유효 품목인 모든 시점. Impact: 단종 품목 발주·구공정 생산이라는 운영 사고를 모델이 표현·차단할 수 없어, 기준 문서를 따르는 시스템이 해당 통제를 각자 임의 구현하게 된다..
  - action: Preserved proposed action: Part에 lifecycle_status(active, phase_out, obsolete)를 추가하고, Routing.status에 obsolete(또는 superseded)를 추가한 뒤 생산 오더 생성 규칙을 두 상태 모두에 연동한다.. Action candidates: fix_before_release, follow_up.
- issue-019 (medium): scrap_rate가 버전·동기화 계약 없는 엑셀 복사 값이어서, 원본 변경 시 온톨로지 데이터의 연속성과 정확성이 무보장 상태로 단절된다. Source finding context: manufacturing-bom-ontology.yaml > BomLine.scrap_rate Source finding context: BomLine.scrap_rate note: "생산계획팀 엑셀로 별도 관리되는 값을 복사해 둔다." Source finding context: MRP 소요량 계산에 직접 쓰이는 계수가 관리되지 않는 사본이면, 원본 엑셀이 갱신될 때마다 온톨로지...
  - root cause: 외부 관리 값의 복사 보유를 허용하면서 동기화 계약을 정의하지 않은 권위 설계 공백(rev 항목과 동일 패턴의 반복)
  - materiality: Upstream artifacts classify this as medium. Affected purpose: BOM 기반 소요량·원가 계산의 입력 데이터 신뢰성(통합 기준 문서 역할). Failure condition: 생산계획팀이 엑셀 원본의 scrap_rate를 갱신했으나 온톨로지 사본이 갱신되지 않은 상태에서 MES/MRP가 온톨로지 값을 소비하는 경우. Impact: 자재 소요량 과소/과대 산정이 발생하며, 어느 값이 맞는지 판정할 권위 기준이 문서에 없어 통합 시스템 간 분쟁을 해결할 수 없다..
  - action: Preserved proposed action: scrap_rate의 원본 권위를 명시하고(생산계획 마스터로 이관 또는 온톨로지로 일원화), 사본으로 유지할 경우 최소한 동기화 주기와 대사 절차를 rev와 동일하게 명문화한다.. Action candidates: fix_now.
- issue-020 (medium): current_eco가 단일 참조여서 한 품목에 동시 진행되는 복수 ECO(open+approved 병행)를 수용할 수 없고, ECO.affected_parts와의 양방향 중복이 불일치 시 판정 기준 없이 남는다. Source finding context: manufacturing-bom-ontology.yaml > Part.current_eco (단일 참조) Source finding context: Part.current_eco: {type: ref, target: ECO, note: "최신 설계변경"} 및 ECO.status: [open, approved,...
  - root cause: ECO-Part 관계를 ECO 쪽(affected_parts)에 이미 정의하고도 Part 쪽에 단일 '최신' 사본 참조(current_eco)를 중복 추가하여, ECO로부터 도출 가능한 파생 값을 원천 관계의 프로젝션이 아닌 독립 속성으로 승격했다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 변경관리 개념의 정합성(요청 요지에 명시된 검토 축). Failure condition: 한 품목에 두 개 이상의 미적용 ECO가 동시에 존재하는 경우 — 다부서 병행 설계변경에서 통상적으로 발생. Impact: current_eco의 의미가 미정의 상태가 되어 MES/PLM이 서로 다른 ECO를 '최신'으로 해석할 수 있다. Source finding context: current_eco의 의미가 미정의 상태가 되어 MES/PLM이 서로 다른 ECO를 '최신'으로 해석할 수 있고, ECO.affected_parts와의 양방향 중복이 불일치 시 판정 기준 없이 남는다..
  - action: Preserved proposed action: current_eco 속성을 제거하고 ECO.affected_parts(+status)로부터의 파생 조회로 대체하거나, 유지해야 한다면 '적용 완료(applied)된 최신 ECO'로 의미를 한정 정의한다.. Action candidates: fix_before_release, follow_up.
- issue-021 (medium): capacity_per_shift의 단위가 레코드별로 달라, 능력 계획(APS/스케줄링) 시스템과의 통합 시 작업장 간 비교·합산이 불가능하다. Source finding context: manufacturing-bom-ontology.yaml > WorkCenter.capacity_per_shift Source finding context: capacity_per_shift note: "단위는 작업장마다 다르다(개수 또는 시간)" Source finding context: 새 작업장 유형이 추가될수록 해석 규칙이 레코드 밖 암묵지로 누적되고, 능력 소요 계산(...
  - root cause: 능력이라는 개념의 차원(개수/시간)을 분리 정의하지 않고 현장별 관행을 그대로 단일 수치 필드로 수용한 설계 결정
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 라우팅��작업장 개념의 정합성과 MES 능력 계획 통합(제조 운영 위험 검토 축). Failure condition: 복수 작업장에 걸친 부하/능력 비교 또는 합산이 필요한 모든 스케줄링·능력 계획 연산. Impact: 단위 미식별 수치는 자동 처리 시 차원이 다른 값끼리 연산되는 silent error를 유발하며, 작업장 추가마다 해석 규칙이 모델 밖에 쌓여 통합 기준 문서의 역할이 잠식된다..
  - action: Preserved proposed action: capacity_uom(또는 capacity_kind: pieces|minutes) 속성을 추가해 단위를 데이터로 명시하거나, 능력을 시간 기준 단일 차원으로 정규화한다.. Action candidates: fix_before_release, follow_up.
- issue-022 (medium): 대체부품 관계의 정의(대칭)와 속성 기본값(one_way)이 모순되어, 외부 시스템 통합 시 대체 가능성 해석이 시스템마다 갈린다. Source finding context: manufacturing-bom-ontology.yaml > AlternatePart.direction Source finding context: AlternatePart definition("A의 대체가 B이면 B의 대체도 A다") vs direction enum note("기본은 one_way") Source finding context: PLM(설계 대체)과 MES(자재 출고 대체)가...
  - root cause: 대칭 관계로 처음 개념화한 정의 문장을, 비대칭 케이스 수용을 위해 direction 속성을 추가하면서 갱신하지 않은 정의-속성 불일치
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품목 대체 개념의 정합성과 자재 대체 투입의 안전성(제조 운영 위험 검토 축). Failure condition: 통합 시스템 중 하나가 정의 문장을, 다른 하나가 direction 속성을 권위로 채택하여 대체 가능 집합을 다르게 계산하는 경우. Impact: 단방향만 허용되어야 할 대체가 역방향으로 적용되면 부적합 자재 투입이라는 실물 품질 사고로 이어질 수 있고, 기준 문서 내부 모순은 통합 양측의 해석 차이를 중재할 수 없게 한다..
  - action: Preserved proposed action: 정의 문장에서 무조건 대칭 선언을 제거하고 'direction이 bidirectional일 때만 대칭'으로 정의를 속성과 일치시킨다.. Action candidates: fix_now.
- issue-028 (medium): effective_date가 경과한 open(미승인) ECO는 '승인되지 않은 변경'이면서 동시에 '생산이 따라야 할 변경'이 되어, 승인 게이트라는 status 개념의 의미와 발효 의무 규칙이 충돌한다. Source finding context: manufacturing-bom-ontology.yaml integrity_rules[2] vs ECO.status (open ECO의 유효일 경과) Source finding context: manufacturing-bom-ontology.yaml:91 vs manufacturing-bom-ontology.yaml:...
  - root cause: integrity_rules[2]의 의무 발효 조건이 시간 조건(effective_date) 단독으로 작성되고, 같은 엔티티가 보유한 상태 게이트(ECO.status)와의 결합 규칙이 정의되지 않았다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: ECO 변경관리에서 승인 게이트가 생산 발효를 통제한다는 계약 Source finding context: ECO 변경관리에서 승인 게이트가 생산 발효를 통제한다는 계약 (ECO.status 정의 70행과 규칙 91행이 함께 구성해야 할 계약). Failure condition: effective_date가 경과했으나 status=open인 ECO가 존재하는 시점 — 승인 지연·반려는 status enum이 허용하는 정상 경로이므로 발생 가능성이 구조적으로 열려 있다. Impact: 미승인 변경의 생산 발효 또는 규칙 위반의 양자택일을 통합 시스템에 강제하여, 변경관리 게이트로서의 status 개념을 무력화한다..
  - action: Preserved proposed action: integrity_rules[2]에 발효 조건을 추가한다 — 'ECO.status가 approved 또는 applied이고 effective_date가 경과한 경우에만 신규 rev 준수 의무가 발효된다'. 유효일 경과 후에도 open인 ECO의 처리(유효일 재설정 또는 자동 보류)를 규칙으로 명시한다.. Action candidates: fix_before_release, follow_up.
- issue-034 (medium): capacity_per_shift는 같은 이름 아래 '교대당 산출 개수'와 '교대당 가용 시간'이라는 서로 다른 두 의미를 담는 동음이의 속성이며, 어느 의미인지 식별할 표지가 모델에 없다. Source finding context: manufacturing-bom-ontology.yaml > entities.WorkCenter.attributes.capacity_per_shift Source finding context: capacity_per_shift note: "교대당 처리 능력. 단위는 작업장마다 다르다(개수 또는 시간)." Source finding...
  - root cause: 작업장별로 상이한 능력 측정 관행(개수 기준/시간 기준)을 단일 속성으로 흡수하면서 단위 차원을 모델에서 누락함
  - materiality: Upstream artifacts classify this as medium. Affected purpose: WorkCenter 능력 데이터의 시스템 간 단일 해석(MES 부하 계산의 정확성). Failure condition: 소비 시스템이 특정 작업장의 capacity_per_shift 단위를 잘못 가정하고 std_time_min 기반 부하와 비교하는 경우. Impact: 능력 초과/과소 판정이 틀어져 생산 일정 신뢰성이 무너지며, 단위 정보가 모델에 없어 오류를 데이터만으로 탐지할 수 없다.
  - action: Preserved proposed action: capacity_uom(또는 capacity_kind: enum [pieces, minutes]) 속성을 추가해 의미를 데이터로 식별 가능하게 하거나, 능력 표현을 시간 기준 하나로 정규화한다.. Action candidates: fix_before_release, follow_up.
- issue-035 (medium): 'Plan(계획)'이라는 이름의 개념이 실행 단계인 Operation의 하위 타입으로 정의되어, 이름이 가리키는 존재론적 유형(규범 명세)과 모델상 유형(라우팅 실행 단계)이 불일치한다. Source finding context: manufacturing-bom-ontology.yaml > entities.InspectionPlan Source finding context: InspectionPlan.definition("검사 계획. ... Operation의 검사 파라미터 확장이다") 및 is_a: Operation, acceptance_criteria not...
  - root cause: 검사 명세(계획)와 검사 실행 단계(공정)라는 두 존재론적 유형을 구분하지 않고, 라우팅 포함 편의를 위해 명세 개념을 Operation 하위로 흡수했다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품질 검사 개념의 PLM(명세 관리)/MES(실행) 간 역할 분담 기준 제공. Failure condition: PLM이 InspectionPlan을 재사용 가능한 검사 명세로, MES가 라우팅 단계로 각각 해석해 검사 기준의 권위 소재가 갈리는 경우. Impact: 검사 기준 개정이 어느 시스템 책임인지 불명확해지고, SOP 원본과 복사된 합격 기준의 불일치가 검사 판정 오류로 이어질 수 있다..
  - action: Preserved proposed action: 엔티티를 InspectionOperation으로 개명해 이름을 실제 분류에 일치시키고, acceptance_criteria에 SOP 참조 식별자(문서번호·버전)를 두어 서술 복사 대신 원본 참조로 전환한다.. Action candidates: fix_before_release, follow_up.
- issue-036 (medium): manufactured_by 관계가 구매품을 포함하는 Part 전체에 선언되어 정의와 어긋나고, 구매/제조 구분이 없어 integrity_rules[1]의 생산 오더 게이트 적용 대상이 모호하다. Source finding context: manufacturing-bom-ontology.yaml > relations[manufactured_by] 및 entities.Part.definition Source finding context: relations: { from: Part, to: Routing, kind: manufactured_by } vs Part.d...
  - root cause: Part 정의에 포함된 구매/제조 이분이 속성(make/buy)으로 승격되지 않아, 제조품에만 유효한 관계(manufactured_by)가 전체 Part에 무조건적으로 선언되었다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품목-라우팅 관계의 적용 범위에 대한 PLM/MES 공통 해석. Failure condition: 통합 구현이 모든 Part에 라우팅을 기대하거나, 구매/제조 구분 없이 integrity_rules[1]의 생산 오더 게이트를 적용하는 경우. Impact: 구매품 처리 경로와 제조품 검증 규칙이 모호해져 생산 오더 생성 게이트의 적용 대상이 시스템마다 달라질 수 있다..
  - action: Preserved proposed action: 관계 이름을 manufactured_per_routing 또는 has_routing으로 바꾸고, Part에 procurement_type(enum [make, buy]) 속성을 추가해 관계 적용 조건을 명시한다.. Action candidates: fix_before_release, follow_up.
- issue-037 (medium): 누적 std_time이라는 산식 파생값이 표준원가 시스템의 독립 수기 입력과 공존함을 문서가 인정하면서도 어느 쪽이 권위인지 선언하지 않아, 분기 대사 사이 기간의 원가 왜곡이 구조적으로 허용된다. Source finding context: manufacturing-bom-ontology.yaml > notes[0] Source finding context: notes[0]: "원가 계산용 누적 std_time은 MES가 Routing에서 계산하지만, 표준원가 시스템에도 수기 입력 필드가 있어 분기마다 대사한다." Source finding context: 누적...
  - root cause: 파생값(누적 std_time)의 권위를 원천 산식(Routing 내 Operation.std_time_min 합산)에 귀속시키는 선언 없이, 표준원가 시스템 수기 입력과의 이중 입력 관행을 기준 문서에 현상 그대로 기술했다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 표준원가 산정의 원천 단일화(개념 기준 문서의 권위 선언 역할). Failure condition: 분기 대사 사이 기간에 라우팅 변경이 발생했으나 수기 필드가 갱신되지 않은 채 표준원가가 산정되는 경우. Impact: 원가 왜곡이 분기 단위로 누적될 수 있고, 기준 문서가 권위를 선언하지 않아 통합 설계가 이중 입력을 그대로 계승한다..
  - action: Preserved proposed action: notes를 규범 선언으로 승격한다 — 누적 std_time의 원천은 Routing의 합산 파생값이며 표준원가 시스템 필드는 읽기 전용 투영(또는 폐기 예정)임을 명시한다.. Action candidates: fix_before_release, follow_up.
- issue-041 (medium): AlternatePart 엔티티가 relations 그래프에서 고아 상태다 — 어떤 relation도 이를 연결하지 않고 alternate_of가 Part→Part 직접 관계로 우회하여, direction 정보가 관계 경로로는 누락된다. Source finding context: manufacturing-bom-ontology.yaml entities.AlternatePart / relations의 alternate_of Source finding context: manufacturing-bom-ontology.yaml:32-40, 86 Source findin...
  - root cause: 대체 관계를 구체화 엔티티(AlternatePart)로 승격하면서 relations 블록을 함께 갱신하지 않고 기존 직접 관계(alternate_of)를 '단축 표기'로 남겨둔 이중 표현 설계가, direction 속성 정보를 선언된 관계 경로에서 도달 불가능하게 만들었다.
  - materiality: Upstream artifacts classify this as medium. Affected purpose: 품목 대체 관계의 정합성 — PLM/MES가 대체 자재 전개 시 따라야 할 단일 기준 경로 제공. Failure condition: 소비 시스템이 relations 블록(alternate_of)만으로 대체 관계를 전개할 때, 그래프에 미연결된 AlternatePart.direction을 누락하여 one_way 대체를 양방향으로 적용하는 경우 Source finding context: 소비 시스템이 relations 블록(alternate_of)만으로 대체 관계를 전개할 때, 그래프에 미연결된 AlternatePart.direction을 누락하여 one_way 대체를 양방향으로 적용하는 경우.. Impact: 잘못된 방향의 자재 대체 투입은 직접적 제조 운영 위험이며, 동일 관계의 이중 표현은 두 통합 시스템이 서로 다른 경로를 진실로 삼는 분기를 만든다..
  - action: Preserved proposed action: relations에 AlternatePart→Part(primary/alternate) 관계 두 건을 추가해 구체화 엔티티를 그래프에 연결하고, alternate_of note에 'direction=one_way인 경우 역방향 추론 금지'를 명시한다.. Action candidates: fix_before_release, follow_up.

## Conditional Consensus
- See material issue entries whose problem framing or deliberation status is narrowed or unresolved.

## Disagreement
- none

## Deliberation Decision
- issue-001: no-deliberation-needed
- issue-002: no-deliberation-needed
- issue-003: resolved
- issue-007: no-deliberation-needed
- issue-008: no-deliberation-needed
- issue-009: no-deliberation-needed
- issue-010: no-deliberation-needed
- issue-016: no-deliberation-needed
- issue-017: no-deliberation-needed
- issue-018: no-deliberation-needed
- issue-025: no-deliberation-needed
- issue-026: resolved
- issue-027: resolved
- issue-030: resolved
- issue-031: resolved
- issue-032: no-deliberation-needed
- issue-033: no-deliberation-needed
- issue-040: no-deliberation-needed
- issue-004: resolved
- issue-005: no-deliberation-needed
- issue-011: no-deliberation-needed
- issue-012: no-deliberation-needed
- issue-019: resolved
- issue-020: no-deliberation-needed
- issue-021: resolved
- issue-022: resolved
- issue-028: no-deliberation-needed
- issue-034: resolved
- issue-035: no-deliberation-needed
- issue-036: no-deliberation-needed
- issue-037: no-deliberation-needed
- issue-041: no-deliberation-needed

## Axiology-Proposed Additional Perspectives
- none

## Purpose Alignment Verification
- issue-001: PLM/MES 통합의 개념 기준 문서라는 선언된 목적 (review-value-alignment-criteria.yaml user-request-intent)
- issue-002: BOM 개념 정합성과 제조 운영 위험 최소화 (user-request-intent)
- issue-003: 변경관리 개념의 정합성 (user-request-intent)
- issue-007: PLM/MES 통합의 개념 기준 문서로서 변경관리(ECO)와 BOM의 정합성 보장
- issue-008: PLM/MES 두 시스템 간 BOM 개념 매핑의 기준 제공
- issue-009: 품목·BOM·라우팅 전반에 대한 변경관리 정합성 (요청문이 명시한 검토 목적)
- issue-010: 여러 시스템이 참조할 단일 개념 기준으로서의 권위 제공
- issue-016: PLM/MES 통합의 개념 기준 문서로서 BOM 구조 불변식을 제공하는 역할
- issue-017: 품목·BOM·변경관리 개념 정합성을 보장하는 PLM/MES 통합 기준 문서 역할(요청 요지에 명시)
- issue-018: ECO effective_date 이후 생산분이 신규 rev를 따른다는 변경관리 무결성 규칙(integrity_rules[2])
- issue-025: PLM/MES 통합의 개념 기준 문서로서 BOM 구조 불변식을 통합 대상 시스템에 일관되게 제공하는 것 (파일 헤더 2행의 선언 목적)
- issue-026: PLM/MES 통합 시 대체 부품 판정 로직의 단일 기준 제공 (문서 목적 선언 2행, AlternatePart 32-40행)
- issue-027: 변경관리(ECO)와 생산 실행 간 rev 정합성 보장 — 요청 요지의 '변경관리 개념의 정합성과 제조 운영 위험' 핵심 축
- issue-030: PLM/MES 통합의 개념 기준 문서로서 대체부품 의미의 단일 해석 제공
- issue-031: PLM/MES 통합 시 자재 소요량 계산의 단일 진실 원천 확립
- issue-032: BOM 전개·소요량·원가 계산이 의존하는 'BomLine=정적 구성 관계' 의미의 일관성
- issue-033: ECO 변경관리 규칙(effective_date 이후 신규 rev 적용)의 실효성과 변경 적용 시점의 정확성
- issue-040: PLM/MES 통합의 개념 기준 문서로서 변경관리(ECO) 개념의 정합성 보장
- issue-004: 품목(대체품) 개념의 정합성 (user-request-intent)
- issue-005: 제조 운영 위험의 식별과 통제 (user-request-intent)
- issue-011: MES 측 자재 소요·생산 실적 계산의 결정성 / 품목 개념의 정합성과 BOM 수량(qty_per)의 시스템 간 일관 해석(PLM/MES 통합 기준 역할)
- issue-012: 품목·라우팅의 도입부터 종료까지의 제조 운영 통제 (요청문의 '제조 운영 위험' 축)
- issue-019: BOM 기반 소요량·원가 계산의 입력 데이터 신뢰성(통합 기준 문서 역할)
- issue-020: 변경관리 개념의 정합성(요청 요지에 명시된 검토 축)
- issue-021: 라우팅��작업장 개념의 정합성과 MES 능력 계획 통합(제조 운영 위험 검토 축)
- issue-022: 품목 대체 개념의 정합성과 자재 대체 투입의 안전성(제조 운영 위험 검토 축)
- issue-028: ECO 변경관리에서 승인 게이트가 생산 발효를 통제한다는 계약 Source finding context: ECO 변경관리에서 승인 게이트가 생산 발효를 통제한다는 계약 (ECO.status 정의 70행과 규칙 91행이 함께 구성해야 할 계약)
- issue-034: WorkCenter 능력 데이터의 시스템 간 단일 해석(MES 부하 계산의 정확성)
- issue-035: 품질 검사 개념의 PLM(명세 관리)/MES(실행) 간 역할 분담 기준 제공
- issue-036: 품목-라우팅 관계의 적용 범위에 대한 PLM/MES 공통 해석
- issue-037: 표준원가 산정의 원천 단일화(개념 기준 문서의 권위 선언 역할)
- issue-041: 품목 대체 관계의 정합성 — PLM/MES가 대체 자재 전개 시 따라야 할 단일 기준 경로 제공

## Final Review Result
32 material issue(s) require attention. Highest-priority issue: issue-001 (high) — manufacturing-bom-ontology.yaml은 헤더에서 'PLM/MES 통합의 개념 기준 문서'를 선언하지만, Part.rev, Part.current_eco, BomLine.scrap_rate, InspectionPlan.acceptance_criteria, std_time 등 핵심 속성 다수의 원본 권위를 외부 시스템(도면 관리대장, ECO 시스템, 생산계획팀 엑셀, 품질팀 SOP, 표준원가 시스템)에 두고 동기화 계약 없는 복사본만 보유한다. 이는 선언된 목적으로부터의 구조적 이탈이며, 6개 렌즈(axiology, coverage, evolution, logic, semantics, structure) 전원이 지지하고 severity high를 유지한 확정 이슈다. 권위 경계를 1급 개념으로 선언하거나 복사 속성을 외부 참조로 격하하는 fix_now 조치가 필요하다. Controlled deliberation did not leave a blocking unresolved disagreement for the highest-priority issue.

## Boundary Notes
- 조직이 이 문서에 값 권위까지 기대하는지 여부는 경계 내 증거로 확정 불가 (.onto/authority/core-lexicon.yaml 등 canonical authority chain 문서는 본 unit 허용 범위 밖)
- 실제 주간 배치 주기와 ECO 발효 빈도의 중첩 확률은 경계 내 증거로 산정할 수 없어 위반 창의 실측 발생률은 미확정으로 남는다.
- Runtime completion: issue-scoped synthesis response was unavailable; prose was projected conservatively from upstream artifacts. [plan:executor] kind=claude_code unit_id=synthesis:issue-007 model=claude-fable-5 sandbox=read-only effort=medium [{"type":"system","subtype":"init","cwd":"/private/var/folders/3h/5ml_qx851hsgcn3h6j6y2l5r0000gn/T/onto-ontology-eval-manufacturing-bom-MOnFqh","session_id":"d560a7b2-d2b8-4a24-8c9f-324590ff7efc","tools":["Task","AskUserQuestion","Bash","CronCreate","CronDelete","CronList","Edit","EnterPlanMode","EnterWorktree","ExitPlanMode","ExitWorktree","Glob","Grep","Monitor","NotebookEdit","PushNotific...

## Immediate Actions Required
- issue-001 (high): fix_now
- issue-002 (high): fix_now
- issue-003 (high): fix_now
- issue-007 (high): fix_now
- issue-008 (high): fix_now
- issue-009 (high): fix_now
- issue-010 (high): fix_now
- issue-016 (high): fix_now
- issue-017 (high): fix_now
- issue-018 (high): fix_now
- issue-025 (high): fix_now
- issue-026 (high): fix_now
- issue-027 (high): fix_now
- issue-030 (high): fix_now
- issue-031 (high): fix_now
- issue-032 (high): fix_now
- issue-033 (high): fix_now
- issue-040 (high): fix_now
- issue-004 (medium): fix_now
- issue-005 (medium): fix_before_release, follow_up
- issue-011 (medium): fix_before_release, follow_up
- issue-012 (medium): fix_before_release, follow_up
- issue-019 (medium): fix_now
- issue-020 (medium): fix_before_release, follow_up
- issue-021 (medium): fix_before_release, follow_up
- issue-022 (medium): fix_now
- issue-028 (medium): fix_before_release, follow_up
- issue-034 (medium): fix_before_release, follow_up
- issue-035 (medium): fix_before_release, follow_up
- issue-036 (medium): fix_before_release, follow_up
- issue-037 (medium): fix_before_release, follow_up
- issue-041 (medium): fix_before_release, follow_up

## Recommendations
- issue-006 (low): capacity_per_shift의 단위가 작업장마다 다르다는 비교 불가능성을 기준 문서가 주석으로 승인하고 있다(가치 정렬 관점의 표면 결함). Source finding context: manufacturing-bom-ontology.yaml WorkCenter.capacity_per_shift Source finding context: manufacturing-bom-ontology.yaml: WorkCenter.capacity_per_shift note '단위는 작업장마다 다르다(개수 또는 시간)' (가치 판단 anchor: review-value-alignment-criteria.yaml user-request-intent, value_type=boundary, alignment_direction=misaligned) Source finding context: 능력 수치의 단위가 작업장마다 다르다는 비교 불가능성을 기준 문서가 정규화하고 있어, 통합 표준이 제공해야 할 횡단 비교 가능성을 스스로 포기한다. Source finding context: 능력 계획·부하 산정은 작업장 간 비교가 본질인데, 단위가 레코드마다 다르면 수치는 그 작업장 담당자에게만 의미 있는 사번호가 된다. 기준 문서가 이 국소 관행을 승인하면 MES 능력 계획 모듈은 문서를 신뢰할 수 없다. 다만 개념 기준 수준에서는 단위 속성 추가로 쉽게 교정 가능한 표면 결함이다. Source finding context: capacity_uom(또는 capacity_kind: qty/time) 속성을 추가하여 단위 차원을 데이터로 명시하고, 주석의 '작업장마다 다르다'를 속성 값으로 흡수한다. Source finding context: .onto/review/20260611-43359fc2/round1/axiology.findings.yaml#axiology-candidate-006
- issue-013 (low): integrity_rule이 참조하는 생산 오더가 엔티티로 정의되지 않았고, 검사 계획만 있고 검사 결과·부적합(NCR) 범주가 없어 MES 측 개념 경계가 불명확하다. Source finding context: manufacturing-bom-ontology.yaml — 생산 실행·품질 결과 측 개념 경계 Source finding context: integrity_rules[1] '생산 오더를 생성할 수 있다' (ProductionOrder 엔티티 부재); InspectionPlan (검사 결과·부적합 개념 부재) Source finding context: PLM/MES 통합 기준이라면 최소한 범위 경계(이 문서는 마스터까지, 실행은 MES 소관)를 명시해야 한다. 현재는 규칙이 미정의 개념(생산 오더)을 참조해 문서가 자기 선언 범위를 스스로 넘고 있어, 독자가 범위를 추측해야 한다. 다만 실행·품질 결과를 의도적으로 제외했을 가능성이 높아 누락 자체보다 경계 미선언이 문제다. Source finding context: 문서에 scope 선언(예: '본 온톨로지는 마스터 개념까지를 다루며 생산 오더·검사 결과는 MES 온톨로지 소관')을 추가하거나, 규칙이 참조하는 외부 개념(ProductionOrder)을 external ref로 표기한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-007
- issue-014 (low): capacity_per_shift의 단위가 작업장마다 다르다고 인정하면서 단위 식별 속성이 없어, 능력 값이 단위 차원을 결여한 비교 불가능한 수치다(coverage 차원 결손 관점). Source finding context: manufacturing-bom-ontology.yaml — WorkCenter.capacity_per_shift Source finding context: WorkCenter.capacity_per_shift note '단위는 작업장마다 다르다(개수 또는 시간)' Source finding context: capacity_per_shift의 단위가 작업장마다 다르다고 인정하면서 단위 식별 속성이 없어, 능력 값이 단위 차원을 결여한 비교 불가능한 수치다. Source finding context: 단위 없는 능력 값은 작업장 간 부하 비교·능력 계획에 사용할 수 없다. 값과 함께 단위를 개념화하지 않은 차원 결손이며, note로 인지된 채 방치되어 있다. Source finding context: capacity_uom(또는 capacity_basis: pieces|minutes) 속성을 WorkCenter에 추가한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-008
- issue-015 (low): 대체 부품 관계에 적용 맥락(어느 상위 조립품/BOM 라인에서 유효한지)과 유효기간 개념이 없다. Source finding context: manufacturing-bom-ontology.yaml — AlternatePart 적용 맥락 Source finding context: AlternatePart.attributes (primary_ref, alternate_ref, direction만 존재) Source finding context: 전역 대체로만 표현되면 '제품 A에서는 대체 가능하지만 제품 B에서는 불가'한 일반적 상황을 모델링할 수 없어, MES 자재 불출 시 부적합 대체 투입을 개념적으로 막지 못한다. Source finding context: AlternatePart에 scope_ref(적용 Assembly 또는 BomLine, 전역이면 null)와 유효기간 속성을 추가한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-009
- issue-023 (low): 라우팅 밖 검사 유형을 추가하려면 InspectionPlan의 상위 개념을 바꾸거나 가짜 라우팅을 만들어야 하는 확장 한계가 있다. Source finding context: manufacturing-bom-ontology.yaml > InspectionPlan (is_a Operation) Source finding context: InspectionPlan.definition: "검사도 하나의 Operation으로 라우팅에 포함되므로, 본 엔티티는 Operation의 검사 파라미터 확장이다." Source finding context: 검사를 라우팅 내 Operation의 하위 유형으로만 모델링하여, 라우팅 밖 검사(수입검사, 출하검사, 정기 감사)를 수용하려면 구조 수정이 필요하다. Source finding context: 수입검사(구매품 입고 시)와 출하검사는 특정 품목 라우팅의 공정 단계가 아닌 경우가 많다. 이런 검사 유형을 추가하려면 InspectionPlan의 상위 개념을 바꾸거나 가짜 라우팅을 만들어야 하므로, 새 범주 수용 시 기존 구조 수정이 강제되는 확장 한계다. 다만 현재 문서가 라우팅 내 공정검사만을 범위로 의도했을 가능성이 있어 표면 지적으로 둔다. Source finding context: 검사 파라미터(sampling_rule, acceptance_criteria)를 독립 개념으로 두고 Operation이 이를 참조하는 구조로 바꾸면, 라우팅 외 검사 컨텍스트가 추가되어도 동일 개념을 재사용할 수 있다. Source finding context: .onto/review/20260611-43359fc2/round1/evolution.findings.yaml#evolution-candidate-009
- issue-024 (low): Routing.status에 종결 상태가 없어 라우팅 개정 시 구 라우팅을 released로 남기면 잘못된 생산 오더가 가능하고, 삭제하면 이력이 소실된다. Source finding context: manufacturing-bom-ontology.yaml > Routing.status enum Source finding context: Routing.status: {type: enum, values: [draft, released]} Source finding context: 라우팅 수명주기에 폐기/대체(obsolete/superseded) 상태가 없어, 라우팅 개정 시나리오에서 구 라우팅의 상태를 표현하려면 enum 수정이 필요하다. Source finding context: integrity_rules[1]이 released만 생산 오더 생성을 허용하므로, 폐기된 라우팅을 released로 남겨두면 잘못된 생산 오더가 가능하고, 삭제하면 이력이 소실된다. 수명주기 상태 추가는 enum 수정이라는 기존 구조 변경을 요구한다. Source finding context: status에 obsolete(또는 superseded)를 추가하고, integrity_rules[1]의 생산 오더 조건은 released 한정으로 유지한다. Source finding context: .onto/review/20260611-43359fc2/round1/evolution.findings.yaml#evolution-candidate-010
- issue-029 (low): Routing.status enum(draft/released)에 후속 상태가 없고 라우팅-rev 연결 규칙이 부재하여, 소비 시스템이 그 공백 지점에서 자체 규칙을 발명하게 된다(현재 명시 claim만으로는 직접 모순 미성립). Source finding context: manufacturing-bom-ontology.yaml Routing.status와 ECO 적용 간 상태 전이 부재 Source finding context: manufacturing-bom-ontology.yaml:56, 90 vs 64-70 Source finding context: Routing.status enum(draft/released)에는 released 이후의 후속 상태(개정중/폐기)가 없어, ECO applied로 품목이 변경된 뒤에도 released 라우팅이 생산 오더 생성 게이트(integrity_rules[1])를 계속 통과한다. 이는 '종결 판정이 가역 상태(품목의 설계 유효성)에 의존하면서 후속 사건(ECO 적용)을 허용'하는 패턴이나, 현재 명시된 claim만으로는 직접 모순을 형성하지 못한다 — 라우팅과 rev/ECO를 연결하는 규칙 자체가 부재하기 때문이다. [satisfiability_note: 구조 부재로 인한 잠재 충돌 / modality_note: possible — released의 영속성이 명시 claim이 아니라 enum 값 부재에서 유도됨 / boundary_handoff_note: '필수 전이 규칙이 적용될 상태 값 자체의 누락'이므로 Logic ↔ Structure 라우팅 기준에 따라 structure lens가 primary. logic 판정으로는 present claims만으로 모순 형성 불가] Source finding context: 명시 규칙 간 직접적인 동시 만족 불가능은 성립하지 않으므로 logic verdict로는 insufficient evidence(구조 부재 기인)이며, structure lens의 1차 검토 대상으로 인계한다. 다만 integrity_rules[1]과 [2]를 함께 만족시키려는 소비 시스템은 라우팅-rev 연결 규칙의 부재 지점에서 자체 규칙을 발명하게 되므로 표면 기록을 남긴다. Source finding context: structure lens 검토와 함께, ECO applied 시 affected_parts의 released Routing 처리 규칙(자동 draft 회귀, 또는 rev별 Routing 버전 분리)을 추가하고 Routing.status에 superseded 류의 후속 상태를 도입하는 방안을 평가한다. Source finding context: .onto/review/20260611-43359fc2/round1/logic.findings.yaml#logic-candidate-005
- issue-038 (low): qty_per의 의미가 child Part의 uom에 암묵적으로 의존하는데 환산 규칙이 모델 밖에 있어, 이름이 약속하는 정량적 의미가 불완전하다. Source finding context: manufacturing-bom-ontology.yaml > entities.BomLine.qty_per 및 entities.Part.uom, notes[1] Source finding context: notes[1]: "uom 변환(kg↔ea 등)은 품목별 환산계수 마스터가 없고, 필요 시 현장에서 환산한다." 및 BomLine.qty_per(단위 무표기) Source finding context: qty_per의 의미가 child Part의 uom에 암묵적으로 의존하는데 환산 규칙이 모델 밖 현장 관행에 있어, qty_per라는 이름이 약속하는 정량적 의미가 불완전하다. Source finding context: 이름이 정량 산식 항(소요량 = qty_per × 생산량)을 의미하는 속성의 단위 의미가 모델에서 닫히지 않으면, 시스템 간 수치 해석이 갈릴 수 있다. 다만 qty_per가 child uom 기준이라는 관행적 해석이 일반적이어서 표면 지적에 그친다. Source finding context: qty_per의 단위 기준이 child_ref.uom임을 note로 명시하고, uom 환산이 필요한 BOM 구간의 처리 원칙(환산계수 마스터 도입 또는 동일 uom 강제)을 한 줄로 선언한다. Source finding context: .onto/review/20260611-43359fc2/round1/semantics.findings.yaml#semantics-candidate-009
- issue-039 (low): Operation이 공정 유형 마스터인지 특정 라우팅 전속 단계인지 모호하여 같은 이름이 두 해석을 오간다. Source finding context: manufacturing-bom-ontology.yaml > entities.Operation 및 Routing.operations Source finding context: Operation.definition("라우팅의 한 공정 단계") vs Operation.attributes.op_code/work_center/std_time_min, Routing.operations(ordered_list of ref) Source finding context: Operation이 재사용 가능한 공정 유형(마스터)인지 특정 라우팅에 귀속된 공정 단계(occurrence)인지 모호하여, 같은 이름이 두 해석을 오간다. Source finding context: 유형/발생(type/occurrence) 미구분은 동음이의 문제의 한 형태이나, 현재 문서만으로는 어느 해석이 의도인지 판정할 수 없어 표면 지적으로 남긴다. PLM/MES 통합 시 공정 마스터 설계에 직접 영향을 주므로 명시할 가치가 있다. Source finding context: Operation이 라우팅 전속인지 공유 마스터인지 정의에 한 줄로 명시하고, 공유 마스터라면 라우팅별 가변 속성(std_time_min 등)을 라우팅-공정 연결 개념으로 분리한다. Source finding context: .onto/review/20260611-43359fc2/round1/semantics.findings.yaml#semantics-candidate-010
- issue-042 (low): Part.current_eco(Part→ECO 참조)에 대응하는 relation 선언이 relations 블록에 없다. Source finding context: manufacturing-bom-ontology.yaml Part.current_eco 속성과 relations 블록의 불일치 Source finding context: manufacturing-bom-ontology.yaml:16, 79-86 Source finding context: relations 블록을 관계의 전수 목록으로 소비하는 시스템은 Part에서 ECO로 가는 경로를 인식하지 못한다. 영향은 제한적이지만 기준 문서의 속성-관계 일관성을 깬다. Source finding context: relations에 { from: Part, to: ECO, kind: current_change } 항목을 추가하거나, relations 블록이 전수 목록이 아니라는 범위 주석을 문서에 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-003
- issue-043 (low): Assembly.bom_lines와 BomLine.parent_ref가 같은 연결을 중복 보관하며 relations에는 한 방향만 선언되어 권위가 불명확하다. Source finding context: manufacturing-bom-ontology.yaml Assembly↔BomLine 양방향 중복 보관 Source finding context: manufacturing-bom-ontology.yaml:22, 27, 80 Source finding context: Assembly.bom_lines(부모→줄)와 BomLine.parent_ref(줄→부모)가 같은 연결을 양쪽에 중복 보관하며, relations에는 한 방향(has_many)만 선언되어 어느 쪽이 권위인지 불명확하다. Source finding context: 개념 기준 문서에서 이중 보관은 통합 시스템 간 동기화 어긋남(목록에는 있는데 parent_ref가 다른 Assembly를 가리키는 줄)의 여지를 남긴다. 한쪽을 파생 투영으로 선언하면 제거되는 위험이다. Source finding context: BomLine.parent_ref를 원본으로 두고 Assembly.bom_lines를 파생(derived) 투영으로 표시하거나 그 반대로 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-004
- issue-044 (info): InspectionPlan은 어떤 관계나 속성에서도 직접 참조되지 않으며 is_a Operation 상속을 통해서만 그래프에 도달 가능하여, '이 품목에 검사 계획이 존재하는가'를 그래프로 답할 수 없다. Source finding context: manufacturing-bom-ontology.yaml entities.InspectionPlan의 그래프 내 도달 경로 Source finding context: manufacturing-bom-ontology.yaml:72-77, 55, 83 Source finding context: InspectionPlan은 어떤 관계나 속성에서도 직접 참조되지 않으며, is_a Operation 상속을 통해서만 그래프에 도달 가능하다. Source finding context: is_a 다형성으로 도달은 가능하므로 엄밀한 고아는 아니지만, 검사 파라미터가 필요한 품목과 InspectionPlan을 잇는 경로가 없어 '이 품목에 검사 계획이 존재하는가'를 그래프로 답할 수 없다. 표면 수준 관찰로 기록한다. Source finding context: 검사 의무가 개념 모델의 요구라면 Part 또는 Routing에서 InspectionPlan으로 가는 명시 관계를 추가하고, 아니라면 다형 포함이 의도임을 relations note로 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-005

## Unique Finding Tagging
- issue-006 (low): capacity_per_shift의 단위가 작업장마다 다르다는 비교 불가능성을 기준 문서가 주석으로 승인하고 있다(가치 정렬 관점의 표면 결함). Source finding context: manufacturing-bom-ontology.yaml WorkCenter.capacity_per_shift Source finding context: manufacturing-bom-ontology.yaml: WorkCenter.capacity_per_shift note '단위는 작업장마다 다르다(개수 또는 시간)' (가치 판단 anchor: review-value-alignment-criteria.yaml user-request-intent, value_type=boundary, alignment_direction=misaligned) Source finding context: 능력 수치의 단위가 작업장마다 다르다는 비교 불가능성을 기준 문서가 정규화하고 있어, 통합 표준이 제공해야 할 횡단 비교 가능성을 스스로 포기한다. Source finding context: 능력 계획·부하 산정은 작업장 간 비교가 본질인데, 단위가 레코드마다 다르면 수치는 그 작업장 담당자에게만 의미 있는 사번호가 된다. 기준 문서가 이 국소 관행을 승인하면 MES 능력 계획 모듈은 문서를 신뢰할 수 없다. 다만 개념 기준 수준에서는 단위 속성 추가로 쉽게 교정 가능한 표면 결함이다. Source finding context: capacity_uom(또는 capacity_kind: qty/time) 속성을 추가하여 단위 차원을 데이터로 명시하고, 주석의 '작업장마다 다르다'를 속성 값으로 흡수한다. Source finding context: .onto/review/20260611-43359fc2/round1/axiology.findings.yaml#axiology-candidate-006
- issue-013 (low): integrity_rule이 참조하는 생산 오더가 엔티티로 정의되지 않았고, 검사 계획만 있고 검사 결과·부적합(NCR) 범주가 없어 MES 측 개념 경계가 불명확하다. Source finding context: manufacturing-bom-ontology.yaml — 생산 실행·품질 결과 측 개념 경계 Source finding context: integrity_rules[1] '생산 오더를 생성할 수 있다' (ProductionOrder 엔티티 부재); InspectionPlan (검사 결과·부적합 개념 부재) Source finding context: PLM/MES 통합 기준이라면 최소한 범위 경계(이 문서는 마스터까지, 실행은 MES 소관)를 명시해야 한다. 현재는 규칙이 미정의 개념(생산 오더)을 참조해 문서가 자기 선언 범위를 스스로 넘고 있어, 독자가 범위를 추측해야 한다. 다만 실행·품질 결과를 의도적으로 제외했을 가능성이 높아 누락 자체보다 경계 미선언이 문제다. Source finding context: 문서에 scope 선언(예: '본 온톨로지는 마스터 개념까지를 다루며 생산 오더·검사 결과는 MES 온톨로지 소관')을 추가하거나, 규칙이 참조하는 외부 개념(ProductionOrder)을 external ref로 표기한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-007
- issue-014 (low): capacity_per_shift의 단위가 작업장마다 다르다고 인정하면서 단위 식별 속성이 없어, 능력 값이 단위 차원을 결여한 비교 불가능한 수치다(coverage 차원 결손 관점). Source finding context: manufacturing-bom-ontology.yaml — WorkCenter.capacity_per_shift Source finding context: WorkCenter.capacity_per_shift note '단위는 작업장마다 다르다(개수 또는 시간)' Source finding context: capacity_per_shift의 단위가 작업장마다 다르다고 인정하면서 단위 식별 속성이 없어, 능력 값이 단위 차원을 결여한 비교 불가능한 수치다. Source finding context: 단위 없는 능력 값은 작업장 간 부하 비교·능력 계획에 사용할 수 없다. 값과 함께 단위를 개념화하지 않은 차원 결손이며, note로 인지된 채 방치되어 있다. Source finding context: capacity_uom(또는 capacity_basis: pieces|minutes) 속성을 WorkCenter에 추가한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-008
- issue-015 (low): 대체 부품 관계에 적용 맥락(어느 상위 조립품/BOM 라인에서 유효한지)과 유효기간 개념이 없다. Source finding context: manufacturing-bom-ontology.yaml — AlternatePart 적용 맥락 Source finding context: AlternatePart.attributes (primary_ref, alternate_ref, direction만 존재) Source finding context: 전역 대체로만 표현되면 '제품 A에서는 대체 가능하지만 제품 B에서는 불가'한 일반적 상황을 모델링할 수 없어, MES 자재 불출 시 부적합 대체 투입을 개념적으로 막지 못한다. Source finding context: AlternatePart에 scope_ref(적용 Assembly 또는 BomLine, 전역이면 null)와 유효기간 속성을 추가한다. Source finding context: .onto/review/20260611-43359fc2/round1/coverage.findings.yaml#coverage-candidate-009
- issue-023 (low): 라우팅 밖 검사 유형을 추가하려면 InspectionPlan의 상위 개념을 바꾸거나 가짜 라우팅을 만들어야 하는 확장 한계가 있다. Source finding context: manufacturing-bom-ontology.yaml > InspectionPlan (is_a Operation) Source finding context: InspectionPlan.definition: "검사도 하나의 Operation으로 라우팅에 포함되므로, 본 엔티티는 Operation의 검사 파라미터 확장이다." Source finding context: 검사를 라우팅 내 Operation의 하위 유형으로만 모델링하여, 라우팅 밖 검사(수입검사, 출하검사, 정기 감사)를 수용하려면 구조 수정이 필요하다. Source finding context: 수입검사(구매품 입고 시)와 출하검사는 특정 품목 라우팅의 공정 단계가 아닌 경우가 많다. 이런 검사 유형을 추가하려면 InspectionPlan의 상위 개념을 바꾸거나 가짜 라우팅을 만들어야 하므로, 새 범주 수용 시 기존 구조 수정이 강제되는 확장 한계다. 다만 현재 문서가 라우팅 내 공정검사만을 범위로 의도했을 가능성이 있어 표면 지적으로 둔다. Source finding context: 검사 파라미터(sampling_rule, acceptance_criteria)를 독립 개념으로 두고 Operation이 이를 참조하는 구조로 바꾸면, 라우팅 외 검사 컨텍스트가 추가되어도 동일 개념을 재사용할 수 있다. Source finding context: .onto/review/20260611-43359fc2/round1/evolution.findings.yaml#evolution-candidate-009
- issue-024 (low): Routing.status에 종결 상태가 없어 라우팅 개정 시 구 라우팅을 released로 남기면 잘못된 생산 오더가 가능하고, 삭제하면 이력이 소실된다. Source finding context: manufacturing-bom-ontology.yaml > Routing.status enum Source finding context: Routing.status: {type: enum, values: [draft, released]} Source finding context: 라우팅 수명주기에 폐기/대체(obsolete/superseded) 상태가 없어, 라우팅 개정 시나리오에서 구 라우팅의 상태를 표현하려면 enum 수정이 필요하다. Source finding context: integrity_rules[1]이 released만 생산 오더 생성을 허용하므로, 폐기된 라우팅을 released로 남겨두면 잘못된 생산 오더가 가능하고, 삭제하면 이력이 소실된다. 수명주기 상태 추가는 enum 수정이라는 기존 구조 변경을 요구한다. Source finding context: status에 obsolete(또는 superseded)를 추가하고, integrity_rules[1]의 생산 오더 조건은 released 한정으로 유지한다. Source finding context: .onto/review/20260611-43359fc2/round1/evolution.findings.yaml#evolution-candidate-010
- issue-029 (low): Routing.status enum(draft/released)에 후속 상태가 없고 라우팅-rev 연결 규칙이 부재하여, 소비 시스템이 그 공백 지점에서 자체 규칙을 발명하게 된다(현재 명시 claim만으로는 직접 모순 미성립). Source finding context: manufacturing-bom-ontology.yaml Routing.status와 ECO 적용 간 상태 전이 부재 Source finding context: manufacturing-bom-ontology.yaml:56, 90 vs 64-70 Source finding context: Routing.status enum(draft/released)에는 released 이후의 후속 상태(개정중/폐기)가 없어, ECO applied로 품목이 변경된 뒤에도 released 라우팅이 생산 오더 생성 게이트(integrity_rules[1])를 계속 통과한다. 이는 '종결 판정이 가역 상태(품목의 설계 유효성)에 의존하면서 후속 사건(ECO 적용)을 허용'하는 패턴이나, 현재 명시된 claim만으로는 직접 모순을 형성하지 못한다 — 라우팅과 rev/ECO를 연결하는 규칙 자체가 부재하기 때문이다. [satisfiability_note: 구조 부재로 인한 잠재 충돌 / modality_note: possible — released의 영속성이 명시 claim이 아니라 enum 값 부재에서 유도됨 / boundary_handoff_note: '필수 전이 규칙이 적용될 상태 값 자체의 누락'이므로 Logic ↔ Structure 라우팅 기준에 따라 structure lens가 primary. logic 판정으로는 present claims만으로 모순 형성 불가] Source finding context: 명시 규칙 간 직접적인 동시 만족 불가능은 성립하지 않으므로 logic verdict로는 insufficient evidence(구조 부재 기인)이며, structure lens의 1차 검토 대상으로 인계한다. 다만 integrity_rules[1]과 [2]를 함께 만족시키려는 소비 시스템은 라우팅-rev 연결 규칙의 부재 지점에서 자체 규칙을 발명하게 되므로 표면 기록을 남긴다. Source finding context: structure lens 검토와 함께, ECO applied 시 affected_parts의 released Routing 처리 규칙(자동 draft 회귀, 또는 rev별 Routing 버전 분리)을 추가하고 Routing.status에 superseded 류의 후속 상태를 도입하는 방안을 평가한다. Source finding context: .onto/review/20260611-43359fc2/round1/logic.findings.yaml#logic-candidate-005
- issue-038 (low): qty_per의 의미가 child Part의 uom에 암묵적으로 의존하는데 환산 규칙이 모델 밖에 있어, 이름이 약속하는 정량적 의미가 불완전하다. Source finding context: manufacturing-bom-ontology.yaml > entities.BomLine.qty_per 및 entities.Part.uom, notes[1] Source finding context: notes[1]: "uom 변환(kg↔ea 등)은 품목별 환산계수 마스터가 없고, 필요 시 현장에서 환산한다." 및 BomLine.qty_per(단위 무표기) Source finding context: qty_per의 의미가 child Part의 uom에 암묵적으로 의존하는데 환산 규칙이 모델 밖 현장 관행에 있어, qty_per라는 이름이 약속하는 정량적 의미가 불완전하다. Source finding context: 이름이 정량 산식 항(소요량 = qty_per × 생산량)을 의미하는 속성의 단위 의미가 모델에서 닫히지 않으면, 시스템 간 수치 해석이 갈릴 수 있다. 다만 qty_per가 child uom 기준이라는 관행적 해석이 일반적이어서 표면 지적에 그친다. Source finding context: qty_per의 단위 기준이 child_ref.uom임을 note로 명시하고, uom 환산이 필요한 BOM 구간의 처리 원칙(환산계수 마스터 도입 또는 동일 uom 강제)을 한 줄로 선언한다. Source finding context: .onto/review/20260611-43359fc2/round1/semantics.findings.yaml#semantics-candidate-009
- issue-039 (low): Operation이 공정 유형 마스터인지 특정 라우팅 전속 단계인지 모호하여 같은 이름이 두 해석을 오간다. Source finding context: manufacturing-bom-ontology.yaml > entities.Operation 및 Routing.operations Source finding context: Operation.definition("라우팅의 한 공정 단계") vs Operation.attributes.op_code/work_center/std_time_min, Routing.operations(ordered_list of ref) Source finding context: Operation이 재사용 가능한 공정 유형(마스터)인지 특정 라우팅에 귀속된 공정 단계(occurrence)인지 모호하여, 같은 이름이 두 해석을 오간다. Source finding context: 유형/발생(type/occurrence) 미구분은 동음이의 문제의 한 형태이나, 현재 문서만으로는 어느 해석이 의도인지 판정할 수 없어 표면 지적으로 남긴다. PLM/MES 통합 시 공정 마스터 설계에 직접 영향을 주므로 명시할 가치가 있다. Source finding context: Operation이 라우팅 전속인지 공유 마스터인지 정의에 한 줄로 명시하고, 공유 마스터라면 라우팅별 가변 속성(std_time_min 등)을 라우팅-공정 연결 개념으로 분리한다. Source finding context: .onto/review/20260611-43359fc2/round1/semantics.findings.yaml#semantics-candidate-010
- issue-042 (low): Part.current_eco(Part→ECO 참조)에 대응하는 relation 선언이 relations 블록에 없다. Source finding context: manufacturing-bom-ontology.yaml Part.current_eco 속성과 relations 블록의 불일치 Source finding context: manufacturing-bom-ontology.yaml:16, 79-86 Source finding context: relations 블록을 관계의 전수 목록으로 소비하는 시스템은 Part에서 ECO로 가는 경로를 인식하지 못한다. 영향은 제한적이지만 기준 문서의 속성-관계 일관성을 깬다. Source finding context: relations에 { from: Part, to: ECO, kind: current_change } 항목을 추가하거나, relations 블록이 전수 목록이 아니라는 범위 주석을 문서에 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-003
- issue-043 (low): Assembly.bom_lines와 BomLine.parent_ref가 같은 연결을 중복 보관하며 relations에는 한 방향만 선언되어 권위가 불명확하다. Source finding context: manufacturing-bom-ontology.yaml Assembly↔BomLine 양방향 중복 보관 Source finding context: manufacturing-bom-ontology.yaml:22, 27, 80 Source finding context: Assembly.bom_lines(부모→줄)와 BomLine.parent_ref(줄→부모)가 같은 연결을 양쪽에 중복 보관하며, relations에는 한 방향(has_many)만 선언되어 어느 쪽이 권위인지 불명확하다. Source finding context: 개념 기준 문서에서 이중 보관은 통합 시스템 간 동기화 어긋남(목록에는 있는데 parent_ref가 다른 Assembly를 가리키는 줄)의 여지를 남긴다. 한쪽을 파생 투영으로 선언하면 제거되는 위험이다. Source finding context: BomLine.parent_ref를 원본으로 두고 Assembly.bom_lines를 파생(derived) 투영으로 표시하거나 그 반대로 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-004
- issue-044 (info): InspectionPlan은 어떤 관계나 속성에서도 직접 참조되지 않으며 is_a Operation 상속을 통해서만 그래프에 도달 가능하여, '이 품목에 검사 계획이 존재하는가'를 그래프로 답할 수 없다. Source finding context: manufacturing-bom-ontology.yaml entities.InspectionPlan의 그래프 내 도달 경로 Source finding context: manufacturing-bom-ontology.yaml:72-77, 55, 83 Source finding context: InspectionPlan은 어떤 관계나 속성에서도 직접 참조되지 않으며, is_a Operation 상속을 통해서만 그래프에 도달 가능하다. Source finding context: is_a 다형성으로 도달은 가능하므로 엄밀한 고아는 아니지만, 검사 파라미터가 필요한 품목과 InspectionPlan을 잇는 경로가 없어 '이 품목에 검사 계획이 존재하는가'를 그래프로 답할 수 없다. 표면 수준 관찰로 기록한다. Source finding context: 검사 의무가 개념 모델의 요구라면 Part 또는 Routing에서 InspectionPlan으로 가는 명시 관계를 추가하고, 아니라면 다형 포함이 의도임을 relations note로 명시한다. Source finding context: .onto/review/20260611-43359fc2/round1/structure.findings.yaml#structure-candidate-005

## Shared Phenomenon Summary
- none
