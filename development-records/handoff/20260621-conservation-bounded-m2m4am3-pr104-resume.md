# Handoff — PR #104 (conservation bounded M2/M4a/M3a) — codex round-2 #3 LANDED, awaiting codex round-3

> `/clear` resume point. Continues the reconstruct conservation Track-1 **bounded subset**. M1 + the blocking_question_refs wiring (#100) + the lineage-chain binding (#101) already merged to main. This PR (#104) lands M2 + M4a + M3a; M3c is a deferred follow-up. **codex round-2 #1/#2/#3 are all addressed and pushed; `@codex review` was re-triggered (comment `#issuecomment-4761412322`). The next concrete action is converging the next codex round** (poll, address, then user-confirm → squash merge).

## 0. Location / state
- cwd `/Users/kangmin/cowork/onto-mcp-claude`. Branch **`feat/conservation-bounded-m2-m4a-m3`** = PR **#104** (base `main`).
- HEAD = **`f7fa65c`**, pushed. Working tree clean (tracked).
- Commits on this branch (oldest→newest): `b3c272a` M2 · `463b7e6` M4a · `6980396` M3a · `e05fd91` codex round-1 (4 fixes) · `8e5b01b` codex round-2 #1+#2 · `11fde8b` codex round-2 #3 · `f7fa65c` codex round-3 (#1 omitted-evidence batching + #2 unknown target_type).
- **codex round-3** (reviewed `11fde8b`) raised 2: **P1** the M2/#1 finalizePayload drop-to-fit let a multi-question payload single-dispatch with omitted evidence (bypassing #2 split-before-shrink) → fixed with `shouldDispatchSingleCompetencyAssessment` (fits AND no omission) + shared `assessmentOmittedObservationCount`; **P2** a stale `domain_context`/unknown `target_type` from resume/manual YAML hit `targetAuthorities[...]===undefined` and the `&&` guard skipped validation → fixed to emit `invalid_enum`. Both in `f7fa65c`; full vitest 1722.
- **codex round-4 (reviewed `f7fa65c`) = CLEAN** (comment 09:04:48Z "Codex Review: Didn't find any major issues. Keep them coming!"). PR #104 **MERGEABLE / mergeState CLEAN**, 2 CI checks SUCCESS. **All rounds converged (R1 4 → R2 3 → R3 2 → R4 0).** ⏳ **NEXT = user-confirm → squash merge + branch cleanup (§4).**
- **Boundary audit (user-requested, 2026-06-21)**: real PR vs origin/main = 7 commits, **8 files +622/−154** (the big `main...HEAD` was stale-local-main noise; TUI/#99 + maturation/#100/#101/#102 already on origin/main). Concept surface **net-neutral-to-reducing**: only persisted-shape change is `target_type` enum value *removed*; 0 new artifacts/enums/persisted fields; 1 contract bump (v4→v5); 1 authority binding (declared==wired); 4 internal test-seam exports (one dedups). Design: 6/9 findings cluster in M2 prompt-budget arithmetic — fragile but **converging** (invariant now explicit: fit→split→drop-only-if-unsplittable). User decision: **continue converging** (not consolidate/defer M2).
- codex posts as `chatgpt-codex-connector`: a **review** (body always 621-char boilerplate; real findings are inline review-thread comments — fetch via GraphQL `reviewThreads`, NOT the flaky `/pulls/N/comments` REST which returned `[]` mid-run) when it has findings, OR a plain **issue comment** "Didn't find any major issues" when clean. Latency ~7-12 min after a trigger.
- ⚠️ Network/gh has been flaky (TLS timeouts) — retry gh in a loop. ⚠️ This worktree needed `npm install` once (tui-watch deps `ink`/`react` missing → ts-core red on unrelated `src/tui/*`); already restored.

## 1. What this PR is (design)
`development-records/design/20260619-reconstruct-conservation-structural-remediation-design.md` v4 §5, **bounded subset**, per-mechanism commits:
- **M2** (`b3c272a`): source-evidence reserve DERIVED per batch under the real prompt budget (was static 24K). Pinned order: measure non-evidence (empty evidence) → `deriveCompetencyAssessmentEvidenceReserveChars(nonEvidenceChars)` = cap−measured−margin clamp≥0 → bind → terminal assert stays. Kept both pre-caps (excerpt 4K + candidate 50); stub=omitted; projection contract `competency_question_assessment_compact_projection` v4→**v5**.
- **M4a** (`463b7e6`): one `isRevisionBlocker(p)` (reject|defer) at stop gate + disclosure; `isRevisionDisclosed(p)` (non-reuse); the disclosure (`appendFinalOutputUnresolvedRevisionSection`) lists ALL non-reuse, split blocking vs non-blocking.
- **M3a** (`6980396`): one shared `isFullExcerptCaptureEligible(kind, ref)` in materialize-preparation.ts used by capture + run.ts projection (closes the half-closed hop, codex M-5); explicit source-language allowlist (`CODE_WHOLE_CAPTURE_EXTENSIONS` + build-language `CODE_WHOLE_CAPTURE_BASENAMES`); config/data code (.json/.yaml/.toml/.xml/.env/.cfg/.conf/.lock) → bounded sample (quality+cost, NOT secret closure — Track 2).
- codex round-1 (`e05fd91`): M2 zero-reserve (no force-keep first item), pretty serializer (size evidence like the prompt), omitted-ids tracked directly; M3a ref/basename-aware predicate threaded through projection path.
- codex round-2 #1+#2 (`8e5b01b`): #1 final payload-level trim (`finalizePayload` drops trailing evidence until the WHOLE payload fits — per-item size missed array nesting overhead); #2 batcher splits when adding a question would force evidence omission (`omitted_observation_count>0`) instead of squeezing evidence out.

## 2. ✅ DONE — codex round-2 #3 (landed in `11fde8b`)
codex (PR #104) flagged 3 P2; #1+#2 fixed in `8e5b01b`; **#3 fixed in `11fde8b` (option B)**. Implementation matched the plan below exactly: `domain_context` dropped from enum/prompt/parse allow-list; `validateRevisionProposal` gained `ontologySeed` and validates every target by authority (failure→failure_ids, claim→failures' claim_id, question→failures' question_id, seed→`knownSeedRefs`); `knownSeedRefs` exported as the single canonical set used by BOTH the author projection (`valid_seed_refs`) and the validator (no drift); `writeRevisionProposalValidationArtifact` reads ontology-seed; registry `revision-proposal-validator.input_authority_refs += ontology-seed.yaml`. Verification: full vitest **1717** (1710 + 7 new tests) · ts-core/guards OK · 0 regression. Pushed; `@codex review` re-triggered with #1/#2/#3 summary. **NEXT = poll for codex round-3 and converge (see §4).** Original plan kept below for reference.

**#3 (run.ts ~`validateRevisionProposal` / disclosure): "Validate targets before publishing every non-reuse proposal."** M4a's disclosure now publishes every non-`reuse` proposal as actionable, but `validateRevisionProposal` (post-seed-validation.ts) only resolves `target_id` for `target_type === "failure"`. So a fabricated/stale non-failure target (e.g. `extend claim X`) is published as carry-forward with no closure check.

**Investigation conclusion (grounded in code + reconstruct purpose):**
- A `ReconstructFailureClassificationEntry` carries `claim_id` and `question_id` (+ `recommended_action` incl. `reject_claim`/`revise_seed`). The revision-proposal author's `userPayload` = the FULL `failure_classification`. So claim/question targets are **legitimate** (author derives target_id from a failure's own claim_id/question_id) — NOT hallucinated. ⇒ "narrow to failure-only" was WRONG (rejects legit claim/question proposals).
- `target_type` enum = `claim | question | failure | seed | domain_context` (artifact-types.ts:2894).
- **`domain_context` is VESTIGIAL**: no backing artifact anywhere in reconstruct (the `.onto/processes/review/` `domain_context_*` is a DIFFERENT pipeline concept). No test/fixture/contract uses it as a target. Origin: `f58354b`.
- `seed` HAS an authority: the ontology-seed's seed_refs via the existing helper **`knownSeedRefs(ontologySeed)`** in post-seed-validation.ts (= `collectOntologySeedRefs(seed)` + seed claim_ids).

**USER DECISION (AskUserQuestion): option B — "권위 공급 (seed/domain 노출)".** Reconciled with the data: thread the **seed** authority (ontology-seed) into author+validator; **drop `domain_context`** (no authority to thread → can't validate → remove the dead enum value, concept economy). I surfaced the domain_context premise-break and proceeded with drop as the coherent completion of "validate via authority."

### #3 implementation plan (option B)
1. **artifact-types.ts**: `ReconstructRevisionProposalEntry.target_type` → remove `"domain_context"` (→ `"claim" | "question" | "failure" | "seed"`). (line ~2894)
2. **post-seed-validation.ts `validateRevisionProposal`** (line ~1963): add `ontologySeed` arg; validate `target_id` per type — `failure`→failure_ids (existing); `claim`→set of failures' non-null `claim_id`; `question`→set of failures' non-null `question_id`; `seed`→`knownSeedRefs(ontologySeed)`. Unresolved → existing `unknown_id` violation. (REVISION_ACTIONS at post-seed-validation.ts:124.)
3. **run.ts `writeRevisionProposal` author** (~8489): `ReconstructRevisionProposalAuthorInput` (run.ts:531) gains the ontology-seed; `userPayload` adds a compact seed projection (seed_refs ids+names) so the author can cite real seed ids; update the JSON-shape prompt line (drop `domain_context`, note claim/question come from failures, seed from seed_refs); update the parse allow-list (~8524) to drop `domain_context`.
4. **run.ts `writeRevisionProposalValidationArtifact`** (post-seed-validation.ts:2315 OR run.ts writer — confirm which is wired): read the ontology-seed, pass to `validateRevisionProposal`.
5. **run.ts wiring**: pass the ontology-seed artifact/path to both writeRevisionProposal and the revision-proposal validation writer.
6. **registry** `.onto/processes/reconstruct/reconstruct-contract-registry.yaml`: add `ontology-seed.yaml` to the revision-proposal validator's `input_authority_refs` (find the `revision-proposal-validator` entry).
7. **Tests**: existing revision-proposal tests use `target_type: "seed", target_id: "seed-1"` (run.test.ts disclosure block ~6395) — update to a real seed_ref OR keep (disclosure tests don't validate). Add `validateRevisionProposal` tests: claim/question resolve via failure refs; seed resolves via knownSeedRefs; fabricated non-failure id → `unknown_id`; domain_context no longer a valid enum. Update the author prompt test if any asserts the 5-type JSON shape.

⚠️ Watch: a guessed `seed` target would now fail validation → halt; that's why the author MUST get the seed (step 3) so it cites real seed_refs. (Validating seed without feeding the author would halt runs.)

## 3. Verification loop (per piece)
`npm run check:ts-core` → `npx vitest run src/core-runtime/reconstruct/run.test.ts src/core-runtime/reconstruct/post-seed-validation.test.ts src/core-runtime/reconstruct/materialize-preparation.test.ts` → guards (`check:import-boundary` / `check:spec-defaults` / `check:invariant-drift`) → full `npm run test:vitest`. Baseline full vitest before #3 = **1710 passed**. Commit explicit paths only (untracked session artifacts present). Trailers: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_016s7b1AKZSiY1gd3LJSBzY4`.

## 4. After #3
1. Commit #3 (its own commit) + push to #104.
2. Re-trigger `@codex review` on #104 (reply summarizing #1/#2/#3). Converge codex rounds (network-flaky polling). When clean → user-confirm → squash merge + delete branch (note: `--delete-branch` fails because `main` is checked out in the `onto-mcp-spreadsheet` worktree → after merge, `git checkout --detach origin/main` here, then delete local+remote; untracked development-records may block checkout → they're already committed via #102, remove identical untracked copies then detach).
3. Then the remaining deferred bounded piece: **M3c** (seed-stage observation snapshot — new canonical artifact `seed-stage-prompt-source-observations.yaml` + registry authority + run-manifest + reuse-match hash + resume fallback measuring the snapshot, fixing `singleDocumentProjectionTruncation` measuring the POST-maturation set). Own focused PR.
4. Deferred hard legs (separate tracks, NOT here): **M4b** (revision_blocker_limitation_refs continuation gating), **G** (obligation-coverage gate + loader extension), **M1.5** (lineage binding), **Track 2** (deterministic sensitive-data preprocessing). Matrix-validator artifact-self binding residual: `development-records/handoff/20260621-matrix-validator-cross-artifact-binding-followup.md`.

## 5. Memory
`contract-runtime-gap-ledger` (updated through #104 round-2 #1/#2). This handoff supersedes it as the #3 START-HERE.
