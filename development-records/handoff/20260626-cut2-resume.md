# RESUME — Cut-2 (segmented value-tile 투영 순수-파생성 입증)

> **START-HERE.** `/clear` 후 fresh 세션이 **이 문서 하나로 시작**. 날짜: 2026-06-26. main baseline `c2b9c41`.
> 설계는 **종결**(4회 교차검증·material 0 수렴). 이 문서는 *구현-프로세스의 첫 cut* 실행 가이드일 뿐 — 설계 재론 금지.

## ✅ Cut-2 build+run + 교차검증 게이트 통과 (2026-06-26) — 다음 = Cut-2b(owner go)
**(a)-Q1 판정(분할) = 별도-스캔 0**(원 질문 답=NO). 게이트 합의 = **gate_pass_with_minor_revisions**(순수-파생성 코어 sound = 양 리뷰어 동의; 코드 버그·harness 재실행 0). 결과·교차검증 전체 박제 = **설계 SSOT §7.1 + §10.4**(`20260625-rescoped-comprehension-engine-design.md`).
- **verdict 2분할**(교차검증 반영): **(i) value-string/type/serialization = PURE-DERIVABLE from `parsed.rows`**(Cut-2가 실측·입증; done-when proven_by_construction + observed_on_fixture) / **(ii) display-only numFmt = SINGLE-PASS-FOLD-REQUIRED/NO-RESCAN**(미구현·미검증; `parsed.rows` 순수파생 아님이나 재스캔도 0 — SAX `cellStyle` 패스 내 가용→fold면 됨, ultracode 독립확인). done-when = **단일 fixture 관찰**(일반 증명 아님; status 표로 강등).
- **교차검증 병행 결과**: **ultracode** `wf_9fbdd2dd-7bc`(26 agent·20→**0 confirmed material**·`gate_pass_clean`·pure_derivability_sound) + **onto** `20260626-5f64b49c`(6 lens·deliberation·6 material→**3 doc root**: R1 verdict분할·R2 done-when fixture-scope·R3 concept매핑). **상보적**(병행의 가치): ultracode가 코드/증거 sound + fold충분 독립확인, onto가 라벨/양상 정밀도. **전부 §7.1 doc 정련으로 반영**(코드는 cosmetic 1=`retained_distinct_entries`→`summed_segment_distinct_count` 정명).
- **실험 코드**(throwaway·production 배선 0): observer **`CUT-2 EXPERIMENTAL`** 블록(`projectSegmentedValueTiles` + `experimentalValueTiles` opt + `cut2_value_tiles?` 선택필드). opt 미전달 production = byte-동일. 회귀 0(tsc·observer 80/80·import-boundary·invariant-drift green; 하니스 재실행 witness·결정성 intact).
- **하니스 fixture**: scratchpad `cut2-harness.ts`(휘발·미커밋).
## ✅ Cut-2b build+run 완료 (2026-06-26) — VIABLE on fixture; 게이트 진행 중
**의미 triage viability = VIABLE on this fixture**. 결과 박제 = **§7.2**. triage는 LLM 단계(§3.4·Layer 2)라 build+run에 비용 발생(triage LLM=Claude 서브에이전트, model-agnostic; production 모델=Cut-4 이연). 하니스 2단: ① 결정론 `cut2b-evidence.ts`(실 observer+value-tile→압축 evidence, 10컬럼: 구조4·의미2·이름신호2·트랩1·이질성1) ② LLM Workflow `wf_85424616-05a`(triage 3 blind 런 + 적대 safety). **3/3 런 동일 배분**, ground-truth 채점: pruning **4/10**(구조집합 정확·과절감0)·숨은의미 충실 **5/5**(txn_date는 value-tile boundary 신호로 포착)·트랩 cls **보수적 depth≥1**(정직계약)·depth-0 4개 전부 escalation 마킹·safety **0 silent blind-spot**. 잔여=단일 fixture/명료 archetype·과-escalation(실데이터)·비용압박/스케일 미검·model-agnostic.
- **▶ ✅ Cut-2b 교차검증 게이트 통과(2026-06-26, §10.5 박제)** = **gate_pass_with_minor_revisions**. **ultracode** `wf_61a29381-d96`(31 agent·25→**0 material**·gate_pass_clean; ground-truth 무누수·하니스 비-rigged 독립확인) + **onto** `20260626-53f719dc`(6 lens·**8 material→2 root, 전부 §7.2 claim 과신**). **상보적**(Cut-2 동일 패턴): ultracode 실험 건전성, onto가 §7.2 과신 포착. **반영(§7.2 narrow)**: 판정 2분할 — **(A) depth 배분+정직-마킹 = VIABLE on fixture** / **(B) 구조적 safety = 미실증·이연**(onto 핵심: "0 blind-spot"은 prompt 마킹+audit 합의일 뿐, §3.4가 안전의 load-bearing으로 둔 **sniff re-entry 복구 + 전체 라이프사이클 필드** 미실증) + model-agnostic 주장 철회(단일 Claude 서브에이전트·transfer 미검). **이연 follow-up(trusted pruning 전 선결)**: §3.4 전체 라이프사이클 mandatory-or-explicit 기록·sniff-trigger/re-entry 복구 fixture 1개·교차모델 triage. 교훈=동일패밀리 self-audit 상관→교차패밀리(onto=gpt-5.5)가 과신 적발(ultracode 단독이면 clean으로 닫혔을 것).
## ✅ Cut-3 build+run 완료 (2026-06-26) — explorer-V(렌더+vision) PoC; 교차검증 게이트만 남음(owner go)
**explorer-V 방향 = 입증 on 3 fixture**. 결과 박제 = **§7.3**. build+run = vision LLM 포함(렌더러 결정론·LLM 0; vision 탐지 = 단일 Claude 서브에이전트, model transfer 미검). production 배선 0. 하니스 = scratchpad `cut3-render.ts`(휘발) — 3 fixture build → 실 observer ground truth + Chrome headless `--screenshot` PNG(within-machine byte-stable, 3/3) + vision 3 blind 서브에이전트(PNG만).
- **신규 능력 #2/#3 feasible**: 렌더러 = Chrome headless 149.0.7827.196(설치 불요·결정성 byte-stable·P5 *방향* 실증, cross-machine 잔여) / vision = PNG 단독 라벨·레이아웃 판독 가능.
- **vision vs explorer-D**: clean=둘 다 정확(쉬운 케이스 안 깨짐)·alltext(R3 low)=vision confident-correct(정상 정전 케이스)·**messy(2-level)=explorer-D high·WRONG**(2-level/title 누락, `col_3/col_4`)인데 **vision이 구조 복구**. 헤드라인 = explorer-V는 explorer-D가 불확실/오판인 곳서 가치 추가, 정확한 곳선 일치.
- **★ R3 게이트-갭(중요·설계 정련 후보)**: §3.2/R3은 vision을 `header_confidence:low`에만 게이팅. messy서 explorer-D는 `merged_ranges`를 *자기 출력에 담고도* `high` 유지 → **low-confidence 게이트가 confidently-wrong 케이스에 발화 안 함**. 정련 후보(병행): (a) vision 게이트를 구조-복잡 신호(헤더존 병합·title 행)에도 트리거 (b) explorer-D confidence 캘리브 강화(2-level/병합/title서 high 금지). → 교차검증 게이트가 가중.
- **잔여**: synthetic 3 archetype·vision=단일 Claude(model-agnostic 미검)·render cross-machine 미검(P5)·image-token 예산 미측정·단일 시트·messy row-번호 오프셋(blank 행 렌더; 구조 판독은 정확).
## ✅ Cut-3 교차검증 게이트 통과 (2026-06-26, §10.6 박제) — gate_pass_with_minor_revisions; 다음 = Cut-4a(owner go)
**합의 = gate_pass_with_minor_revisions**(양 측 동일). Cut-2/2b와 차이 = **ultracode 측도 material 4**(not clean) → 두 패밀리가 **독립 수렴**으로 동일 honesty 갭 포착(최강 신호). **판정 2분할 반영**(§7.3): (A) explorer-V 방향 = sound(양 패밀리 PNG 직접 재검증·headline 생존) / (B) rigor·recording 비계 = 부분→narrow+이연.
- **ultracode** `wf_4ff930b6-692`(39 agent·33 candidate→13 confirmed→**4 material medium**): `direction_sound=true`·`render_claim_honest=false`·`r3_gate_gap_real=true`(R3 갭 **코드레벨 구조 확정**: detectHeaderRow가 merged_ranges를 confidence 입력으로 안 씀, 라이브 일반화 재현). adversarial verify가 review단계 high들을 medium/low 강등.
- **onto** `20260626-2a7c6669`(6 lens·gpt-5.5·finding-relation-graph서 halt=lens+ledger 완비, lens-level 직독): **12 findings(10 med·2 low)·high 0**, 지배 테마=vision 비-artifact(**5/6 lens 수렴**).
- **교차패밀리 수렴 4 테마**(각 §7.3 반영): ① vision 비-artifact·blindness 미강제(M1·H2 / onto 5/6) ② 렌더=HTML proxy≠xlsx fidelity(F3·M3 / coverage-003) ③ vision n=1 안정성 미검(F1·H4 / coverage-002) ④ fixture 비대칭·vision FP율 미측정(F2 / coverage-001) + R3 갭 sound·정련=candidate(benign-control 매트릭스 선결) + 고유(ultracode filename leak·stale 주석).
## ✅ Cut-3b + 게이트 통과 (2026-06-26, §7.4+§10.7 박제) — (B) (1)(3)(4) *부분 닫음(특성화)*; #2 이연
게이트 (B) 이연 4건 중 3건을 throwaway Cut-3b로 부분 닫음. 하니스=결정론 `cut3b-harness.ts`(7-fixture: 원본3+benign-control2+vision-stress2·content-hash PNG·R3 트리거 혼동행렬) + Workflow `wf_4b90ed01-3ad`(21 blind 구조화 vision·PNG만·결정론 채점). **교차검증 게이트(§10.7)=gate_pass_with_minor_revisions**(양 측; **Cut-3 동일 = 두 패밀리 독립 수렴**): ultracode `wf_90a23655-9b2`(38agent·20 confirmed→**1 material medium**·`recording_closed=false`·`vision_fp_test_fair=true`·`r3_matrix_sound=true`·**적대 재도출로 headlines 무오염 확인 + blindness 실측 입증** messy `[3,4]`≠authored `[4,5]`) + onto `20260626-351e62bb`(6 lens·halt후 직독·15 findings 14med·high0).
- **(1) 기록규율 = substantively 개선·*완전 닫음 아님*(recording_closed=false)**: 해시 파일명·3 reps·structured·citable wf id·**7/7 unanimous**·실측 blindness. ⚠️ 단 literal deliverable 미완(blindness=honor-system·fs 미샌드박스·answer key 同디렉토리·raw transcript 미영속·scorer off-record) → "✅닫음"→"부분". headlines 무오염(양 패밀리 재도출)이라 non-material.
- **(4) vision *구조* FP = 0/21**(=FP-opportunity 0/6·2 archetype×3 within-model): stress 2종+benign 정답. ⚠️ column-label/band-pairing fidelity 미채점·geometry-only proxy라 style-induced FP 미실증·stress=mild.
- **(3) R3 트리거**: naive 룰 precision **0.50**·recall **0.67**. ⚠️ title 분지=authored-oracle·INERT(매트릭스 사실상 merge-only). 핵심=**두 신호 상보적→캘리브 게이트(`low` OR `high-with-merge`)=oracle 없음**; over-trigger는 이 fixture서 구조FP0이라 비용-bound.
- **▶ NEXT = Cut-4a(resume 계약·§4.4·품질무관·싼 fixture) → Cut-4b — owner go.** ⚠️ production 배선은 Cut-4 통과+승인 후. **후속 선결(누적)**: display-only fold(Cut-2)·Cut-2b 안전경로B·**Cut-3 #2(실-xlsx fidelity)+cross-model vision+image-token+(1)완전닫음**(raw transcript 영속+fs-sandbox+materialized scorer)+column-label fidelity·R3 캘리브 게이트(benign-control=SSOT 입력).

## 현 상태 (한 줄)
## ✅ Cut-4a build+run 완료 (2026-06-26, §7.5 박제) — resume 계약; 게이트만 남음(owner go)
**staged non-circular `llm_touch_fingerprint` 계약 = 실현 가능 + §4.4 7 테스트 by-construction 통과**(순수 결정론·LLM 0·품질 무관). production 배선 0(reference impl). 하니스=scratchpad `cut4a-harness.ts`(휘발·순수 TS).
- **§4.4 = 10/10 통과**: crash-resume·epoch-rotation(ⓑ 14필드 전부)·model-identity-rotation(DET-1 수동 bump 0)·triage-policy-rotation↔allocation-no-rotate(policy↔allocation 타입 경계)·non-circular-key(키∩ⓒ=∅)·layer1-cross-epoch-reuse(cv변경→Layer1 재사용·Layer2 회전)·llm-touch-validator(type-lock+coverage 둘 다 fail-closed).
- **★ 실코드 audit**(`authoredArtifactReuseMatch`): 10 ⓐ+7 ⓑ+**0 ⓒ** → **비순환 이미 암묵 성립**(자기 LLM 출력 미fold)·SourceScoutPack=결정론 확인 → 계약이 실 확장점에 깨끗이 매핑·잠복 순환 0. B1/F1 모델됨·D1=에포크 레벨만.
- **잔여**: reference impl(run.ts 미배선·비순환은 타입 construction 증명→validator가 구조 가드)·5 future-stage ⓑ(prompt hash·equivalence·triage policy·vision·deep-mode)는 comprehension 엔진 미구축이라 오늘 필드 없음(정상; validator 누락-fail-closed는 입증)·D1 progress-journal 노드 레벨 미실증.
- **▶ ✅ Cut-4a 교차검증 게이트 통과(§10.8 박제) = gate_pass_with_minor_revisions**(양 패밀리). ultracode `wf_af969a00-eed`(33agent·17 confirmed→4 material medium) + onto `20260626-37c39787`(6 lens·5 findings 4med·high0; onto는 MCP 재연결 후 *순차* 실행 = ultracode-narrow된 §7.5 검증·독립성 일부↓). **realizability+non-circularity headline 무반증·정정 전부 doc precision.**
  - **★ 최고가치 = 기존 shipping DET-1 갭**(Cut-4a 산물 아님·코드 검증·양 패밀리 재포착): **CG-2** resume 키가 authoring-model identity 미fold(`semantic_author_realization`=리터럴 `"direct_call"`·model_id 없음→다른 모델 재개 시 silent 재사용)·**CG-1** ~20 authoring prompt 중 1개만 fold. = 설계 DET-1 silent-stale 실증 → ▶ production 전 llm-touch coverage validator 실배선 선결.
  - **★ 수렴 finding = llm-touch-validator coverage = dependency-discovery 미해결**(ultracode F4 + onto coverage-001/evolution-002): 하니스는 *closure 주어졌을 때* fail-closed만 입증·전체 dep 열거 메커니즘 미해결(load-bearing·Cut-4b 선결).
  - 기타: RC-1("10 ⓐ" 과대=upstream-LLM 투영)·F2(B1 escalation 필드 미테스트 #144)·onto structure(staged 하니스가 RC-1 전 분류 보유·§7.5↔§10.8 coherence). 전부 §7.5 narrow 반영.
- **▶ NEXT = Cut-4b(comprehension 품질: 1 frame + 사이드카 + 고잔차 워크북 + reduce 1~2레벨 + triage + 저신뢰 시트) — owner go.** production 배선은 Cut-4 통과+승인 후.

## 현 상태 (한 줄)
재절단 이해(comprehension) 엔진 **설계 + 4회 교차검증 종결**(distinct material 8→2→2→0) + **Cut-2·2b·3·3b·4a 게이트 전부 통과**(전부 pass_with_minor_revisions). **SSOT = `development-records/design/20260625-rescoped-comprehension-engine-design.md`**(§7.1~§7.5 cut 결과 + §10.4~§10.8 cut 게이트). **de-risk 시퀀스 Cut-1~4b 전부 종결**(Cut-2~4b 게이트 통과; Cut-4b는 §7.6/§10.9·gate_pass_with_minor_revisions). **Cut-4b 결론(narrow 후)**: 공유 프레임-중립 spine = *읽기는 실 101MB 데이터서 검증·faithful·honest·frame-neutral*; 단 *양-소비자 충분도는 시뮬 소비자만*(실 파이프라인 미먹임)·*reconstruct unblock은 후보*. P8은 strong upside·비-blocker. **다음 = (owner 결정) production 배선**(별도 승인+선결). 아래 §7.6 스코핑 이력 참조.
**Cut-4b(comprehension 품질) 스코핑 이력**(§7.6 박제). **★ owner 재구성: Cut-4b="한 엔진으로 둘 다"=*최적화*이지 blocker 아님**(안 되면 따로-실행 fallback; 실패=재설계 아님). 계획=단계 상승 4b-0→4b-1→4b-2·셋 다 완료. **✅ 4b-0(종이)=§5.7 under-spec→정련**(spine=인벤토리 동반). **✅ 4b-1(작은 실제 실행 wf `wf_46ffa720-3ec`)=`one_spine_sufficient`**(review 4/4·reconstruct 5/5·frame-neutrality 성립). **✅ 4b-2**: (a) 실 reconstruct(onto_reconstruct)=spreadsheet서 **BLOCKED/shallow**(parse_execution_not_evidenced·file-level서 막힘)→spine이 데이터→온톨로지 파싱 제공해 **unblock** (b) 실 **101MB 수익인식 워크북**에 spine(wf `wf_62d094fe-17f`·explorer-D 15.8s)=judge **`sufficient_with_minor_gaps`**(faithful·honest[무의미×3·계약부채 빈컬럼·coordinated date append 경계 3212/3213 포착]·**frame-neutrality 유지**·spine≫현 실-reconstruct). frame-specific "holes"=규범 판단 층(소비자 몫·설계 정합). **✅ Cut-4b 게이트(§10.9)=gate_pass_with_minor_revisions**(양 패밀리 *강한 수렴*=내 "VALIDATED/unblock" 과신 공동 포착): ultracode `wf_dc5aab62-1ce`(42agent·14 confirmed→4 material medium·`p8_validated_sound=false`·`real_data_faithful=true`·`frame_neutrality_reasoning_sound=true`·`reconstruct_unblock_sound=false`)+onto `20260626-417d6527`(14 findings·1 high·12med·독립 재포착). **narrow 반영(§7.6)**: P8=**read-level 검증 + consumer 충분도 *시뮬*만**(LLM projection+judge·실 코드패스 미먹임·4b-2a BLOCKED)·unblock=**후보(E2E 미입증)**·synthetic caveat=**read-level만 해소**. **stand**: 4b-2b faithfulness/honesty·frame-neutrality 추론. owner 재구성상 P8 비-blocker라 narrow 저-stakes. **▶ de-risk 시퀀스 Cut-1~4b 종결.** **다음 = (owner 결정) production 배선** = Cut-4 통과 + 선결[CG-2/CG-1·llm-touch dependency-discovery·display-only fold·Cut-2b 안전경로B·Cut-3 #2/cross-model/image-token·실-소비자 E2E sidecar] + 승인. ⚠️ production 배선은 Cut-4 통과+승인 후. **production 전 선결(게이트 발견)**: CG-2/CG-1 shipping DET-1 갭(model/prompt 미fold) + llm-touch-validator dependency-discovery + display-only fold + Cut-2b 안전경로B + Cut-3 #2/cross-model/image-token.

## ⚡ ON RESUME — DO THIS FIRST
1. **SSOT 읽기**(필수 절만): 설계 doc의 **§3.1**(Cut-2가 만드는 표면)·**§6**(신규 능력 게이트)·**§7**(cut 표·규율·done-when·baked-in 테스트)·**§8**(코드 앵커)·**§9.1 P6/P12**(Cut-2가 실측할 잔여=window 캘리브·메모리 델타). §1~§5는 *맥락*으로만.
2. 아래 "Cut-2 정의/done-when/방법"대로 **throwaway 하니스**를 만들고 실 워크북에 돌려 측정한다.
3. 규율: **각 cut = 교차검증 게이트**(ultracode + onto). Cut-2 하니스 build+run 자체는 **결정론(LLM 0)이라 비용 ~0** → 진행 가능; *끝의 ultracode+onto 검증 게이트는 비용 발생 → owner go 대기*. production 배선은 Cut-4 통과+승인 후(지금은 금지).

## Cut-2 정의 — 무엇을 입증하나
**(a)-Q1 순수-파생성**: §3.1 **segmented value-tile 투영**이 *공유 base 관측 패스에서 순수 파생 가능*한가(별도 스캔 불요), 아니면 별도-스캔 예외인가. **실 artifact로 측정**(§3.1 마지막 bullet).

- **왜 필요**: Cut-1이 드러낸 갭 — 현 observer는 컬럼당 majority type 1개 + **원시값 0**(aggregate-counts-only, design-C)이라 reduce가 모순을 ground할 타일이 없다. segmented value-tile 투영은 **이 엔진의 *유일한* 새 결정론 표면**(§3.1)이라 전면커밋 전 게이트 필수(§6-1).
- **투영이 만드는 것**: **전 컬럼**(구조적 배제 0)을 row-window로 잘라 *세그먼트별* `{타입 분포 · serialization 시그니처 · distinct-vocab}` + **경계 후보**를 **싼 1패스**로 결정론 추출. **원시값 덤프 아님** — 세그먼트 시그니처만(P12: aggregate-only·raw 미보존). **explorer-D 소유**(결정론·resume substrate) — 비전·LLM 아님.
- **이름 주의**(설계 §2 R8/§3.2 TIER 2b): 이건 `value-signature tile`(bounded 시그니처). exact 값 멤버십은 *별도* `exact-membership post-pass`(§5.6)지 이 타일 아님.

## Cut-2 done-when (성공 기준)
- [ ] **순수-파생성 판정**: 투영이 *기존 단일 observer 패스의 순수함수*로 산출되는가(재스캔 0). 그렇다면 입증; 아니면 **별도-스캔 예외를 명확히 특성화**(추가 비용·왜 필요한지)해 owner 판단 자료로.
- [ ] **결정성**: 같은 입력 → byte-안정 투영(재실행 동일).
- [ ] **bounded 메모리(P12)**: O(컬럼×세그먼트), O(셀) 아님. segments-per-column 캡 동작. (기존 1.4GB 트랙과 별개; **메모리 델타 실측** — §9.1 P12 잔여.)
- [ ] **`intra_tile_note` 경계 witness 산출**(직전포맷 마지막행 + 신포맷 첫행) — §3.1①·§13 load-bearing 메커니즘.
- [ ] **실 고잔차 워크북에서 측정**(golden 아님). window 크기 캘리브 1차 관찰(§9.1 P6 잔여; window=benchmark-backed SSOT 후보값).

## 방법 (throwaway 하니스 — production 배선 0)
- observer에 segmented value-tile 투영을 **실험 경로로** 추가(프로덕션 흐름 미배선). 실 워크북 1개로 run → 위 done-when 측정.
- Cut-1/1b 선례: throwaway workflow + `/tmp` fixture(휘발). 결론만 §10/§13에 박제. Cut-2도 동일 — fixture는 scratchpad, 결론은 doc.
- 통과 시에만 **Cut-2b**(의미 triage viability) → Cut-3(vision PoC) → Cut-4a(resume 계약) → Cut-4b(comprehension 품질). 순서 엄수(§7).

## 코드 앵커 (§8)
- **추가 지점**: `src/core-runtime/spreadsheet-structure-observer.ts` — 스트리밍 fflate+saxes 패스(렌더러 없음); 기존 `columnResidualKey`·`header_confidence`·`merged_ranges`·`distinct_value_vocab`·`cross_sheet_key_overlap`·`projectInventoryForPrompt`@2449. value-tile 투영은 여기 1패스에 얹는다.
- reconstruct resume/reuse(나중 Cut-4a): `src/core-runtime/reconstruct/run.ts`(`sourceObservationsReuseSha256`@1104·`content_sha256`+`adapter_version` fold).
- 가드: INVARIANTS.md(INV-BENCH-1·INV-MODEL-1·INV-SHARD-1·INV-MATERIAL-1). window/예산 신규 축은 **benchmark-backed SSOT(G4·R10)**로 source(INV-BENCH-1은 검증방식만 거버넌스).

## 비-목표 / 가드 (지금 단계)
- ❌ production 배선(Cut-4 통과+승인 전)·explorer-V/vision(Cut-3)·triage(Cut-2b)·LLM 경로(Cut-2는 순수 결정론).
- ❌ **마스킹/redaction 재도입**(레포 정책·source 내용 redaction=reconstruct 범위 밖, owner 결정). 재추가 금지.
- ❌ 설계 재론(4× 교차검증 종결). 새 결함 발견 시에만 SSOT 갱신 + 교차검증.

## 포인터
- **설계 SSOT**: `development-records/design/20260625-rescoped-comprehension-engine-design.md`(§7 cut 표·§3.1 투영·§10.3 수렴 종결).
- **설계+4회 교차검증 이력**(격리): `development-records/handoff/20260625-rescoped-comprehension-engine-resume.md`(PROGRESS-1~3).
- 메모리: [[unified-comprehension-engine-track]]·[[design-validation-ultracode-onto]]·[[contract-runtime-gap-ledger]].
- Cut-1/1b 실증 선례(de-risk): 상위 doc `20260625-unified-explore-frame-recursive-comprehension-design.md` §13.
