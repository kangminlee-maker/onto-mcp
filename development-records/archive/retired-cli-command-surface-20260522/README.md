# Retired CLI Command Surface

Status: archived

This directory contains the former `.onto/commands/` slash-command and CLI
contract surface. It is kept as historical reference only.

Active product interface:

- MCP tools in `src/mcp/`
- Review semantics in `.onto/processes/review/`
- Runtime implementation in `src/core-runtime/`
- Library facade in `src/core-api/`

The archived command files are not active authority and must not be used as
runtime input.
