# Onto Install

Runs the first-time setup wizard for runtime config, credentials, and output
language.

Read:

- `process.md`
- `.onto/processes/install.md`
- `.onto/processes/configuration.md`

Then execute the install process.

Run once after installing the plugin or npm package. Project-level domain and
review setup is handled by `/onto:onboard`.
