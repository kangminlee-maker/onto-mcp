{
  "workflow": "wf_bb8e5698-0ff (g-tractable-design-cross-validation)",
  "design_reviewed": "development-records/design/20260622-g-tractable-governance-parity-design.md",
  "material_count": 40,
  "confirmed_count": 34,
  "confirmed": [
    {
      "lens": "mechanism",
      "severity": "high",
      "title": "G(c) registry/guard misses 3 of 8 actually-emitted final-output sections (truncation siblings), and the bindings array (5) is not the section universe (8)",
      "location": "src/core-runtime/reconstruct/run.ts:13280-13446 (emit sequence); appendFinalOutputDocumentProjectionTruncationSection (10030), appendFinalOutputWorkbookInventoryProjectionTruncationSection (10065), appendFinalOutputUnresolvedRevisionSection (9981)",
      "claim": "The design models the final-output section universe as the 5 provenance-binding sections + unresolved-revision-proposals = 6. The runtime actually emits 8 sections at the 13280-13446 call site: the 5 provenance-binding sections PLUS three conditional appends NOT in either runtime list — Source Projection Truncation (13435), Workbook Inventory Projection Truncation (13439), and Unresolved Revision Proposals (13443). The design's §3.3 'covering the canonical final-output sections' and the guard clause (b) 'provenance-binding section_ids equal the registry section_ids (minus any declared non-binding sections)' will create a registry that silently omits two real sections, defeating the stated purpose (one canonical list). The 'minus any declared non-binding sections' escape hatch is exactly where these would be lost.",
      "recommendation": "Either (a) enumerate all 8 emitted sections in the registry with an explicit append_owner/activation/provenance_binding_required per row (the two truncation sections are activation-gated and have no provenance binding), and have the guard assert the registry covers every appendFinalOutput*Section call at 13280-13446 — not just the bindings array; or (b) narrow G(c) scope explicitly to the 5 provenance-bound sections + unresolved-revision and DOCUMENT that the two truncation siblings are out of the canonical-list model, with a rationale. As written the design half-covers them, which is the worst outcome (a guard that looks complete but is foolable by adding a 9th truncation-style section).",
      "verifier_reasoning": "CONFIRMED REAL. Code at src/core-runtime/reconstruct/run.ts runReconstruct emit site (13280-13446) appends 8 distinct markdown sections via upsertMarkdownSection: the 5 provenance-binding sections from finalOutputProvenanceSectionBindings (10394-10613) PLUS appendFinalOutputDocumentProjectionTruncationSection (\"## Source Projection Truncation\", call at 13435, def 10030), appendFinalOutputWorkbookInventoryProjectionTruncationSection (\"## Workbook Inventory Projection Truncation\", call 13439, def 10065), and appendFinalOutputUnresolvedRevisionSection (\"## Unresolved Revision Proposals\", call 13443, def 9981). The design models the universe as 5 bindings + unresolved-revision = 6 (§2 line 15, §3.3 lines 33-35) and grep over the design note for \"truncation\"/\"Source Projection\"/\"Workbook Inventory\" returns EMPTY — the two truncation sections are entirely unmodeled. No .onto registry or contract-registry.ts node enumerates them either (grep empty), so the design is not relying on a pre-existing list.\n\nThe defect is a genuine mechanism flaw, not a nitpick: the two truncation sections are conditional (if length===0 return) but so is appendFinalOutputUnresolvedRevisionSection (line 9990) which the design DOES include — conditionality cannot justify the asymmetry. Guard clause (b) in §3.3 (\"provenance-binding section_ids equal the registry section_ids minus any declared non-binding sections\") has an escape hatch (\"minus any declared non-binding sections\") that the unlisted truncation sections fall through silently, so a built registry omitting them passes the guard while violating the stated \"one canonical list\" goal (success criterion line 48). The guard reconciles only the bindings array (5) and the prompt-policy deterministic_runtime_append_sections (5 underscore ids, run.ts:4447-4453) — never the actual appendFinalOutput*Section call sequence — so a future 9th truncation-style section is undetectable. The recommendation (enumerate all 8 with per-row provenance_binding_required + assert coverage of every append call, OR explicitly narrow scope and document the two siblings as out-of-model) is correct.\n\nSeverity high (not blocker): this is the design's self-identified \"risky core\" and produces a complete-looking-but-foolable guard (the worst outcome), but it is a pre-build design-completeness gap trivially fixed by listing the two sections before implementation, not a shipped correctness break. Minor wording calibration: the claim says the design \"half-covers\" them; strictly the design gives them zero narrative coverage and the half-coverage risk is emergent from guard clause (b)'s escape hatch — does not change the verdict."
    },
    {
      "lens": "interactions",
      "severity": "high",
      "title": "Loader extension transitively rotates the governing-snapshot hash AND the authored-artifact reuse hash — contradicts the design's \"zero regression / data-only\" claim",
      "location": "src/core-runtime/reconstruct/governing-snapshot.ts:445 snapshotFamily (validator_records family at :575-581) -> run.ts:1262 authoredArtifactReuseMatch (governing_snapshot_sha256)",
      "claim": "Design §3.1 / §5 assert Part 1 (adding validation_obligations + conditional_validation_obligations to the parsed ReconstructValidatorRecord) 'only makes the data available' with 'zero runtime regression'. This is false. buildReconstructRunGoverningSnapshot builds a snapshot family with familyId='validator_records' whose value is the WHOLE parsed args.contractRegistry.validator_records (governing-snapshot.ts:576-581), and snapshotFamily computes sha256: sha256Text(stableJson(args.value)) (governing-snapshot.ts:445). Adding two fields to every parsed validator record changes that stableJson, so the validator_records family sha256 changes. That snapshot is then hashed whole into the reuse key: run.ts:1262 governing_snapshot_sha256: sha256Text(stableJson(args.governingSnapshot)), inside authoredArtifactReuseMatch (run.ts:1167-1278, the single call site run.ts:10917). Net effect: the loader extension rotates governing_snapshot_sha256 -> rotates the authored-artifact reuse hash -> every previously-authored artifact fails explicit-resume reuse (forced re-author, run.ts:1301-1305), and any persisted/golden governing-snapshot family sha fixtures mismatch under the snapshot verifier (governing-snapshot.ts:925 stableJson(actual)!==stableJson(expected)).",
      "recommendation": "Either (a) project the validator records BEFORE embedding in the snapshot family — pass a stripped {validator_id,validator_version,gate_ids,output_ref,input_authority_refs} projection (mirroring validator_versions at governing-snapshot.ts:546-553) so obligations are NOT in the family sha; or (b) accept and document the one-time reuse-hash rotation explicitly and bump/refresh the affected snapshot fixtures. The design must drop the 'zero runtime regression' and 'data-only' framing for Part 1 and own this transitive hash dependency. Verify with the governing-snapshot.test.ts + reuse-provenance tests after the change.",
      "verifier_reasoning": "CONFIRMED REAL, code-grounded. The full causal chain the claim asserts holds:\n\n1. Part 1 of the design (§3.1) adds `validation_obligations` + `conditional_validation_obligations` to the object returned by `parseValidatorRecord` (src/core-runtime/reconstruct/contract-registry.ts:703-709, currently 5 fields only).\n2. That parsed object is stored in `args.contractRegistry.validator_records`, which is fed WHOLE (no projection) into the `validator_records` snapshot family: `value: args.contractRegistry.validator_records` (governing-snapshot.ts:575-581).\n3. `snapshotFamily` hashes the whole value: `sha256: sha256Text(stableJson(args.value))` (governing-snapshot.ts:445). Adding two fields rotates this family sha256.\n4. The whole snapshot is hashed into the reuse key: `governing_snapshot_sha256: sha256Text(stableJson(args.governingSnapshot))` (run.ts:1262) inside `authoredArtifactReuseMatch`, and `reuseMatchHash = sha256Text(stableJson(reuseMatch))` (run.ts:1103-1105).\n5. On explicit resume (`resumeMode === \"reuse_existing_authored_artifacts\"`, run.ts:10631-10648), a `reuse_match_hash` mismatch THROWS (run.ts:1300-1305) → forced re-author.\n\nThis directly contradicts the design's §3.1 \"only makes the data available\" and §5 \"Zero runtime regression\" framing. The reuse hash IS runtime behavior and it WILL rotate. The recommended fix is correct and matches the existing pattern: `validator_versions` at governing-snapshot.ts:546-553 already projects validator records to 4 fields precisely to control what enters the snapshot; the `validator_records` family should do the same, or the design must drop the \"data-only / zero regression\" claim and own a one-time reuse rotation.\n\nSEVERITY DOWNGRADE blocker -> high: The claim's secondary \"golden governing-snapshot family sha fixtures mismatch\" sub-claim is overstated. The live verifier rebuilds `expected` via `buildReconstructRunGoverningSnapshot` (governing-snapshot.ts:803), so a FRESH run is internally consistent (actual and expected both built with the new parser). I searched and found NO persisted governing-snapshot fixture with a hardcoded `validator_records` family sha256 (governing-snapshot.test.ts:74 is an unrelated definition_sha256; run.test.ts:5792 is a dummy \"0\".repeat(64)). The real exposure is bounded: (a) explicit-resume sessions authored before the change must re-author once (a one-time invalidation, not corruption), and (b) a pre-Part1 run manifest replayed in live_terminal mode mismatches on governing_snapshot.snapshot_families (governing-snapshot.ts:848-851,925). Both are real regressions that falsify the design's central Part-1 claim and should be fixed before build, but they do not break fresh runs or normal CI and the fix is a one-line projection — so high, not blocker.\n\nTargets cited in the claim are accurate (governing-snapshot.ts:445/575-581/925, run.ts:1262/1167-1278/single call site 10917, 1300-1305). LENS interactions is apt: this is a cross-module data dependency (loader -> snapshot family -> reuse hash) that the design's per-part analysis missed."
    },
    {
      "lens": "interactions",
      "severity": "high",
      "title": "G(c) underscore->hyphen flip breaks an existing run.test.ts final-output assertion, directly contradicting the design's test plan",
      "location": "run.ts:4447-4453 (deterministic_runtime_append_sections) consumed at run.ts:9435 (compactFinalOutputPromptPayload -> userPrompt) vs run.test.ts:4204-4211",
      "claim": "Design §3.3/§6 say the prompt-policy ids flip to hyphen and 'run.test.ts final-output assertions [are] unchanged'. But run.test.ts:4204-4211 asserts deterministic_runtime_append_sections toEqual expect.arrayContaining(['claim_projection','artifact_truth','provenance_footer']) — the underscore form. Flipping to hyphen (claim-projection/artifact-truth) makes this assertion fail (and 'provenance_footer' has no hyphen counterpart at all — see G-3). The policy object is JSON.stringified into the final-output LLM userPrompt at run.ts:9435, so the flip also changes real prompt text. Design §6's 'assertions unchanged' is self-contradictory with §3.3's flip.",
      "recommendation": "Acknowledge that run.test.ts:4204-4211 must be UPDATED (not 'unchanged') if ids flip, and list it as an expected diff in the test plan. Reuse-hash safety holds here only because writeFinalOutput (run.ts:9427-9450) uses callLlmRecorded kind:'initial' with no reuseMatch/reuse-provenance — so the prompt-text change does NOT rotate a reuse hash. The design's Q2 should record THAT as the reason it is safe, not leave it open.",
      "verifier_reasoning": "VERIFIED REAL. Every element of the claim is confirmed in code.\n\n1. run.test.ts:4204-4211 asserts the UNDERSCORE form against the LIVE runtime output. finalOutputPayloads is populated at run.test.ts:3688 via `finalOutputPayloads.push(JSON.parse(userPrompt))`, where userPrompt is the actual `JSON.stringify(compactFinalOutputPromptPayload(input), null, 2)` emitted at run.ts:9435 (writeFinalOutput). The assertion does `deterministic_runtime_append_sections` `.toEqual(expect.arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"]))`. If §3.3 (design line 34) flips runtime ids to hyphen, the live array becomes [claim-projection, artifact-truth, ...] and arrayContaining([\"claim_projection\",...]) is NOT satisfied → assertion FAILS. So this test must be UPDATED, not left \"unchanged.\"\n\n2. The flip changes REAL prompt text. The policy object (run.ts:4441-4456, built inside compactFinalOutputPromptPayload at run.ts:4333) is JSON.stringify-ed into the final-output LLM userPrompt at run.ts:9435. Confirmed.\n\n3. G-3 sub-point confirmed: the hyphen bindings (finalOutputProvenanceSectionBindings, run.ts:10394; ids at 10473/10484/10580/10593/10601) are seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings. The underscore `provenance_footer` maps to runtime-artifact-truth-footer, NOT a clean `provenance-footer`; `provenance_bindings` maps to runtime-provenance-bindings. A mechanical underscore→hyphen swap produces ids that match NO binding — reconciliation is a non-trivial rename, reinforcing the defect.\n\n4. Design self-contradiction confirmed: §3.3 line 34 (flip prompt-policy ids underscore→hyphen, derive from registry) vs §6 line 55 (\"run.test.ts final-output assertions unchanged\") and §5 line 49 (\"all run.test.ts final-output assertions still pass\"). These cannot both hold. A builder following §6 would be misled into expecting no test diff.\n\n5. Reuse-hash safety reasoning is correct: writeFinalOutput (run.ts:9427-9450) calls callLlmRecorded with kind:\"initial\" and NO reuseMatch. callLlmRecorded (run.ts:6316; RecordedLlmCallArgs at 6295-6306) has no reuse-hash parameter or logic at all — it only records telemetry and invokes the LLM. The reuse-hash machinery (reuseMatchHash run.ts:1103, writeAuthoredArtifactWithReuseProvenance) is entirely separate and not on the final-output path. So the prompt-text change does NOT rotate any reuse hash. The recommendation to record THIS as the §Q2 safety reason (rather than leaving Q2 open) is sound.\n\nSeverity HIGH is appropriate (not blocker): the design is not fully blind — §3.3 line 36 explicitly flags the risk and Q2 line 59 raises this exact open question — but §5/§6's flat assertions are concrete false statements that directly contradict the planned change and would mislead the builder; the named test WILL fail. The fix is small and well-specified (update the test, list it as expected diff, record the reuse-hash-safety reason). Line/function targets in the claim are all accurate."
    },
    {
      "lens": "interactions",
      "severity": "high",
      "title": "Design's canonical section 'unresolved-revision-proposals' does not exist in the runtime — the M4a disclosure is a markdown heading, not an identified section",
      "location": "run.ts:9981-10019 appendFinalOutputUnresolvedRevisionSection (heading '## Unresolved Revision Proposals')",
      "claim": "Design §3.3/§2 require the canonical registry list to INCLUDE section_id 'unresolved-revision-proposals' and the guard §3.3(c) to assert its presence. But the M4a section has no section_id anywhere: appendFinalOutputUnresolvedRevisionSection emits only a markdown '## Unresolved Revision Proposals' heading (run.ts:9996), it is in neither the prompt-policy list nor finalOutputProvenanceSectionBindings, and it carries no provenance binding (it is upserted into prose, run.ts:10018). grep confirms no 'unresolved-revision-proposals' / 'unresolved_revision_proposals' token exists in src or the registry. So the guard's 'is present' target is a string the codebase must first CREATE, and the section has provenance_binding_required=false (unlike the {section_id,...,provenance_binding_required} shape the registry node assumes for all rows).",
      "recommendation": "Treat unresolved-revision-proposals as a NEW canonical concept that needs a real id + activation predicate (disclosed.length>0) and an explicit provenance_binding_required=false. Either give the markdown section a registered section_id and wire it, or model the registry row's binding-requirement as optional. Do not write a guard that asserts presence of an id no runtime site emits.",
      "verifier_reasoning": "CONFIRMED REAL. Adversarial verification against the actual code substantiates every load-bearing assertion of the claim.\n\nEvidence:\n1. grep for `unresolved-revision-proposals`/`unresolved_revision_proposals` finds the token ONLY in the design note and `.onto/review/.../*.prompt.md` packets — NOT in src/ and NOT in the registry. In src/ the only artifact is the literal markdown heading `## Unresolved Revision Proposals` (run.ts:9996).\n2. `final_output_prompt_policy.deterministic_runtime_append_sections` (run.ts:4447-4453) lists exactly 5 underscore ids (seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings). The unresolved-revision id is absent.\n3. `finalOutputProvenanceSectionBindings()` (run.ts:10394, body 10471-10613) returns exactly 5 hyphen rows (seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings), and EVERY row carries authority_refs + validation_refs + required_fragments (all provenance-bound). The unresolved-revision section is not a row.\n4. `appendFinalOutputUnresolvedRevisionSection()` (run.ts:9981-10019) emits the heading as PROSE via `upsertMarkdownSection` (run.ts:10018, helper in markdown-section.ts:50). It is called at run.ts:13443 AFTER the binding set is constructed (run.ts:13203) and is deliberately unbound — the doc comment (run.ts:9977-9979) states it uses operational wording only so it never trips final-output provenance forbidden fragments. Activation is `disclosed.length>0` (run.ts:9990).\n5. The registry has NO `final_output_append_sections`/`prompt_projection_contracts` node and no `activation_predicate`/`append_owner`/`provenance_binding_required` keys yet (grep empty).\n\nDesign defect: §2 (line 15) correctly observes the M4a section is in NEITHER list, but §3.3 (lines 33-35) then folds `unresolved-revision-proposals` into the uniform row shape `{section_id, heading, activation_predicate, append_owner, provenance_binding_required}` and a guard §3.3(c) that asserts the id 'is present'. As written this is unbuildable as specified: (a) no runtime site emits any section_id for this section, so the guard targets a string the build must first invent; (b) its activation is `disclosed.length>0`, unlike the always-bound 5; (c) it must carry `provenance_binding_required=false`, an asymmetry the uniform row model does not anticipate (all 5 existing sections are binding-required). A guard built per §3.3(c) would be either vacuous or assert presence of an id no runtime site produces.\n\nSeverity high (not blocker): it is a design-stage modeling gap with no runtime impact today, the design shows partial awareness (notes the NEITHER-list state), and the remediation is well-scoped (assign a real section_id + activation predicate + provenance_binding_required=false, OR model the binding-requirement as optional). But as specified the §3.3 registry row and §3.3(c) guard cannot be built correctly, which would silently produce a foolable/vacuous guard — a real correctness defect in the design. The recommendation in the claim is sound."
    },
    {
      "lens": "completeness",
      "severity": "high",
      "title": "G(c) hyphen flip breaks an explicit run.test.ts assertion on the underscore append-section ids — site not enumerated",
      "location": "src/core-runtime/reconstruct/run.test.ts lines 3481-3484 (type) and 4204-4211 (assertion) vs design §3.3 line 36 / Q2",
      "claim": "The design's §3.3 risk note and Q2 ask whether flipping deterministic_runtime_append_sections from underscore to hyphen breaks a test, but answer it vaguely ('must be verified against run.test.ts final-output assertions') and do not name the site. The site is concrete: run.test.ts:4204-4211 asserts `deterministic_runtime_append_sections` toEqual arrayContaining(['claim_projection','artifact_truth','provenance_footer']) — underscore literals. Flipping to hyphen ('claim-projection' etc.) WILL fail this test, and the inline type at 3481-3484 also encodes the field. This is a guaranteed edit site the completeness checklist must include, not a 'verify' afterthought.",
      "recommendation": "Add run.test.ts:4204-4211 (and the 3481-3484 type, harmless but adjacent) to the explicit edit-site list. Better: reconsider Q1 — keeping the prompt-policy ids as-is and adding a parity guard that maps registry hyphen section_ids to the prompt-policy underscore form (an explicit alias map) avoids touching the prompt surface AND this test entirely. The prompt-policy list is informational (it does NOT feed AuthoredArtifactReuseMatch — only the CQ assessment contract sha does, run.ts:698/1199), so the only breakage is this test, but a no-prompt-change design is strictly lower risk for the load-bearing final-output path.",
      "verifier_reasoning": "VERIFIED REAL. The claim is code-grounded and correct on every load-bearing point.\n\n1) The concrete site exists exactly as cited. src/core-runtime/reconstruct/run.test.ts:4204-4211 asserts `finalOutputPayloads[0].final_output_prompt_policy.deterministic_runtime_append_sections` `toEqual(expect.arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"]))` — underscore literals. The inline type at 3481-3484 declares the same field.\n\n2) The hyphen flip guarantees failure. run.ts:4441-4456 is the literal source: `final_output_prompt_policy.deterministic_runtime_append_sections = [\"seed_answerability\",\"claim_projection\",\"artifact_truth\",\"provenance_footer\",\"provenance_bindings\"]`. Design §3.3 (line 34) decides canonical id form = hyphen and replaces this hand-maintained underscore list with registry-derived hyphen ids (per §2 line 15: `seed-answerability, artifact-truth, claim-projection, ...`). `arrayContaining([\"claim_projection\",...])` requires those exact underscore strings; hyphen entries match none, so the assertion fails deterministically. This list flows directly into finalOutputPayloads (no transformation), so the test is hit.\n\n3) The reuse-hash reasoning in the finding is sound. reuseMatchHash (run.ts:1103-1105) hashes only the AuthoredArtifactReuseMatch struct (run.ts:693-724), which carries the CQ assessment contract version/sha (697-698, populated 1197-1199) but NOT the prompt-policy append-sections list. So the underscore→hyphen flip does not rotate the reuse hash — the only breakage is this test. The finding's claim that the prompt-policy list is informational (does not feed AuthoredArtifactReuseMatch) is confirmed.\n\n4) The design genuinely omits the site AND contradicts itself. §3.3 line 36 and Q2 (line 59) only say \"must be verified against run.test.ts final-output assertions\" / \"does flipping ... break a test?\" — vague, no named site. Worse, success criteria line 49 (\"all run.test.ts final-output assertions still pass\") and test plan line 55 (\"run.test.ts final-output assertions unchanged\") affirmatively claim zero test change, which is false: the hyphen decision forces editing 4204-4211. This is a real completeness defect: a guaranteed edit site is left as a \"verify\" afterthought and the stated success criterion is unattainable as written under the chosen canonical form.\n\nThe finding's recommendation is also correct: Q1's alternative (keep prompt-policy ids as-is + a registry-to-prompt-policy alias/parity guard, no prompt-surface change) avoids the test edit entirely and is strictly lower-risk for the load-bearing final-output path, since the prompt-policy list does not feed the reuse hash.\n\nSeverity high is appropriate: it is not a build-blocker (the fix is small and the path is verifiable), but it is a concrete missing edit site plus an internal contradiction in success criteria on the load-bearing final-output provenance surface that a pre-build completeness review must surface.\n\nMinor note (does not reduce validity): the finding labels the 3481-3484 type change \"harmless but adjacent\" — accurate; the type is permissive (string[]) and would not itself fail compilation, but it is correctly flagged as an adjacent site to keep consistent."
    },
    {
      "lens": "authority-economy",
      "severity": "high",
      "title": "G(c) underscore→hyphen flip will break a live run.test.ts assertion the design did not identify",
      "location": "src/core-runtime/reconstruct/run.test.ts:4204-4211 (assertion on deterministic_runtime_append_sections) vs design §3.3/§3.6/Q2",
      "claim": "The design says flipping deterministic_runtime_append_sections ids from underscore→hyphen 'must be verified against run.test.ts final-output assertions' but flags it only as a hypothetical reuse-hash risk. The concrete breakage already exists: run.test.ts:4204-4211 asserts the emitted prompt-policy list contains the UNDERSCORE ids `claim_projection`, `artifact_truth`, `provenance_footer` via expect.arrayContaining. Deriving that list from a hyphen-canonical registry (per §3.3) changes those emitted strings and fails this test deterministically. Design §3.6 'if the prompt-policy ids are purely informational the change is safe' is also wrong-headed: this test pins them as part of the prompt surface.",
      "recommendation": "Either (a) keep the prompt-policy list emitting its current underscore ids and make the registry/guard map underscore prompt ids ↔ hyphen binding ids (a declared alias map), or (b) explicitly plan to update run.test.ts:4204-4211 and confirm the prompt-text change is acceptable. Cite this exact assertion in the design's risk section. Prefer (a): the prompt-policy ids are an existing public prompt surface and flipping them is a gratuitous prompt regression.",
      "verifier_reasoning": "CONFIRMED real, code-grounded defect.\n\nEvidence:\n- src/core-runtime/reconstruct/run.test.ts:4204-4211 asserts: deterministic_runtime_append_sections .toEqual(expect.arrayContaining([\"claim_projection\", \"artifact_truth\", \"provenance_footer\"])) — the exact UNDERSCORE ids.\n- src/core-runtime/reconstruct/run.ts:4447-4453 (the final_output_prompt_policy object inside the final-output payload builder) emits that underscore list: [\"seed_answerability\",\"claim_projection\",\"artifact_truth\",\"provenance_footer\",\"provenance_bindings\"]. This is an emitted prompt-surface field, not internal-only.\n- src/core-runtime/reconstruct/run.ts:10473-10610 finalOutputProvenanceSectionBindings uses HYPHEN section_ids (seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings).\n- Design §3.3 (line 34) decides \"canonical id form = hyphen\" and makes the prompt-policy deterministic_runtime_append_sections DERIVED from the registry's hyphen section_ids, \"replacing the hand-maintained underscore list.\" Deriving from hyphen ids changes the emitted strings (claim_projection -> claim-projection, etc.).\n- Because the test uses arrayContaining on the underscore literals, a hyphen-derived array fails this assertion deterministically.\n\nThe design's risk handling is inadequate exactly as claimed: the ⚠️ Risk bullet at the end of §3.3 (line 36) only says the change \"must be verified against run.test.ts final-output assertions\" and hedges \"if the prompt-policy ids are purely informational, the change is safe\" — but it never identifies run.test.ts:4204-4211, the concrete assertion that pins these ids as a checked surface and would break. Q2 (line 59) likewise leaves it an open question. So the design genuinely failed to identify a deterministic test breakage from its own §3.3 reconciliation decision.\n\nSeverity high is appropriate: it is a deterministic CI failure flowing directly from the design's load-bearing reconciliation core (G(c)), it touches an emitted prompt surface (prompt-text regression, not merely a test), and the success criterion §5 line 49 explicitly promises \"all run.test.ts final-output assertions still pass\" — which §3.3 as written would violate.\n\nMinor imprecision in the claim (not material): the doc has no numbered \"§3.6\"; the relevant text is the ⚠️ Risk bullet ending §3.3 (line 36) plus Q2 (line 59). The substance, location of the assertion (4204-4211), the underscore->hyphen mechanism, and the recommendation are all accurate. Recommendation (a) declared alias map preserving the underscore prompt surface, or (b) explicitly plan + cite the assertion and confirm the prompt-text change, is correct and actionable; preferring (a) to avoid a gratuitous prompt regression is well-reasoned since the prompt-policy ids are an existing emitted surface."
    },
    {
      "lens": "authority-economy",
      "severity": "high",
      "title": "Hyphen section_ids are load-bearing required_fragments, not informational — flipping them risks the final-output provenance gate, not just a test",
      "location": "src/core-runtime/reconstruct/run.ts:10606-10611 (runtime-provenance-bindings.required_fragments lists the hyphen section_ids as output-text fragments)",
      "claim": "The `runtime-provenance-bindings` binding requires the literal strings `seed-answerability`, `artifact-truth`, `claim-projection`, `runtime-artifact-truth-footer` to appear in the final output text (required_fragments). The hyphen section_ids are therefore validated content in the provenance gate, contradicting design §3.6's premise that section ids might be 'purely informational.' Any G(c) change that renames a binding section_id (even to align with registry) must also update this required_fragments list and the final-output provenance validation, or the gate breaks. The design does not name finalOutputProvenanceSectionBindings:10606-10611 as a reconcile site.",
      "recommendation": "Add run.ts:10606-10611 to the explicit reconcile-site list. Decision: keep the hyphen binding section_ids EXACTLY as-is (they are the validated/load-bearing form, as the design already half-recognizes) and make the registry adopt those exact strings as canonical section_id; do not rename them. The guard then asserts equality to the unchanged bindings — zero runtime text change on the load-bearing path.",
      "verifier_reasoning": "CONFIRMED, code-grounded. The four strings at src/core-runtime/reconstruct/run.ts:10606-10611 (`runtime-provenance-bindings` binding's `required_fragments` = [\"seed-answerability\",\"artifact-truth\",\"claim-projection\",\"runtime-artifact-truth-footer\"]) are load-bearing validated content, not informational:\n\n1. `appendFinalOutputProvenanceBindingsSection` (run.ts:10113-10114) renders every binding as `- ${binding.section_id}: ${binding.claim_summary}`, so each sibling section_id appears literally in the \"Runtime Provenance Bindings\" section text.\n2. `validateFinalOutputProvenance` (post-seed-validation.ts:2121-2140) scopes each binding's required_fragments to that binding's section via `sectionText.includes(fragment)` (line 2131-2132). So those four hyphen strings MUST appear in the rendered bindings section or the gate emits `final_output_provenance_missing` and `writeFinalOutputProvenanceValidationArtifact` (run.ts:10363) sets validation_status=\"invalid\" — a RUNTIME gate failure, exactly as the TITLE states, not merely a test break.\n3. Therefore renaming ANY of those four binding section_ids changes the rendered line at 10114 but leaves the hard-coded required_fragments at 10606-10611 stale → gate breaks. This is a cross-binding coupling (one binding's required_fragments hard-code four OTHER bindings' ids).\n\nThe design does NOT name 10606-10611 as a reconcile site: §3.3 line 34 only generically says the bindings \"keep their ... required_fragments in code\" and that their \"section_id/heading set is asserted to equal the registry's.\" That assertion is precisely the trap — if the registry's canonical section_id for any of these four does not exactly match the hyphen strings, the new parity guard (scripts/check-append-sections-parity.ts) would force a rename that silently invalidates the provenance gate. run.test.ts:2780-2783 only asserts the HEADING strings (\"Seed Answerability\", etc.), not the hyphen section_ids, so those specific tests would NOT catch the rename — reinforcing that the failure surfaces as a runtime gate regression, the worst-caught kind.\n\nPARTIAL OVERSTATEMENT (does not negate the finding): the claim attributes a \"purely informational\" premise to the design re these binding ids. The design's actual \"purely informational\" hedge (§3.3 line 36 / Q2 line 59) is scoped to the PROMPT-POLICY underscore ids in `deterministic_runtime_append_sections` (run.ts:4447-4453), not the provenance-binding hyphen ids. The design (line 34) already explicitly calls hyphen \"the validated/load-bearing one\" and decides NOT to rename the bindings. So under the design's PRIMARY plan (canonical=hyphen, no rename) the gate does not break; the gate-break is conditional on the registry adopting non-matching ids via the line-34 \"assert equal to registry\" step. That conditionality is why this is high, not blocker.\n\nThe RECOMMENDATION is correct and valuable: (a) add run.ts:10606-10611 explicitly to the reconcile-site list (the design's reconcile coverage is genuinely incomplete here — it never flags the cross-binding hard-coded id strings), and (b) pin the registry's canonical section_id to these EXACT hyphen strings so the guard asserts equality with zero runtime text change. This is exactly the silent gate regression G(c) exists to prevent, so flagging it pre-build is warranted."
    },
    {
      "lens": "authority-economy",
      "severity": "high",
      "title": "G(c) prefer parity-guard over derive; Q1 should be resolved to 'guard, do not derive'",
      "location": "design §3.3 + Q1; run.ts:4447 (prompt list) and run.ts:10472 (bindings)",
      "claim": "The design leaves Q1 (derive prompt-policy list from registry vs parity-guard an unchanged list) open and tentatively picks 'derive.' Given G-1 (breaks a test), G-2 (no clean transform), and G-3 (binding ids are validated fragments), deriving the prompt list and renaming ids touches three load-bearing surfaces for no behavioral gain. A parity guard over the EXISTING (reconciled-by-equality) lists adds the drift protection without changing any emitted string or rotating any surface. The 'concept economy −1 hand-maintained list' benefit (§4) is not worth the regression surface.",
      "recommendation": "Resolve Q1 as: parity-guard, do not derive. Registry declares the canonical {section_id, prompt_policy_id, append_owner, ...} rows; the guard asserts (i) the prompt-policy list equals the declared prompt_policy_ids and (ii) the binding section_ids equal the declared section_ids. Leave both runtime lists textually unchanged. This is strictly lower-risk and still kills the drift class.",
      "verifier_reasoning": "CONFIRMED real. The design's §3.3 commits to DERIVING the prompt-policy deterministic_runtime_append_sections from the registry and renaming underscore->hyphen, and Q1 leaves the guard-vs-derive choice nominally open. The lens's recommendation (parity-guard, do not derive) is the correct, lower-risk resolution, grounded in code:\n\nG-1 (breaks a test): run.test.ts:4204-4211 asserts deterministic_runtime_append_sections via arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"]). The hyphen flip breaks this. The design's own §3.3 risk note + Q2 admit this.\n\nPrompt-surface regression (stronger than design states): compactFinalOutputPromptPayload (run.ts:4333) builds final_output_prompt_policy with the append-section ids at run.ts:4447, and run.ts:9435 JSON.stringifies that payload into the writeFinalOutput userPrompt. So the ids are literally part of the host-LLM final-output prompt text; deriving+renaming rotates the prompt surface for zero behavioral gain.\n\nG-3 (binding ids are validated fragments): finalOutputProvenanceSectionBindings (run.ts:10471) lists hyphen section_ids, and runtime-provenance-bindings.required_fragments (run.ts:10606-10611) enumerates seed-answerability/artifact-truth/claim-projection/runtime-artifact-truth-footer; appendFinalOutputProvenanceBindingsSection (run.ts:10367) actually checks required_fragments presence. Renaming binding ids churns load-bearing validated fragments.\n\nNo reuse-hash benefit on this surface: the reuse match (buildAuthoredArtifactReuseMatch, run.ts:1192+) covers competency_question_assessment_projection_contract_version/_sha256 (the G(b) surface) but NOT the final-output prompt-policy list, so G(c) deriving adds no reuse-correctness gain.\n\nOne framing correction (non-fatal): the lens calls the lists 'reconciled-by-equality,' but they are NOT equal even after underscore->hyphen normalization (provenance_footer != runtime-artifact-truth-footer; provenance_bindings != runtime-provenance-bindings). A parity guard therefore still needs a registry-declared canonical mapping, not pure set equality. This makes the recommendation marginally more involved but does not change the conclusion: a registry-declared {section_id} mapping + parity guard leaves both runtime lists textually unchanged, breaks no test, rotates no prompt string, and still kills the drift class — strictly lower risk than derive+rename.\n\nHigh (not blocker) because the design explicitly gates on ultracode/onto cross-validation before build; this is the pre-build defect that review is meant to catch, but it would cause a concrete regression (test break + prompt churn + validated-fragment churn) if built as written."
    },
    {
      "lens": "scope-risk",
      "severity": "high",
      "title": "G(c) provenance guard would key on section_id, but the load-bearing field is heading — partially vacuous guard",
      "location": "src/core-runtime/reconstruct/post-seed-validation.ts:validateFinalOutputProvenance (lines 2121-2141) vs design §3.3 guard scripts/check-append-sections-parity.ts",
      "claim": "validateFinalOutputProvenance() matches each provenance-bound section by binding.heading (markdownSectionText(args.finalOutputText, binding.heading)); section_id is used ONLY for the diagnostic subjectId. The actually load-bearing identity is the HEADING string ('Seed Answerability', 'Artifact Truth', etc.), not the section_id. The design's G(c) guard asserts 'provenance-binding section_ids equal the registry section_ids' and picks hyphen as canonical section_id — but a drift in the heading (the field that decides whether provenance validation passes) would pass the proposed guard. The registry node shape in §3.3 is {section_id, heading, ...} yet the parity assertion described covers section_id, not heading.",
      "recommendation": "Make the G(c) guard assert BOTH section_id AND heading parity between registry and finalOutputProvenanceSectionBindings(); the heading set is the runtime-load-bearing identity and must be the primary parity key. Otherwise the guard gives false confidence on the path it claims to protect.",
      "verifier_reasoning": "VERIFIED REAL. Code-grounded confirmation:\n\n1. src/core-runtime/reconstruct/post-seed-validation.ts:validateFinalOutputProvenance (lines 2121-2141) matches each bound section by markdownSectionText(args.finalOutputText, binding.heading) (line 2122). binding.section_id appears ONLY as the diagnostic subjectId (lines 2127, 2137). The heading is the runtime-load-bearing identity for pass/fail.\n\n2. src/core-runtime/reconstruct/markdown-section.ts:markdownSectionText (lines 83-91) discovers a section by exact `## ${heading}` match; section_id never participates in discovery (confirmed by upsert/match co-design comments at lines 40-65).\n\n3. run.test.ts asserts on heading strings (\"Seed Answerability\"/2780, \"Artifact Truth\"/2782, \"Runtime Artifact Truth Footer\"/3763, /4299), not on section_id — so the runtime contract the tests guard is the heading set.\n\n4. finalOutputProvenanceSectionBindings (run.ts:10471-10613) defines section_id (hyphen: seed-answerability, artifact-truth, …) and heading (Title Case) as INDEPENDENT strings. A heading-only drift (e.g. \"Artifact Truth\"→\"Artifacts Truth\") leaves section_id unchanged, so a guard keyed only on section_id parity passes while provenance validation silently breaks.\n\nDesign inconsistency confirmed: §3.3 line 35 guard clause (b) is specified as \"the provenance-binding section_ids equal the registry section_ids\" — heading omitted. §3.3 line 34 prose loosely says \"their section_id/heading set is asserted to equal the registry's.\" The formal guard enumeration (the implementable spec) names only section_id; a literal implementation yields a partially vacuous guard on the exact path (provenance pass/fail) it claims to protect. No existing guard covers heading (GUARDS array in check-invariant-drift.ts:30-63 has none), and the registry has no final_output_append_sections node yet, so this is the only proposed protection.\n\nNUANCE the claim omitted (strengthens, not weakens it): the runtime-provenance-bindings section's required_fragments (run.ts:10606-10611) ARE the hyphen section_ids as literal emitted text, so section_id is independently load-bearing in that one place. Therefore the correct fix is to assert BOTH section_id AND heading parity (heading primary for the matcher path), exactly as the recommendation states.\n\nSeverity high, not blocker: the guard is still net-positive (catches set add/drop, footer fragments, unresolved-revision-proposals presence) and is a fixable pre-build spec ambiguity that the design's own line-34 prose already half-acknowledges; it is not a fundamental design flaw. The recommendation is sound and should be folded into §3.3(b)."
    },
    {
      "lens": "scope-risk",
      "severity": "high",
      "title": "G(c) underscore->hyphen reconciliation is two semantic renames, not a punctuation transform, and breaks a concrete test",
      "location": "src/core-runtime/reconstruct/run.ts:4447-4453 (prompt-policy) and run.ts:10593/10601 (bindings); broken assertion at src/core-runtime/reconstruct/run.test.ts:4204-4211",
      "claim": "Design §3.3/Q2 frames the change as 'prompt-policy ids underscore->hyphen'. But the mapping is not mechanical: prompt-policy 'provenance_footer' -> binding 'runtime-artifact-truth-footer' and 'provenance_bindings' -> 'runtime-provenance-bindings' are renames, not de-underscoring. Only 3 of 5 are punctuation-equivalent. Additionally run.test.ts:4204-4211 asserts deterministic_runtime_append_sections arrayContaining(['claim_projection','artifact_truth','provenance_footer']) — flipping the prompt-policy list WILL break this test. The design's §3.3 warning ('must be verified against run.test.ts final-output assertions') is correct that a test exists, but understates that the reconciliation is a rename mapping and that this specific assertion is a guaranteed break, not a maybe.",
      "recommendation": "Document the explicit 5-way name map in the design and update run.test.ts:4204-4211 as part of G(c). Prefer the lower-risk Q1 path: keep the runtime lists unchanged and add a parity guard with an explicit registry->runtime alias map, rather than mutating the prompt surface. If you do flip, the prompt text change must be called out as an intended final-output prompt-surface change.",
      "verifier_reasoning": "CONFIRMED real and code-grounded. Verified every cited location.\n\n5-way mapping (claim's core): prompt-policy ids at run.ts:4448-4452 are [seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings]; binding section_ids at run.ts:10473/10484/10580/10593/10601 are [seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings]. Under \"canonical = hyphen\" (design §3.3): 3 are punctuation-only (seed_answerability→seed-answerability, claim_projection→claim-projection, artifact_truth→artifact-truth) but 2 are genuine renames with new tokens — provenance_footer→runtime-artifact-truth-footer and provenance_bindings→runtime-provenance-bindings. The claim's \"only 3 of 5 are punctuation-equivalent, the other 2 are renames\" is exactly correct. This matters concretely: a naive de-underscore transform in the proposed scripts/check-append-sections-parity.ts would be wrong for 2 of 5 sections.\n\nTest break (claim's second point): run.test.ts:4204-4211 asserts deterministic_runtime_append_sections toEqual(arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"])). Under the hyphen flip the runtime list contains NONE of these literals (provenance_footer becomes runtime-artifact-truth-footer, the others gain hyphens). arrayContaining requires each listed element present, so this is a GUARANTEED failure, not a maybe. Citation is exact; line 4210 literally contains \"provenance_footer\" (one of the rename cases, the hardest break). M4a unresolved-revision section confirmed in neither list (appended as raw \"## Unresolved Revision Proposals\" text at run.ts:13443 via appendFinalOutputUnresolvedRevisionSection at 9981), consistent with design §2.\n\nCalibration note (why not blocker): the design is NOT blind to this — §3.3 ⚠️ flags \"changing the prompt-policy ids (underscore→hyphen) alters the final-output PROMPT text... must be verified against run.test.ts final-output assertions,\" and Q2 explicitly asks whether the flip breaks a test/reuse-hash, with Q1 offered as the lower-risk parity-guard-over-unchanged-list alternative. So the defect is an under-specification of a risk the design already surfaced for cross-validation, not a missed hazard. The claim's own framing concedes this (\"the design's §3.3 warning is correct that a test exists, but understates...\").\n\nSeverity high (not medium) because: (1) the punctuation-vs-rename distinction changes the guard implementation correctness (a mechanical transform would be wrong), and (2) the test break is a concrete deterministic CI failure the build plan must explicitly handle, not just \"verify.\" The recommendation is sound: document the explicit 5-way name map, prefer the Q1 parity-guard with an alias map to avoid mutating the load-bearing final-output prompt surface, and if flipping, update run.test.ts:4204-4211 and call out the prompt-surface change intentionally. On reuse-hash: confirmed the prompt-policy list does NOT feed the reuse hash (only projection_contract_version + sha at run.ts:803/851/1197 rotate it), so the Q2 reuse-hash concern is benign — but the test break stands independently."
    },
    {
      "lens": "mechanism",
      "severity": "medium",
      "title": "G(c) canonical mapping is wrong for 2 of 5 sections: provenance_footer/provenance_bindings are NOT hyphen variants of the canonical ids",
      "location": "development-records/design/20260622-...md §3.3; src/core-runtime/reconstruct/run.ts:4447-4453 (deterministic_runtime_append_sections) vs run.ts:10473-10611 (finalOutputProvenanceSectionBindings section_ids)",
      "claim": "The design frames the divergence as 'underscore vs hyphen' and decides 'canonical id form = hyphen', deriving the prompt-policy list by hyphenating. But only 3 of 5 ids are pure hyphen swaps (seed_answerability→seed-answerability, claim_projection→claim-projection, artifact_truth→artifact-truth). The other two have DIFFERENT STEMS: prompt-policy 'provenance_footer' corresponds to provenance-binding 'runtime-artifact-truth-footer' (heading 'Runtime Artifact Truth Footer', appendFinalOutputProvenanceFooter run.ts:10091/10095), and prompt-policy 'provenance_bindings' corresponds to 'runtime-provenance-bindings' (heading 'Runtime Provenance Bindings', appendFinalOutputProvenanceBindingsSection run.ts:10105/10109). A naive hyphenation produces 'provenance-footer'/'provenance-bindings', which do NOT equal the canonical hyphen ids. The 'derive prompt-policy from registry section_ids' step in §3.3 is therefore underspecified — it needs an explicit per-id rename map, not a hyphenation rule. The guard as described ('prompt-policy section ids derive from / equal the registry runtime section_ids') would either fail or pass on the wrong mapping depending on the rename direction.",
      "recommendation": "Replace the implicit 'underscore→hyphen' framing with an explicit alias table {seed_answerability→seed-answerability, claim_projection→claim-projection, artifact_truth→artifact-truth, provenance_footer→runtime-artifact-truth-footer, provenance_bindings→runtime-provenance-bindings} and have the guard assert against that table. State the two non-hyphen renames in §3.3 so the reconciliation reviewer cannot miss them.",
      "verifier_reasoning": "VERIFIED FACTUAL KERNEL (real): The claim's core code-grounded fact is correct. The prompt-policy underscore ids at src/core-runtime/reconstruct/run.ts:4448-4452 (final_output_prompt_policy.deterministic_runtime_append_sections: seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings) do NOT all map by pure hyphen-swap to the provenance-binding hyphen ids at run.ts:10473-10601 (finalOutputProvenanceSectionBindings: seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings). Confirmed 3 are pure hyphen swaps and 2 have DIFFERENT STEMS: provenance_footer↔runtime-artifact-truth-footer (appendFinalOutputProvenanceFooter, run.ts:10091, heading run.ts:10095 \"## Runtime Artifact Truth Footer\") and provenance_bindings↔runtime-provenance-bindings (appendFinalOutputProvenanceBindingsSection, run.ts:10105, heading run.ts:10109 \"## Runtime Provenance Bindings\"). Every function/heading/line citation in the claim checks out. A mechanical underscore→hyphen transform would indeed produce the wrong ids (provenance-footer/provenance-bindings).\\n\\nWHERE THE CLAIM OVERREACHES (hence not high, not the title's framing): (1) The claim asserts the design 'derives the prompt-policy list by hyphenating' and 'frames the divergence as underscore vs hyphen.' But grep for hyphenat|underscore.?to.?hyphen|transform|swap across the design returns NOTHING — the word 'hyphenating' appears only in the claim, never the design. §3.3 line 34's actual mechanism is 'the prompt-policy deterministic_runtime_append_sections is DERIVED from the registry list's section_ids ... replacing the hand-maintained underscore list.' Replace-with-registry-section_ids is not a hyphenation transform; under that mechanism the stem-difference bug does not arise because the underscore list is discarded, not transformed. (2) The design did NOT hide the stem difference: §2 line 15 enumerates both full id sets verbatim, explicitly listing runtime-artifact-truth-footer and runtime-provenance-bindings as the hyphen forms next to the underscore ids. (3) The guard §3.3 line 35(a) 'prompt-policy ids derive from / equal the registry section_ids' is well-defined after replacement (they literally ARE the registry ids), not foolable as claimed.\\n\\nNET: A real spec-clarity/underspecification defect, not a wrong-mapping decision. §3.3 line 36's loose parenthetical '(underscore→hyphen)' and §1 line 9's 'underscore vs hyphen' framing genuinely invite an implementer who reads only §3.3 to do a mechanical hyphenation and silently emit provenance-footer/provenance-bindings — wrong for 2 sections that sit on the load-bearing final-output PROMPT surface (the deterministic_runtime_append_sections object is emitted to the host LLM; the design's own Q2 flags the reuse-hash/prompt-surface risk). The recommendation (explicit alias table {provenance_footer→runtime-artifact-truth-footer, provenance_bindings→runtime-provenance-bindings, ...} stated in §3.3, guard asserts against the table) is sound and hardens the design. But severity is medium not high: the design's actual stated mechanism and its §2 verbatim enumeration of both stems prevent the wrong mapping from being the design's intent; the fix is doc/spec hardening, not a redesign. The claim correctly identifies a residual ambiguity but mischaracterizes it as the design having decided a wrong/foolable canonical mapping."
    },
    {
      "lens": "mechanism",
      "severity": "medium",
      "title": "G(c) prompt-policy id flip will break an existing run.test.ts assertion the design claims may be unaffected",
      "location": "src/core-runtime/reconstruct/run.test.ts:4204-4211; design §3.3 ⚠️ / Q2",
      "claim": "run.test.ts:4205-4211 asserts deterministic_runtime_append_sections via expect.arrayContaining(['claim_projection','artifact_truth','provenance_footer']) — the UNDERSCORE ids. Flipping the prompt-policy ids to the canonical hyphen form (G-1) WILL break this test. The design hedges ('if the prompt-policy ids are purely informational, the change is safe but must be verified against run.test.ts final-output assertions') but does not name this concrete assertion or commit to updating it. Success-criterion §5 'all run.test.ts final-output assertions still pass' is therefore false as stated for G(c): this test must CHANGE, not pass unchanged. Conflating 'tests must be updated' with 'zero regression' hides a required test edit.",
      "recommendation": "Name run.test.ts:4204-4211 as a REQUIRED edit in the test plan (§6/§7-Q2) and reword success criterion §5 to 'final-output PROVENANCE VALIDATION unchanged; the one prompt-policy assertion is updated to the canonical ids'. Confirm separately (this review did) that the flip does NOT rotate the reuse hash: AuthoredArtifactReuseMatch (run.ts:693-719) hashes the assessment contract sha + source/seed shas, not final_output_prompt_policy, so Q2's reuse-hash worry is resolvable as 'no rotation' — state that conclusion instead of leaving it open.",
      "verifier_reasoning": "CONFIRMED real, code-grounded.\n\n(1) The asserted test exists with underscore ids: src/core-runtime/reconstruct/run.test.ts:4204-4211 asserts deterministic_runtime_append_sections via expect.arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"]) — underscore form.\n\n(2) The G(c) flip will break it: the runtime source at src/core-runtime/reconstruct/run.ts:4441-4456 (final_output_prompt_policy.deterministic_runtime_append_sections, lines 4447-4453) currently emits the underscore ids. Design §3.3 explicitly decides \"canonical id form = hyphen\" and makes the prompt-policy list DERIVED from the registry's hyphen section_ids, replacing the hand-maintained underscore list. Post-flip the array contains \"claim-projection\"/\"artifact-truth\"/etc., so arrayContaining([\"claim_projection\",...]) fails. This assertion must CHANGE, not pass unchanged.\n\n(3) Internal inconsistency confirmed: §5 success criterion says \"all run.test.ts final-output assertions still pass\" and §6 says \"run.test.ts final-output assertions unchanged,\" yet §3.3 ⚠️ and Q2 leave the test impact open without naming run.test.ts:4204-4211 as a required edit. So §5/§6 are false-as-stated for this one assertion; the design conflates \"required test edit\" with \"zero regression.\"\n\n(4) The recommendation's secondary claim — that Q2's reuse-hash worry resolves to \"no rotation\" — is ALSO verified true: AuthoredArtifactReuseMatch (run.ts:693-724) and its construction (run.ts:1193-1200) hash competency_question_assessment_projection_contract_sha256 = competencyQuestionAssessmentProjectionContractSha256() = sha256(stableJson(competencyQuestionAssessmentProjectionContract())) (run.ts:843-845), plus source/seed shas and document_excerpt_projection_budget. Nothing hashes final_output_prompt_policy or deterministic_runtime_append_sections (grep shows only lines 4441/4447 reference them, neither in the reuse-hash path). So the prompt-policy id flip does NOT rotate the reuse hash; runtime reuse is unaffected and only the final-output prompt TEXT changes.\n\nSeverity adjusted from claimed high to medium: the defect is real and must-fix (a stale assertion + two inaccurate success/test-plan statements that would mislead the builder), but blast radius is small — the underlying flip is verified safe (no hash rotation, provenance-binding bodies untouched), and the remedy is a one-line test edit plus rewording §5/§6. Recommendation is correct and should be adopted: name run.test.ts:4204-4211 as a REQUIRED edit in §6/§7-Q2, reword §5 to \"final-output provenance validation unchanged; the one prompt-policy assertion is updated to canonical hyphen ids,\" and record the resolved Q2 conclusion (\"no reuse-hash rotation\")."
    },
    {
      "lens": "mechanism",
      "severity": "medium",
      "title": "Loader conditional shape in the design (Q4) does not match the real YAML keys",
      "location": "design §3.1 / Q4; .onto/processes/reconstruct/reconstruct-contract-registry.yaml:2245-2248, 2304-2313, 2701",
      "claim": "The design proposes conditional_validation_obligations as 'flat string[] or a structured {obligation, required_when}' and says to 'inspect the 3 existing conditional entries'. The actual 3 entries have keys {obligation_id, activation_condition, input_authority_refs[]} (e.g. yaml:2246-2248: obligation_id / activation_condition / input_authority_refs). Neither proposed shape (string[] nor {obligation, required_when}) matches — the real keys are obligation_id + activation_condition + input_authority_refs. Parsing into the design's guessed shape would drop input_authority_refs (the same class of data-drop bug — codex H-B — that motivated this PR). Also note these obligation entries reference the parallel conditional_input_authority_refs block (yaml:2231-2235) by the same activation_condition, so a faithful typed parse should preserve activation_condition to keep that linkage.",
      "recommendation": "Type ConditionalValidationObligation as { obligation_id: string; activation_condition: string; input_authority_refs: string[] } and parse all three keys (default input_authority_refs to []). Update Q4 in the design with the real key names before build so the implementer does not under-parse.",
      "verifier_reasoning": "VERIFIED REAL. The claim accurately describes the mismatch between the design's Q4/§3.1 proposed shapes and the actual YAML.\n\nActual conditional_validation_obligations key shape (confirmed in .onto/processes/reconstruct/reconstruct-contract-registry.yaml):\n- yaml:2246-2248 (candidate-disposition-validator): obligation_id / activation_condition / input_authority_refs\n- yaml:2305-2313 (ontology-seed-validator, 3 entries in one block): same three keys\n- yaml:2702-2704 (maturation-answer-claims-validator): same three keys\n\nDesign text confirmed:\n- §3.1 (design line 20): \"conditional_validation_obligations: ConditionalValidationObligation[] (or a bounded typed shape; inspect the 3 existing conditional entries).\"\n- Q4 (design line 61): \"the 3 conditional_validation_obligations — flat string[] or a structured {obligation, required_when}? Inspect and type minimally.\"\n\nThe claim is correct on every concrete point: (1) neither proposed shape (string[] nor {obligation, required_when}) matches; the real keys are obligation_id + activation_condition + input_authority_refs[]. (2) Parsing into {obligation, required_when} would drop input_authority_refs — the same data-drop class as codex H-B (loader dropping input_authority_refs) that the design itself names as the motivation (design line 13). (3) The linkage observation is also real: conditional_input_authority_refs (yaml:2231-2235) carries the same activation_condition value (source_purpose_candidate_runtime_is_implemented) as the conditional obligation at yaml:2247, so a faithful parse should preserve activation_condition to keep that cross-block linkage resolvable. The recommended type {obligation_id, activation_condition, input_authority_refs[]} matches the YAML exactly.\n\nTwo minor corrections to the claim's framing (do not negate it): (a) The design and claim both say \"3 existing conditional entries,\" but there are actually 5 obligation entries across 3 validator blocks (ontology-seed-validator's block alone has 3). The design undercounts the entries, which slightly understates the parse surface — if anything this strengthens the finding. (b) The design frames Q4 as an OPEN cross-validation question and §3.1 explicitly instructs \"inspect the 3 existing conditional entries\" before typing, so the author has flagged this for resolution rather than committed to the wrong shape. That self-hedging is why this is medium and not high: the wrong shape is unlikely to survive an actual inspection-before-build. But the proposed shapes are concretely wrong and would reintroduce the exact bug the PR exists to fix if an implementer followed Q4 literally, so updating Q4/§3.1 with the real key names before build is a valid, code-grounded design improvement.\n\nSEVERITY: medium. Real, code-grounded, ties directly to the PR's motivating bug class, but mitigated by the design self-flagging it as an open question requiring inspection."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "medium",
      "title": "G(c) underscore→hyphen is NOT a 1:1 rename — two of five ids have different semantics, and the design's reconciliation undercounts this",
      "location": ".onto design §2 (line 15) / §3.3 (line 34) vs src/core-runtime/reconstruct/run.ts:4447-4453 (deterministic_runtime_append_sections) and run.ts:10471-10612 (finalOutputProvenanceSectionBindings)",
      "claim": "The design frames the reconciliation as 'underscore→hyphen' (Q2/Risk line 36, 59). But the two lists are not the same five sections in two spellings. Prompt-policy ids: seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings. Provenance-binding section_ids: seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings. `provenance_footer` ≠ `runtime-artifact-truth-footer` and `provenance_bindings` ≠ `runtime-provenance-bindings` — these are name changes, not hyphenations. A pure s/_/-/ flip yields `provenance-footer`/`provenance-bindings`, which match NEITHER the binding ids NOR each other. The reconciliation therefore requires an explicit id-mapping decision (3 of 5 map cleanly, 2 must be renamed), which the design does not enumerate.",
      "recommendation": "Replace the 'underscore→hyphen' framing with an explicit 5-row mapping table in the design before build: seed_answerability→seed-answerability, claim_projection→claim-projection, artifact_truth→artifact-truth, provenance_footer→runtime-artifact-truth-footer, provenance_bindings→runtime-provenance-bindings. Confirm with the owner that the prompt-policy informational labels SHOULD adopt the binding's fuller names (runtime-artifact-truth-footer/runtime-provenance-bindings) rather than the shorter forms.",
      "verifier_reasoning": "VERIFIED REAL. The factual core of the claim is exactly correct against the code.\n\nPrompt-policy ids (src/core-runtime/reconstruct/run.ts:4447-4453, inside finalOutputCompactSummaryProjection's final_output_prompt_policy): seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings.\nProvenance-binding section_ids (run.ts: finalOutputProvenanceSectionBindings, :10473/10484/10580/10593/10601): seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings.\n\nDirect string check confirms the claim's math: a pure s/_/-/ flip maps 3 of 5 cleanly (seed_answerability→seed-answerability, claim_projection→claim-projection, artifact_truth→artifact-truth) but yields provenance-footer / provenance-bindings, which match NEITHER the binding ids (runtime-artifact-truth-footer / runtime-provenance-bindings) NOR each other. So 2 of 5 are genuine renames, not hyphenations. The design's reconciliation (§3.3 line 34: prompt-policy 'derived from the registry list's section_ids ... replacing the hand-maintained underscore list') and its risk framing (Risk line 36 and Q2 line 59: 'changing the prompt-policy ids (underscore→hyphen)') do NOT enumerate these 2 renames; no mapping table exists in the design. The recommendation's table is correct.\n\nCONCRETE BUILD IMPACT (strengthens the claim beyond what it cited): run.test.ts:4204-4211 asserts deterministic_runtime_append_sections arrayContaining ['claim_projection','artifact_truth','provenance_footer']. Under the design's chosen hyphen-canonical reconciliation, provenance_footer becomes runtime-artifact-truth-footer (a rename) — this test WILL break, directly contradicting success criteria lines 49/55 ('all run.test.ts final-output assertions still pass / unchanged'). deterministic_runtime_append_sections is built only at run.ts:4447 and consumed only by this test; it is part of the host-LLM final-output payload, so id changes do alter prompt text (design Risk line 36 is right to flag this).\n\nWHY MEDIUM not HIGH: (1) The design is not blind to the divergence — §2 line 15 prints both full id sets verbatim and Q1 line 58 explicitly says 'the underscore/hyphen + set divergence must be reconciled first', so the claim slightly overstates by implying no awareness. (2) The design's own Q2/Risk already direct the builder to 'verify against run.test.ts final-output assertions', so the gap would surface as a caught test failure, not a silent ship. (3) This is a design completeness/clarity defect, not a guard-soundness or runtime-correctness trap (the stated LENS is drift-guard-soundness, but the defect is actually about the id-mapping spec, not a vacuous guard). Net: a real, actionable pre-build gap worth fixing with the recommended 5-row mapping table + owner confirmation on adopting the longer runtime-* names, but mitigated by existing risk flags."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "medium",
      "title": "G(c) parity guard is vacuous if the prompt-policy list is 'derived' from the registry — comparing the registry to itself",
      "location": ".onto design §3.3 (lines 34-35: 'derived from the registry list' AND guard asserts 'derive from / equal the registry'); guard target scripts/check-append-sections-parity.ts (proposed)",
      "claim": "The design simultaneously (a) makes deterministic_runtime_append_sections DERIVED from the registry section_ids (filtered to append_owner=runtime), and (b) adds a guard asserting the prompt-policy ids 'derive from / equal the registry'. If the runtime list is computed FROM the registry at runtime, then a guard comparing that computed list back to the registry is tautological — it can never fail, exactly the 'comparing the registry to itself' anti-pattern. There is no independent hand-maintained surface left to drift. The only real risk after derivation is the derivation/filter logic itself, which the parity guard does not test.",
      "recommendation": "Pick one model, not both. EITHER: keep deterministic_runtime_append_sections as an independent hand-maintained literal and have the guard assert exact-set equality registry-vs-runtime-literal (real parity). OR: derive it and drop the (a) prompt-policy guard entirely, replacing it with a unit test of the derivation+activation filter (e.g. append_owner=runtime filter produces the expected ordered ids). Do not 'guard' a derived value against its own source.",
      "verifier_reasoning": "CONFIRMED REAL, but severity is MEDIUM not HIGH (the title overstates).\n\nThe narrow defect is genuine. Design §3.3 (line 34) commits to deriving `deterministic_runtime_append_sections` from the registry (`final_output_append_sections` filtered to `append_owner=runtime` + activation), then line 35 guard `check-append-sections-parity.ts` sub-assertion (a) asserts the prompt-policy ids \"derive from / equal the registry append_owner=runtime section_ids.\" If the runtime value is literally registry.filter(append_owner=runtime).map(section_id), a guard recomputing that same filter and comparing back is comparing the registry to itself — it cannot fail. There is no independent hand-maintained surface left for (a) to protect. The real residual risk is the derivation/mapping logic, which (a) does not test — and that mapping is NON-TRIVIAL: today's underscore list (run.ts:4447 `seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings`) does NOT map by mechanical underscore→hyphen to the hyphen binding ids (run.ts:10471 `seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings`); `provenance_footer`→`runtime-artifact-truth-footer` and `provenance_bindings`→`runtime-provenance-bindings` require an explicit mapping/filter — exactly what a parity-vs-self guard misses and a derivation unit test would catch. The claim's recommendation (pick one: independent literal + exact-set guard, OR derive + unit-test the filter, never guard a derived value against its source) is correct. I also confirmed `deterministic_runtime_append_sections` is purely informational: set once at run.ts:4447, read only by run.test.ts:4206, never consumed by the actual append logic (real appends are appendFinalOutputUnresolvedRevisionSection run.ts:9981/13443 + the 10471 bindings). So deriving-then-self-guarding this informational hint protects nothing load-bearing — strengthening the point.\n\nWHY MEDIUM, NOT HIGH:\n1. The guard has THREE sub-assertions; only (a) is vacuous. Sub-assertion (b) [provenance-binding section_ids == registry section_ids] IS a real parity check — the bindings at run.ts:10471 remain hand-maintained code literals, an independent surface. (c) [unresolved-revision-proposals present] is also real (that M4a section, appendFinalOutputUnresolvedRevisionSection, is in NEITHER current list — a true gap). So \"the G(c) parity guard is vacuous\" (title) is an overstatement; the guard is partly vacuous.\n2. The design ALREADY flags this exact tension as open question Q1 (line 58: \"is deriving the prompt-policy list from the registry the right call, or is a parity guard over an unchanged but reconciled runtime list lower-risk\"). The design is internally inconsistent (§3.3 commits to derive AND lists guard-(a), while Q1 reopens derive-vs-guard), which is itself a real pre-build defect — but it is surfaced, not hidden, and this is a pre-build cross-validation note where exactly such resolution is expected.\n\nOther design checks while here (sound, noted briefly): the loader-extension targets are accurate (parseValidatorRecord contract-registry.ts:695-710 indeed drops validation_obligations/conditional_validation_obligations; the conditional shape is structured {obligation_id, activation_condition, input_authority_refs} per yaml:2245-2248, so Q4's \"flat string[]\" option is wrong but Q4 leaves it open). Import-boundary (Q3): scripts importing run.ts would NOT trip check-import-boundary.ts (it only scans src/**, not scripts/), so that risk is real but unguarded — separate concern. The G(b) projection-contract field list in §3.2 line 26 matches competencyQuestionAssessmentProjectionContract() run.ts:800-841 exactly, and budgets line 28 match constants run.ts:786-798.\n\nNet: the claimed defect (guarding a derived value against its own source = vacuous self-comparison, with the real derivation-mapping risk left untested) is real and code-grounded, and the recommendation is correct. Severity medium because it is one of three guard sub-assertions, the rest provide real parity, the design already raises the underlying tension in Q1, and it is caught at the intended pre-build review gate."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "medium",
      "title": "M4a unresolved-revision section has NO section_id concept in code — it is a conditional markdown heading, so the G(c) provenance-binding parity guard cannot assert it the way other sections are asserted",
      "location": "src/core-runtime/reconstruct/run.ts:9981-10018 (appendFinalOutputUnresolvedRevisionSection) vs design §3.3 (lines 33,35: include unresolved-revision-proposals in the registry and assert provenance-binding section_ids equal registry minus non-binding)",
      "claim": "The M4a section is emitted via upsertMarkdownSection with the literal heading `## Unresolved Revision Proposals` (run.ts:9996) and is CONDITIONAL (early-returns when disclosed.length===0, run.ts:9990). It has no section_id, no entry in finalOutputProvenanceSectionBindings (run.ts:10471-10612), and no entry in deterministic_runtime_append_sections (run.ts:4447). So when the registry lists `unresolved-revision-proposals`, the guard's rule (b) 'provenance-binding section_ids equal registry section_ids (minus declared non-binding)' will FAIL unless unresolved-revision-proposals is declared non-binding AND non-runtime — i.e. it sits in the registry purely as a documented section the guard must EXCLUDE from both the prompt-policy set (it is not in the underscore list) and the binding set (it has no binding). The design's §3.3(c) 'unresolved-revision-proposals is present' only checks registry presence, which is trivially true and does not tie it to the actual markdown emitter.",
      "recommendation": "Define the section's category explicitly in the registry shape (e.g. append_owner=runtime_markdown, provenance_binding_required=false, activation_predicate=disclosed_proposals_nonempty) and have the guard tie it to the real emitter: assert run.ts contains the heading literal `## Unresolved Revision Proposals` for that section_id (a source-text presence check), since there is no structural id to compare. Otherwise the guard 'covers' a section it cannot actually detect drift on.",
      "verifier_reasoning": "PARTIALLY REAL but mischaracterized; severity lowered high→medium.\n\nCode facts all confirmed: The M4a section is emitted at src/core-runtime/reconstruct/run.ts:9996 via upsertMarkdownSection with the literal heading `## Unresolved Revision Proposals`, is conditional (early-return at run.ts:9990 when disclosed.length===0), has NO section_id, is absent from finalOutputProvenanceSectionBindings (run.ts:10471-10612, which returns exactly 5 hyphen sections: seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings) and absent from deterministic_runtime_append_sections (run.ts:4447-4453, 5 underscore ids). `unresolved-revision-proposals` appears nowhere in reconstruct-contract-registry.yaml; in the repo it exists only as the run.ts heading and two run.test.ts toContain assertions (run.test.ts:6527,6543).\n\nBut the claim's HEADLINE is WRONG. It asserts guard rule (b) 'will FAIL unless unresolved-revision-proposals is declared non-binding AND non-runtime.' The design already handles exactly this: §3.3 line 33 defines the registry section shape WITH `append_owner` and `provenance_binding_required` fields, and rule (b) (line 35) is explicitly worded 'provenance-binding section_ids equal registry section_ids (MINUS any declared non-binding sections)'. Setting the M4a entry to provenance_binding_required=false puts it in the 'minus' set, so rule (b) does NOT fail. Likewise rule (a) keys off append_owner=runtime, correctly excluding it. The claim's RECOMMENDATION (add append_owner, provenance_binding_required=false, activation_predicate) is already in the design.\n\nThe GENUINE, narrower residual (real for the drift-guard-soundness lens): rule (c) 'unresolved-revision-proposals is present' only checks REGISTRY PRESENCE. Unlike the 5 binding sections, this section has no code-level section_id anchor — the guard has nothing structural to compare against the real emitter (run.ts:9981 appendFinalOutputUnresolvedRevisionSection). So rule (c) is a vacuous presence check: it cannot detect drift if the heading literal at run.ts:9996 changes or the emitter is deleted. The fix the claim suggests for THIS part — a source-text heading-literal presence check tied to the section_id — is sound and not currently specified by the design (which only says 'is present'). This is a legitimate guard-completeness gap worth fixing before build, but it is one conditional informational section, the design is pre-build and explicitly soliciting cross-validation (Q1/Q2 already flag G(c) risk), so high overstates it. Medium."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "medium",
      "title": "G(c) id flip breaks an existing run.test.ts assertion (and that assertion is a misleading subset/arrayContaining match)",
      "location": "src/core-runtime/reconstruct/run.test.ts:4204-4211 vs design §3.3 risk (line 36) / success criterion line 49 ('all run.test.ts final-output assertions still pass')",
      "claim": "run.test.ts:4207-4211 asserts deterministic_runtime_append_sections via expect.arrayContaining(['claim_projection','artifact_truth','provenance_footer']). If G(c) flips the prompt-policy ids to the hyphen/renamed forms (per G-1), all three of these literals disappear (provenance_footer is renamed entirely) and this assertion FAILS. The design's success criterion line 49 claims 'all run.test.ts final-output assertions still pass' — that is false for this change; the test MUST be updated. Separately, this is an arrayContaining (subset) assertion, so even today it would not catch a dropped section — note when updating it that the parity guard, not this test, is the exact-set authority.",
      "recommendation": "Amend the design: G(c) WILL require editing run.test.ts:4207-4211 to the reconciled ids; list it as an expected test delta, not a 'zero test change'. Consider tightening this assertion to toEqual(exact ordered list) so the test and the new guard agree on exact-set semantics.",
      "verifier_reasoning": "CONFIRMED real and code-grounded.\n\nEvidence:\n- src/core-runtime/reconstruct/run.ts:4447-4453 — runtime `deterministic_runtime_append_sections` currently emits five UNDERSCORE ids: seed_answerability, claim_projection, artifact_truth, provenance_footer, provenance_bindings.\n- src/core-runtime/reconstruct/run.test.ts:4204-4211 — asserts `...deterministic_runtime_append_sections` `.toEqual(expect.arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"]))`.\n- Design §3.3 (line 34) makes a flat DECISION: \"canonical id form = hyphen ... The prompt-policy deterministic_runtime_append_sections is derived from the registry list's section_ids ... replacing the hand-maintained underscore list.\" The hyphen forms (run.ts:10471-... finalOutputProvenanceSectionBindings) are seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings.\n\nUnder the flip: claim_projection -> claim-projection, artifact_truth -> artifact-truth, provenance_footer -> runtime-artifact-truth-footer (renamed entirely). All three asserted literals at run.test.ts:4208-4210 cease to be array members, so arrayContaining FAILS. Therefore the design's success criterion line 49 (\"all run.test.ts final-output assertions still pass\") and the test plan line 55 (\"run.test.ts final-output assertions unchanged\") are FALSE for this change — internally contradicted by §3.3 line 36's own risk note (\"changing the prompt-policy ids (underscore->hyphen) alters the final-output PROMPT text\"). The test MUST be updated; G(c) is not a zero-test-change slice.\n\nSecondary observation also correct: `toEqual(expect.arrayContaining([...]))` is a subset matcher (run.test.ts:4207) — it passes regardless of extras or of OTHER dropped sections, so today it would not catch a dropped section. The exact-set authority should be the new parity guard, and tightening this to an exact ordered toEqual is a reasonable hardening that aligns test and guard semantics.\n\nScope of severity: this is a genuine design/success-criterion inconsistency that would surface as a CI test failure at build time, but the remediation is a bounded one-line test edit plus a doc amendment (list it as an expected test delta). It is not a runtime/soundness blocker, so medium rather than high. The recommendation in the claim (amend the design to mark the test delta; consider exact-set toEqual) is sound."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "medium",
      "title": "Guard scripts importing run.ts would violate the established scripts-layering pattern, and check-import-boundary cannot catch it (it scans only src/**)",
      "location": "scripts/check-import-boundary.ts:20-21,77-82 (SRC_ROOT only) vs design §3.2 (line 30) / Q3 (line 60); existing pattern scripts/reconstruct-pipeline-benchmark.ts:34-53 (imports small modules, never run.ts)",
      "claim": "The design's primary option is to export competencyQuestionAssessmentProjectionContract FROM run.ts and import it in scripts/check-prompt-projection-parity.ts. run.ts is ~13k lines with heavy transitive/side-effecting imports; no existing script imports it (they import small dedicated modules: benchmark-evidence.js, artifact-types.js, semantic-quality-gate.js). Worse, check-import-boundary.ts only walks SRC_ROOT (src/**, line 21,80), so a script→run.ts import is NOT structurally prevented — the boundary guard gives false comfort. Pulling run.ts into a guard process also risks executing module-level side effects.",
      "recommendation": "Resolve Q3 decisively toward EXTRACTION: move competencyQuestionAssessmentProjectionContract() + the 4 budget constants into a tiny side-effect-free module (e.g. competency-assessment-projection-contract.ts) that both run.ts and the guard import. Do NOT import run.ts from scripts/. Mirrors the existing benchmark-evidence.js pattern.",
      "verifier_reasoning": "CONFIRMED, with one minor overstatement. Code-grounded verification:\n\n1. check-import-boundary.ts scans ONLY src/** — CONFIRMED. `SRC_ROOT = path.join(PROJECT_ROOT, \"src\")` (line 21); `main()` walks `listSourceFiles(SRC_ROOT)` (line 80). scripts/ is never walked. A `scripts/check-prompt-projection-parity.ts` → `run.ts` import is structurally invisible to the boundary guard, so it gives false comfort. The guard's own rules (test-fixtures, mock-llm-realization, review→cli, tui→core-runtime) are all src-scoped; there is no scripts-layering rule at all.\n\n2. No existing script imports run.ts — CONFIRMED. `grep -rln 'from \"../src/core-runtime/reconstruct/run' scripts/` → NONE. The only run.ts reference in scripts/ is check-no-hardcoded-spec-defaults.ts:93 which lists it as a STRING path to scan, not an import. Existing scripts import small dedicated modules: reconstruct-pipeline-benchmark.ts imports benchmark-evidence.js, benchmark-failure-class.js, semantic-quality-gate.js, artifact-types.js, execution-telemetry.js, route-identity.js, and the core-api facade (createOntoReconstructCoreApi) — never run.ts. check-supported-models.ts imports settings-chain.js/supported-models.js. The benchmark-evidence.js pattern the recommendation cites is real.\n\n3. run.ts is large and heavy — CONFIRMED. 13561 lines; top-level imports include llm-caller.js (callLlm), the LLM layer, and ~30 reconstruct submodules. Pulling this graph into a deterministic structural guard is exactly the layering smell.\n\n4. Design surface — the claim says this is the design's \"primary option.\" Slightly overstated: design §3.2 line 30 phrases it as \"export ... from run.ts (or a small projection-contract module)\" and §7 Q3 (line 60) EXPLICITLY raises this as an open question (\"scripts importing run.ts\"). So the design does not firmly commit to the run.ts path — it flags it. This makes the finding less a defect-in-the-plan and more a confirmation that Q3 must be resolved toward extraction. The recommendation aligns precisely with the design's own uncertainty.\n\nMINOR OVERSTATEMENT: \"risks executing module-level side effects\" — run.ts has no top-level `await main()`/IIFE; it is a pure library module (verified: no top-level execution lines). The real cost is transitive graph weight (importing the LLM/reconstruct graph into a structural guard process), not literal side-effect execution. This does not undermine the core defect.\n\nNet: the layering risk is real, the boundary guard genuinely cannot catch a scripts→run.ts import, and the extraction recommendation is sound and concept-economical (mirrors benchmark-evidence.js). Medium severity is right: it is a pre-build design-decision risk that the design itself already flags (Q3), with a clear low-cost mitigation, not a hard correctness blocker.\n\nRelevant files: /Users/kangmin/cowork/onto-mcp-claude/scripts/check-import-boundary.ts (SRC_ROOT line 21, listSourceFiles(SRC_ROOT) line 80); /Users/kangmin/cowork/onto-mcp-claude/development-records/design/20260622-g-tractable-governance-parity-design.md (§3.2 line 30, Q3 line 60); /Users/kangmin/cowork/onto-mcp-claude/scripts/reconstruct-pipeline-benchmark.ts (imports lines 29-54); /Users/kangmin/cowork/onto-mcp-claude/scripts/check-invariant-drift.ts (GUARDS array, where the new guard would register)."
    },
    {
      "lens": "interactions",
      "severity": "medium",
      "title": "Loader Part 1 well-formedness parse must not silently allow the real conditional shape to be lossy; design's Q4 guessed field names are wrong",
      "location": "contract-registry.ts:695-710 parseValidatorRecord; registry conditional rows at reconstruct-contract-registry.yaml:2245-2249,2304-2311,2701-2705",
      "claim": "Design §3.1/Q4 propose conditional_validation_obligations as flat string[] or {obligation, required_when}. The actual YAML shape is {obligation_id, activation_condition, input_authority_refs[]} (e.g. registry :2246-2248). A flat string[] parse would drop activation_condition + input_authority_refs (the very fields that make a conditional obligation conditional), reintroducing the same 'parsed away' class the design is fixing. validation_obligations themselves are flat string ids (43 blocks), so string[] is correct for those only.",
      "recommendation": "Type conditional_validation_obligations as a bounded record {obligation_id:string, activation_condition:string, input_authority_refs:string[]} and parse all three fields; keep validation_obligations as string[]. Update §3.1/Q4 in the design with the real shape before build.",
      "verifier_reasoning": "VERIFIED REAL. The registry shape the claim cites is exactly correct. At .onto/processes/reconstruct/reconstruct-contract-registry.yaml:2245-2249, :2304-2313, and :2701-2705, every conditional_validation_obligations entry is `{obligation_id, activation_condition, input_authority_refs[]}` — three load-bearing fields (activation_condition = when the obligation applies; input_authority_refs = the artifacts it consumes). The design's Q4 (line 61) literally proposes \"flat string[] or a structured `{obligation, required_when}`\": both are factually wrong vs. the real field names (obligation_id / activation_condition / input_authority_refs, not obligation / required_when), and the flat-string[] option would drop the two fields that make a conditional obligation conditional — reintroducing the same \"parsed away\" loss class (codex H-B) that Part 1 of the design exists to fix per design line 13. Design line 21's phrasing \"a minimal well-formedness parse (string ids)\" reinforces the trap by conflating both arrays as plain string ids.\n\nThe claim's other factual leg also checks out: validation_obligations (e.g. registry :2236-2244, :2264-2272) ARE flat string-id arrays, so the recommendation to keep validation_obligations:string[] and type conditional_validation_obligations as the bounded 3-field record is exactly the correct resolution. Current ReconstructValidatorRecord (contract-registry.ts:134-140) has only 5 fields, and parseValidatorRecord (contract-registry.ts:695-710) drops both obligation arrays — the design's premise is accurate. I also confirmed the loader has no existing parser for conditional_input_authority_refs (a sibling conditional shape at :2283-2292 using artifact_ref/activation_condition/consumed_for), so there is no precedent helper to lean on; a new typed parse must be written.\n\nWHY MEDIUM, NOT HIGHER: the design did NOT commit to the wrong shape. Line 20 proposes `ConditionalValidationObligation[]` \"(or a bounded typed shape; inspect the 3 existing conditional entries)\" and Q4 ends \"Inspect and type minimally.\" — i.e. the shape is deliberately posed as an OPEN cross-validation question, exactly the right place to surface this, and Part 1 carries no gate (so a wrong parse can't silently fail a coverage check; G(a) is deferred). So this is a correct-and-actionable correction to a pre-build design's enumerated candidate shapes, not a shipped defect. The claim's own severity (medium) is well-calibrated; the design should be amended in §3.1/Q4 with the real shape {obligation_id:string, activation_condition:string, input_authority_refs:string[]} and parse all three fields before build."
    },
    {
      "lens": "interactions",
      "severity": "medium",
      "title": "Q3 import-boundary concern is real but under-specified: G1 guard does not police scripts/ importing run.ts, so 'safe' is not enforced — and run.ts is a ~13k-line module",
      "location": "scripts/check-import-boundary.ts (SRC_ROOT scan only) vs design §3.2/§3.3 new guards importing competencyQuestionAssessmentProjectionContract from run.ts",
      "claim": "Design §3.2/Q3 want to export the projection-contract fn from run.ts and import it from scripts/check-prompt-projection-parity.ts. check-import-boundary.ts scans only src/** (SRC_ROOT) and enforces review->cli; it does NOT forbid scripts/ -> src/core-runtime/reconstruct/run.ts. So nothing would FAIL, but a guard script importing the entire 13k-line run.ts (with its live LLM/provider/fs surface) pulls a heavy module-load graph into a CI guard and couples the guard to run.ts internals. The same risk applies to the append-sections guard needing finalOutputProvenanceSectionBindings / the prompt-policy literal — both currently live deep inside run.ts.",
      "recommendation": "Extract the projection contract (and the canonical append-section list) into a small dedicated module (e.g. reconstruct/projection-contracts.ts) that both run.ts and the guards import — the option the design lists second in Q3. This keeps guards off run.ts, respects concept-economy (single source), and avoids loading the runtime to run a static parity check. Resolve Q3 to 'extract module', not 'export from run.ts'.",
      "verifier_reasoning": "CONFIRMED real and code-grounded. Verified facts:\n\n1. Design §3.2 (line 30) primary instruction: \"export competencyQuestionAssessmentProjectionContract (+ budget constants) from run.ts ... prefer exporting the existing function over duplicating the field list.\" New guard scripts/check-prompt-projection-parity.ts (line 29) would import it. §3.3 (line 35) adds a second guard needing finalOutputProvenanceSectionBindings and the deterministic_runtime_append_sections literal.\n\n2. scripts/check-import-boundary.ts (G1) scans ONLY SRC_ROOT = PROJECT_ROOT/src (line 21, listSourceFiles(SRC_ROOT) at line 80). Its rules: test-fixtures, mock-llm-realization, review->cli, tui->core-runtime. There is NO rule covering scripts/ as importer (grep \"scripts\" in the file = none). So a guard importing run.ts would NOT fail any guard — the claim's \"safe is not enforced\" is accurate (the design itself never claims G1 enforces it; it leaves Q3 open).\n\n3. Importing run.ts is genuinely heavy: src/core-runtime/reconstruct/run.ts is 13,561 lines; line 1 imports node:crypto, line 2 node:fs/promises, line 92 imports callLlm from ../llm/llm-caller.js — i.e. the live LLM/provider/fs surface. A static CI parity guard would module-load this entire graph.\n\n4. Both functions the guards need are currently NON-exported internals of run.ts: competencyQuestionAssessmentProjectionContract (run.ts:800), finalOutputProvenanceSectionBindings (run.ts:10394), deterministic_runtime_append_sections literal (run.ts:4447). The append-sections guard faces the identical coupling, as the claim states.\n\n5. Precedent supports the recommendation: existing scripts (effort-calibration-report.ts, reconstruct-pipeline-benchmark.ts) already import from core-runtime, but always from SMALL focused modules (route-identity.js, benchmark-evidence.js, execution-telemetry.js) — never from run.ts. The module-extraction option is the established pattern.\n\n6. The claim fairly acknowledges the design already lists extract-module as Q3's second alternative (design line 30 \"(or a small projection-contract module)\", Q3 line 60). The defect is that §3.2's primary instruction biases toward the run.ts-export path and Q3 is left unresolved; the recommendation (resolve Q3 to \"extract reconstruct/projection-contracts.ts that both run.ts and guards import\") is correct and respects concept-economy (single source).\n\nSeverity medium is right: this is design-hygiene/architecture coupling, not a correctness blocker, and the corrective option is already named in the design. Minor framing nuance (\"safe is not enforced\") slightly overstates an implied claim, but the substance is fully real. Note (adjacent, not the assigned claim): run.test.ts:4204-4211 asserts deterministic_runtime_append_sections contains the UNDERSCORE ids (claim_projection/artifact_truth/provenance_footer), so the §3.3 underscore->hyphen flip WILL break that test — the design's Q2/⚠️ flags this but understates it as merely \"to be verified\"; it will require a test update, since the literal sits inside final_output_prompt_policy (prompt surface)."
    },
    {
      "lens": "interactions",
      "severity": "medium",
      "title": "Scope: Part 1 (loader) is no longer the low-risk piece due to G-1; Part 3 (G(c) reconcile) carries the rename + new-concept risk — Q5 split should land in favor of splitting",
      "location": "design §3.1 (Part 1), §3.3 (Part 3), Q5",
      "claim": "The design treats Part 1 as trivially safe and leaves Q5 (split G(c)) open. Given G-1 (loader rotates snapshot+reuse hashes) and G-3/G-4 (G(c) needs 2 real renames + a brand-new section concept + run.test.ts edits), bundling loader + G(b) + G(c) in one PR mixes three independent regression surfaces (snapshot/reuse hash, projection-contract parity, final-output prompt+provenance) whose failures are hard to bisect.",
      "recommendation": "Split: PR-A = loader extension + the snapshot-projection fix (G-1) + obligation-shape (G-5), proven by loader + governing-snapshot + reuse tests. PR-B = G(b) projection-parity (with the extracted module, G-6). PR-C = G(c) append-section reconciliation (id mapping G-3, unresolved-revision-proposals concept G-4, run.test.ts update G-2). Resolve Q5 = split.",
      "verifier_reasoning": "CONFIRMED (directionally correct; the G-1 sub-premise is overstated but does not defeat the recommendation).\n\nG-1 (loader change touches a hash) — REAL but narrow. Adding `validation_obligations`/`conditional_validation_obligations` to the parsed `ReconstructValidatorRecord` (src/core-runtime/reconstruct/contract-registry.ts:703-709 `parseValidatorRecord`) flows into the governing snapshot: governing-snapshot.ts:576-580 places the full parsed `args.contractRegistry.validator_records` into `snapshotFamily`, and `snapshotFamily` hashes it at line 445 via `sha256Text(stableJson(value))`; `stableJson` (lines 30-40) serializes every key, so the `validator_records` family sha256 WILL rotate. The design's §3.1 omits this entirely — it only promises \"obligations present on the parsed record\" and frames Part 1 as gate-free/trivial, missing the snapshot-family reconciliation site. So the claim's premise that Part 1 is not a zero-surface change is code-confirmed.\n\nHowever the claim's wording overstates blast radius: (1) `registry_sha256` (governing-snapshot.ts:530) hashes RAW YAML text, which already contains the obligations, so it is UNCHANGED by the loader edit — the \"reuse hash\" half of \"snapshot+reuse hashes\" is wrong. (2) At the live runtime path, `validateReconstructRunGoverningSnapshot` (governing-snapshot.ts:803) rebuilds `expected` from the same registry, so both compared sides include the new field and always match — NO live regression and NO failing CI test (post-seed-validation.test.ts uses `snapshot_families: []`; no fixture/golden records a validator_records family hash). The only breakage is a pre-change recorded manifest replayed against unchanged YAML reaching the family comparison at lines 848-850. So Part 1's real risk is one snapshot-family hash with no current test/live break — smaller than the finding implies.\n\nG(c)/Part 3 risk — CONFIRMED. The two divergent id lists are real: underscore prompt-policy list at run.ts:4448-4452, hyphen provenance sections at run.ts:10473-10601, and `unresolved-revision-proposals` is in NEITHER (grep empty), so G(c) genuinely requires 2 underscore→hyphen renames, a brand-new section concept, 3-representation reconciliation, and exposure to the final-output PROMPT surface plus ~111 final-output/provenance assertion lines in run.test.ts. The design itself flags this (§3.3 ⚠️ and Q5 open).\n\nNet: the recommendation (resolve Q5 = split; isolate G(c)'s rename+new-concept+prompt-surface risk from loader/G(b)) is sound and code-grounded, since the three surfaces (snapshot-family hash, projection-contract parity, final-output prompt/provenance) fail independently and are hard to bisect together. Medium is appropriate: this is a pre-build scope-hygiene finding, not a correctness blocker, and the actionable core (split G(c)) is correct even though the \"Part 1 is no longer low-risk\" framing is partly overstated."
    },
    {
      "lens": "authority-economy",
      "severity": "medium",
      "title": "G(b) should NOT export from run.ts; extract a focused projection-contract module to match the established guard-imports-runtime pattern",
      "location": "design §3.2/Q3; run.ts:800 competencyQuestionAssessmentProjectionContract (currently non-exported); precedent scripts/check-supported-models.ts:36-44",
      "claim": "Design §3.2/Q3 leaves open 'export from run.ts vs tiny module' and leans toward exporting from run.ts ('prefer exporting the existing function'). run.ts is 13,561 lines; a guard importing it pulls the entire reconstruct module graph (artifact-io, yaml, every reconstruct dep) at CI time. The established pattern (G7 check-supported-models.ts) imports the gate/registry from FOCUSED modules — settings-chain.ts and supported-models.ts — explicitly to 'call the very same gate the runtime uses' without dragging the world. contract-registry.ts is similarly lightweight (4 std imports + 1 local). No existing scripts/check-*.ts imports run.ts.",
      "recommendation": "Resolve Q3 as: extract competencyQuestionAssessmentProjectionContract + its 4 budget constants into a small module (e.g. competency-question-projection-contract.ts, sibling to seed-claim-projections.ts), have run.ts import from it, and have the guard import from that module. This matches G7, respects the spirit of the import boundary, and keeps the guard cheap. Note: scripts/check-import-boundary.ts only scans src/ (SRC_ROOT), so it will NOT catch a script→run.ts import — the boundary is a design discipline here, not a mechanically enforced one, which is exactly why following the focused-module precedent matters.",
      "verifier_reasoning": "VERDICT: REAL, but severity is medium (not high) — it is a sound design-discipline finding, not a correctness blocker.\n\nEvery factual claim checks out against the code:\n- run.ts is 13,561 lines (confirmed: `wc -l`). `competencyQuestionAssessmentProjectionContract` at run.ts:800 is NON-exported (`function competencyQuestionAssessmentProjectionContract(): Record<string, unknown>`, no `export`). Its 4 budget constants are adjacent (run.ts:786-798), and `...ProjectionContractSha256` (843), `...PromptPolicy` (847) depend on it — a cohesive cluster, clean to extract.\n- contract-registry.ts is lightweight: 4 std imports + 1 local (`target-material-kind.js`), 1,319 lines — as claimed.\n- G7 precedent (scripts/check-supported-models.ts:36-44) imports from FOCUSED modules `discovery/settings-chain.js` + `discovery/supported-models.js`, not run.ts, and its docstring explicitly says it calls \"the very same gate the runtime uses\" — exactly the focused-module-parity pattern the claim cites.\n- No existing scripts/check-*.ts imports run.ts. The one run.ts reference (check-no-hardcoded-spec-defaults.ts:93) is a STRING LITERAL in a config object (file scanned as text), not an `import` — so it does not pull run.ts's module graph. Confirmed.\n- check-import-boundary.ts iterates `listSourceFiles(SRC_ROOT)` only (SRC_ROOT = PROJECT_ROOT/src), so scripts/ is NOT scanned — a script→run.ts import would NOT be mechanically caught. The claim's caveat is correct.\n- A sibling focused-module precedent already exists: `seed-claim-projections.ts` (12KB), and run.ts ALREADY imports from it (run.ts:218 `} from \"./seed-claim-projections.js\";`). So the recommended pattern (run.ts imports a focused projection module; guard imports the same module) is structurally consistent with existing code — strengthens the recommendation.\n- Design quotes are accurate: §3.2 line 30 says \"prefer exporting the existing function over duplicating the field list\" and Q3 leaves \"export from run.ts vs tiny module\" open. The claim that the design \"leans toward exporting from run.ts\" is fair.\n\nOne refinement to the claim's cost argument (does not change the verdict): GUARDS in check-invariant-drift.ts (GUARDS array, lines 30-63) run each guard as a separate `npx tsx` SUBPROCESS via execFileAsync, not in one process. So the cost is not a shared-graph penalty but a per-guard-process module-load cost. Either way, importing run.ts pulls the full reconstruct graph (artifact-io, yaml, all deps) into the guard's process on every CI run, which the focused-module pattern avoids — the claim's conclusion holds.\n\nSeverity calibration: the design itself flags this exact concern as open question Q3 (\"which respects import boundaries (scripts importing run.ts)?\"), so the design has NOT committed the mistake yet — the claim is the correct resolution of an already-open question, not the discovery of a latent bug. Because the boundary is unenforced (scripts/ unscanned), getting Q3 wrong would silently bloat CI and drift from the established G7 pattern without any guard catching it — a real authority-economy/concept-economy cost — but it is reversible and non-functional. Hence medium, not high. The recommendation (extract competency-question-projection-contract.ts as a sibling to seed-claim-projections.ts; run.ts and the guard both import it) is correct and well-grounded."
    },
    {
      "lens": "authority-economy",
      "severity": "medium",
      "title": "Q4 conditional shape is wrong: conditional_validation_obligations are structured objects with a per-row consumed_for, and a SEPARATE conditional_input_authority_refs field also exists",
      "location": ".onto/.../reconstruct-contract-registry.yaml:2245-2249, 2304-2313, 2283-2292; design §3.1/Q4",
      "claim": "Design §3.1 calls conditional_validation_obligations a flat parse and Q4 asks 'flat string[] or {obligation, required_when}'. The real YAML shape is `{obligation_id, activation_condition, input_authority_refs: string[]}` (lines 2245-2249, 2304-2313) — three keys, with input_authority_refs as a nested array. Additionally the ontology-seed validator carries a DISTINCT field `conditional_input_authority_refs` with shape `{artifact_ref, activation_condition, consumed_for: string[]}` (2283-2292) that the design never mentions. Typing conditional_validation_obligations as flat string[] (the design's leaning) would lose activation_condition + input_authority_refs and mis-parse the data G(a) later needs.",
      "recommendation": "Type ConditionalValidationObligation as { obligation_id: string; activation_condition: string; input_authority_refs: string[] }. Separately decide whether to also preserve conditional_input_authority_refs ({artifact_ref, activation_condition, consumed_for}) — it is a different concept (input gating, not obligation gating) and is currently also dropped by parseValidatorRecord; scope it explicitly in or out. Inspect both before fixing the count: design says '3 conditional entries' but there are 3 conditional_validation_obligations blocks AND 1 conditional_input_authority_refs block.",
      "verifier_reasoning": "CONFIRMED against the YAML. Two distinct sub-claims both verify:\n\n1) conditional_validation_obligations real shape = {obligation_id, activation_condition, input_authority_refs: string[]} — three keys, nested array. Verified at reconstruct-contract-registry.yaml:2245-2249 (candidate-disposition-validator) and 2304-2313 (ontology-seed-validator), plus a third block at 2701. The design's Q4 (note line 61) guesses 'flat string[] or {obligation, required_when}' — wrong key names AND missing input_authority_refs. The design's leaning toward flat string[] (§3.1 line 20-21 'string ids') would drop activation_condition + input_authority_refs, the exact substrate the deferred G(a) obligation-coverage proof later needs (it must know each conditional obligation's activation predicate and gated input authority). So mis-typing here is not cosmetic; it corrupts the data G(a) consumes.\n\n2) A SEPARATE field conditional_input_authority_refs exists with shape {artifact_ref, activation_condition, consumed_for: string[]} — verified at 2231-2235 and 2283-2292 (and 7 more occurrences: 2208, 2430, 2658, 2687, 2791, 2858, 3007). It is a different concept (input-artifact gating, not obligation gating) and is ALSO dropped by parseValidatorRecord (contract-registry.ts:703-709, which keeps only validator_id/gate_ids/validator_version/input_authority_refs/output_ref). The design §2 (line 13) and §3.1 enumerate exactly which fields the loader preserves and never mentions conditional_input_authority_refs — a genuine blind spot in the field-parity scope, sharing the same drop site and the same 'preserve the data' rationale.\n\nMinor inaccuracy in the claim that does NOT undermine it: it says 'there are 3 conditional_validation_obligations blocks AND 1 conditional_input_authority_refs block.' Actual conditional_input_authority_refs count is ~9 (grep: 2208/2231/2283/2430/2658/2687/2791/2858/3007). This understates rather than overstates the omission, so the finding is if anything stronger than stated.\n\nSeverity medium (not higher): the design explicitly instructs 'inspect the 3 existing conditional entries' / 'inspect and type minimally' (§3.1, Q4), so a builder is gated on looking at the YAML before typing — a partial backstop for sub-claim 1. But Q4's stated wrong shape would propagate if taken at face value, and conditional_input_authority_refs is not flagged for inspection at all, so the omission is a true gap. The recommendation (type ConditionalValidationObligation as {obligation_id, activation_condition, input_authority_refs: string[]} and explicitly scope conditional_input_authority_refs in/out) is correct and actionable. Not a blocker because it's a pre-build design with an inspection step, and G(a) consumption is explicitly deferred."
    },
    {
      "lens": "authority-economy",
      "severity": "medium",
      "title": "unresolved-revision-proposals has no section_id in code today; the registry would assert a section identity the runtime does not emit",
      "location": "run.ts:9981-10018 appendFinalOutputUnresolvedRevisionSection (uses heading '## Unresolved Revision Proposals' via upsertMarkdownSection, no section_id); design §3.3 wants registry id 'unresolved-revision-proposals'",
      "claim": "Design §3.3 requires the registry final_output_append_sections list to INCLUDE `unresolved-revision-proposals` and the guard to assert its presence. But appendFinalOutputUnresolvedRevisionSection emits only a markdown heading 'Unresolved Revision Proposals' — there is no `section_id: unresolved-revision-proposals` anywhere in the codebase (grep confirms zero hits). It is also NOT in either of the two reconciled lists (prompt-policy or provenance bindings). So the guard's clause (c) 'unresolved-revision-proposals is present' would assert against a section that has no runtime section_id binding — the guard can only check the registry contains the literal, which is vacuous self-agreement unless it also ties to a runtime emission.",
      "recommendation": "Decide the binding point for this section: either give the M4a section a real section_id constant in run.ts:9981 and assert the registry id equals it, or classify it in the registry as append_owner=runtime with NO provenance binding and have the guard assert the runtime function emits its heading. Without one of these, the 'present' check is a vacuous/foolable guard (it only checks the registry agrees with itself).",
      "verifier_reasoning": "Confirmed real and code-grounded. Facts verified: (1) src/core-runtime/reconstruct/run.ts:9981 appendFinalOutputUnresolvedRevisionSection emits ONLY the markdown heading \"## Unresolved Revision Proposals\" via upsertMarkdownSection (markdown-section.ts, which keys purely on the heading line) — there is no section_id in that emission path. (2) The hyphen literal `unresolved-revision-proposals` has zero hits in src/ (grep confirmed). (3) The 5 provenance-binding section_ids at run.ts:10473/10484/10580/10593/10601 are seed-answerability, artifact-truth, claim-projection, runtime-artifact-truth-footer, runtime-provenance-bindings — the unresolved section is absent. (4) The prompt-policy deterministic_runtime_append_sections (run.ts:4447-4453) has 5 underscore ids, none for the unresolved section. The design itself admits (§2 line 15) the section is \"in NEITHER list.\"\n\nThe defect in the DESIGN is genuine: §3.3 line 33 says the registry list must INCLUDE `unresolved-revision-proposals`, and guard clause (c) at line 35 asserts only that it \"is present.\" Because the runtime emits this section by heading string with no section_id, and the design proposes no runtime tie for it (no section_id constant, no assertion that appendFinalOutputUnresolvedRevisionSection emits the heading), clause (c) reduces to \"the registry list, which the guard itself loads, contains a hardcoded literal\" — vacuous self-agreement that proves nothing about runtime behavior. That is a foolable guard clause. The recommendation (classify append_owner=runtime / provenance_binding_required=false and have the guard assert the runtime function emits the heading) is correct.\n\nSeverity medium, not higher: the design's own registry schema (line 33) already includes append_owner and provenance_binding_required, and clause (b) already excludes \"declared non-binding sections,\" so the author is aware this section has no provenance binding and the structural slots for the fix already exist; the fix is small and the weakness is bounded to one section. Existing tests (run.test.ts:6508-6560) assert the heading literal, giving a ready runtime anchor the guard could bind to. Not a blocker/high since no runtime regression results — it is a verification-value gap (false assurance) in one guard clause, which is exactly a medium-grade foolable-guard finding."
    },
    {
      "lens": "authority-economy",
      "severity": "medium",
      "title": "Scope: G(c) reconciliation should split from G(b)+loader (resolve Q5 as split)",
      "location": "design §3.3 + Q5; reconcile sites run.ts:4447, 10472-10612, 10606-10611, run.test.ts:4204-4211",
      "claim": "Q5 asks whether G(c) is too large to share a PR with G(b)+loader. Evidence says yes: G(c) touches 3+ load-bearing sites (prompt list, binding section_ids, binding required_fragments) plus at least one existing test, has an unresolved id-mapping (G-2), an unbound new section id (G-8), and a real provenance-gate risk (G-3). G(b)+loader are clean, additive, low-risk (parse 2 optional arrays; declare+parity-guard a field set with no runtime text change). Bundling G(c)'s risk with the safe parts jeopardizes the whole PR and muddies review.",
      "recommendation": "Split: PR1 = loader extension (Part 1) + G(b) field-parity (Part 2). PR2 = G(c) append-sections reconcile, after the id-mapping (G-2), binding-id decision (G-3), and unresolved-section binding (G-8) are settled. The design already isolates G(a); isolate G(c) too.",
      "verifier_reasoning": "VERIFIED REAL (medium). The scope-split claim is code-grounded and the risk asymmetry is accurate.\n\nG(c) is genuinely the heavy, risky part:\n- It touches 3+ load-bearing sites with two divergent id-spaces: prompt-policy underscore ids at run.ts:4447-4453 (`deterministic_runtime_append_sections` in `final_output_prompt_policy`, which feeds the host-LLM final-output payload — i.e. real prompt surface), and the hyphen provenance bindings in `finalOutputProvenanceSectionBindings` (run.ts:10471-10613) whose `section_id`s, `required_fragments` path lists, and the self-referential `runtime-provenance-bindings.required_fragments` literal hyphen strings (run.ts:10606-10611) are emitted-text gate material (G-3 risk confirmed real).\n- It WILL break an existing test: run.test.ts:4205-4211 asserts the UNDERSCORE ids via `expect.arrayContaining([\"claim_projection\",\"artifact_truth\",\"provenance_footer\"])`. The design's canonical-hyphen decision (§3.3) flips these (e.g. provenance_footer → runtime-artifact-truth-footer), breaking that assertion. The design's own success criterion §5 (\"all run.test.ts final-output assertions still pass\") is then in direct tension with §3.3 unless this test is reconciled. Real, concrete.\n- G-2 (underscore↔hyphen mapping unresolved), G-8 (M4a section unbound) both confirmed: the M4a disclosure is `## Unresolved Revision Proposals` (run.ts:9996, appendFinalOutputUnresolvedRevisionSection), a bare markdown heading present in NEITHER list — so the new registry node must invent its canonical section_id and bind it. Real.\n\nG(b)+loader are genuinely clean/additive:\n- Loader: parseValidatorRecord (contract-registry.ts:703-709) currently drops `validation_obligations`/`conditional_validation_obligations`; the YAML data already exists (registry lines 1992+, 3 conditional entries). Adding 2 optional array fields to ReconstructValidatorRecord (interface at contract-registry.ts:134-140) + parse defaults is low-risk, no runtime-text change.\n- G(b): registry node + parity guard + 1 export; no prompt/output text change.\n\nSo bundling G(c)'s prompt-surface + provenance-gate + test-breaking risk with the safe additive parts does jeopardize the whole PR — the recommendation to split is sound and code-justified.\n\nCALIBRATION (why medium, not high): this is not a latent defect the design overlooked — the design EXPLICITLY raises this as Q5 (\"is G(c)'s reconciliation too large/risky… should G(c) split?\") and already flags every cited risk (§3.3 ⚠️ Risk note, Q1, Q2, Q3). The claim is effectively answering Q5 = 'yes, split'. That is a strong, well-supported review recommendation that improves PR safety, but it resolves an open design question rather than catching a hidden flaw. It does not make the design wrong.\n\nMinor location nits (do not change the verdict): claim cites run.test.ts:4204-4211 (assertion is 4205-4211) and run.ts:10472-10612 (binding array 10471-10613); both resolve correctly. Also worth noting for the build: the design's G(b)/Q3 plan to import competencyQuestionAssessmentProjectionContract from run.ts into scripts/check-prompt-projection-parity.ts would be the first scripts→run.ts import (no current script imports run.ts; the G1 check-import-boundary.ts only restricts review→cli + mock boundaries, so it is NOT a guard violation, but it pulls the ~13k-line run.ts graph into a CLI guard — supports extracting a tiny projection-contract module, which the split also makes easier to sequence)."
    },
    {
      "lens": "scope-risk",
      "severity": "medium",
      "title": "Bundling G(c) (load-bearing 3-representation reconciliation) with loader + G(b) is too large for one PR — split G(c)",
      "location": "design §3.3 + Q5; touches run.ts:4447, run.ts:10394-10614, run.test.ts:4204, post-seed-validation provenance path",
      "claim": "Loader extension is pure additive parse of dead data (zero behavioral effect, confirmed: no TS reads validation_obligations). G(b) adds a registry node + guard + 1 export and does NOT mutate the contract object, so it cannot rotate the reuse hash (the hash flows from competencyQuestionAssessmentProjectionContractSha256() at run.ts:1199-1200, which G(b) leaves untouched). Both are low-risk. G(c) is categorically different: it reconciles the final-output provenance surface (the validated, load-bearing path), mutates prompt text (F2), breaks a test, and risks the heading-parity gap (F1). Mixing a no-op loader + safe field-parity guard with a load-bearing prompt/provenance mutation in one PR makes review and rollback coarse.",
      "recommendation": "Land PR A = loader extension + G(b) (both safe, hash-neutral, no prompt change). Land PR B = G(c) reconciliation alone, where the prompt-surface and provenance risk is isolated and reviewable. The design already raises this as Q5; the recommended default should be SPLIT, not 'either way'.",
      "verifier_reasoning": "CONFIRMED real, code-grounded scope-risk finding. The defect's factual asymmetry holds up against the code:\n\nPR A side (safe):\n- Loader extension is a pure additive parse of dead data. `grep -rn \"validation_obligations|conditional_validation_obligations\" src/` returns ZERO references (no reads, no test assertions). `parseValidatorRecord` (contract-registry.ts:695-710) currently drops them; adding optional arrays has no behavioral effect.\n- G(b) is hash-neutral. The reuse-match hash (`authoredArtifactReuseMatch`, run.ts:1167-1200) incorporates `competency_question_assessment_projection_contract_sha256()` (run.ts:1199-1200, sha at 843-845). G(b) per design §3.2 only EXPORTS the existing `competencyQuestionAssessmentProjectionContract()` (run.ts:800-841) and adds a parity guard; it does not mutate the contract object, so the sha and reuse hash are untouched. Confirmed.\n\nPR B side (load-bearing):\n- G(c) mutates the LLM prompt surface. `deterministic_runtime_append_sections` (run.ts:4447-4453, underscore ids) lives inside `final_output_prompt_policy`, which is part of the final-output authoring user payload serialized via `JSON.stringify(args.userPayload, null, 2)` (run.ts:6435). Flipping underscore→hyphen changes the prompt sent to the host LLM (F2 accurate).\n- G(c) breaks an existing test. run.test.ts:4204-4211 asserts `deterministic_runtime_append_sections` contains the underscore ids `claim_projection`, `artifact_truth`, `provenance_footer`. The reconciliation breaks this.\n- Heading-parity risk (F1) is real: `finalOutputProvenanceSectionBindings` (run.ts:10471-10613) already uses hyphen ids, and `runtime-provenance-bindings.required_fragments` (run.ts:10606-10611) hard-lists the hyphen section ids as REQUIRED text fragments, so the canonical set must match both the prompt-policy derivation and these required fragments or provenance validation breaks.\n\nThe asymmetry the defect draws (A = behavior/test/hash-neutral; B = prompt-mutating + test-breaking + load-bearing provenance path) is accurate. Bundling a no-op loader + safe field-parity guard with a prompt/provenance mutation in one PR genuinely makes review and rollback coarse. The design itself raises this as Q5 but leaves it 'either way'; recommending SPLIT as the default is sound.\n\nRefinements that strengthen (not weaken) the finding: (1) The design's Q2 and this defect both worry G(c) might rotate a reuse hash — verification shows `final_output_prompt_policy` does NOT flow into the reuse-match hash, so the only G(c) blast radius is the LLM prompt text + run.test.ts:4204 + provenance required_fragments, not reuse invalidation. The split rationale still stands on the prompt/test/provenance grounds. (2) Note for completeness: PR A is NOT free of the 'scripts importing run.ts' boundary question (G(b) requires the export + a guard script that calls the runtime contract), but `check-import-boundary.ts` only scans `src/**` not `scripts/**`, so no gate flags it — this is the design's Q3, orthogonal to the split decision and not a fault in the split recommendation.\n\nSeverity medium (not high): this is a reviewability/rollback-granularity recommendation, not a correctness bug — a carefully implemented bundled PR would still be correct. The split is the better default but the bundle is not broken per se."
    },
    {
      "lens": "scope-risk",
      "severity": "medium",
      "title": "conditional_validation_obligations is a structured object array, NOT string[]; design Q4 must resolve to the structured shape and disambiguate from two other conditional fields",
      "location": ".onto/processes/reconstruct/reconstruct-contract-registry.yaml:2245-2249, 2304-2313, 2701; design §3.1 + Q4",
      "claim": "The 3 conditional_validation_obligations entries are {obligation_id, activation_condition, input_authority_refs[]} objects (e.g. yaml 2246-2248), not flat strings. Design line 20 hedges 'ConditionalValidationObligation[] (or a bounded typed shape; inspect the 3 entries)' which is correct, but Q4 still poses 'flat string[] or structured {obligation, required_when}?' — the real field names are obligation_id / activation_condition / input_authority_refs, not obligation/required_when. Separately, the YAML also has DISTINCT sibling fields the design does not mention: conditional_input_authority_rule (yaml:2048, a free-text string) and conditional_input_authority_refs (yaml:2283-2292, a different object array). A loader extension that only parses validation_obligations + conditional_validation_obligations will silently keep dropping those two, re-introducing the exact 'declared-but-not-loaded' class the design says G closes.",
      "recommendation": "Type ConditionalValidationObligation as {obligation_id: string; activation_condition: string; input_authority_refs: string[]} matching the YAML. Either also parse conditional_input_authority_rule and conditional_input_authority_refs (to fully close the loader-drop class) or explicitly scope them out in the design so they are not assumed loaded. Drop the 'obligation/required_when' wording from Q4.",
      "verifier_reasoning": "CONFIRMED against the actual files. Every factual assertion in the claim checks out.\n\nField shape (TRUE): The 3 `conditional_validation_obligations` entries are structured objects, not string[]. Verified at .onto/processes/reconstruct/reconstruct-contract-registry.yaml:2245-2249 (candidate-disposition-validator), :2304-2313 (ontology-seed-validator), :2701-2704 (maturation-answer-claims-validator). Shape is {obligation_id, activation_condition, input_authority_refs[]}. `grep -c` confirms exactly 3 occurrences. The design's Q4 (line 61) literally proposes \"flat string[] or a structured {obligation, required_when}\" — the field names obligation/required_when do NOT exist anywhere; the real names are obligation_id/activation_condition/input_authority_refs. So Q4's wording is factually wrong on the field names. Note: design line 20 already hedges correctly (\"or a bounded typed shape; inspect the 3 existing conditional entries\"), so the implementer is steered to inspect — this softens (but does not eliminate) the Q4 wording defect.\n\nOmitted sibling fields (TRUE, the more material point): The YAML has distinct sibling conditional fields the design's loader scope (§3.1) never names: `conditional_input_authority_rule` (free-text string, yaml:2048 — grep confirms 1 true occurrence; the other 2 grep hits at 2429/2857 are the different `dynamic_input_authority_rule` field) and `conditional_input_authority_refs` (object array {artifact_ref, activation_condition, consumed_for[]}, yaml:2283-2292 and 8 other validators — grep -c = 9). There is also `dynamic_input_authority_rule` (2x). \n\nLoader-drop re-introduction (TRUE): parseValidatorRecord (contract-registry.ts:703-709) keeps ONLY validator_id/gate_ids/validator_version/input_authority_refs/output_ref. ALL of validation_obligations, conditional_validation_obligations, conditional_input_authority_rule, conditional_input_authority_refs, dynamic_input_authority_rule are parsed away. The design §1/§5 frames G as closing the \"declared in registry but not loader-enforced\" class, yet §3.1 scopes the loader extension to only validation_obligations + conditional_validation_obligations — leaving 3+ other declared field families silently dropped, partially re-creating the exact drift class the design claims to close.\n\nSeverity = medium is appropriate: this is a pre-build design scope/completeness gap, not a runtime bug. The line-20 hedge means the structured shape will likely be caught at build time, so the Q4 wording alone would be low. The actionable core is the unmentioned conditional_input_authority_rule / conditional_input_authority_refs fields, which need an explicit scope decision (parse them too, or explicitly scope them out) so the design's own \"G closes the drift class\" framing is honest. Not blocker/high because nothing here causes incorrect runtime behavior — G(a) coverage gate is already deferred, and the dropped fields remain unenforced exactly as they are on main today.\n\nRecommendation as stated is sound: type ConditionalValidationObligation as {obligation_id; activation_condition; input_authority_refs[]}, either also parse the two conditional_input_authority_* fields or explicitly scope them out in §3.1, and drop the obligation/required_when wording from Q4."
    },
    {
      "lens": "scope-risk",
      "severity": "medium",
      "title": "G(b) export of competencyQuestionAssessmentProjectionContract from run.ts would be the first script->run.ts (13561-line) import; extract a focused module instead",
      "location": "src/core-runtime/reconstruct/run.ts:800 (currently un-exported); precedent: scripts/*.ts import focused modules (settings-chain.js, supported-models.js, semantic-quality-gate.js), benchmark imports core-api facade — none import run.ts",
      "claim": "No existing script imports run.ts; all import small focused modules or the reconstruct-api facade. run.ts is 13561 lines with 46 exports and is side-effectful. The import-boundary guard (scripts/check-import-boundary.ts) scans only src/** and would NOT catch a scripts/->run.ts import, so it would not be mechanically blocked — but it would still pull the entire module graph into a CI guard for the sake of one pure function. The design's Q3 raises exactly this and prefers extracting a tiny projection-contract module; that is the correct call and should be the stated default, not an open question.",
      "recommendation": "Default to extracting competencyQuestionAssessmentProjectionContract() + its budget constants into a small projection-contract module imported by both run.ts and the guard. Do not export from run.ts. Concept economy is preserved (single source of truth) and the script never imports the 13k-line module.",
      "verifier_reasoning": "CONFIRMED REAL. All load-bearing facts verified against code:\n\n1. No script imports run.ts. The only `scripts/` reference to it is a string-literal path in `scripts/check-no-hardcoded-spec-defaults.ts:91` (a redaction-policy allowlist entry), NOT an import. Confirmed via grep of scripts/.\n\n2. Precedent is focused modules / the facade. scripts/*.ts import `discovery/settings-chain.js` (5x), `discovery/supported-models.js`, `reconstruct/semantic-quality-gate.js`, `route-identity.js`, and the `core-api/reconstruct-api.js` facade — never run.ts.\n\n3. `competencyQuestionAssessmentProjectionContract()` at run.ts:800 is un-exported (plain `function`, absent from run.ts's 46 exports). The G(b) guard needs to call it, so the design must create an import path.\n\n4. The import-boundary guard would NOT catch a scripts→run.ts edge. scripts/check-import-boundary.ts:21,80 scans only `SRC_ROOT` (`src/`) via `listSourceFiles(SRC_ROOT)`; scripts/ is out of scope. So this is unenforced — exactly as the claim states — meaning the bad edge is possible and silent.\n\n5. run.ts is 13561 lines / 46 exports (confirmed exactly). The G(b) guard pulling that whole module graph (35 transitive imports) for one pure function is a real, accurately-stated cost.\n\n6. The design genuinely leaves this as an OPEN question, and §3.2 (run.ts:30) even leans the WRONG way: \"export ... from run.ts (or a small projection-contract module). Concept economy: prefer exporting the existing function over duplicating the field list.\" Q3 (line 60) frames module-vs-run.ts-export as undecided. The claim's recommendation — make module extraction the stated default — is correct.\n\n7. Extraction is clean and feasible: competencyQuestionAssessmentProjectionContract() is pure, depending only on the 4-5 budget constants (run.ts:786-798); its sha/policy wrappers depend on run.ts-local stableJson/sha256Text but the guard does not need them. Moving the constants + pure function to a small module (run.ts imports them back; the 7/4/2/5 in-file uses resolve via the back-import) preserves a single source of truth — concept economy intact.\n\nMinor inaccuracy in the claim, non-fatal: it calls run.ts \"side-effectful,\" but run.ts has zero top-level executable statements, so importing it would not RUN anything. The real harm is graph weight + precedent, not side-effects-on-import. This overstatement does not undermine the defect.\n\nSeverity medium (not higher): no guard enforces it, so it is not a mechanical blocker, and it is a precedent/maintainability concern rather than a correctness bug — but it IS the first scripts→run.ts edge and the design's own §3.2 nudges toward the worse option, so it warrants a committed default before build rather than info-only."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "low",
      "title": "New guards have no INV id mapping and the design doesn't specify their GUARDS entry shape — risk of a guard that is registered but exempt from CI failure semantics",
      "location": "scripts/check-invariant-drift.ts:30-63 (GUARDS entries require {guard,invariants,command}) vs design §3.2/§3.3 ('Register in check-invariant-drift.ts')",
      "claim": "GUARDS entries carry an `invariants: string[]` field tying each guard to INV ids (line 33,38,43,55,60). The two new guards close a 'declared-but-not-enforced' drift class that maps to no existing INVARIANT id. The design says 'register in GUARDS' but does not state which INV ids these guards assert, nor confirm that a non-zero exit from the new scripts propagates (runGuard at lines 65-79 does treat any throw/non-zero as ok:false → drift → exit 1, so propagation is fine PROVIDED the scripts exit non-zero on failure). The gap is the missing invariant binding, which is how this repo declares what a guard protects.",
      "recommendation": "Either introduce a new invariant id (e.g. INV-REGISTRY-PARITY-1) in INVARIANTS.md for the prompt-surface/append-section parity class and reference it in both GUARDS entries, or explicitly map them to an existing invariant. Confirm both new scripts call process.exit(1) on mismatch (not just console.error) so check-invariant-drift flags them.",
      "verifier_reasoning": "PARTIALLY confirmed, but the claim's headline risk is wrong; the underlying gap is real at low severity.\n\nVERIFIED facts: The `GuardRun` interface (scripts/check-invariant-drift.ts:22-26) requires `invariants: string[]`, and all 5 existing GUARDS entries bind ≥1 INV id (lines 33,38,43,55,60). INVARIANTS.md (lines 84-91) owns a guard↔invariant table where every guard (G1,G2,G3,G4,G5,G7) maps to an invariant; no existing INV covers a \"prompt-surface/append-section parity\" class. The design (20260622-...-design.md §3.2 line 29, §3.3 line 35) only says \"Register in check-invariant-drift.ts GUARDS\" and success criteria (lines 47-48) say drift \"fails CI\", but it never names which INV id the two new guards (check-prompt-projection-parity.ts, check-append-sections-parity.ts) carry, and `grep` for process.exit/exit-code in the design returns nothing — so the new scripts' exit(1)-on-failure requirement is also unstated.\n\nWHERE THE CLAIM OVERSTATES (drives severity down to low): The claim's stated risk — \"a guard that is registered but exempt from CI failure semantics\" — is FALSE. In check-invariant-drift.ts the `invariants` field is consumed ONLY as a JSON report label (line 117). Pass/fail and process.exit(1) (lines 108, 126-131) derive entirely from `r.ok`, which runGuard (lines 65-79) sets from the subprocess exit code. A guard registered with `invariants: []` would still fail CI correctly. So there is NO CI-soundness hole; the field is not a gate input in the drift aggregator (contrast scripts/check-invariant-change-marker.ts:153 where target.invariants IS gating logic — a different mechanism, G4). The claim even concedes \"propagation is fine PROVIDED the scripts exit non-zero,\" contradicting its own title.\n\nWHAT IS GENUINELY REAL: a governance/documentation-completeness gap, not an enforcement defect. The required interface field + repo convention + INVARIANTS.md's guard table all expect each guard to declare its protected INV id(s); the design omits this and omits the exit(1) contract for the new scripts. A builder following the design literally would invent an ad-hoc id or pass a placeholder, leaving INVARIANTS.md's guard↔invariant table out of sync. The recommendation (introduce INV-REGISTRY-PARITY-1 or map to an existing id; confirm process.exit(1) on mismatch) is sound and is exactly what pre-build cross-validation should surface. Severity is low because no CI enforcement is actually broken (the headline risk does not exist) and this is a pre-build design note where the omission is a fillable detail, not a structural flaw."
    },
    {
      "lens": "drift-guard-soundness",
      "severity": "low",
      "title": "Conditional-obligation type shape in the design (Q4 / §3.1) names the wrong fields; the real YAML uses {obligation_id, activation_condition, input_authority_refs}",
      "location": ".onto/processes/reconstruct/reconstruct-contract-registry.yaml:2245-2248, 2304-2310 vs design §3.1 (line 20: 'ConditionalValidationObligation') and Q4 (line 61: 'flat string[] or {obligation, required_when}')",
      "claim": "The design's Q4 speculates the conditional shape is either flat string[] or {obligation, required_when}. The actual 3 conditional_validation_obligations entries are objects with fields obligation_id, activation_condition, and input_authority_refs (yaml:2246-2248, 2305-2310). Neither Q4 candidate matches. A loader parse coded to {obligation, required_when} would silently drop/mis-parse all three.",
      "recommendation": "Type ConditionalValidationObligation as { obligation_id: string; activation_condition: string; input_authority_refs?: string[] } to match the real entries, and have the loader test assert these exact fields survive load (per the 'real loader, not grep' rule). Note this is distinct from conditional_input_authority_refs (yaml:2283-2292) which has a different shape (artifact_ref/consumed_for) — do not conflate.",
      "verifier_reasoning": "VERIFIED REAL (factual core confirmed against code/YAML), but severity is overstated as medium; low is accurate.\n\nConfirmed facts:\n- The actual conditional_validation_obligations entries are objects with fields {obligation_id, activation_condition, input_authority_refs}. Verified at .onto/processes/reconstruct/reconstruct-contract-registry.yaml:2246-2248 (candidate-disposition-validator), 2305-2310 (ontology-seed-validator, 3 entries), and 2701-2704 (answer-support judge). Three validators host these blocks; total entry count is actually 5 (1+3+1), not literally \"3 entries\" as the claim says, though the design §2 also says \"(3)\" referring to the count of hosting blocks/validators. Field shape is uniform across all of them.\n- Design Q4 (line 61) speculates two candidate shapes: \"flat string[] or a structured {obligation, required_when}\". Neither matches the real {obligation_id, activation_condition, input_authority_refs}. The claim is correct that both candidates are wrong.\n- The claim's \"do not conflate with conditional_input_authority_refs\" note is correct: that sibling field (yaml:2231-2235, 2283-2292) genuinely uses a different shape {artifact_ref, activation_condition, consumed_for}.\n- parseValidatorRecord (src/core-runtime/reconstruct/contract-registry.ts:695-710) indeed drops both validation_obligations and conditional_validation_obligations today, so the loader extension in §3.1 is the place this type would be coded.\n- The recommendation (type as {obligation_id, activation_condition, input_authority_refs?: string[]} and assert via the real loadReconstructContractRegistry test) is exactly correct and matches the repo's documented \"real loader, not grep\" rule.\n\nWhy severity is low, not medium: The claim's framing (\"§3.1 names the wrong fields\") slightly overstates. §3.1 line 20 does NOT commit to field names — it explicitly says \"or a bounded typed shape; inspect the 3 existing conditional entries\", and Q4 is explicitly posed as an OPEN QUESTION (\"Inspect and type minimally\"). The design self-identified this exact gap and deferred it to inspection-at-build. The risk the claim describes (a loader coded to {obligation, required_when} mis-parsing) is a hypothetical mis-build, not what the design commits to. One real hazard remains: §3.1 line 21's \"minimal well-formedness parse (string ids)\" hint actively points toward the wrong string[] realization, so a literal builder could code the wrong parse. So this is a correct, useful pre-build refinement of an already-flagged open question — worth fixing, but not a latent shipping bug. Low."
    },
    {
      "lens": "completeness",
      "severity": "low",
      "title": "conditional_validation_obligations are structured objects, not string ids — loader parse spec in §3.1 is wrong",
      "location": ".onto/processes/reconstruct/reconstruct-contract-registry.yaml conditional_validation_obligations (lines 2245-2249, 2304-2310, ~2702) vs design §3.1 lines 20-21",
      "claim": "The design §3.1 says parseValidatorRecord 'parses both (optional arrays; default []) ... a minimal well-formedness parse (string ids)'. But the 3 conditional_validation_obligations entries are NOT strings — each is an object {obligation_id, activation_condition, input_authority_refs?}. A `string[]` parse (the only shape the §3.1 body prescribes) would either throw or silently coerce objects to garbage. The structured shape is only hedged in Q4 ('flat string[] or structured'), not resolved, so the success criterion 'preserves conditional_validation_obligations on every parsed validator record' (§5) is unbuildable as written — it needs a ConditionalValidationObligation type with at least {obligation_id: string, activation_condition: string, input_authority_refs?: string[]}.",
      "recommendation": "Resolve Q4 inside the design before build: define ConditionalValidationObligation = {obligation_id: string; activation_condition: string; input_authority_refs?: string[]} and add a parseConditionalValidationObligation helper in contract-registry.ts. validation_obligations IS a flat string[] (correct), but the two fields need DIFFERENT parsers. Update ReconstructValidatorRecord += validation_obligations: string[]; conditional_validation_obligations: ConditionalValidationObligation[].",
      "verifier_reasoning": "PARTIALLY CONFIRMED, but overstated — real defect, low severity (not high).\n\nVERIFIED FACTS (code-grounded):\n- Registry reality matches the claim: `conditional_validation_obligations` entries are objects, not strings. The 3 entries (reconstruct-contract-registry.yaml lines 2245-2248, 2304-2313, 2701-2704) each have shape `{obligation_id: string, activation_condition: string, input_authority_refs?: string[]}`. Meanwhile `validation_obligations` IS a flat string[] (e.g. lines 2236-2244, 2696-2700). So the claim's core observation — the two fields need DIFFERENT parsers, and a naive `string[]` parse over the conditional field would throw (stringArray/optionalStringArray in contract-registry.ts would reject objects) — is technically correct.\n- The recommended type `{obligation_id; activation_condition; input_authority_refs?}` exactly matches the real YAML shape (the claim got the field names right, unlike the design's own Q4 guess `{obligation, required_when}` which does NOT match).\n- parseValidatorRecord (contract-registry.ts:695-709) and ReconstructValidatorRecord (134-140) currently drop both fields — design §2/§3.1 targets are accurate.\n\nWHERE THE CLAIM OVERSTATES (why low, not high):\n1. The claim asserts \"the only shape the §3.1 body prescribes is string[].\" That is false. §3.1 line 20 explicitly writes `conditional_validation_obligations: ConditionalValidationObligation[]` and adds \"(or a bounded typed shape; inspect the 3 existing conditional entries).\" The design already names a typed-array type and an inspection instruction — it does NOT prescribe string[] for the conditional field.\n2. The \"(string ids)\" parenthetical the claim quotes is at line 21 (\"a minimal well-formedness parse (string ids)\"). It is genuinely loose/misleading wording, but read in context it applies most naturally to validation_obligations (which ARE string ids); for the conditional field the design already deferred the precise shape.\n3. Q4 (line 61) explicitly raises this exact question (\"flat string[] or a structured ... Inspect and type minimally\") as an open cross-validation question. This is a DESIGN being reviewed pre-build whose stated purpose is to resolve such shape questions. An unresolved open question with a named placeholder type is not an \"unbuildable\" success criterion — it is exactly the kind of item cross-validation exists to close.\n\nNET: There IS a real, fixable precision gap: (a) the line-21 \"(string ids)\" parenthetical is inaccurate for the conditional field and could mislead a literal implementer into a throwing string[] parse; (b) Q4's hypothesized shape `{obligation, required_when}` is wrong vs the actual `{obligation_id, activation_condition, input_authority_refs?}`. The claim's recommended type is correct and worth adopting. But because the design already types the field as ConditionalValidationObligation[] and explicitly defers the exact shape to Q4 with an inspect-the-entries instruction, the success criterion is buildable, just under-specified — a low-severity doc/precision fix (correct Q4's shape + tighten the line-21 parenthetical), not a high-severity blocker."
    },
    {
      "lens": "completeness",
      "severity": "low",
      "title": "Export of competencyQuestionAssessmentProjectionContract is required and currently absent — confirm but note module-import weight",
      "location": "src/core-runtime/reconstruct/run.ts:800 (function is module-private; no `export`) vs design §3.2 line 30 / Q3",
      "claim": "Confirmed: competencyQuestionAssessmentProjectionContract (run.ts:800) is NOT exported (no `export` keyword; grep for export of the symbol returns nothing). The design correctly identifies the need to export it (or the budget constants) for the G(b) guard. However the completeness checklist must also note: the guard script imports run.ts, which transitively imports llm-caller.js (run.ts:92) — the mock-realization boundary — plus ~40 other modules and the full ~13K-line file. scripts/check-import-boundary.ts only scans src/** (SRC_ROOT), so scripts importing run.ts is NOT caught by G1, but pulling the entire reconstruct runtime + llm-caller into a tiny parity guard is a real coupling/startup cost.",
      "recommendation": "Prefer Q3's 'extract a tiny projection-contract module' option: move competencyQuestionAssessmentProjectionContract + the 4 budget constants into src/core-runtime/reconstruct/competency-projection-contract.ts and re-import into run.ts. Both run.ts and the guard import the small module; the guard avoids dragging in llm-caller and the whole pipeline. This also keeps the export surface minimal (concept economy) rather than exporting an internal from a 13K-line god-module.",
      "verifier_reasoning": "All checkable facts in the claim are accurate.\n\nCONFIRMED (1): `competencyQuestionAssessmentProjectionContract` at src/core-runtime/reconstruct/run.ts:800 is module-private — `function competencyQuestionAssessmentProjectionContract(): Record<string, unknown>` with no `export`. Grep for any `export function/const/{ }` of the symbol returns nothing; the only exported nearby symbol is the unrelated interface `ReconstructCompetencyQuestionAssessmentAuthorInput` (line 513). So the G(b) guard (design §3.2 line 29) cannot call it as written.\n\nCONFIRMED (2): The guard must call the runtime contract — design §3.2 line 30 and Q3 (line 60) both state the export is required.\n\nCONFIRMED (3): scripts/check-import-boundary.ts:21 sets `SRC_ROOT = path.join(PROJECT_ROOT, \"src\")` and `listSourceFiles(SRC_ROOT)` walks only src/**; scripts/ is outside the scan, so G1 will NOT flag a script importing run.ts. run.ts:92 imports `../llm/llm-caller.js` (the mock-realization boundary), the file is 13,561 lines with 35 import statements, so importing run.ts into a tiny guard pulls the whole reconstruct runtime + llm-caller in. No existing script imports run.ts as a module today (the check-no-hardcoded-spec-defaults.ts reference is a string path it scans, not an import), so the coupling would be novel — the completeness/coupling observation is genuine and is NOT already in the design.\n\nWHY LOW (not medium): The finding self-labels 'Confirm but note module-import weight' and its recommendation (extract competency-projection-contract.ts) is the SAME mitigation the design already offers as an explicit option in §3.2 line 30 ('or a small projection-contract module') and raises for cross-validation in Q3 ('export ... vs extract a tiny projection-contract module — which respects import boundaries'). So this is not a design defect (wrong target, missing reconciliation, foolable guard); it is a soft steer toward option B in an open question the design itself surfaced, plus an accurate but non-blocking note that G1 won't auto-police the import. The export gap is trivially fixable and already anticipated. Real, accurate, but low impact."
    },
    {
      "lens": "completeness",
      "severity": "low",
      "title": "Guards run as subprocess scripts via execFile — §3.2/§3.3 'guard loads registry + calls the runtime contract' must be a script main(), and GUARDS array entries are under-specified",
      "location": "scripts/check-invariant-drift.ts GUARDS array (lines 30-63) and runGuard (65-79) vs design §3.2 line 29 / §3.3 line 35",
      "claim": "check-invariant-drift registers guards as {guard, invariants, command:['npx','tsx','scripts/...ts']} and runs them with execFileAsync expecting non-zero exit on failure. The design says 'Register in check-invariant-drift.ts GUARDS' (correct mechanism) but never states the two new scripts must (a) be standalone executables with their own main()/process.exit(1), and (b) declare an `invariants` tag — and there is NO existing invariant id for prompt-projection-parity or append-section-parity. The G(b)/G(c) guards protect a drift class that maps to no INVARIANTS.md id, so the GUARDS entry has nothing meaningful to put in `invariants`, and G4 (check-invariant-change-marker) won't gate these new registry nodes.",
      "recommendation": "Decide whether G(b)/G(c) get a new invariant id (e.g. INV-PROMPT-PARITY-1) — if yes that is an INVARIANT-CHANGE-marked edit to INVARIANTS.md + invariants.yml (extra scope), if no the GUARDS entry should carry an empty/structural-only invariants tag and the design should say so. Specify each new script ends with `await main()` and exits 1 on drift (matching check-supported-models.ts shape). Add both scripts as npm scripts too (the pattern: check:* names).",
      "verifier_reasoning": "VERDICT: real but low (claim says medium; the bulk is fix-by-pattern boilerplate, with one genuine open scope decision).\n\nThe mechanism the claim describes is accurate against code. scripts/check-invariant-drift.ts:22-26 defines `GuardRun{guard,invariants:string[],command:string[]}`; the GUARDS array (lines 30-63) has every entry carrying a NON-EMPTY `invariants` array; runGuard (lines 65-79) runs each via `execFileAsync` and treats a thrown error (non-zero exit) as failure. So a registered guard must be a standalone executable that `process.exit(1)`s on drift. The cited templates confirm the shape the design omits: check-supported-models.ts ends `await main()` (line 157) with `process.exit(1)` on failure (lines 119,131,138); check-import-boundary.ts (lines 147,167) and check-invariant-change-marker.ts (lines 188,191) are identical. Every guard also has a `check:*` npm script (package.json:47-51).\n\nCONFIRMED gaps in the design:\n1) §3.2 (line 29) and §3.3 (line 35) say \"Register in check-invariant-drift.ts GUARDS\" and describe behavior (\"Fail on add/drop/changed-value\") but never specify the executable contract (main()/await main()/process.exit(1)) nor the matching `check:*` npm script. True omission.\n2) No invariant id exists for prompt-projection-parity or append-section-parity. INVARIANTS.md ids are AUTH/CFG/TEST/SCHEMA/MOCK/BENCH/MODEL/EXP/MATERIAL/LOOP/SCOPE only; grep for \"parity\" in INVARIANTS.md/invariants.yml returns nothing. The design never states what the GUARDS `invariants` tag will carry. This is the load-bearing, substantive part: deciding new id vs reuse vs empty tag has real scope implications (a new INV id is an INVARIANT-CHANGE-marked edit to INVARIANTS.md + invariants.yml).\n3) G4 (check-invariant-change-marker.ts PROTECTED_TARGETS, lines 35-80) does NOT list reconstruct-contract-registry.yaml, so the new registry nodes are not marker-gated — confirmed.\n\nWhere the claim OVER-REACHES (why low, not medium):\n- \"the GUARDS entry has nothing meaningful to put in `invariants`\" is overstated. INV-SCHEMA-1 (\"stage output contract/schema is single source\") is a natural existing fit for BOTH G(b) (prompt-projection field set) and G(c) (canonical append-section set) — these are exactly stage-output-contract parity concerns. A meaningful reuse exists without minting INV-PROMPT-PARITY-1, so the claim's \"either new id or empty tag\" framing is not the only option.\n- \"G4 won't gate these new registry nodes\" is true but the parity guards themselves ARE the registry↔runtime drift protection; G4 marker-gating is not strictly required for nodes whose consistency is already enforced by a dedicated guard.\n- The executable-shape and npm-script omissions are pure pattern-following boilerplate any build would supply from the cited aggregator/templates — not a correctness defect, wrong code target, or missing reconciliation site.\n\nNet: a legitimate completeness/under-specification finding. The recommendation (decide invariant-id policy; specify main()/exit(1); add npm scripts) is correct and actionable. But it concerns implementation-shape detail plus one open decision, not a code-level defect, so low is the honest severity rather than medium."
    },
    {
      "lens": "authority-economy",
      "severity": "low",
      "title": "budget_fields includes candidate_limit=50 'if declared', but it is not in the contract output — parity guard cannot see it",
      "location": "run.ts:795 COMPETENCY_QUESTION_ASSESSMENT_EVIDENCE_CANDIDATE_LIMIT (constant) vs run.ts:801-840 (contract output omits it); design §3.2 budget_fields",
      "claim": "Design §3.2 lists `candidate_limit=50 if declared` as a budget_field. The constant COMPETENCY_QUESTION_ASSESSMENT_EVIDENCE_CANDIDATE_LIMIT=50 exists (run.ts:795) but is NOT a key in competencyQuestionAssessmentProjectionContract()'s returned object (run.ts:801-840 returns only the 3 budget-bearing values: prompt_char_limit, source_evidence_excerpt_char_limit, build_budget_reserve_chars). A parity guard that asserts 'declared budgets == runtime contract values' has no contract field to compare candidate_limit against, so declaring it in the registry would either be vacuous or force a false 'must add it to the contract' coupling that rotates the reuse-hash sha (run.ts:843-844 hashes the whole contract; run.test.ts:4123-4130 pins that sha changes on any field change).",
      "recommendation": "Drop candidate_limit from budget_fields (it is an internal runaway-guard cap, not part of the prompt-surface contract). Declare exactly the 3 budgets the contract embeds. If candidate_limit must be governed, that is a separate decision — adding it to the contract output is a v5→v6 sha rotation and a deliberate prompt-surface change, out of scope for a parity guard.",
      "verifier_reasoning": "Code-grounded core of the claim is accurate. `COMPETENCY_QUESTION_ASSESSMENT_EVIDENCE_CANDIDATE_LIMIT = 50` is defined at run.ts:795 and used ONLY at run.ts:4142-4145 as a `.slice(0, ...)` pre-cap on candidate observation ids (a \"cheap runaway guard ... this only caps projection work,\" per its own comment). It is NOT a key in `competencyQuestionAssessmentProjectionContract()`'s returned object (run.ts:801-840), which embeds exactly 3 budget values: prompt_char_limit (50000), source_evidence_excerpt_char_limit (4000), and nested batching_policy.build_budget_reserve_chars (1000). The sha is hashed over the whole contract (`competencyQuestionAssessmentProjectionContractSha256`, run.ts:843-844) and run.test.ts:4123-4130 pins that the sha changes on any field mutation. So a parity guard that compared declared budgets against CONTRACT-OUTPUT keys would indeed have no field for candidate_limit, and shoving it into the contract output would rotate the v5 sha and be a deliberate prompt-surface change. That is a genuine ambiguity in design §3.2's budget_fields grouping — it lumps candidate_limit (constant-only) with three constants that are also contract keys.\\n\\nBut the claim overstates the consequence as an inescapable dilemma (\"either vacuous or force a false add-to-contract coupling that rotates the sha\"). Design §3.2 actually specifies the guard \"asserts ... the declared budgets equal the runtime CONSTANTS,\" and candidate_limit=50 IS a runtime constant (run.ts:795) the guard can import and compare against directly — precisely what the design's own \"+ candidate_limit=50 if declared\" hedge anticipates. That third path governs the runaway cap with a real, non-vacuous assertion, without touching the contract output or the reuse-hash. So the recommended \"drop it\" is one valid resolution, not the only correct one; the design already contains the safe alternative. Severity is therefore low rather than medium: it is a real design ambiguity worth pinning down (does the guard compare against contract output keys or against the source constants?), but it does not force the sha regression the claim presents, and it does not block build."
    }
  ]
}